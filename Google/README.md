# Google 数据源

对称结构（精简后）：

```text
Google/
  raw/           # Takeout 等原始导出
  structured/    # 清洗后的 SQLite / CSV / scripts
```

`analysis/` 画像与 HTML 报告已归档到：

`_recycle/2026-07-12_structure_cleanup/Google/analysis/`

主分析与知识检索请使用 `integration/`。
