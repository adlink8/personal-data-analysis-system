# Agent/structured（精简）

本目录仅保留 **db/**：

- `agent_conversations.sqlite` — Phase 14 知识单元证据库
- `agentsview_normalized.sqlite` — AgentView 规范化快照
- `agent_data.sqlite` — 历史 Agent 结构化库
- `backups/` — 备份

`by_source` / `by_type` / `details` / `raw_index` / `scripts` 等已移至：

`_recycle/2026-07-12_structure_cleanup/Agent/structured/`
