---
phase: 54
plan: 02
type: execute
wave: 2
depends_on: [54-01]
files_modified: [ops/runtime/start-agent-stack.ps1, governance/manifests/ai/pi-ai-entrypoints.json, tests/e2e/test_pi_activation_rollback.py, ops/reports/evidence/pi-primary-activation.json, .planning/phases/PDA-54-primary-activation-and-exact-rollback/54-ACTIVATION-REPORT.md]
autonomous: false
requirements: [ACT-02]
must_haves:
  truths:
    - "D-05: rollback restores legacy atomically without deleting Pi history or authority data."
    - "D-06: primary/rollback/forward-restore require explicit user confirmation; auto only downgrades."
---

# Phase 54-02 Plan — Primary switch, forced failure, exact rollback and restore

<objective>将 Pi 激活为唯一主 AI Runtime，并以强制失败证明 exact rollback 和 forward-restore。</objective>
<threat_model>Assets: primary AI availability, user authorization, runtime history and authority integrity. Threats: premature primary, hidden legacy bypass, rollback split-brain, data deletion and automatic re-upgrade. Controls: manual checkpoints, receipt readiness, atomic pointer+supervisor transition, forced-failure drill, fingerprints and append-only history.</threat_model>
<tasks>
<task type="checkpoint:human-action">
  <name>54-02-01 — Activate primary only after final readiness and user confirmation</name>
  <read_first>
    - ops/reports/evidence/pi-shadow-canary.json
    - governance/manifests/ai/pi-runtime-policy.json
    - governance/manifests/ai/pi-ai-entrypoints.json
    - ops/runtime/start-agent-stack.ps1
  </read_first>
  <files>ops/runtime/start-agent-stack.ps1, governance/manifests/ai/pi-ai-entrypoints.json, ops/reports/evidence/pi-primary-activation.json</files>
  <action>Run final preflight: accepted Phase 48 package, Phase 49–52 verification, Phase 53 proceed decision, accepted canary, complete entrypoint inventory, healthy Kernel/Python/Cockpit and frozen rollback target. Present exact primary preview and require user confirmation. Atomically set primary route and restart/reload only owned services. Execute one bounded smoke per AI purpose and assert every receipt is Pi; legacy remains disabled standby.</action>
  <acceptance_criteria>
    - Primary cannot activate with any missing/stale prerequisite or unclassified/bypass entrypoint.
    - User confirmation checksum matches applied activation event.
    - Every bounded production-purpose smoke has Pi task/session/event/model receipts and no legacy receipt.
    - Authority fingerprints remain unchanged except explicitly confirmed domain fixture writes.</acceptance_criteria>
  <verify><automated>python -m pytest tests/integration/test_pi_primary_routing.py -q</automated></verify>
</task>
<task type="checkpoint:human-verify">
  <name>54-02-02 — Force failure, exact rollback and confirmed forward-restore</name>
  <read_first>
    - ops/reports/evidence/pi-primary-activation.json
    - src/personal_knowledge/services/pi_runtime_activation.py
    - tests/e2e/test_pi_activation_rollback.py
    - .planning/phases/PDA-54-primary-activation-and-exact-rollback/54-VALIDATION.md
  </read_first>
  <files>tests/e2e/test_pi_activation_rollback.py, ops/reports/evidence/pi-primary-activation.json, .planning/phases/PDA-54-primary-activation-and-exact-rollback/54-ACTIVATION-REPORT.md</files>
  <action>Inject a declared critical Kernel failure and prove automatic downgrade stops new Pi claims, restores legacy route/supervisor readiness atomically and preserves Pi Session/Event/Candidate history. Verify no authority/watermark/promotion/active pointer change. After user verifies rollback, prepare and require separate confirmation for forward-restore to primary; automatic re-upgrade is forbidden. Record all activation ledger events, timings, receipts and user acceptance.</action>
  <acceptance_criteria>
    - Forced failure meets stop condition and restores legacy within preregistered bound without split-brain/double call.
    - Pi historical DB rows remain intact; protected authority fingerprints are unchanged.
    - Forward-restore occurs only after a new explicit confirmation and returns all AI receipts to Pi.
    - User signs primary, rollback and restore acceptance in 54-ACTIVATION-REPORT.md.</acceptance_criteria>
  <verify><automated>python -m pytest tests/e2e/test_pi_activation_rollback.py -q</automated></verify>
</task>
</tasks>
<verification>Contract/integration/e2e tests plus three explicit user checkpoints are mandatory.</verification>
<success_criteria>Pi is the sole primary AI controller, legacy is standby-only, and exact rollback/restore is proven without authority mutation.</success_criteria>
