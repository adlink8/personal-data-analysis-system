---
phase: 54
plan: 01
type: execute
wave: 1
depends_on: [53-02]
files_modified: [src/personal_knowledge/services/pi_runtime_activation.py, governance/manifests/ai/pi-runtime-policy.json, tools/supported/pi_runtime.py, tests/contract/test_pi_runtime_activation.py, tests/integration/test_pi_primary_routing.py]
autonomous: false
requirements: [ACT-02]
must_haves:
  truths:
    - "D-01: runtime mode is ledgered legacy/shadow/canary/primary and defaults legacy."
    - "D-02: shadow cannot duplicate real Provider side effects."
    - "D-03: canary cohort/budget/stops/rollback target are frozen before switch."
    - "D-04: primary readiness requires Pi receipts for every production AI entrypoint."
---

# Phase 54-01 Plan — Activation authority, shadow and canary

<objective>建立可审计 activation ledger/policy，并在人工确认下完成无双调用 shadow 和有界 canary。</objective>
<threat_model>Assets: runtime routing, provider side effects and rollback target. Threats: automatic upgrade, double call, stale decision, canary scope creep and bypassing Pi. Controls: explicit confirmation, frozen manifest/checksum, append-only ledger, single side-effect route and entrypoint receipt gate.</threat_model>
<tasks>
<task type="execute">
  <name>54-01-01 — Implement activation policy/ledger and readiness gate</name>
  <read_first>
    - .planning/phases/PDA-53-real-baseline-fault-injection-and-uat/53-ACTIVATION-DECISION.md
    - governance/manifests/ai/pi-ai-entrypoints.json
    - ops/runtime/start-agent-stack.ps1
    - src/personal_knowledge/intelligence/analysis/providers.py
  </read_first>
  <files>src/personal_knowledge/services/pi_runtime_activation.py, governance/manifests/ai/pi-runtime-policy.json, tools/supported/pi_runtime.py, tests/contract/test_pi_runtime_activation.py</files>
  <action>Define immutable transition manifest and append-only ledger for legacy/shadow/canary/primary with current pointer checksum, actor, reason, evidence, cohort, budget, stop conditions and rollback target. CLI defaults inspect/dry-run; prepare emits exact preview and confirm requires checksum/idempotency/user phrase. Validate Phase 53 decision and entrypoint inventory before upgrade. Automatic logic may only transition downward.</action>
  <acceptance_criteria>
    - Default fresh state is legacy; no upgrade occurs without exact confirmation.
    - Illegal/stale/missing-evidence transition creates no ledger/pointer change.
    - Ledger is append-only and current pointer matches its latest applied event checksum.
    - Activation contract tests exit 0.</acceptance_criteria>
  <verify><automated>python -m pytest tests/contract/test_pi_runtime_activation.py -q</automated></verify>
</task>
<task type="checkpoint:human-verify">
  <name>54-01-02 — Run approved shadow then bounded canary with stop conditions</name>
  <read_first>
    - governance/manifests/ai/pi-runtime-policy.json
    - .planning/phases/PDA-53-real-baseline-fault-injection-and-uat/53-ACTIVATION-DECISION.md
    - tests/integration/test_pi_primary_routing.py
  </read_first>
  <files>tests/integration/test_pi_primary_routing.py, ops/reports/evidence/pi-shadow-canary.json</files>
  <action>With explicit user confirmation, activate shadow using replay/captured inputs so only one real Provider effect occurs. Verify receipts and stop behavior, then prepare/confirm canary only if Phase 53 permits it. Freeze cohort/window/budget/stops/rollback target; monitor critical privacy/authority/replay/bypass conditions and automatically downgrade on breach. Record metadata-only evidence and user acceptance.</action>
  <acceptance_criteria>
    - Shadow creates no second real Provider side effect.
    - Canary processes only frozen cohort/window/budget and every call has Pi receipts.
    - Stop fixture triggers automatic downgrade to declared rollback target.
    - User explicitly accepts or rejects canary before primary planning continues.</acceptance_criteria>
  <verify><automated>python -m pytest tests/integration/test_pi_primary_routing.py -q</automated></verify>
</task>
</tasks>
<verification>Activation schema/tests and signed shadow/canary evidence are required; no primary switch in this plan.</verification>
<success_criteria>Shadow and canary prove single-side-effect Pi routing with bounded automatic downgrade.</success_criteria>
