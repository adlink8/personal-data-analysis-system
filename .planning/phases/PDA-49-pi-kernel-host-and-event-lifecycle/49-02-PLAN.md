---
phase: 49
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified: [apps/personal_intelligence_kernel/src/server.mjs, apps/personal_intelligence_kernel/src/transport/sse.mjs, apps/personal_intelligence_kernel/test/server.test.mjs, tests/contract/test_pi_kernel_host.py, tests/integration/test_pi_kernel_events.py]
autonomous: true
requirements: [KERNEL-01, KERNEL-02]
must_haves:
  truths:
    - "D-05: unknown events, inline bodies and non-loopback bind fail closed."
    - "D-06: Phase 49 remains standalone and does not edit the production supervisor."
---

# Phase 49-02 Plan — Loopback transport, SSE replay and lifecycle verification

<objective>提供最小 loopback HTTP/SSE transport，并证明 readiness、重连、关闭、重启和隐私行为。</objective>

<threat_model>
Assets: local event stream and process ownership. Threats: remote bind, raw error leakage, cursor skip/duplication, listener-only readiness and orphan process. Controls: fixed loopback host, route/method allowlist, safe codes, durable Last-Event-ID mapping and bounded dispose.
</threat_model>

<tasks>
<task type="execute">
  <name>49-02-01 — Add health/readiness, event ingress and durable SSE</name>
  <read_first>
    - apps/personal_intelligence_kernel/src/kernel-host.mjs
    - apps/personal_intelligence_kernel/src/events/journal.mjs
    - src/personal_knowledge/services/api_server.py
    - apps/personal_data_chatgpt/server.mjs
  </read_first>
  <files>apps/personal_intelligence_kernel/src/server.mjs, apps/personal_intelligence_kernel/src/transport/sse.mjs, apps/personal_intelligence_kernel/test/server.test.mjs</files>
  <action>Implement only GET `/health`, GET `/ready`, POST `/v1/events`, GET `/v1/events/stream`. Bind literal 127.0.0.1:8790 by default and reject wildcard/non-loopback configuration. `/ready` checks package decision, resource registry, schema migration and SQLite integrity. SSE maps Last-Event-ID to durable sequence, sends bounded heartbeat metadata, and never serializes raw exception/path/body. POST accepts only validated metadata events and returns exact replay status.</action>
  <acceptance_criteria>
    - Unknown route/method and malformed event use fixed safe codes without echoed input.
    - SSE reconnect from every tested cursor has no missing or duplicate event_id.
    - Non-loopback bind and listener-with-failed-integrity do not become ready.
    - Server tests exit 0.</acceptance_criteria>
  <verify><automated>npm test --prefix apps/personal_intelligence_kernel -- --test-name-pattern=server</automated></verify>
</task>
<task type="execute">
  <name>49-02-02 — Verify cross-process lifecycle, privacy and zero authority mutation</name>
  <read_first>
    - apps/personal_intelligence_kernel/src/server.mjs
    - tests/ops/test_agent_stack_script.py
    - .planning/phases/PDA-49-pi-kernel-host-and-event-lifecycle/49-VALIDATION.md
  </read_first>
  <files>tests/contract/test_pi_kernel_host.py, tests/integration/test_pi_kernel_events.py</files>
  <action>Start the Host as an owned test subprocess on a random loopback port; verify health vs readiness, graceful stop, forced kill/restart, journal replay and port-conflict preservation. Seed credential/path/personal-body values and assert absence from HTTP/SSE/stdout/stderr. Fingerprint canonical DBs, watermark and active pointer before/after. Do not edit `start-agent-stack.ps1` in this phase.</action>
  <acceptance_criteria>
    - Cross-process tests pass for restart, conflict, SSE reconnect and bounded shutdown.
    - Seeded sensitive values never appear in outputs.
    - Protected authority fingerprints are identical.
    - `git diff -- ops/runtime/start-agent-stack.ps1` is empty for this phase.</acceptance_criteria>
  <verify><automated>python -m pytest tests/contract/test_pi_kernel_host.py tests/integration/test_pi_kernel_events.py -q</automated></verify>
</task>
</tasks>
<verification>Run Node and Python Phase 49 suites; Provider call count must be zero.</verification>
<success_criteria>Standalone Kernel Host is durable, loopback-only, replay-safe, privacy-safe and ready for Phase 50.</success_criteria>
