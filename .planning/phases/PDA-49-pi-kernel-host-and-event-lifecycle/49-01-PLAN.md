---
phase: 49
plan: 01
type: execute
wave: 1
depends_on: [48-03]
files_modified: [apps/personal_intelligence_kernel/src/events/schema.mjs, apps/personal_intelligence_kernel/src/events/journal.mjs, apps/personal_intelligence_kernel/src/kernel-host.mjs, apps/personal_intelligence_kernel/test/event-schema.test.mjs, apps/personal_intelligence_kernel/test/event-journal.test.mjs]
autonomous: true
requirements: [KERNEL-01, KERNEL-02]
must_haves:
  truths:
    - "D-01: Kernel is an isolated loopback host on 127.0.0.1:8790."
    - "D-02: pi_kernel_event_v1 owns the public event contract."
    - "D-03: event identity and replay are deterministic."
    - "D-04: journal stores metadata references, not personal bodies."
---

# Phase 49-01 Plan — Kernel host, event schema and durable journal

<objective>建立 project-owned event contract、append-only SQLite journal 和单一 Pi Kernel Host bootstrap。</objective>

<threat_model>
Assets: event identity, journal integrity, personal metadata and Kernel lifecycle. Threats: inline private body, duplicate replay, SQLite corruption, non-loopback exposure and SDK-shape leakage. Controls: strict schema, canonical checksum, unique idempotency index, metadata allowlist, FK/integrity checks and explicit bind.
</threat_model>

<tasks>
<task type="execute">
  <name>49-01-01 — Implement pi_kernel_event_v1 and canonical identity</name>
  <read_first>
    - .planning/phases/PDA-49-pi-kernel-host-and-event-lifecycle/49-CONTEXT.md
    - .planning/phases/PDA-49-pi-kernel-host-and-event-lifecycle/49-AI-SPEC.md
    - .planning/spikes/pi-embedded-personal-kernel/prototype/streaming_control.mjs
    - apps/personal_intelligence_kernel/src/runtime/resource-policy.mjs
  </read_first>
  <files>apps/personal_intelligence_kernel/src/events/schema.mjs, apps/personal_intelligence_kernel/test/event-schema.test.mjs</files>
  <action>Define exact event types and `pi_kernel_event_v1` validation for event_id/type/source/authority/snapshot/correlation_id/causation_id/idempotency_key/occurred_at/payload_ref/privacy_class. Canonicalize JSON with fixed key ordering and derive event_id from schema version plus identity fields. Reject inline payload/body/content/prompt/completion, unknown keys/types, missing binding and privacy above the event policy. Normalize Pi SDK events into project types without exporting SDK-private objects.</action>
  <acceptance_criteria>
    - Same valid identity produces the same event_id; one changed binding produces a different ID.
    - Unknown/missing fields and any inline private-body key return typed schema errors.
    - Node event-schema tests cover at least 12 valid/invalid fixtures and exit 0.
  </acceptance_criteria>
  <verify><automated>npm test --prefix apps/personal_intelligence_kernel -- --test-name-pattern=event-schema</automated></verify>
</task>
<task type="execute">
  <name>49-01-02 — Implement append-only event journal and Host bootstrap</name>
  <read_first>
    - apps/personal_intelligence_kernel/src/events/schema.mjs
    - apps/personal_intelligence_kernel/src/runtime/resource-policy.mjs
    - governance/manifests/ai/pi-package-decision.json
    - .planning/phases/PDA-49-pi-kernel-host-and-event-lifecycle/49-RESEARCH.md
  </read_first>
  <files>apps/personal_intelligence_kernel/src/events/journal.mjs, apps/personal_intelligence_kernel/src/kernel-host.mjs, apps/personal_intelligence_kernel/test/event-journal.test.mjs</files>
  <action>Create schema-versioned `pi_kernel_events` and migration ledger in `var/db/pi_kernel_events.sqlite`, with monotonic sequence, unique event_id and idempotency identity, checksum and created_at. Implement transactional append/replay/cursor reads and integrity preflight. Build one Host factory that refuses startup unless Phase 48 decision is accepted and ResourceLoader policy is exact; Host owns disposal and bounded shutdown but makes no Provider call.</action>
  <acceptance_criteria>
    - Duplicate append returns the original sequence/event without a new row.
    - Restart preserves sequence, cursor replay and integrity_check=ok.
    - Missing/expired/non-accepted package decision makes Host readiness false.
    - Journal tests exit 0 and assert no inline personal body columns exist.
  </acceptance_criteria>
  <verify><automated>npm test --prefix apps/personal_intelligence_kernel -- --test-name-pattern=event-journal</automated></verify>
</task>
</tasks>

<verification>Run all Node event tests from a Phase 48 accepted exact lock; journal and authority fingerprint checks must pass.</verification>
<success_criteria>Deterministic event contract, durable idempotent journal and single contained Host exist with zero Provider calls.</success_criteria>
