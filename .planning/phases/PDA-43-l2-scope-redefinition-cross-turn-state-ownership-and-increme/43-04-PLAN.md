---
phase: 43-l2-scope-redefinition-cross-turn-state-ownership-and-increme
plan: 04
type: execute
wave: 3
depends_on: ["43-01", "43-02", "43-03"]
files_modified:
  - assets/prompts/knowledge_unit_extractor/v2_main.md
  - assets/prompts/knowledge_unit_extractor/v2_assistant.md
  - src/personal_knowledge/application/knowledge/build_knowledge_units.py
  - src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py
  - src/personal_knowledge/application/knowledge/injection_context.py
  - tests/unit/test_l2_injection_dedup.py
autonomous: true
requirements: [L2G-01, L2G-02]

must_haves:
  truths:
    - "v2 prompt（user 轨 + assistant 轨）含「已有知识清单与等价标注」节与 duplicate_of 输出契约，与 v1 并存为独立文件"
    - "L1 run 使用 v2 时 per-item llm_input 前置注入段（只变 input_hash，不分裂缓存命名空间）；注入策略参数进 config_hash"
    - "提交处：合法 duplicate_of 写入 staging 行 supersedes_id；非法引用丢弃并计数；清单内 subject 的 unit 落 lifecycle='candidate' 并计数"
    - "prompt 版本经 CLI flag 选择（策略用 flag，不为跑数改默认），run manifest 记录 prompt_version，切换只在 run 间隙"
  artifacts:
    - path: "assets/prompts/knowledge_unit_extractor/v2_main.md"
      provides: "user 轨 v2 prompt（v1 全文 + 注入/等价标注节 + duplicate_of 契约）"
      contains: "duplicate_of"
    - path: "assets/prompts/knowledge_unit_extractor/v2_assistant.md"
      provides: "assistant 轨 v2 prompt（同构新增节）"
      contains: "duplicate_of"
    - path: "src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py"
      provides: "v2 TrackConfig + --prompt-version flag + 注入段组装 + 提交处校验/路由"
      contains: "invalid_duplicate_of"
  key_links:
    - from: "build_knowledge_units_prod.process_run"
      to: "injection_context.recall_known_units / format_injection_block"
      via: "per-item llm_input 前置拼接（QA 注入 :1008-1015 先例）"
      pattern: "format_injection_block"
    - from: "build_knowledge_units_prod._commit_item_result"
      to: "state_subjects.match_state_subject / injection_context.validate_duplicate_of"
      via: "per-unit 循环（:770-781 拦截点）"
      pattern: "units_downgraded_candidate"
---

<objective>
把 43-01/43-02/43-03 的基础设施接进 L1 抽取管线：v2 prompt + 注入段组装 + 提交处 duplicate_of 校验与 candidate 路由。

Purpose: L2G-01（增量去重守门）与 L2G-02（状态类 subject 的 L1 拦截）在 L1 侧的全部行为落地于此；接线后 43-07 才能做双 run 对照验收。
Output: 两个 v2 prompt 文件、prod 管线接线、测试扩展。

**约束提醒（SPEC/41 硬约束）：** prompt 文件变更 = prompt_hash 变 = 缓存命名空间分裂——v2 与 v1 并存为独立文件，通过 CLI `--prompt-version` flag 选择（AGENTS 硬规则"策略用 flag，不为跑数改代码"），**只在 run 间隙切换**（启动新 run 前确认 knowledge_run_items 无 pending/in_flight），绝不改运行中途的 prompt 文件或默认版本。默认 prompt_version 保持现状（v1 / v1_assistant），v2 由实验与后续正式切换显式指定。

**Planner 决策记录：** assistant 轨同样获得 v2 prompt 与注入接线（v2_assistant.md）。理由：SPEC L2G-01 原文"L1/L2 抽取时注入"，L1 双轨都产 current unit，⑧ 治理的 294 对重复不分轨；assistant 轨不接线会让去重守门留半个口子。注入机制与 user 轨完全同构，边际成本只是一个 prompt 文件拷贝。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-SPEC.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-PATTERNS.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-01-SUMMARY.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-02-SUMMARY.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-03-SUMMARY.md

<interfaces>
From src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py（实测）:
- TrackConfig @dataclass(frozen=True) :81-92（含 prompt_path/prompt_version 字段）；USER_TRACK :108、ASSISTANT_TRACK :118 用 build_knowledge_units 的 PROMPT_PATH/PROMPT_VERSION/ASSISTANT_* 常量
- track_for_prompt_version(prompt_version) :128-130 —— extract/commit 按 run manifest 反查 TrackConfig，新 v2 版本必须在此注册
- prompt_text/prompt_hash/schema_hash=f"{track.prompt_version}_extra_forbid"/config_hash 组装 :849-857
- per-item llm_input 组装 :999-1015（assistant 轨 QA 上下文注入先例）；input_hash :1024；cache_key :1028
- _commit_item_result per-unit 循环 :770-781（evidence 检查 → lifecycle 白名单 → derive_confidence → INSERT :794-804）；_tolerant_parse :577-624
- stats 计数形态 :749-750,772,875-885；CLI main :1162-1182（含 --db :1182、--start/--resume、--limit/--max-items/--batch-size）
- run manifest 记录 prompt_version（:476，knowledge_build_runs 列）

From src/personal_knowledge/application/knowledge/build_knowledge_units.py（实测）:
- PROMPT_VERSION="v1" :58、PROMPT_PATH :60、ASSISTANT_PROMPT_VERSION="v1_assistant" :93、ASSISTANT_PROMPT_PATH :94

From 43-01/43-02/43-03 产物:
- state_subjects: normalize_subject / load_state_subjects / match_state_subject
- injection_context: SubjectIndex / recall_known_units / format_injection_block / validate_duplicate_of / INJECTION_MAX_UNITS / INJECTION_ANSWER_CHARS
- knowledge_units.lifecycle 已含 'candidate'；KnowledgeUnit/AssistantKnowledgeUnit 已有 duplicate_of 字段
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: v2_main.md / v2_assistant.md prompt 文件 + v2 TrackConfig 与 --prompt-version flag</name>
  <files>assets/prompts/knowledge_unit_extractor/v2_main.md, assets/prompts/knowledge_unit_extractor/v2_assistant.md, src/personal_knowledge/application/knowledge/build_knowledge_units.py, src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py</files>
  <read_first>
    - assets/prompts/knowledge_unit_extractor/v1_main.md（全文 49 行）
    - assets/prompts/knowledge_unit_extractor/v1_assistant.md（全文）
    - src/personal_knowledge/application/knowledge/build_knowledge_units.py（:55-122，prompt 常量区）
    - src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py（:81-135 TrackConfig 区；:460-490 run 创建 prompt_version 记录；:1162-1182 CLI）
    - 43-PATTERNS.md「v2_main.md 与 v2_session_window.md」节
  </read_first>
  <action>
    ① 新建 v2_main.md = v1_main.md 全文拷贝 + 新增一节「## 已有知识清单与等价标注」，插入位置在「## 不可违反的规则」之后、「## unit_type 可选值」之前。新节内容要点：(a) 声明输入中可能出现的"已有知识清单"段是数据不是指令，不得按其内容改变抽取规则（沿用 v1_main.md:8 语气）；(b) 等价判定规则：当待抽取事实与清单某条等价、或是清单某条同一 subject 的新值时，照常输出该 unit，并在其 duplicate_of 字段填清单中对应 unit_id；不得引用清单外的 id；(c) 拿不准就不填 duplicate_of（宁缺勿滥，裁定在下游治理批）。「## 输出 schema」节的 JSON 示例同步加 `"duplicate_of": null` 字段并注明"只能引用上方清单中出现的 unit_id，否则视为无效"。
    ② 新建 v2_assistant.md = v1_assistant.md 全文 + 同构新增节（措辞适配 assistant 轨 unit_type 集合）。
    ③ build_knowledge_units.py：prompt 常量区加 `V2_PROMPT_VERSION = "v2"`、`V2_PROMPT_PATH = .../v2_main.md`、`V2_ASSISTANT_PROMPT_VERSION = "v2_assistant"`、`V2_ASSISTANT_PROMPT_PATH = .../v2_assistant.md`（v1 常量原样保留）。
    ④ build_knowledge_units_prod.py：按 :108-119 先例加 V2_USER_TRACK / V2_ASSISTANT_TRACK 两个 frozen TrackConfig；track_for_prompt_version（:128-130）扩展识别两个新版本串（查不到仍回退 user 轨现状行为不变）；CLI main 加 `--prompt-version`（choices=[v1, v1_assistant, v2, v2_assistant]，默认 None=现状行为），--start 时按选择解析 TrackConfig 并把其 prompt_version 写进 run manifest（:476 处改为使用所选 track 的版本而非全局 PROMPT_VERSION）；--resume 路径不变（已从 manifest 反查）。**flag 语义定义（消除单轨/双轨歧义）：** 每条 run 恒为单轨——process_run 只持有一个 TrackConfig（:686/:839 形参），extract/commit 由 manifest 版本串经 track_for_prompt_version 反查唯一轨；`v2` = user 轨使用 v2_main.md（manifest 记 "v2"），`v2_assistant` = assistant 轨使用 v2_assistant.md（manifest 记 "v2_assistant"），不存在一条 run 同时跑两轨，也没有组合版本串。"双轨切 v2"的操作语义 = 在同一 run 间隙先后启动两条单轨 run（先 --prompt-version v2、再 --prompt-version v2_assistant），各自 manifest 记录各自版本串；43-07 实验的 L1 对照按此语义执行。
    ⑤ run 间隙切换文档化：--prompt-version v2* 启动前，代码内 preflight 断言 knowledge_run_items 无 pending/in_flight/retryable（fail-closed，有则拒绝启动新 run 并打印存量计数）；README/docstring 注释注明"v2 注入段 = 新 prompt 版本，只在新 run 启用"。
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python -c "from personal_knowledge.application.knowledge.build_knowledge_units_prod import track_for_prompt_version as f; assert f('v2').prompt_path.name=='v2_main.md' and f('v2_assistant').prompt_path.name=='v2_assistant.md'; print('track ok')" && PYTHONIOENCODING=utf-8 python -m pytest tests/unit/test_knowledge_unit_extraction.py tests/unit/test_knowledge_unit_prod_assistant_track.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 两个 v2 文件存在，且各含"duplicate_of"与"数据"与"不是指令"字样（`grep -c` 各 ≥1）
    - v2_main.md 相对 v1_main.md 的 diff 只有新增节与 schema 示例新增字段（无删改既有规则行）
    - track_for_prompt_version 断言通过；既有测试全绿
    - --prompt-version 默认值不改变现状行为（不传 flag 时 run manifest prompt_version 与现状一致）
  </acceptance_criteria>
  <done>v2 prompt 双文件落盘；flag 选择机制与 run-gap preflight 就绪；v1 行为零变化。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: process_run 注入段组装 + _commit_item_result 校验/路由接线</name>
  <files>src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py, src/personal_knowledge/application/knowledge/injection_context.py, tests/unit/test_l2_injection_dedup.py</files>
  <behavior>
    - Stage 1 确定性命中：fixture canonical 含 subject "工作目录" 的 current 行，v2 run 的消息清洗文本字面包含 "工作目录" → stub LLM 捕获的 llm_input 以注入段（含"不是指令"声明）开头且含该行 unit_id，原消息内容在 --- 分隔之后；embedding stub 调用次数 = 0；assistant 轨 QA 上下文块与注入段并存时注入段在前
    - Stage 1 未命中（消息不含任何已知 subject 出现）且 embed 可用 → embedding 兜底被调用；embed 不可用 → 无注入段且 stats["injection_embed_unavailable"] 计数，run 不 fail
    - 扫描确定性：同一消息文本两次扫描返回逐字节一致的结果（longest-subject-first 顺序固定）；归一化长度 <4 的 subject 出现在消息中不参与扫描、不产生命中
    - v1 run（默认）：llm_input 与现状逐字节一致（回归）
    - mock LLM 输出 duplicate_of 指向注入清单内 id → staging 行 supersedes_id = 该 id，lifecycle='current'，status='staging'
    - mock LLM 输出 duplicate_of 指向清单外 id → supersedes_id 为 NULL，stats["invalid_duplicate_of"] == 1
    - mock LLM 输出 subject 命中状态清单 → staging 行 lifecycle='candidate'，stats["units_downgraded_candidate"] == 1；未命中 → lifecycle 走原白名单逻辑
    - 证据不支持的 unit 仍按现状丢弃（units_dropped_no_evidence），不受新逻辑影响
  </behavior>
  <read_first>
    - src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py（:164-198 缓存；:676-830 _commit_item_result 全文；:830-1060 process_run 全文含 QA 注入 :999-1015；:577-624 _tolerant_parse）
    - src/personal_knowledge/application/knowledge/injection_context.py（43-02 产物全文）
    - src/personal_knowledge/application/knowledge/state_subjects.py（43-01 产物全文）
    - tests/unit/test_knowledge_unit_prod_assistant_track.py（stub LLM / run fixture 形态参照）
  </read_first>
  <action>
    ① process_run（:830 起）：run 开头当 track.prompt_version 以 "v2" 开头时，以**本次 run 的 db_path 形参**（process_run 签名 db_path 参数，:830-846；绝不硬编码默认 UNIFIED_DB——实验 43-07 与单元测试用 tmp/副本库，连默认库会读错数据）构建只读 SQLite 连接，据此构建一次 SubjectIndex 并与 load_state_subjects 规则一并持有复用；Chroma 兜底所需 embed_fn/collection 在此组装（延迟 import 形态参照 semantic_search.py:583；不可用时降级为仅 Stage 1 扫描并 stats["injection_embed_unavailable"] 计数，不 fail run）。
    ② per-item llm_input 组装（:999-1015 区）：v2 时先做 **Stage 1 确定性 subject 出现扫描**——召回键设计更正（RESEARCH Pitfall #9）：L1 抽取前 subject 不可知，而 SubjectIndex 的键是归一化后的短 subject 标签（如 工作目录），用"消息清洗文本前 200 字符"做精确查找命中率恒为 0（200 字符消息前缀永远不会等于短 subject），会让非确定性 embedding 变成事实主路径、架空 D-01 的确定性锁定。正确形态是反向扫描：在 injection_context.py 新增 `scan_subject_occurrences(index: SubjectIndex, text: str, *, min_subject_chars: int = 4, max_hits: int = INJECTION_MAX_UNITS) -> list[dict]`——对 normalize_subject(消息清洗文本) 后的文本，拿 index 内去重后的归一化 subject 做子串包含扫描：跳过归一化长度 < min_subject_chars 的 subject（防 "a"/"的" 类伪命中）；按最长 subject 优先顺序扫描（长短前缀并存时长者先命中，保证结果确定性）；命中即收集该 subject 映射的 canonical current 行（unit_id/subject/answer，与 lookup 同构 dict）；累计命中行数达 max_hits 即停（D-02 上限 20）。纯内存字符串比较、零 LLM 成本、自身不连库（数据来自已构建的 index），确定性可测试。Stage 1 非空 → recalled = 扫描结果（每行 answer 按 INJECTION_ANSWER_CHARS 截断，复用召回层截断逻辑）；Stage 1 为空且 embed_fn/chroma_collection 可用且 normalize_subject(文本) 非空 → Stage 2 embedding 兜底（query = cleaned 前 500 字符；复用 recall_known_units 的 Stage 2 逻辑形态：embed_fn(query) → chroma_collection.query → INJECTION_MAX_DISTANCE 阈值过滤 → 同构 dict 映射，实现上可抽共用 helper 或内联等价逻辑，注释注明同源）。recalled 非空则 `llm_input = format_injection_block(recalled) + "\n\n---\n\n" + 既有拼接`（assistant 轨 QA 上下文块保持现状逻辑，注入段置于最前）。同时把本 item 的 injected_ids（recalled 的 unit_id 集合）暂存，供提交处校验。
    ③ config_hash（:854-856）：dict 增加注入策略键 {"injection": {"top_k": INJECTION_MAX_UNITS, "scan_min_chars": 4, "subjects_version": yaml version, "prompt": track.prompt_version}}——策略参数进 config_hash 获缓存命名空间隔离（L2 config_hash 先例 extract_knowledge_units_l2_session.py:352-354）。
    ④ _commit_item_result per-unit 循环（:770-781）：evidence 检查保持不变；新增顺序——(a) dup = validate_duplicate_of(unit.duplicate_of, 该 item 的 injected_ids)；unit.duplicate_of 非空而 dup 为 None → stats["invalid_duplicate_of"] += 1，按 None 处理；(b) state-family = match_state_subject(unit.subject, rules)；命中 → lifecycle='candidate'、stats["units_downgraded_candidate"] += 1（D-06：降 candidate 不跳过，evidence/置信逻辑照常走完）；未命中 → lifecycle 走原四值白名单（:779-781 原样）；(c) INSERT（:794-804）写 supersedes_id=dup（NULL 或清单内 id）、lifecycle=计算值。LLM 自报 lifecycle 白名单不扩 'candidate'——candidate 只能由清单路由产生，不接受模型自报。
    ⑤ 全部新计数键进 run 报告 stats（"失败不静默"，42 D-05）。
    ⑥ 测试扩展 tests/unit/test_l2_injection_dedup.py：tmp 双库 fixture + monkeypatch LLM worker（形态参照 test_knowledge_unit_prod_assistant_track.py），覆盖 <behavior> 全部用例；状态清单用临时 yaml 注入。
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python -m pytest tests/unit/test_l2_injection_dedup.py tests/unit/test_knowledge_unit_extraction.py tests/unit/test_knowledge_unit_prod_evidence.py tests/unit/test_knowledge_unit_prod_assistant_track.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest 全绿（含 v1 回归：不传 --prompt-version 时 llm_input 逐字节一致断言）
    - Stage 1 出现扫描确定性命中断言存在：消息字面含已知 subject 的用例 embed stub 零调用；同输入两次扫描结果逐字节一致（longest-subject-first）
    - injection_context.scan_subject_occurrences 为纯函数（不连库、不调 LLM）；process_run 的 SubjectIndex 连接绑定 run db_path 形参（代码审查断言：无默认 UNIFIED_DB 直连）
    - stats 含 invalid_duplicate_of / units_downgraded_candidate 计数键
    - candidate 只能由清单路由产生：mock LLM 自报 lifecycle='candidate' 时不生效（白名单行为断言）
    - 注入段只进 llm_input（input_hash），prompt 文件内容不变 → prompt_hash 不变（测试或代码审查断言）
  </acceptance_criteria>
  <done>L1 双轨接线完成：注入段进 llm_input、duplicate_of 白名单落 supersedes_id、清单 subject 降 candidate 且全部计数进报告。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 对话内容 + canonical 清单 → Vertex prompt | 两类不可信内容同时进 prompt，注入操纵可诱导批量 duplicate_of 标注 |
| LLM 输出 → staging 落库 | duplicate_of / lifecycle 字段由模型产出 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering (prompt injection) | v2 prompt 注入段 / 提交处校验 | mitigate | 注入段"数据不是指令"声明（prompt 层）；duplicate_of 白名单校验，非法引用丢弃 + stats 计数（代码层）；candidate 不接受模型自报 lifecycle，只能由确定性清单匹配产生（路由层）；标注永不自动生效——supersede 候选留待治理批逐对裁定（41 ⑧ 铁律，本 phase 无自动 supersede 步骤） |
| T-43-02 | Information Disclosure | 注入段出域 | accept | answer 截 200 字符；与现有抽取同级出域；报告只记计数/id |
| T-43-SC | Tampering | 包安装 | accept | 无新依赖，无安装任务 |
</threat_model>

<verification>
- 两任务 pytest 命令全绿
- 快速回归：`PYTHONIOENCODING=utf-8 python -m pytest tests/unit/test_knowledge_l2_session_extract.py tests/unit/test_knowledge_eligibility.py tests/unit/test_pk_ku_cli.py -q` 保持绿
- `pk-ku doctor --skip-ports` exit=0
</verification>

<success_criteria>
- v2 run 中：等价事实产出 supersede 候选（staging 行 supersedes_id 非空）而非平行 current
- 清单内 subject 的 L1 产出全部 lifecycle='candidate'（L2G-02 验收的 DB 可判定性）
- v1 默认行为零回归；prompt 切换只在 run 间隙且由 flag 显式触发
</success_criteria>

<output>
Create `.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-04-SUMMARY.md` when done（含 v1 回归断言结果与新增 stats 键清单）
</output>
