---
phase: 43-l2-scope-redefinition-cross-turn-state-ownership-and-increme
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personal_knowledge/application/knowledge/history_knowledge_units.py
  - src/personal_knowledge/retrieval/unified_search.py
  - src/personal_knowledge/retrieval/semantic_search.py
  - tests/unit/test_history_knowledge_units.py
  - tests/contract/test_knowledge_search_contracts.py
autonomous: true
requirements: [L2G-03]

must_haves:
  truths:
    - "pk-ku history --subject 对 ≥2 次变更的状态类 subject 输出完整 supersede 链且唯一一行标注「← 当前值」"
    - "rag-search semantic 子命令有 --current-only flag，检索结果恒为 lifecycle='current'（flag 显式化既有默认行为）"
    - "全程零 SQLite/Chroma schema 新字段（当前值 = 派生字段，不落库）"
  artifacts:
    - path: "src/personal_knowledge/application/knowledge/history_knowledge_units.py"
      provides: "HistoryRow.is_current_value 派生字段 + format_table「← 当前值」标注"
      contains: "is_current_value"
    - path: "src/personal_knowledge/retrieval/unified_search.py"
      provides: "semantic 子命令 --current-only flag"
      contains: "--current-only"
    - path: "src/personal_knowledge/retrieval/semantic_search.py"
      provides: "search_knowledge_units 显式 current_only 形参（契约文档化）"
      contains: "current_only"
  key_links:
    - from: "history_knowledge_units.list_history_for_subject"
      to: "canonical_knowledge_units.supersedes_id 链"
      via: "派生计算（链上唯一 lifecycle='current' 行 = 当前值）"
      pattern: "is_current_value"
    - from: "unified_search semantic subparser"
      to: "semantic_search.search_knowledge_units"
      via: "--current-only → current_only 形参透传"
      pattern: "current_only"
---

<objective>
落地 L2G-03：复用现有 supersede 链的"当前值/历史值"查询语义，零 schema 新字段（D-07 两个 CLI 落点）。

Purpose: 查询侧能区分"某 subject 的当前值 vs 历史值"是 L2 状态管辖语义的可读出口；不做 Cockpit 端点（D-07 明确，projection 层后续自然消费）。
Output: history 当前值标注、rag-search --current-only flag、两侧测试。

**Planner 决策记录（RESEARCH Open Question #3 / Pitfall #5 — rag-search 路线二选一）：采纳最小路线。**
- 现状实测：索引构建只收 `lifecycle='current'`（build_knowledge_unit_vector_store.py:226），检索层再硬过滤非 current（semantic_search.py:651-653）——检索结果本就 current-only。
- 因此 D-07 的"对 superseded/deprecated 降权"在现状下被**排除**（exclusion = 极限降权）所覆盖；`--current-only` 落地为**显式声明既有默认行为的 flag** + backend 显式形参（契约文档化），不动索引 WHERE、不动打分。
- "查询接口返回唯一当前值 + 完整历史链"的验收由 `pk-ku history --subject` 扩展承担（43-SPEC acceptance 未点名 rag-search 必须含历史值）。
- 若未来验收要求语义检索结果内出现降权的历史值，再启动"索引纳入 superseded + 降权"路线（改动点：build_knowledge_unit_vector_store.py:226 WHERE + semantic_search.py:658 score），届时索引重建 + canary + promote 全流程重走——本 phase 明确不做。
- **锁定决策替代实现的显式声明：** D-07 字面要求"rag-search 对 superseded/deprecated 降权"，本 plan 以"排除 = 极限降权（现状本就 current-only）"实现——这是对已锁定决策的**替代实现而非字面实现**。理由充分（RESEARCH OQ-3 / Pitfall #5 背书、默认行为已 current-only），但按流程必须在 phase 收尾时向用户显式提出本偏离并获确认；此义务同步写入 <output> 的 SUMMARY 要求，执行期不得静默带过。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-SPEC.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-PATTERNS.md

<interfaces>
From src/personal_knowledge/application/knowledge/history_knowledge_units.py（PATTERNS 核对）:
- HistoryRow dataclass :33-46（unit_id/subject/unit_type/lifecycle/supersedes_id/confidence/created_at/answer_snippet/version/status/question/lifecycle_events）
- list_history_for_subject :72-173（按 subject 出全 lifecycle 链，created_at DESC；GROWTH_LINE_LIFECYCLES :26-28 容忍 corrected/historical）
- format_table :205-223（lifecycle 列后 = 标注追加位置）
- 只读连接铁律 :103-104（mode=ro + row_factory）

From src/personal_knowledge/retrieval/unified_search.py:113-126（PATTERNS 核对）:
- semantic 子命令参数区（query/--top-k/--dedup/--json 等），_cli :61 入口

From src/personal_knowledge/retrieval/semantic_search.py（PATTERNS 核对）:
- search_knowledge_units(query, top_k, source, include_evidence, collection_override, fallback_policy, allow_legacy_pad) :472-480
- 知识层循环 :650-661 硬过滤 `if lc not in ("current",): continue`（:651-653）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: history --subject「← 当前值」标注（派生字段，零 schema 变更）</name>
  <files>src/personal_knowledge/application/knowledge/history_knowledge_units.py, tests/unit/test_history_knowledge_units.py</files>
  <behavior>
    - fixture：subject "工作目录" 链 = 3 行（current 1 行 + superseded 2 行，supersedes_id 串链）→ format_table 输出恰好 1 行含「← 当前值」，且该行为 lifecycle='current' 行
    - fixture：subject 全部行均 superseded/deprecated（无当前值，异常态）→ 零标注且不报错（链完整可见即可）
    - JSON 输出路径（如现有 --json）行 dict 含 is_current_value 布尔键
    - 零 schema 断言：PRAGMA table_info(canonical_knowledge_units) 列集合与既有 11 列完全一致（无新增列）
  </behavior>
  <read_first>
    - src/personal_knowledge/application/knowledge/history_knowledge_units.py（:1-60 常量与 HistoryRow；:72-173 查询；:205-240 渲染与 main）
    - tests/unit/test_history_knowledge_units.py（现有 fixture 形态）
    - 43-PATTERNS.md「history_knowledge_units.py」节
  </read_first>
  <action>
    history_knowledge_units.py：
    ① HistoryRow（:33-46）加派生字段 `is_current_value: bool = False`（纯内存派生，不落库）。
    ② list_history_for_subject（:72-173）查询后、返回前计算：链内 lifecycle=='current' 且未被链内其他行经 supersedes_id 指向为"被取代者"的行置 True（正常态恰一行；多行 current 的异常态全部标注并在 docstring 注明——治理侧 conflict 另议，不在此隐藏）；行 dict 序列化处同步输出该键。
    ③ format_table（:205-223）：lifecycle 列后追加「← 当前值」标记（is_current_value 为 True 时），列宽对齐微调；人类可读输出之外不改变任何既有列。
    保持只读连接（:103-104）与参数化 SQL 不变；不新增任何 DB 写入。
    测试扩展 tests/unit/test_history_knowledge_units.py：覆盖 <behavior> 全部用例（fixture 沿用该文件现有 tmp 库形态；零 schema 断言用 PRAGMA table_info 列集合比对）。
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python -m pytest tests/unit/test_history_knowledge_units.py -q && PYTHONIOENCODING=utf-8 python -m personal_knowledge.application.ku history --subject 工作目录 --limit 5（exit=0，只读）</automated>
  </verify>
  <acceptance_criteria>
    - pytest 全绿；CLI smoke exit=0
    - history_knowledge_units.py 无 INSERT/UPDATE/DELETE（grep 断言只读面不破）
    - 链上当前值标注唯一性由测试断言（≥2 次变更 subject fixture）
  </acceptance_criteria>
  <done>history CLI 对状态类 subject 输出唯一「← 当前值」标注 + 完整历史链；零 schema 新字段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rag-search --current-only flag（显式化既有默认）+ 契约测试</name>
  <files>src/personal_knowledge/retrieval/unified_search.py, src/personal_knowledge/retrieval/semantic_search.py, tests/contract/test_knowledge_search_contracts.py</files>
  <behavior>
    - search_knowledge_units(..., current_only=True)（默认）与显式传 --current-only 的 CLI 输出逐条一致（flag 是既有默认的显式化）
    - 所有返回行的 lifecycle == 'current'（含 flag 与不 flag 两种调用）
    - current_only=False 时结果仍全为 current（索引只含 current，Pitfall #5）——docstring 与 flag help 文本如实注明该现状语义
    - CLI --help 含 --current-only 条目
  </behavior>
  <read_first>
    - src/personal_knowledge/retrieval/unified_search.py（:55-140 _cli 与 semantic 子命令参数区）
    - src/personal_knowledge/retrieval/semantic_search.py（:472-500 签名与 docstring；:640-665 知识层过滤循环）
    - tests/contract/test_knowledge_search_contracts.py（现有契约 fixture 形态）
    - 43-PATTERNS.md「unified_search.py + semantic_search.py」节
  </read_first>
  <action>
    ① semantic_search.py：search_knowledge_units 签名（:473-480）加关键字形参 `current_only: bool = True`，docstring 如实写明：当前索引与检索双层均只放行 lifecycle='current'，此参数是默认行为的显式契约；current_only=False 目前不改变结果（索引不含非 current），保留该形参是为未来"索引纳入历史值 + 降权"路线预留的稳定 API。知识层循环（:650-661）硬过滤逻辑不变。
    ② unified_search.py：semantic 子命令（:113-126）加 `--current-only`（action="store_true"，help 注明"仅返回当前值 unit（现行默认行为，flag 用于显式声明）"）；调用 search_knowledge_units 处透传 current_only=True（无论 flag 与否都传 True，保持语义诚实——flag 是声明而非开关）。
    ③ 契约测试扩展 tests/contract/test_knowledge_search_contracts.py：覆盖 <behavior> 全部用例（含 flag/不 flag 输出一致性、全 current 断言、--help 文本断言）。
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python -m pytest tests/contract/test_knowledge_search_contracts.py tests/unit/test_vector_store_filter.py -q && PYTHONIOENCODING=utf-8 rag-search semantic "工作目录" --current-only --top-k 3（exit=0，结果全 current）</automated>
  </verify>
  <acceptance_criteria>
    - pytest 全绿；CLI smoke exit=0
    - build_knowledge_unit_vector_store.py:226 的 WHERE 子句与 semantic_search.py:651-653 的硬过滤**零改动**（git diff 断言该两文件无变更）
    - --current-only flag 出现在 unified_search.py（grep 断言）
  </acceptance_criteria>
  <done>--current-only 作为显式契约落地；索引与过滤层未动；最小路线决策完整文档化于 docstring。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 输入（subject/query）→ SQLite/Chroma | 用户输入进入只读查询 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | Tampering (SQL injection) | history / search 查询 | mitigate | 沿用现有参数化绑定（history :115-133 先例）；本 plan 不新增任何拼接 SQL；只读连接（mode=ro）不变 |
| T-43-01 | Tampering (prompt injection) | — | accept | 本 plan 无 LLM 面 |
| T-43-SC | Tampering | 包安装 | accept | 无新依赖，无安装任务 |
</threat_model>

<verification>
- 两任务 pytest/CLI 命令全绿
- 回归：`PYTHONIOENCODING=utf-8 python -m pytest tests/unit/test_history_knowledge_units.py tests/contract/test_knowledge_search_contracts.py tests/unit/test_vector_store_filter.py -q` 全绿
- `pk-ku doctor --skip-ports` exit=0
</verification>

<success_criteria>
- 对 ≥2 次变更的状态类 subject，`pk-ku history --subject` 返回唯一「← 当前值」+ 完整历史链（L2G-03 acceptance 原文）
- 未新增任何 SQLite/Chroma schema 字段（L2G-03 硬性边界）
- rag-search --current-only 语义诚实文档化（排除即极限降权）
</success_criteria>

<output>
Create `.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-06-SUMMARY.md` when done（含最小路线决策复述与"索引纳入历史值"未来路线的触发条件；**必须含"D-07 替代实现待用户确认"节**——"排除 = 极限降权"是对 D-07 字面"降权"要求的替代实现，phase 收尾时须把本偏离显式提交用户确认，不得静默带过）
</output>
