---
phase: 43-l2-scope-redefinition-cross-turn-state-ownership-and-increme
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/migrations/add_candidate_lifecycle.py
  - src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py
  - src/personal_knowledge/application/knowledge/publish_incremental_run.py
  - src/personal_knowledge/application/knowledge/promote_units.py
  - src/personal_knowledge/application/ku.py
  - tests/unit/test_publish_candidate_exclusion.py
  - tests/unit/test_promote_units.py
autonomous: true
requirements: [L2G-02, L2G-04]

must_haves:
  truths:
    - "knowledge_units.lifecycle 合法值含 'candidate'（CHECK 整表重建迁移后），既有 39,880+ 行数据零丢失、FK 完整"
    - "publish_incremental_run 的 staging→current 翻转不再触碰 lifecycle='candidate' 行，且报告含 candidate 排除计数"
    - "pk-ku promote-units 可按 unit 人工转正（dry-run 默认、--write 前快照、单事务），转正前 re-match quote 到 eligible 消息（D-09）"
  artifacts:
    - path: "tools/migrations/add_candidate_lifecycle.py"
      provides: "lifecycle CHECK 加 'candidate' 的整表重建迁移（dry-run/--write/快照/单事务）"
    - path: "src/personal_knowledge/application/knowledge/promote_units.py"
      provides: "per-unit 转正逻辑（re-match 预检 + 双表翻转 + 报告）"
      exports: ["promote_units", "rematch_unit_evidence"]
    - path: "src/personal_knowledge/application/ku.py"
      provides: "pk-ku promote-units 子命令注册与转调（`promote` 名已被 ku.py:250 既有 candidate 索引转正 parser 占用，新通道命名 promote-units 避冲突）"
      contains: "promote-units"
    - path: "tests/unit/test_publish_candidate_exclusion.py"
      provides: "publish 排除 candidate 回归测试"
    - path: "tests/unit/test_promote_units.py"
      provides: "promote dry-run/--write/re-match 失败路径测试"
  key_links:
    - from: "src/personal_knowledge/application/knowledge/publish_incremental_run.py"
      to: "knowledge_units.lifecycle='candidate'"
      via: "UPDATE ... WHERE run_id=? AND status='staging' AND lifecycle<>'candidate'"
      pattern: "lifecycle<>'candidate'"
    - from: "src/personal_knowledge/application/knowledge/promote_units.py"
      to: "agent_conversations.sqlite canonical_messages"
      via: "quote[:40] instr 探针（参数化）+ _evidence_supported 回查 + compute_eligible_messages 复核"
      pattern: "instr\\(content, \\?\\)"
---

<objective>
落地 candidate 的落库形态（D-06）+ publish 排除（Pitfall #7）+ candidate→current / 存量真知识的人工转正通道（D-09，RESEARCH OQ-6）。

Purpose: L2G-02 要求"L1 产出可识别为 candidate 且不被 publish 一并转正"；L2G-04 的"保留转正"档与 D-06 的人工转正通道共用同一个 per-unit 转正接口。
Output: 迁移脚本、publish 排除条件、`pk-ku promote-units` 子命令、两侧测试。

**Planner 决策记录（RESEARCH Open Question #1 — candidate 落库形态三选一）：选 ① CHECK 整表重建，给 `knowledge_units.lifecycle` CHECK 增加 'candidate'。** 理由：
- (b) 复用 canonical `status='review'` 不成立：candidate 判定发生在 L1 提交写入 `knowledge_units` 时，而 `knowledge_units.status` CHECK（:110）无 'review' 值，照样要迁移，且 status 语义（流转状态）与 lifecycle 语义（时效状态）会双轨污染。
- (c) `supersedes_id` 侧车不成立：状态类 subject 降级时没有 supersede 目标 id（supersedes_id 留给 D-03 的 supersede 候选标记用），无键可标，且语义不可 SQL 查询（L2G-02 验收要"L1 current 新增=0 或全部 candidate 标记"的 DB 可判定性）。
- (a) 成本可控：**实测只有 `knowledge_units.lifecycle`（migrate:105）有 CHECK；`canonical_knowledge_units.lifecycle`（migrate:133）无 CHECK** ——整表重建只涉及一张表，FK 坑面减半，且 lifecycle_events.py:130-180 有含三个 FK 坑注释的现成先例。
- SPEC 约束区分：这是 lifecycle **合法值扩展**（既有列的 CHECK 值域 +1），不是新增 SQLite/Chroma schema 字段——L2G-03 的"零新字段"承诺不受影响。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-SPEC.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-PATTERNS.md

<interfaces>
From src/personal_knowledge/application/knowledge/lifecycle_events.py:130-180（CHECK 整表重建先例，FK 三坑注释 :131-138）:
- PRAGMA foreign_keys=OFF → PRAGMA legacy_alter_table=ON → RENAME 旧表 → executescript(新 DDL) → INSERT SELECT → DROP → **先 commit 再恢复 PRAGMA**（事务内 PRAGMA 恢复静默无效）。

From src/personal_knowledge/application/knowledge/publish_incremental_run.py:95-108（实测）:
- UPDATE knowledge_units SET status='current' WHERE run_id=? AND status='staging'
- UPDATE canonical_knowledge_units SET status='current' WHERE run_id=? AND status='staging'

From src/personal_knowledge/application/ku.py:346-348,833-848,873-986（PATTERNS 核对）:
- 子命令注册（:346-348 history 例）；_cmd_history 转调形态（argv 拼装 + main(argv)）；_cmd_watermark 的 dry-run 默认 + --write 持久语义（:945-962）。

From var/tmp/relink_orphan_evidence.py:27-31（re-match 探针，41 实操 1237 条成功）:
- probe = quote[:40]；SELECT canonical_message_id FROM canonical_messages WHERE instr(content, ?) > 0 LIMIT 1（参数化）。

From src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py:557-571:
- _evidence_supported(quote, source)：10 字连续窗口严格回查。

From src/personal_knowledge/application/knowledge/eligibility.py:109-228:
- compute_eligible_messages(AGENT_CONVERSATIONS_DB) → eligible ref 集合（纯 SQL 不可复现，必须 Python 侧产出）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: add_candidate_lifecycle.py 迁移脚本 + 基础 DDL 同步加 'candidate'</name>
  <files>tools/migrations/add_candidate_lifecycle.py, src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py</files>
  <read_first>
    - src/personal_knowledge/application/knowledge/lifecycle_events.py（:125-185，ensure_lifecycle_schema 全文 + FK 三坑注释）
    - tools/migrations/backfill_ku_data_debts.py（:1-60，脚本标准形态 + 快照条款 docstring）
    - src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py（:95-172，knowledge_units 全 DDL + 索引清单）
    - 43-PATTERNS.md「candidate 落库（schema 决策点）」节
  </read_first>
  <action>
    ① migrate_add_knowledge_unit_tables.py:105：lifecycle CHECK 改为 `CHECK(lifecycle IN ('current','deprecated','superseded','conflict','candidate'))`——新库/测试库直接获得新值域（canonical_knowledge_units.lifecycle :133 无 CHECK，不动）。
    ② 新建 tools/migrations/add_candidate_lifecycle.py（存量库迁移，形态仿 backfill/salvage 脚本）：
    - docstring 写明用法（默认 dry-run；`--write` 落库）与安全条款（写前 cp UNIFIED_DB → var/backups/personal_system_<UTC ts>.sqlite；全程单事务）。
    - dry-run：只读检查——打印当前 lifecycle 值分布、是否已含 'candidate'（幂等：已迁移则报 already_applied exit=0）、FK 完整性预检。
    - --write：严格按 lifecycle_events.py:130-180 先例执行——PRAGMA foreign_keys=OFF；PRAGMA legacy_alter_table=ON；ALTER TABLE knowledge_units RENAME TO knowledge_units_pre_candidate；executescript(新 knowledge_units DDL + 其全部索引，与 ① 同步后的 SCHEMA 保持一致)；INSERT INTO knowledge_units SELECT *（列清单显式枚举，不用 SELECT * 防列序漂移）；DROP TABLE knowledge_units_pre_candidate；**先 con.commit() 再在事务外恢复 PRAGMA**；收尾跑 PRAGMA foreign_key_check 与行数守恒校验（迁移前后 COUNT(*) 相等、39,880+ 零丢失），任一失败即 rollback 并报错。
    - canonical_knowledge_units 不重建（无 CHECK）；但 dry-run/--write 报告里各打印一句说明，防误读。
    - 执行器流程：先在 var/tmp/ 的 DB 副本上 --write 验证（FK check + candidate 写入冒烟），再对真实 UNIFIED_DB dry-run → 快照 → --write。迁移窗口与任何 publish/extract run 互斥（执行前确认 knowledge_run_items 无 pending/in_flight）。**Phase 43 执行序约束（wave 1 并发安全）：** 整表重建 --write 与 wave 1 其他读 KU DB 的任务（43-01 Task 2 suggest 读 canonical subject、43-06 history/search CLI smoke）并发不安全——执行器必须**先单独完成本任务**（副本验证 + 真实库迁移 + doctor），再启动 wave 1 其余任务；frontmatter wave 编号不变（wave = 依赖序，本条是同 wave 内的显式串行化条款）。
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python tools/migrations/add_candidate_lifecycle.py --dry-run（exit=0）&& PYTHONIOENCODING=utf-8 python -m pytest tests/unit/test_knowledge_eligibility.py tests/integration/test_knowledge_incremental_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 副本库 --write 后：`PRAGMA foreign_key_check` 零违规；迁移前后 knowledge_units COUNT(*) 相等；可 INSERT 一行 lifecycle='candidate' 再 DELETE 冒烟成功
    - 真实库迁移后 `pk-ku doctor --skip-ports` exit=0
    - 既有测试（eligibility + incremental pipeline，内部重建 SCHEMA）全绿
    - 迁移脚本含 var/backups/ 快照步骤与 BEGIN IMMEDIATE/单事务（grep 断言）
  </acceptance_criteria>
  <done>'candidate' 成为 knowledge_units.lifecycle 合法值；存量数据零丢失；doctor 绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: publish_incremental_run 排除 candidate + 回归测试</name>
  <files>src/personal_knowledge/application/knowledge/publish_incremental_run.py, tests/unit/test_publish_candidate_exclusion.py</files>
  <behavior>
    - fixture run 含 3 条 staging：lifecycle 分别 current/conflict/candidate → publish 后前两条 status='current'，candidate 行仍 status='staging' 且 lifecycle 不变
    - canonical_knowledge_units 侧同规则排除
    - publish report dict 含 candidates_excluded 计数（= 被排除的 candidate 行数，两表分别计）
    - 无 candidate 行的 run 行为与现状完全一致（回归）
  </behavior>
  <read_first>
    - src/personal_knowledge/application/knowledge/publish_incremental_run.py（:1-134 全文，重点 :58-72 计数段与 :95-108 UPDATE 段）
    - tests/integration/test_knowledge_incremental_pipeline.py（:190,396-416，tmp DB fixture 形态）
    - 43-PATTERNS.md「publish_incremental_run.py」节（Pitfall #7）
  </read_first>
  <action>
    publish_incremental_run.py：两条 UPDATE（:98-99 knowledge_units、:104-105 canonical_knowledge_units）的 WHERE 各追加 `AND lifecycle<>'candidate'`；在 UPDATE 前各加一条 SELECT COUNT(*)（同 WHERE 条件）写入 report["candidates_excluded"]={"knowledge_units": n1, "canonical": n2}（"失败不静默"计数形态，参照现有 :58-72 的 staging/current 计数）。canonical 表无 lifecycle CHECK，lifecycle='candidate' 值可直接存在，排除条件两表一致。
    新建 tests/unit/test_publish_candidate_exclusion.py：tmp_path + SCHEMA_SQL fixture 造 run + 双表种子行，覆盖 <behavior> 全部用例。
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python -m pytest tests/unit/test_publish_candidate_exclusion.py tests/integration/test_knowledge_incremental_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest 全绿
    - publish_incremental_run.py 中两条 UPDATE 均含 `lifecycle<>'candidate'`（grep -c 断言 = 2）
    - report 含 candidates_excluded 键
  </acceptance_criteria>
  <done>candidate 行不再被 publish 全量翻转误转正；排除量进报告。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: promote_units.py + pk-ku promote-units 子命令（人工转正通道，D-09 re-match 门）</name>
  <files>src/personal_knowledge/application/knowledge/promote_units.py, src/personal_knowledge/application/ku.py, tests/unit/test_promote_units.py</files>
  <behavior>
    - dry-run（默认）：只输出计划（每 unit 的 re-match 结果与将执行的翻转），零写入
    - --write：candidate unit（双表）→ lifecycle='current' + status='current'；staging 真知识 unit → status='current'（lifecycle 不动）
    - quote re-match 成功且新 ref 与旧 ref 不同 → 更新 source_message_ref 且在报告里记录 old→new 映射（42 D-02 ref 重铸映射表铁律）
    - re-match 失败（探针无命中 / _evidence_supported 回查失败 / 新 ref 不在 eligible 集合）→ 该 unit 跳过，计数 rematch_failed，不硬转正
    - unit_id 或 canonical_unit_id 均可寻址（经 canonical_unit_members 互查）
  </behavior>
  <read_first>
    - var/tmp/relink_orphan_evidence.py（:1-70，探针 + BEGIN IMMEDIATE + 更新形态全文）
    - src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py（:557-571 _evidence_supported）
    - src/personal_knowledge/application/knowledge/eligibility.py（:109-228 compute_eligible_messages 签名与返回结构）
    - src/personal_knowledge/application/ku.py（:340-360 子命令注册区；:833-848 _cmd_history；:873-986 _cmd_watermark 的 dry-run/--write/报告落盘形态）
    - src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py（:95-150，双表 + members 表结构）
    - tools/migrations/backfill_ku_data_debts.py（:19-48，快照 + sys.path 形态）
  </read_first>
  <action>
    ① 新建 src/personal_knowledge/application/knowledge/promote_units.py：
    - `rematch_unit_evidence(unified_con, agent_con, unit_row, eligible_refs) -> str | None`：probe = evidence_quote[:40]；`SELECT canonical_message_id, content FROM canonical_messages WHERE instr(content, ?) > 0`（参数化，agent_con 为只读连接）取候选；逐个用 _evidence_supported 的 10 字窗口逻辑严格回查（复用或等价实现，注释注明同源）；命中且 canonical_message_id ∈ eligible_refs → 返回新 ref；否则 None。
    - `promote_units(unified_db, agent_db, unit_ids, *, write=False) -> dict`：对每 id 解析双表行（knowledge_units.unit_id 或经 canonical_unit_members 映射到 canonical_unit_id）；eligible_refs 一次性由 compute_eligible_messages 产出；逐 unit dry-run 计划 = {id, 现 status/lifecycle, rematch 结果, 将执行动作}；动作规则：re-match 失败 → skip + rematch_failed 计数；lifecycle='candidate' → 置 'current'；status='staging' → 置 'current'（双表同事务）；source_message_ref 变更 → 更新并在 report["ref_remap"] 记录 old→new。write=True 时：先 cp 快照 var/backups/personal_system_<UTC ts>.sqlite，再 BEGIN IMMEDIATE 单事务执行全部 UPDATE，commit 后守恒校验（请求数 = promoted + rematch_failed + already_current）。报告只记计数/id/ref 映射，不记 answer 原文。
    ② ku.py：注册 `promote-units` 子命令（注册区 :346 附近，形态同 history）——命名约束：`promote` 名已被既有 candidate 索引转正占用（ku.py:250 `add_parser("promote")`、:707 `_cmd_promote`、:1017 dispatch），argparse 重名注册会直接抛 ArgumentError，故本通道命名 `promote-units`；handler 新建 `_cmd_promote_units`（不得复用/覆盖既有 `_cmd_promote`），dispatch 区（:1017 附近）新增 `if args.command == "promote-units": return _cmd_promote_units(args)` 分支。参数 `--unit-id`（append 多次或逗号分隔）、`--write`（默认 dry-run）、`--report`（报告 JSON 落盘路径，默认 var/reports/）；_cmd_promote_units 转调 promote_units.main(argv)（仿 _cmd_history :833-848 的 argv 拼装 + 延迟 import 形态；dry-run/--write 语义仿 _cmd_watermark :945-962）。
    ③ 新建 tests/unit/test_promote_units.py：tmp 双库 fixture（unified: SCHEMA_SQL + candidate/staging unit 种子；agent: canonical_messages 最小表 + eligible 口径可命中的消息行），覆盖 <behavior> 全部用例（eligible 集合在测试中直接构造传入，不跑全量 compute_eligible_messages）。
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python -m pytest tests/unit/test_promote_units.py tests/unit/test_pk_ku_cli.py -q && PYTHONIOENCODING=utf-8 python -m personal_knowledge.application.ku promote-units --unit-id nonexistent --report var/reports/promote_smoke.json（dry-run，exit=0，零写入）</automated>
  </verify>
  <acceptance_criteria>
    - pytest 全绿（含 pk-ku CLI 既有测试不回归）
    - promote_units.py 无字符串拼接 SQL；含 var/backups/ 快照 + BEGIN IMMEDIATE（grep 断言）
    - dry-run 对任何输入零 DB 写入（测试断言：前后 COUNT/行内容不变）
    - ref 变更必在报告 ref_remap 有映射记录
  </acceptance_criteria>
  <done>`pk-ku promote-units` 人工转正通道可用，D-09 re-match 门生效；dry-run/--write/快照/单事务四铁律齐备。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 迁移/转正脚本 → UNIFIED_DB | 写库操作，误操作 = 数据损坏（41 事故先例：59,477 行证据被误删） |
| CLI 输入（unit-id）→ SQL | 用户提供的 id 进入查询 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | Tampering / Denial of Service (data) | add_candidate_lifecycle.py / promote_units.py | mitigate | dry-run 默认；--write 前 var/backups/ 快照；PRAGMA 规程按 lifecycle_events.py:131-138 三坑注释；BEGIN IMMEDIATE 单事务；迁移前后行数守恒校验 + foreign_key_check；ref 变更留映射表（42 D-02） |
| T-43-02a | Tampering (SQL injection) | 全部新查询 | mitigate | 参数化绑定（instr(content, ?) / IN (?,...) 批量占位），unit-id 不拼字符串 |
| T-43-SC | Tampering | 包安装 | accept | 无新依赖，无安装任务 |
</threat_model>

<verification>
- 三个任务各自的 pytest/命令验证全绿
- `pk-ku doctor --skip-ports` exit=0（迁移后）
- 快速回归：`PYTHONIOENCODING=utf-8 python -m pytest tests/unit -q` 全绿
</verification>

<success_criteria>
- lifecycle='candidate' 在存量库与新库均合法，迁移幂等
- publish 对 candidate 零误转正，排除量可报告
- `pk-ku promote-units` 是 candidate→current 与 L2G-04 真知识转正的唯一人工通道，D-09 re-match 不过不转正
</success_criteria>

<output>
Create `.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-03-SUMMARY.md` when done（含迁移快照路径、行数守恒校验结果、doctor 输出）
</output>
