---
phase: 43-l2-scope-redefinition-cross-turn-state-ownership-and-increme
plan: 07
type: execute
wave: 4
depends_on: ["43-04", "43-05"]
files_modified:
  - tests/integration/test_l2g01_dedup_gate.py
  - tools/analysis/run_l2g_experiment.py
  - .planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-EXPERIMENT-EVIDENCE.md
autonomous: true
requirements: [L2G-01, L2G-02]

must_haves:
  truths:
    - "集成测试：同一事实两会话 fixture 下，注入 run 的平行重述 current 新增数低于无注入对照 run，且 supersede 候选行（staging + supersedes_id 非空）可见"
    - "实验库实跑记录：清单内 subject 的 L1 current 新增 = 0（全部 candidate），L2 侧有对应 unit 或 no-change 证据"
    - "全部实验在 var/tmp 实验库副本上进行，canonical 零触碰"
  artifacts:
    - path: "tests/integration/test_l2g01_dedup_gate.py"
      provides: "双会话双 run 对照集成测试（stub LLM，tmp 实验库）"
      min_lines: 80
    - path: "tools/analysis/run_l2g_experiment.py"
      provides: "实验库实跑驱动（副本建库 → 双 run → 计数对比 → 报告落盘）"
    - path: ".planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-EXPERIMENT-EVIDENCE.md"
      provides: "L2G-01/L2G-02 acceptance 的实跑证据（计数、run_id、判据结论）"
  key_links:
    - from: "test_l2g01_dedup_gate / run_l2g_experiment"
      to: "build_knowledge_units_prod.process_run / extract_knowledge_units_l2_session.process_l2_run"
      via: "--db 实验库 + --prompt-version v2* flag（43-04/43-05 产物）"
      pattern: "prompt_version"
---

<objective>
L2G-01 与 L2G-02 的验收闭环：先用 stub-LLM 集成测试钉死对照语义，再在实验库副本上真实双轨 run 产出 acceptance 计数证据。

Purpose: SPEC acceptance 要求"平行重述 current 新增数相对基线显著下降 + supersede 记录可见"与"清单内 subject 的 L1 current 新增 = 0（或全部 candidate）"——两条都必须是可判 PASS/FAIL 的计数断言，不是演示。
Output: 集成测试、实跑驱动脚本、证据文档。

**约束提醒（SPEC/AGENTS）：** 对照实验全程走 var/tmp 实验库副本，不动 canonical；不需要 PK_KU_ALLOW_FULL_INVENTORY_START（该门禁针对 canonical 全量 --start；实验副本上的 fixture run 用 --limit/--max-items 控制规模）；Vertex 配额纪律（gemini-3.5-flash-lite + 6s 间隔 + 429 冷却 65s）由既有 call_llm_with_retry 基础设施保证；python 命令加 PYTHONIOENCODING=utf-8。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-SPEC.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-VALIDATION.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-04-SUMMARY.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-05-SUMMARY.md

<interfaces>
From tests/integration/test_knowledge_incremental_pipeline.py（实测 :190,396-416）:
- tmp_path + SCHEMA_SQL 建库 fixture 形态；con.executescript(SCHEMA_SQL)

From src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py（实测）:
- main CLI：--start/--resume/--status、--inventory、--model、--limit、--max-items、--batch-size、--db（:1162-1182）；43-04 新增 --prompt-version
- process_run(run_id, model, db_path=UNIFIED_DB, ...)（:830-846，db_path 参数化）

From 43-04/43-05 产物:
- --prompt-version flag（v1/v1_assistant/v2/v2_assistant；L2 侧 v1_session_window/v2_session_window）；v2 run 的注入段/duplicate_of/candidate 路由行为
- run stats 新键：invalid_duplicate_of / units_downgraded_candidate / injection_empty
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/integration/test_l2g01_dedup_gate.py — 双会话双 run 对照集成测试（VALIDATION Wave 0 项）</name>
  <files>tests/integration/test_l2g01_dedup_gate.py</files>
  <behavior>
    - fixture：tmp 实验库含会话 A 已抽取并 publish 为 canonical current 的事实 unit（如 "工作目录 = D:/ADLINK/数据分析"）；会话 B 消息重复同一事实，且**会话 B 消息文本须字面包含 canonical subject 词面（如 "工作目录"）**——43-04 的 Stage 1 是确定性 subject 出现扫描，消息不字面含已知 subject 则注入不会发生（Pitfall #9 修正后的命中条件）
    - 对照 run（v1 prompt，无注入）：stub LLM 返回等价 unit（无 duplicate_of）→ 落 staging 为平行 current（supersedes_id 为 NULL）
    - 实验 run（v2 prompt + 注入）：stub LLM 返回等价 unit 且 duplicate_of 指向 canonical 已注入 id → staging 行 supersedes_id = 该 id（supersede 候选），lifecycle='current'
    - 计数断言：实验 run 的"平行重述 current（supersedes_id 为 NULL 的等价 unit）"数 < 对照 run 同口径数；supersede 候选行数 ≥ 1
    - 变体：stub 返回 duplicate_of 指向未注入 id → supersedes_id 为 NULL 且 stats["invalid_duplicate_of"] == 1
    - 状态清单变体：会话 B 消息含清单内 subject（临时 yaml 注入）→ v2 run 中该 unit lifecycle='candidate'
  </behavior>
  <read_first>
    - tests/integration/test_knowledge_incremental_pipeline.py（:1-80 import/fixture 形态；:190 executescript；:396-416 tmp 库 fixture）
    - tests/unit/test_knowledge_unit_prod_assistant_track.py（stub LLM worker 的 monkeypatch 形态）
    - src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py（:830-1060 process_run；:1162-1182 CLI）
    - src/personal_knowledge/application/knowledge/injection_context.py（43-02 产物）
  </read_first>
  <action>
    新建 tests/integration/test_l2g01_dedup_gate.py：
    - fixture：tmp_path 双库（unified: SCHEMA_SQL + 会话 A 已 publish 的 canonical current unit + 会话 B 的 knowledge_run_items 种子；agent: canonical_messages 最小种子）。LLM 全程 monkeypatch（call_llm_with_retry stub 按用例返回预定 JSON），不落真 Vertex。
    - 用例 1（对照）：v1 prompt_version run → 断言等价 unit 落 staging、supersedes_id 为 NULL。
    - 用例 2（实验）：fixture 会话 B 消息字面包含 canonical subject 词面（Stage 1 subject 出现扫描的命中条件，见 43-04 Task 2）→ v2 run 断言注入段进 llm_input（stub 捕获）、supersedes_id = 注入 id、stats 无 invalid_duplicate_of。
    - 用例 3（计数对照断言）：同一 fixture 下两 run 的"平行重述 current"计数比较（实验 < 对照）与 supersede 候选 ≥1。
    - 用例 4（非法引用）：duplicate_of 指向未注入 id → NULL + 计数。
    - 用例 5（L2G-02 candidate）：临时 state_subjects yaml + 清单内 subject 消息 → v2 run 该 unit lifecycle='candidate'，stats["units_downgraded_candidate"] == 1。
    - 全部断言走 DB 查询（status/lifecycle/supersedes_id/stats JSON），不断言 prompt 文本细节（文本格式由 unit 层测试覆盖）。
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python -m pytest tests/integration/test_l2g01_dedup_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 该测试文件 5 个用例全绿
    - 测试全程无真实 Vertex 调用（stub 断言调用路径）且只写 tmp_path 库
    - 计数对照断言（实验 < 对照）为硬断言，非打印
  </acceptance_criteria>
  <done>L2G-01/L2G-02 的对照语义由集成测试钉死；VALIDATION Wave 0 第三项关闭。</done>
</task>

<task type="auto">
  <name>Task 2: run_l2g_experiment.py 实验库实跑 + 43-EXPERIMENT-EVIDENCE.md 证据落盘</name>
  <files>tools/analysis/run_l2g_experiment.py, .planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-EXPERIMENT-EVIDENCE.md</files>
  <read_first>
    - docs/runbooks/ku-incremental.md（增量流水线 Gate B/E/F 与 watermark 语义）
    - tools/migrations/backfill_ku_data_debts.py（:1-60 脚本标准形态）
    - src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py（:1162-1182 CLI 参数；43-04 --prompt-version）
    - src/personal_knowledge/application/knowledge/extract_knowledge_units_l2_session.py（CLI 区；43-05 --prompt-version）
    - 43-SPEC.md Acceptance Criteria 节
  </read_first>
  <action>
    ① 新建 tools/analysis/run_l2g_experiment.py（dry-run 默认打印计划；--write 才实跑）：
    - 准备：cp UNIFIED_DB → var/tmp/l2g_experiment_<UTC ts>.sqlite（实验库副本；canonical 零触碰），cp AGENT_CONVERSATIONS_DB 路径照用只读。
    - fixture 选择：从最近 eligible 会话中挑出含"同一事实在 ≥2 个会话重复出现"特征的小样本（脚本内按 subject 词面重叠启发式选 3-5 个会话，选中清单进报告），规模用 --limit/--max-items 硬控（LLM 调用 ≤ 30 条量级，预估成本打印）。
    - 双 run：baseline = --prompt-version v1（L1）+ v1_session_window（L2）；experiment = v2（user 轨；单轨 run 语义见 43-04 Task 1 ④——如需 assistant 轨对照则追加一条 --prompt-version v2_assistant run）/ v2_session_window；均 --db 指向实验副本，顺序执行、各自独立 run_id。
    - 计数对比（SQL 只读）：(a) 平行重述口径——实验 run 中与 canonical 现存 current 等价（normalize_subject 相同 + compute_similarity ≥ 0.85）且 supersedes_id 为 NULL 的 unit 数 vs baseline 同口径数；(b) supersede 候选行数（supersedes_id 非空）；(c) 清单内 subject 的 L1 产出 lifecycle 分布（candidate 数 / current 数）；(d) invalid_duplicate_of 等 stats 新键。
    - 报告：var/reports/analysis/l2g_experiment_<UTC ts>.json（计数/run_id/fixture 清单/判据结论，不记 answer 原文）。
    ② 执行：dry-run 审查计划 → --write 实跑（Vertex 配额纪律由基础设施保证；后台执行可恢复）。
    ③ 新建 43-EXPERIMENT-EVIDENCE.md：抄录报告计数 + 对照 SPEC acceptance 原文逐条判 PASS/FAIL——(i) 平行重述 current 新增数显著下降（给出两 run 计数与相对降幅）；(ii) supersede 候选可见（staging 行 supersedes_id 非空计数）；(iii) 清单内 subject 的 L1 current 新增 = 0（或全部 candidate）；(iv) L2 侧对应 unit 或 no-change 证据（v2 L2 run 的 unit 列表/stats）。若某条 FAIL，如实记录并给出归因假设，不粉饰。**范围注记：** 实验 run 产出的新 supersede 候选（staging 行 supersedes_id 非空）在本 phase 内**不做裁定、无任何任务消费**——其治理裁定属后续例行治理批（routine ops）范畴；证据文档只需证明候选可见性与计数，不得把它们呈现为本 phase 待处置项（避免 acceptance 歧义）。
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python tools/analysis/run_l2g_experiment.py（dry-run：打印 fixture 清单与估计 LLM 调用数，exit=0）&& PYTHONIOENCODING=utf-8 python -m pytest tests/integration/test_l2g01_dedup_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - dry-run exit=0 且估计 LLM 调用数 ≤ 60（双 run 合计）
    - --write 实跑后报告 JSON 落盘 var/reports/analysis/，含 (a)-(d) 四组计数
    - 43-EXPERIMENT-EVIDENCE.md 存在，逐条对照 acceptance 判 PASS/FAIL 且附 run_id 与计数
    - 全程 canonical UNIFIED_DB 零写入（实验后 `git status` 无 var/db 变更 / 文件 mtime 未变的说明记入证据文档）
  </acceptance_criteria>
  <done>L2G-01/L2G-02 acceptance 的实跑计数证据落盘；实验库与 canonical 边界清晰。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 实验脚本 → var/tmp 实验副本 | 写库操作，但目标是副本；误指向 canonical 是唯一高危面 |
| 对话内容 → Vertex | 与生产抽取同级（已有面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | Tampering (data) | run_l2g_experiment.py | mitigate | --db 恒指向 var/tmp 副本（脚本内断言 db 路径前缀为 var/tmp，防误传 canonical 路径）；dry-run 默认；canonical 只读连接；证据文档记录零写入校验 |
| T-43-01 | Tampering (prompt injection) | 实验 run LLM 面 | mitigate | 与 43-04/43-05 相同防线（白名单校验 + 计数）；实验结论只读计数 |
| T-43-SC | Tampering | 包安装 | accept | 无新依赖，无安装任务 |
</threat_model>

<verification>
- `PYTHONIOENCODING=utf-8 python -m pytest tests/integration/test_l2g01_dedup_gate.py -q` 全绿
- 实跑报告 + 证据文档落盘
- 全量回归：`PYTHONIOENCODING=utf-8 python -m pytest tests/unit tests/integration tests/contract -q` 全绿
- `pk-ku doctor --skip-ports` exit=0
</verification>

<success_criteria>
- SPEC acceptance 前三条中的 (1)(2) 有可复核的计数证据：平行重述下降 + supersede 候选可见 + 清单内 L1 current 新增 = 0（或全 candidate）
- 集成测试钉死对照语义，后续改动可回归
</success_criteria>

<output>
Create `.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-07-SUMMARY.md` when done（含 PASS/FAIL 结论摘要与报告路径）
</output>
