---
phase: 43-l2-scope-redefinition-cross-turn-state-ownership-and-increme
plan: 09
type: execute
wave: 3
depends_on: ["43-03", "43-08"]
files_modified:
  - var/tmp/triage_supersede_batch.py
  - var/tmp/triage_deprecate_batch.py
  - .planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-WATERMARK-NOTE.md
autonomous: false
requirements: [L2G-04]

must_haves:
  truths:
    - "duplicate 档经治理链 supersede 批（≤50/批、逐对人工检视、manifest 链完整）处置，superseded 记录可查"
    - "noise 档经治理链 deprecate 批处置（evidence 走 unit 自引通道），deprecated 记录可查，零硬删"
    - "true_knowledge 档经 pk-ku promote-units 转正（D-09 re-match 门），match 不上的不硬转正"
    - "43-WATERMARK-NOTE.md 写明 delta 归因、42-03 yield gate 收尾判定、处置台账与 watermark 推进判据；推进后 inspect delta 干净、doctor exit=0"
  artifacts:
    - path: "var/tmp/triage_supersede_batch.py"
      provides: "duplicate 档 supersede 批驱动（build→review→register→apply，BATCH=50）"
    - path: "var/tmp/triage_deprecate_batch.py"
      provides: "noise 档 deprecate 批驱动（unit 自引 evidence 通道）"
    - path: ".planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-WATERMARK-NOTE.md"
      provides: "watermark 收敛执行笔记（归因 + 收尾判定 + 台账 + 推进记录）"
      contains: "yield gate"
  key_links:
    - from: "var/tmp/triage_*_batch.py"
      to: "lifecycle_events.build_manifest / finalize_review / register_manifest / apply_manifest"
      via: "治理链四步（MAX_ACTIONS=50，乐观锁，可 rollback）"
      pattern: "apply_manifest"
    - from: "43-WATERMARK-NOTE.md"
      to: "pk-ku watermark --advance --from-canonical --write"
      via: "Gate F fail-closed 前置（check_watermark_advance_preconditions）"
      pattern: "watermark --advance"
---

<objective>
落地 L2G-04 后半：三档处置清单经治理链分批处置（铁律：manifest 链完整、不硬删、每批 ≤50 逐对人工检视、改库前 var/backups/ 快照），随后按 D-10 收敛 watermark 并落执行笔记。

Purpose: 42-03 挂账的 12,496 deleted_refs 只有完成分级处置 + 归因落盘 + watermark 推进才算闭环；Gate B 恢复干净基线依赖本 plan。
Output: 两个批驱动脚本、处置台账（manifest 链）、43-WATERMARK-NOTE.md、watermark 推进记录。

**关键坑位预案（RESEARCH Pitfall #6，11,008 处置必中）：** 这些 unit 的 source_message_ref 已不在 eligible 集合，`apply_manifest` 的 EvidenceResolver 门会拒——deprecate 批的 evidence_refs 一律用 **unit 自引**（`cu|<canonical_unit_id>` 或 unit id 自引，`_default_evidence_validator` 自动识别 ref 类型，lifecycle_events.py:357-362，41 已验证）；supersede 批的存活侧 unit evidence 若不过 resolver 门则跳过该对并计数（supersede_batch.py:61 先例），不硬上。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-SPEC.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-PATTERNS.md
@.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-08-SUMMARY.md
@docs/runbooks/ku-incremental.md

<interfaces>
From src/personal_knowledge/application/knowledge/lifecycle_events.py（RESEARCH/PATTERNS 核对）:
- ALLOWED_ACTIONS={"supersede","conflict","correct","restore","deprecate"} :22；MAX_ACTIONS=50 :24
- build_manifest :183 → finalize_review :297（review 绑定 proposal checksum、decisions 全覆盖；reviewer_type='llm' 需 model_id/review_run_id/prompt_version 三件套 :118-122；human reviewer 不能标识 agent/model :110-127）→ register_manifest(write=True) :267 → apply_manifest :365（逐 action 乐观锁 expected_version+lifecycle :396-397、evidence 全 eligible :399-400）→ rollback_manifest :449
- _default_evidence_validator :357-362（unit 自引 cu|/unit id 自动识别）

From var/tmp/supersede_batch.py（PATTERNS 全文核对）:
- BATCH=50 :21；只取 lifecycle='current' :44-47；EvidenceResolver 预检跳过 :61-64；逐对打印检视 :73-75；finalize→register→apply 链 :121-131

From src/personal_knowledge/application/ku.py:873-986 + refresh_knowledge_units.py:1236,1253:
- pk-ku watermark --advance --from-canonical（dry-run 默认、--write 持久）；前置 check_watermark_advance_preconditions（有 pending/in_flight/retryable 拒绝；terminal_failed 须 --acknowledge-failures 记 knowledge_dead_refs 后放行，ku.py:970,979-980）

From 43-03 产物:
- pk-ku promote-units --unit-id ... [--write]（D-09 re-match 门、快照、单事务、ref_remap 映射报告）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 批处置准备 — supersede/deprecate 批驱动脚本 + promote dry-run 台账（全部 dry-run）</name>
  <files>var/tmp/triage_supersede_batch.py, var/tmp/triage_deprecate_batch.py</files>
  <read_first>
    - var/tmp/supersede_batch.py（139 行全文，批驱动形态母本）
    - var/tmp/deprecate_batch.py、var/tmp/conflict_apply_batch.py（处置变体形态）
    - src/personal_knowledge/application/knowledge/lifecycle_events.py（:1-130 API 区；:183-507 四步链）
    - var/reports/analysis/triage_legacy_staging_<UTC ts>.json（43-08 分级报告，处置清单来源）
    - 43-PATTERNS.md「治理批处置」节
  </read_first>
  <action>
    ① 新建 var/tmp/triage_supersede_batch.py（形态照 supersede_batch.py 母本）：输入 = 43-08 报告的 duplicate 档（含 LLM 复核改判 duplicate 的条目）——每对 = (staging duplicate unit, 存活 canonical current unit)；逐对 EvidenceResolver 预检（存活侧 evidence 不 ok → skip + 计数，Pitfall #6）；只处理 lifecycle='current' 的存活侧（:44-47 先例）；build_manifest（BATCH=50）→ 打印逐对检视清单（OLD/NEW 摘要 + similarity，:73-75 形态）→ dry-run 落 proposal JSON，不 register 不 apply。
    ② 新建 var/tmp/triage_deprecate_batch.py：输入 = noise 档（含 LLM 改判 noise 的条目）；deprecate action 的 evidence_refs **一律用 unit 自引**（cu|/unit id，_default_evidence_validator 自动识别，41 验证通道）；BATCH=50；build_manifest → 逐条检视清单打印 → dry-run 落 proposal。
    ③ true_knowledge 档：`pk-ku promote-units --unit-id <逐条> --report var/reports/promote_triage_<ts>.json`（dry-run）——产出 promote 计划台账（每 unit 的 re-match 结果与将执行动作）；re-match 失败条目单列清单，按 D-09 走 candidate 保留或并入 deprecate 提案（在台账中逐条注明去向建议，最终裁定归 Task 2 人工）。
    ④ 产出汇总 dry-run 台账 var/reports/analysis/triage_disposition_plan_<ts>.json：三档各自批次数（≤50/批）、每批 proposal 路径、skip/预检失败计数、promote 计划路径。
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python var/tmp/triage_supersede_batch.py（dry-run：打印批次数与逐对清单，exit=0）&& PYTHONIOENCODING=utf-8 python var/tmp/triage_deprecate_batch.py（dry-run，exit=0）&& PYTHONIOENCODING=utf-8 python -m personal_knowledge.application.ku promote-units --unit-id <任一 true_knowledge 档 id>（dry-run，exit=0）</automated>
  </verify>
  <acceptance_criteria>
    - 三条 dry-run 命令 exit=0，台账 JSON 落盘且批次数 = ceil(档内条数 / 50)
    - deprecate 批 proposal 的 evidence_refs 全部为 unit 自引形式（grep/JSON 断言 100%）
    - supersede 批预检 skip 计数进台账（非零也正常，须可见）
    - 全程零 DB 写入（dry-run 后 knowledge_lifecycle_events 无新增行——SQL 计数前后一致断言）
  </acceptance_criteria>
  <done>三档处置计划全部 dry-run 就绪；批次编排遵守 ≤50 铁律；人工检视材料齐备。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 逐批人工检视 + 治理链 apply（supersede / deprecate / promote 三通道）</name>
  <files>var/tmp/triage_supersede_batch.py, var/tmp/triage_deprecate_batch.py</files>
  <action>agent 逐批展示检视清单（supersede 逐对 OLD/NEW + similarity；deprecate 逐条摘要；promote re-match 结果）；人工逐批批准后，agent 对批准批执行：cp 快照 var/backups/personal_system_<UTC ts>.sqlite → finalize_review（human reviewer_id，遵守 lifecycle_events.py:110-127）→ register_manifest(write=True) → apply_manifest，promote 通道执行 pk-ku promote-units --write；skip/覆写按人工指示落可审计清单；每批报告 applied/skipped/failed 计数，异常批 rollback_manifest 后重来；全部完成后把 manifest id 链 + promote 报告 + skip 清单汇总落盘 var/reports/analysis/triage_disposition_applied_<ts>.json。</action>
  <read_first>
    - var/reports/analysis/triage_disposition_plan_<ts>.json（Task 1 台账）
    - 各批 proposal JSON（Task 1 产物）
    - src/personal_knowledge/application/knowledge/lifecycle_events.py（:297-365 finalize/register/apply 语义）
  </read_first>
  <what-built>Task 1 已产出三档全部批次的 proposal 与 promote 计划（dry-run）。本步执行真实的逐批裁定与落库：supersede 批、deprecate 批经 finalize_review → register --write → apply；true_knowledge 档经 pk-ku promote-units --write 转正。</what-built>
  <how-to-verify>
    1. agent 逐批展示检视清单（supersede：OLD/NEW 逐对 + similarity；deprecate：逐条摘要；promote：re-match 结果）。
    2. 你逐批过目，对异常对/条给出 skip 或方向覆写（参照 var/tmp/conflict_apply_batch.py 的可审计覆写清单模式）；无异议则批准该批。
    3. agent 对批准的批次执行：改库前 cp 快照 var/backups/personal_system_<UTC ts>.sqlite → finalize_review（human reviewer_id，遵守 lifecycle_events.py:110-127 身份规则）→ register_manifest(write=True) → apply_manifest → 报告 applied 计数与 event id；promote 通道执行 pk-ku promote-units --write（自带快照与单事务）。
    4. 每批完成后 agent 报告累计台账（applied/skipped/failed）；异常批可 rollback_manifest 后重来。
    5. 全部批次完成后复核：knowledge_lifecycle_events 新增事件链完整、staging 目标集清零（或仅剩显式记录保留的条目）。
  </how-to-verify>
  <resume-signal>逐批复："batch N approved" / "batch N skip: <unit_ids>"；全部完成后 "all batches done"</resume-signal>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 python -c "import sqlite3; from personal_knowledge.core.project_paths import UNIFIED_DB; con=sqlite3.connect(f'file:{UNIFIED_DB.as_posix()}?mode=ro',uri=True); ev=con.execute('SELECT COUNT(*) FROM knowledge_lifecycle_events').fetchone()[0]; st=con.execute(\"SELECT COUNT(*) FROM knowledge_units WHERE status='staging'\").fetchone()[0]; print(f'events={ev} staging_remaining={st}')"（全部批次后：events 显著增加、staging 目标集清零或仅剩台账记录项）</automated>
  </verify>
  <acceptance_criteria>
    - 每批 ≤50 actions（manifest JSON 断言），每批有对应 human review 记录（reviewer 身份合规）
    - 改库前快照存在于 var/backups/（文件清单进 SUMMARY）
    - 零 DELETE：knowledge_units/canonical_knowledge_units 行数处置前后不减（lifecycle/status 翻转不算减——COUNT(*) 守恒断言）
    - 处置台账（applied manifest id 链、promote 报告、skip 清单）汇总落盘 var/reports/analysis/triage_disposition_applied_<ts>.json
  </acceptance_criteria>
  <done>三档处置全部经治理链落库：superseded/deprecated/promoted 记录可查，manifest 链完整可回滚。</done>
</task>

<task type="auto">
  <name>Task 3: 43-WATERMARK-NOTE.md 执行笔记 + watermark 推进（D-10）</name>
  <files>.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-WATERMARK-NOTE.md</files>
  <read_first>
    - docs/runbooks/ku-incremental.md（:242-246 watermark 语义；Gate B/E/F）
    - src/personal_knowledge/application/ku.py（:873-986 _cmd_watermark 全文）
    - src/personal_knowledge/application/knowledge/refresh_knowledge_units.py（:1236 advance_watermark；:1253 check_watermark_advance_preconditions）
    - var/reports/analysis/ai_context/reconcile_full_20260727.json（reconcile 基线）
    - var/reports/analysis/triage_disposition_applied_<ts>.json（Task 2 台账）
    - 43-SPEC.md Background 节（42-03 delta 数字与 yield gate 失败值）
  </read_first>
  <action>
    ① 新建 43-WATERMARK-NOTE.md，内容逐节落实：
    - **delta 归因**：42-03 inspect delta（new_refs=1,995 / deleted_refs=12,496 / affected_subjects=8,100）的构成——88% 为 Phase 41 eligibility 收紧口径账（56% 工具前缀 + 32% 清洗后 ≤30 字），非会话合并副本退出（42-03 已实测归因，本节归档为正式记录）。
    - **42-03 dual-track strict yield gate 收尾判定**：现场分析 user 0.0141 / assistant 0.1381 失败的归因（执行时实测：抽查 watermark 后 new ref 对应消息的已抽取覆盖情况，判定低 yield 是"存量已抽净"还是"口径过严"），写明判定过程、证据与结论（gate 保持/调整建议留给后续，本 phase 只收尾记录不改 gate）。
    - **处置台账**：三档计数 + applied manifest id 链 + promote 报告路径 + skip/保留清单（引 Task 2 台账）。
    - **索引重建时机**（Pitfall #5）：deprecated/superseded 只在下一次 `pk-ku vector --write` 重建 + canary strict PASS + promote 后退出 active 索引；本节记录本次是否执行重建（若执行：collection 名、canary 结果；若安排后续：写明触发条件与负责人）。
    - **watermark 推进判据与记录**：判据 = 分级处置完 + 本笔记落盘（D-10）；执行 `pk-ku watermark --advance --from-canonical`（dry-run 先审）→ 前置检查（pending/in_flight/retryable 须为零；terminal_failed 若有须先 --acknowledge-failures 记 knowledge_dead_refs 并在笔记列明条目与理由）→ --write 推进 → 推进后 `pk-ku inspect` 复算 delta（预期 12,496 deleted_refs 随推进消失，new_refs 只剩真实新增）→ 记录推进前后 checksum 与 inspect 输出。
    ② 执行上述 watermark 推进（CLI 全程；Gate F fail-closed 不绕）。
    ③ 收尾验证：`pk-ku doctor --skip-ports` exit=0；`PYTHONIOENCODING=utf-8 python -m pytest tests/unit tests/integration -q` 全绿。
  </action>
  <verify>
    <automated>PYTHONIOENCODING=utf-8 pk-ku doctor --skip-ports && PYTHONIOENCODING=utf-8 python -m pytest tests/unit tests/integration -q && PYTHONIOENCODING=utf-8 pk-ku inspect（delta 相对新 watermark 复算，无未归因大额挂账）</automated>
  </verify>
  <acceptance_criteria>
    - 43-WATERMARK-NOTE.md 存在且含五节（归因/yield gate 收尾/台账/索引时机/推进记录），含 "yield gate" 与推进前后 checksum
    - watermark 推进后 inspect 输出无未归因 deleted_refs 大额挂账
    - doctor exit=0；unit+integration pytest 全绿
    - knowledge_source_watermark 的 committed checksum 已更新且与 inspect 基准一致
  </acceptance_criteria>
  <done>watermark 收敛闭环：处置台账 + 归因 + 推进记录落盘，Gate B 恢复干净基线。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 批驱动脚本 → UNIFIED_DB | 大规模写库（11,008 条量级分档处置），误操作 = 41 式事故 |
| CLI（watermark advance）→ watermark 表 | 推进后 inspect 基准改变，误推进丢失 delta 可见性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | Tampering / Destruction (data) | 全部批处置 + watermark 推进 | mitigate | dry-run 默认与 --write 门禁分离；每批改库前 var/backups/ 快照；治理链 append-only + 乐观锁 + rollback_manifest 可逆；每批 ≤50 且逐对人工检视（human checkpoint，非可跳过）；COUNT(*) 守恒断言（零硬删）；watermark 推进走 Gate F fail-closed 前置，terminal_failed 须显式 acknowledge 留痕；全部推进/处置记录进 manifest 链与本笔记（可回滚） |
| T-43-02b | Tampering (evidence gate bypass) | deprecate 批 evidence | mitigate | evidence_refs 只用 unit 自引通道（_default_evidence_validator 自动识别，41 验证）；supersede 批 resolver 预检不过即 skip 计数，禁止绕门 |
| T-43-SC | Tampering | 包安装 | accept | 无新依赖，无安装任务 |
</threat_model>

<verification>
- Task 2 checkpoint 的 events/staging 计数断言
- Task 3 的 doctor + pytest + inspect 三连验证
- 全量回归：`PYTHONIOENCODING=utf-8 python -m pytest tests/unit tests/integration tests/contract -q` 全绿
</verification>

<success_criteria>
- SPEC acceptance 第 4、5 条成立：分级报告 + 治理链逐批处置记录 + 执行笔记（watermark 推进或挂起判据与依据、delta 有归因）
- SPEC acceptance 第 6 条：pk-ku doctor exit=0，相关 pytest 全绿
- 治理链四铁律（manifest 完整 / 不硬删 / ≤50 逐对检视 / 改前快照）全程可审计
</success_criteria>

<output>
Create `.planning/phases/PDA-43-l2-scope-redefinition-cross-turn-state-ownership-and-increme/43-09-SUMMARY.md` when done（含处置台账摘要、快照清单、watermark 推进前后 checksum、doctor/pytest 结果）
</output>
