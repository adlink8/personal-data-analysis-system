# Phase 16-02 PLAN：Google structure lifecycle + consumer contract

**Status:** complete  
**Depends on:** 16-01  

## Goal

可审计构建、stage→gate→promote、上游删除传播、只读消费契约。

## Delivered

| Wave | Content |
|---|---|
| W1 | Stable `activity_event_id` + orphan DELETE on rebuild + input/dataset hash |
| W2 | `google_structure_runs` + promote log |
| W3 | Staged assertions + privacy gate + promote/rollback |
| W4 | RO consumer: list/get backend + REST + MCP |
| W5 | Docs + tests |

## Out of scope

Dialogue KU on Google; AgentsView writes; Chroma rebuild required.
