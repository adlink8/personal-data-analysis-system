---
phase: 33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personal_knowledge/intelligence/orchestration/__init__.py
  - src/personal_knowledge/intelligence/orchestration/schema.py
  - src/personal_knowledge/intelligence/orchestration/models.py
  - src/personal_knowledge/intelligence/orchestration/service.py
  - tests/unit/test_orchestration_core.py
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-04]
must_haves:
  truths:
    - "D-01 D-02: prepare binds exact snapshots, goal, constraints, weights and low risk without provider calls or authority writes."
    - "D-03 D-04: an explicit transition table and checksum-verified append-only ledger define resumable state."
    - "D-05 D-06 D-07 D-08: short-lived operation-specific confirmations, expected sequence and idempotency are mandatory and concurrency safe."
    - "D-13 D-14 D-15: risk, freshness and transition failures abstain with stable reason codes before effects."
  artifacts:
    - path: src/personal_knowledge/intelligence/orchestration/service.py
      provides: prepare, confirmation, append, resume and explain core
    - path: tests/unit/test_orchestration_core.py
      provides: deterministic lifecycle and negative gate matrix
---

<objective>Build the isolated orchestration authority, pure prepare preview and confirmation/state-machine core.</objective>
<threat_model>
- T-33-01 confirmation forgery or replay: bind token to session, actor, operation, preview checksum, expected sequence and expiry.
- T-33-02 concurrent double append: unique idempotency and BEGIN IMMEDIATE compare-and-append.
- T-33-03 authority drift: revalidate exact snapshot/binding hashes before issuing or consuming a preview.
- Block on any mutation without valid confirmation or any prepare-side provider/downstream write.
</threat_model>
<tasks>
<task type="execute"><name>Create orchestration schema and immutable models</name><files>src/personal_knowledge/intelligence/orchestration/schema.py; src/personal_knowledge/intelligence/orchestration/models.py; src/personal_knowledge/intelligence/orchestration/__init__.py</files><read_first>33-CONTEXT.md; src/personal_knowledge/intelligence/pilot/schema.py; src/personal_knowledge/intelligence/pilot/workflow.py; src/personal_knowledge/intelligence/analysis/schema.py</read_first><action>Define `decision_orchestration_v1` SQLite schema with `orchestration_sessions`, `orchestration_events`, `orchestration_confirmations`, and `orchestration_invocations`. Add immutable-update/delete triggers, foreign keys, unique `(session_id,idempotency_key)`, sequence uniqueness and canonical checksum helpers. Define typed Preview, Confirmation, SessionEvent and OperationResult dataclasses plus `OrchestrationError(code, detail)`.</action><acceptance_criteria>Schema inspection reports applied only when all four tables, indexes and immutable triggers exist; canonical IDs/checksums are stable; update/delete attempts fail.</acceptance_criteria><verify>python -m pytest tests/unit/test_orchestration_core.py -q -k "schema or checksum"</verify></task>
<task type="execute"><name>Implement pure prepare and confirmation manifests</name><files>src/personal_knowledge/intelligence/orchestration/service.py; tests/unit/test_orchestration_core.py</files><read_first>src/personal_knowledge/intelligence/orchestration/models.py; src/personal_knowledge/intelligence/decision/context_binding.py; src/personal_knowledge/services/decision_intelligence_reads.py; 33-CONTEXT.md</read_first><action>Implement `OrchestrationService.prepare`, `issue_confirmation`, and `confirm`. Prepare only accepts domain `project`, risk `low`, validated goal/constraints/weights/actor and current exact Personal/External binding; it returns a canonical preview checksum without creating rows. Issue a five-minute token whose digest binds session_id, operation, preview checksum, actor_identity_hash, expected_sequence and expires_at. Confirm validates exact binding/freshness and atomically creates the session plus sequence-1 `confirmed` event.</action><acceptance_criteria>Prepare leaves orchestration/Analysis/Pilot/Calibration fingerprints unchanged and provider_calls=0; token mismatch/expiry/actor drift return stable codes; confirm with exact token creates one checksum-valid session/event.</acceptance_criteria><verify>python -m pytest tests/unit/test_orchestration_core.py -q -k "prepare or confirmation"</verify></task>
<task type="execute"><name>Implement state transition, replay and explain core</name><files>src/personal_knowledge/intelligence/orchestration/service.py; tests/unit/test_orchestration_core.py</files><read_first>src/personal_knowledge/intelligence/orchestration/schema.py; src/personal_knowledge/intelligence/pilot/workflow.py; 33-RESEARCH.md</read_first><action>Add the exact transition table `confirmed→generated→published→decided→observed→calibrated`, checksum-chain reconstruction, `resume`, `get`, `explain`, and a single internal compare-and-append function. Validate expected sequence and confirmation before `BEGIN IMMEDIATE`; exact idempotency replay returns the stored OperationResult, differing payload returns `idempotency_conflict`, stale sequence returns `stale_expected_sequence`, invalid edge returns `illegal_transition`.</action><acceptance_criteria>Resume derives state only from a verified chain; corruption fails closed; concurrent identical append produces one event and identical replay; all negative cases preserve file fingerprints.</acceptance_criteria><verify>python -m pytest tests/unit/test_orchestration_core.py -q</verify></task>
</tasks>
<verification>python -m pytest tests/unit/test_orchestration_core.py -q</verification>
<success_criteria>ORCH-01 and the shared confirmation/replay foundation for ORCH-02..04 are implemented with zero unconfirmed effects.</success_criteria>
