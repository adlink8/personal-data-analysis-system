---
phase: 33
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - src/personal_knowledge/intelligence/orchestration/generation.py
  - src/personal_knowledge/intelligence/orchestration/service.py
  - tests/integration/test_orchestration_replay.py
autonomous: true
requirements: [ORCH-02, ORCH-03, ORCH-04]
must_haves:
  truths:
    - "D-09: confirmed generation uses a durable reservation and never blindly repeats a provider call."
    - "D-10: completed generation stores only verified Analysis IDs and checksums in the orchestration ledger."
    - "D-14 D-16: drift, expiry, stale state and provider-unknown paths have stable zero-duplicate behavior."
---

<objective>Add crash-aware at-most-once generation over the existing Analysis executor.</objective>
<threat_model>
- T-33-04 duplicate paid/non-deterministic provider call: durable reservation before call and fail-closed unknown outcome.
- T-33-05 provider result authority bypass: existing deterministic Analysis executor/gates remain the only publisher.
- T-33-06 crash ambiguity: reserved-but-unfinalized invocation reports `provider_outcome_unknown`, never automatic retry.
</threat_model>
<tasks>
<task type="execute"><name>Implement durable generation reservation</name><files>src/personal_knowledge/intelligence/orchestration/generation.py; src/personal_knowledge/intelligence/orchestration/service.py; tests/integration/test_orchestration_replay.py</files><read_first>src/personal_knowledge/intelligence/orchestration/service.py; src/personal_knowledge/intelligence/analysis/executor.py; src/personal_knowledge/intelligence/analysis/live_uat.py; src/personal_knowledge/intelligence/analysis/providers.py</read_first><action>Implement `reserve_generation` and `finalize_generation`. Reservation validates a generate preview/confirmation and inserts one `reserved` invocation containing request checksum before any provider call. Finalization requires matching reservation and records status `completed`, provider receipt checksum, run_id/candidate_id/checksums, then appends the `generated` event. Existing completed replay returns stored result; existing reserved replay returns `provider_outcome_unknown`; conflicting request returns `idempotency_conflict`.</action><acceptance_criteria>Injected provider call counter remains exactly one for repeated completed requests and zero for reserved replay; a conflicting payload never calls provider; finalized event references a checksum-valid Analysis run.</acceptance_criteria><verify>python -m pytest tests/integration/test_orchestration_replay.py -q -k generation</verify></task>
<task type="execute"><name>Wire injectable Analysis generation adapter</name><files>src/personal_knowledge/intelligence/orchestration/generation.py; tests/integration/test_orchestration_replay.py</files><read_first>src/personal_knowledge/intelligence/analysis/executor.py; src/personal_knowledge/intelligence/analysis/inputs.py; src/personal_knowledge/intelligence/decision/context_binding.py; 33-CONTEXT.md</read_first><action>Add an injectable adapter that converts the confirmed session manifest into the existing `execute_analysis` arguments, preserves exact evidence allowlists and `max_attempts=1`, and publishes only through the Analysis authority. Do not add a second candidate schema or raw provider body to orchestration. On deterministic abstain finalize the invocation without a generated transition; on process/network uncertainty retain reserved state.</action><acceptance_criteria>Stub provider success yields one Analysis run and one generated event; deterministic analysis abstention has no generated event; orchestration rows contain no raw response, credentials or hidden reasoning.</acceptance_criteria><verify>python -m pytest tests/integration/test_orchestration_replay.py -q</verify></task>
</tasks>
<verification>python -m pytest tests/unit/test_orchestration_core.py tests/integration/test_orchestration_replay.py -q</verification>
<success_criteria>Confirmed generate is recoverable and at-most-once across client/network retries.</success_criteria>
