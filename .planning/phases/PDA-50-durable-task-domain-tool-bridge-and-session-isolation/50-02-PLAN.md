---
phase: 50
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified: [src/personal_knowledge/services/pi_domain_gateway.py, src/personal_knowledge/services/api_server.py, apps/personal_intelligence_kernel/src/tools/domain-bridge.mjs, governance/manifests/ai/pi-domain-tools.json, tests/contract/test_pi_domain_gateway.py, tests/integration/test_pi_task_recovery.py, tests/integration/test_pi_artifact_isolation.py]
autonomous: true
requirements: [KERNEL-03, TOOL-01, DATA-01, DATA-02, SESSION-01]
must_haves:
  truths:
    - "D-03: Node reaches Python only through typed operation IDs."
    - "D-04: guarded writes retain Python preview/confirm/replay authority."
    - "D-06: crash/cancel/outcome_unknown never advances protected state."
---

# Phase 50-02 Plan — Typed Domain Gateway and recovery/isolation gate

<objective>把 allowlisted Pi Tools 映射到现有 Python authority logic，并完成跨进程 crash/replay/zero-mutation 验证。</objective>
<threat_model>Assets: Domain authority, confirmation token, protected pointers and task outcome. Threats: arbitrary operation/SQL/path, write bypass, duplicate dispatch, response loss and secret leakage. Controls: static operation registry/schema, internal capability auth, Python-owned confirmation, idempotency reconciliation, safe envelopes and fingerprints.</threat_model>
<tasks>
<task type="execute">
  <name>50-02-01 — Add static Python Domain Gateway and Pi Tool bridge</name>
  <read_first>
    - src/personal_knowledge/services/orchestration_service.py
    - src/personal_knowledge/services/agent_contract.py
    - src/personal_knowledge/services/api_server.py
    - apps/personal_intelligence_kernel/src/tasks/ledger.mjs
    - governance/manifests/ai/pi-tool-registry.json
  </read_first>
  <files>src/personal_knowledge/services/pi_domain_gateway.py, src/personal_knowledge/services/api_server.py, apps/personal_intelligence_kernel/src/tools/domain-bridge.mjs, governance/manifests/ai/pi-domain-tools.json, tests/contract/test_pi_domain_gateway.py</files>
  <action>Define versioned operation registry for explicit read and guarded orchestration operations with per-operation input/output schemas, privacy ceiling and write classification. Add loopback internal route protected by injected capability secret and fixed safe errors; reject arbitrary module/function/path/SQL parameters. Node tools map exact names to operation IDs, include task/idempotency/binding and never receive authority credentials. Guarded writes call existing Python preview/confirm/exact replay unchanged.</action>
  <acceptance_criteria>
    - Unknown operation/key/schema/capability is rejected before domain invocation.
    - Write operation without valid preview/confirmation/idempotency creates no row/event.
    - Tool manifest names exactly equal runtime custom tool names.
    - Gateway contract tests exit 0 and seeded secrets are absent.</acceptance_criteria>
  <verify><automated>python -m pytest tests/contract/test_pi_domain_gateway.py -q</automated></verify>
</task>
<task type="execute">
  <name>50-02-02 — Prove kill, duplicate, outcome_unknown and artifact isolation</name>
  <read_first>
    - src/personal_knowledge/services/pi_domain_gateway.py
    - apps/personal_intelligence_kernel/src/tools/domain-bridge.mjs
    - apps/personal_intelligence_kernel/src/tasks/ledger.mjs
    - .planning/phases/PDA-50-durable-task-domain-tool-bridge-and-session-isolation/50-VALIDATION.md
  </read_first>
  <files>tests/integration/test_pi_task_recovery.py, tests/integration/test_pi_artifact_isolation.py</files>
  <action>Inject Node kill before dispatch, Python kill during response, response drop after domain commit, duplicate claim, cancel race, DB busy and restart. Require reconciliation by idempotency key, exact replay or outcome_unknown without automatic retry. Fingerprint canonical/knowledge/serving/watermark and all three Pi DBs; assert only expected task/session/candidate rows change and no Candidate enters active/retrieval.</action>
  <acceptance_criteria>
    - Every fault has a deterministic terminal/recovery state and bounded timeout.
    - Committed domain effect is never duplicated after response loss.
    - Protected authority fingerprints change only for explicitly confirmed fixture writes; all unconfirmed faults are zero-change.
    - Both integration test files exit 0.</acceptance_criteria>
  <verify><automated>python -m pytest tests/integration/test_pi_task_recovery.py tests/integration/test_pi_artifact_isolation.py -q</automated></verify>
</task>
</tasks>
<verification>Run Node store tests and all Phase 50 Python tests; real Provider count remains zero.</verification>
<success_criteria>Pi has durable execution and typed Domain Tools without gaining authority or unsafe retry power.</success_criteria>
