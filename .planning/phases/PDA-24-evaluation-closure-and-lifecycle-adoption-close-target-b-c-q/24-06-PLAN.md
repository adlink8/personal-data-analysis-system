---
phase: 24
plan: 06
type: execute
wave: 5
depends_on: [24-05]
gap_closure: true
files_modified:
  - var/runtime/private_evals/lifecycle_review_v2.private.json
  - var/runtime/private_evals/lifecycle_reviewed_v2.private.json
  - .planning/phases/PDA-24-evaluation-closure-and-lifecycle-adoption-close-target-b-c-q/24-03-CHECKPOINT.md
autonomous: false
requirements: [LIFE-01, LIFE-02]
must_haves:
  truths:
    - Every adopted action refers to real current units and resolvable eligible evidence.
    - Review provenance and exact manifest checksum precede every write.
    - Conflict, correction, supersede and restore transitions are represented by append-only events.
  artifacts:
    - private bounded lifecycle review packet and LLM review receipt
    - approved immutable action manifest
    - backup and append-only event ledger evidence
---

<objective>
Adopt a bounded real lifecycle cohort with auditable LLM review and reversible,
append-only state transitions.
</objective>

<tasks>
<task type="execute">
  <name>Derive evidence-eligible real lifecycle cases</name>
  <read_first>src/personal_knowledge/application/knowledge/reconcile_knowledge_index.py; src/personal_knowledge/retrieval/evidence.py; var/runtime/private_evals/lifecycle_reviewed_v1.private.json</read_first>
  <action>Search bounded multi-current subject/type groups for real candidates. Require distinct resolvable eligible evidence, temporal or factual contradiction/supersession support, and exact unit/version checksums. Include explicit no-action cases. Keep all private bodies under var/runtime.</action>
  <verify>pk-ku reconcile --max-subjects 500 --json</verify>
  <acceptance_criteria>The packet contains at least one genuinely supportable transition for each required event family or records a truthful requirement gap; no heuristic-only action is eligible.</acceptance_criteria>
</task>
<task type="execute">
  <name>LLM-review and apply only approved actions</name>
  <read_first>src/personal_knowledge/application/knowledge/review_lifecycle.py; src/personal_knowledge/application/knowledge/apply_lifecycle_manifest.py; docs/runbooks/ku-incremental.md</read_first>
  <action>Review each action with configured gpt-5.6-luna provenance, per-item confidence, prompt/model/run IDs and checksums. Create an online SQLite backup. Apply only approved checksum-bound actions in one bounded transaction; validate integrity, current-only retrieval and history. Exercise inverse restore using the recorded rollback link, then forward-restore the reviewed end state.</action>
  <verify>python -m personal_knowledge.application.knowledge.lifecycle_status --strict; pk-ku doctor --json --skip-ports</verify>
  <acceptance_criteria>Required event types and reviewed manifest are present, history is immutable, current-only retrieval is correct, restore/forward-restore succeeds, and no hard deletion occurs.</acceptance_criteria>
</task>
</tasks>

<verification>
python -m pytest tests/integration -k "lifecycle or reconcile or history" -q
python -m personal_knowledge.application.knowledge.lifecycle_status --strict
</verification>

<success_criteria>
- LIFE-01 and LIFE-02 have real live evidence, not fixture-only evidence.
- The cohort is reversible and Active serving authority remains coherent.
</success_criteria>

