---
phase: 24
plan: 05
type: execute
wave: 4
depends_on: [24-01, 24-02]
gap_closure: true
files_modified:
  - src/personal_knowledge/application/knowledge/build_knowledge_unit_vector_store.py
  - tests/unit/test_build_knowledge_unit_vector_store.py
autonomous: true
requirements: [QUAL-01, QUAL-02]
must_haves:
  truths:
    - Candidate embeddings include bounded semantics from resolvable eligible evidence.
    - Chroma documents and product responses still contain canonical safe text only.
    - Ineligible, missing and private-unresolved evidence never enters embedding input.
  artifacts:
    - evidence-aware vector builder with deterministic limits
    - privacy and determinism tests
    - candidate collection distinct from Active
---

<objective>
Repair the proven query-to-knowledge-unit semantic alignment gap without
weakening gates or exposing private evidence in product output.
</objective>

<tasks>
<task type="execute">
  <name>Separate embedding input from returned document</name>
  <read_first>src/personal_knowledge/application/knowledge/build_knowledge_unit_vector_store.py; src/personal_knowledge/retrieval/evidence.py; src/personal_knowledge/retrieval/semantic_search.py; src/personal_knowledge/core/chroma_client.py</read_first>
  <action>Add a deterministic embedding-text builder that starts with canonical subject/question/answer and appends only bounded evidence text whose reference resolves through the existing evidence authority and is eligible for semantic use. Keep the Chroma document field canonical question/answer only. Record enrichment counts/checksum in candidate metadata without recording evidence bodies.</action>
  <verify>python -m pytest tests/unit/test_build_knowledge_unit_vector_store.py -q</verify>
  <acceptance_criteria>Tests prove eligible enrichment, missing/ineligible exclusion, stable ordering and limits, and absence of raw evidence from stored documents and emitted metadata.</acceptance_criteria>
</task>
<task type="execute">
  <name>Build a non-active real candidate</name>
  <read_first>src/personal_knowledge/application/ku.py; docs/runbooks/ku-incremental.md; src/personal_knowledge/application/knowledge/promote_knowledge_index.py</read_first>
  <action>Run the vector build against the current 32,184 real canonical units. Write only a new candidate collection/version row. Capture collection name, unit count, embedding policy/checksum and active pointer before/after. Abort if any active authority changes.</action>
  <verify>pk-ku vector --write; pk-ku doctor --json --skip-ports</verify>
  <acceptance_criteria>Candidate count matches eligible current canonical units, candidate is queryable, Doctor has zero critical failures, and Active snapshot/collection are byte-for-byte unchanged.</acceptance_criteria>
</task>
</tasks>

<verification>
python -m pytest tests/unit/test_build_knowledge_unit_vector_store.py tests/unit/test_evidence_resolver.py -q
pk-ku doctor --json --skip-ports
</verification>

<success_criteria>
- A real evidence-aware candidate exists without serving activation.
- Privacy-safe product documents are unchanged while embedding semantics are enriched.
</success_criteria>

