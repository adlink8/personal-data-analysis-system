---
phase: 59
plan: 01
type: execute
wave: 1
depends_on: [58-02]
files_modified: [apps/personal_intelligence_kernel/src/control/operation-schema.mjs, apps/personal_intelligence_kernel/src/control/runtime-control.mjs, apps/personal_intelligence_kernel/src/events/schema.mjs, apps/personal_intelligence_kernel/test/runtime-control.test.mjs]
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "D-01: Pi SDK Kernel is the only AI coordinator; no Local Pi or second Agent runtime exists."
    - "D-02: every operation is bound to Task/Session/correlation/authority/receipt metadata."
    - "D-03: recovery actions use typed Kernel/Python authority entrypoints only."
    - "D-04: outcome_unknown is reconciled before any side-effect retry."
---

# Phase 59-01 Plan — Kernel operation control and recovery model

<objective>为 Task、Session、Skill、Tool、Provider 和 authority transaction 建立统一、可恢复的 Kernel operation 控制模型。</objective>
<threat_model>Assets: coordinator uniqueness, provider/data side effects, operation truth and receipts. Threats: duplicate controller, replayed side effect, stale cancel, forged completion and blind outcome_unknown retry. Controls: one Kernel owner, typed state machine, expected-state CAS, correlation, receipt reconciliation and authority fingerprint checks.</threat_model>

<tasks>
<task type="execute">
  <name>59-01-01 — Implement operation schema and deterministic reducer</name>
  <read_first>
    - .planning/phases/PDA-59-kernel-control-plane-and-runtime-observability/59-CONTEXT.md
    - apps/personal_intelligence_kernel/src/server.mjs
    - apps/personal_intelligence_kernel/src/events/schema.mjs
    - apps/personal_intelligence_kernel/src/skills/engine.mjs
  </read_first>
  <files>apps/personal_intelligence_kernel/src/control/operation-schema.mjs, apps/personal_intelligence_kernel/src/control/runtime-control.mjs, apps/personal_intelligence_kernel/src/events/schema.mjs, apps/personal_intelligence_kernel/test/runtime-control.test.mjs</files>
  <action>Define a versioned operation envelope for kernel_task, kernel_session, kernel_skill, domain_tool, provider and authority_transaction with operation/task/session IDs, correlation/causation, authority/side-effect classes, state, attempt/budget, receipt refs, fingerprint refs and allowed recovery actions. Implement a deterministic reducer that rejects illegal, stale, duplicate and out-of-order transitions and never imports local `pi` CLI or RPC runtime.</action>
  <acceptance_criteria>
    - Schema and reducer tests cover every operation kind and legal terminal/recovery state.
    - Duplicate/out-of-order events are idempotent or rejected without changing the last valid state.
    - Production code has one Kernel coordinator and no Local Pi launcher, handoff or ambient capability path.</acceptance_criteria>
  <verify><automated>node --test apps/personal_intelligence_kernel/test/runtime-control.test.mjs</automated></verify>
</task>
<task type="execute">
  <name>59-01-02 — Implement cancel, resume and receipt-first reconciliation</name>
  <read_first>
    - apps/personal_intelligence_kernel/src/control/runtime-control.mjs
    - apps/personal_intelligence_kernel/src/tools/domain-bridge.mjs
    - apps/personal_intelligence_kernel/src/skills/engine.mjs
    - src/personal_knowledge/services/pi_runtime_activation.py
  </read_first>
  <files>apps/personal_intelligence_kernel/src/control/runtime-control.mjs, apps/personal_intelligence_kernel/test/runtime-control.test.mjs</files>
  <action>Add expected-state and idempotency guarded cancel/resume/reconcile commands. For Provider or authority side effects in outcome_unknown, fetch linked receipts and fingerprint evidence first; return completed, resumable, compensatable or manual_review without blind retry. Journal command intent and result in the existing event ledger.</action>
  <acceptance_criteria>
    - Cancel/resume/reconcile are idempotent and reject stale expected states.
    - outcome_unknown never retries a side effect before receipt/fingerprint reconciliation.
    - Every action yields a linked metadata-only event and typed safe recovery result.</acceptance_criteria>
  <verify><automated>node --test apps/personal_intelligence_kernel/test/runtime-control.test.mjs</automated></verify>
</task>
</tasks>

<verification>Run reducer, duplicate event, provider timeout and authority receipt reconciliation fixtures with zero real Provider calls.</verification>
<success_criteria>One Pi SDK Kernel truthfully controls and recovers all AI/data operations without a second Agent runtime or duplicated side effect.</success_criteria>
