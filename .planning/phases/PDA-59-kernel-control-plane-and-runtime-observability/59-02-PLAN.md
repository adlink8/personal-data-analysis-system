---
phase: 59
plan: 02
type: execute
wave: 2
depends_on: [59-01]
files_modified: [src/personal_knowledge/services/pi_operation_projection.py, src/personal_knowledge/services/api_server.py, apps/personal_decision_cockpit/src/api/schemas.ts, apps/personal_decision_cockpit/src/api/hooks.ts, apps/personal_decision_cockpit/src/pages/system/SystemPage.tsx, tests/integration/test_pi_runtime_control.py, tests/contract/test_pi_operation_projection.py, apps/personal_decision_cockpit/src/test/PiOperationStatus.test.tsx]
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "D-05: telemetry distinguishes every Kernel/runtime/authority operation plane."
    - "D-06: projection and UI expose metadata only, never personal or credential-bearing bodies."
    - "Cockpit control intents cannot bypass Kernel or Python authority."
---

# Phase 59-02 Plan — Operation projection, Cockpit controls and recovery integration

<objective>提供同源、无正文的 Kernel operation 状态投影和受控 cancel/resume/reconcile UI，并验证跨平面恢复。</objective>
<threat_model>Assets: operation metadata, personal privacy, recovery authority and user trust. Threats: body leakage, fake health, stale UI mutation, direct authority bypass and hidden unknown outcomes. Controls: server-owned projection, forbidden-key scan, expected-state command, same-origin API, typed recovery and truthful degraded states.</threat_model>

<tasks>
<task type="execute">
  <name>59-02-01 — Add same-origin operation projection and guarded controls</name>
  <read_first>
    - src/personal_knowledge/services/pi_runtime_projection.py
    - src/personal_knowledge/services/api_server.py
    - apps/personal_decision_cockpit/src/pages/system/SystemPage.tsx
    - apps/personal_decision_cockpit/src/api/schemas.ts
  </read_first>
  <files>src/personal_knowledge/services/pi_operation_projection.py, src/personal_knowledge/services/api_server.py, apps/personal_decision_cockpit/src/api/schemas.ts, apps/personal_decision_cockpit/src/api/hooks.ts, apps/personal_decision_cockpit/src/pages/system/SystemPage.tsx, tests/contract/test_pi_operation_projection.py, apps/personal_decision_cockpit/src/test/PiOperationStatus.test.tsx</files>
  <action>Add same-origin `/api/pi/operations`, `/api/pi/operations/{id}` and guarded cancel/resume/reconcile routes backed by Kernel control APIs. Project only kind, stable IDs, state, safe reason, budget counters, timestamps, authority/receipt/fingerprint refs and allowed actions. Extend the existing System/Pi surface with distinct Kernel Task/Session, Skill, Tool, Provider, authority transaction and recovery states; never render prompt, response body, credential, personal content or local path.</action>
  <acceptance_criteria>
    - UI clearly distinguishes all operation kinds and terminal/degraded/outcome-unknown states.
    - Mutations require expected state and route through Kernel/Python authority contracts.
    - Python and frontend tests pass forbidden-key/privacy scans and truthful offline behavior.</acceptance_criteria>
  <verify><automated>python -m pytest tests/contract/test_pi_operation_projection.py -q; npm test --prefix apps/personal_decision_cockpit -- --run PiOperationStatus</automated></verify>
</task>
<task type="execute">
  <name>59-02-02 — Validate cross-plane recovery and single-coordinator invariants</name>
  <read_first>
    - apps/personal_intelligence_kernel/src/control/runtime-control.mjs
    - src/personal_knowledge/services/pi_operation_projection.py
    - src/personal_knowledge/services/warehouse_mutations.py
    - .planning/phases/PDA-59-kernel-control-plane-and-runtime-observability/59-VALIDATION.md
  </read_first>
  <files>tests/integration/test_pi_runtime_control.py, tests/contract/test_pi_operation_projection.py</files>
  <action>Use deterministic fixtures to exercise Skill crash/resume, Provider timeout/outcome_unknown, Tool cancellation, authority transaction compensation and stale UI commands. Assert one Kernel owner, complete correlation receipts, no duplicate provider/data side effect, unchanged or correctly compensated authority fingerprints, and zero Local Pi/runtime dependency.</action>
  <acceptance_criteria>
    - Every failure resolves to completed, resumable, compensated or manual_review with linked evidence.
    - Duplicate side effects and split-coordinator counts remain zero.
    - All integration tests run without local `pi` CLI, external model calls or credentials.</acceptance_criteria>
  <verify><automated>python -m pytest tests/integration/test_pi_runtime_control.py tests/contract/test_pi_operation_projection.py -q</automated></verify>
</task>
</tasks>

<verification>Automated Kernel control, operation projection, Cockpit privacy and cross-plane recovery tests.</verification>
<success_criteria>Users can observe and control every Kernel operation through one authority-safe control plane, with no Local Pi Agent dependency.</success_criteria>
