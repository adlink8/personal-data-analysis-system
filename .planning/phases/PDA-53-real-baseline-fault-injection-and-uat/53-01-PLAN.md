---
phase: 53
plan: 01
type: execute
wave: 1
depends_on: [52-02]
files_modified: [governance/manifests/ai/pi-baseline-preregistration.json, tests/e2e/test_pi_legacy_baseline.py, tests/e2e/test_pi_kernel_fault_matrix.py, tools/supported/evaluate_pi_kernel.py, ops/reports/evidence/pi-kernel-fault-matrix.json]
autonomous: true
requirements: [EVAL-01, EVAL-02, ACT-01]
must_haves:
  truths:
    - "D-01: paired baseline freezes cohort/model/input/budget before calls."
    - "D-02: one-shot arms and honest INCONCLUSIVE are mandatory."
    - "D-03: quality/cost/latency/recovery/tool choice remain separate."
    - "D-04: the full cross-process fault matrix is blocking."
---

# Phase 53-01 Plan — Preregistered baseline harness and fault matrix

<objective>冻结真实比较协议并先完成不产生费用的 replay/fault infrastructure，使真实调用只填充已锁定的 receipts。</objective>
<threat_model>Assets: evaluation validity, provider budget and authority integrity. Threats: post-hoc metric changes, unequal arms, hidden retry, fault-induced duplicate effect and synthetic evidence mislabeled real. Controls: immutable preregistration checksum, attempt ledger, paired inputs, explicit evidence class and fingerprints.</threat_model>
<tasks>
<task type="execute">
  <name>53-01-01 — Implement immutable paired baseline protocol and evaluator</name>
  <read_first>
    - .planning/phases/PDA-53-real-baseline-fault-injection-and-uat/53-CONTEXT.md
    - .planning/phases/31-recommendation-calibration-product-uat/31-CONTEXT.md
    - governance/manifests/ai/pi-ai-entrypoints.json
    - apps/personal_intelligence_kernel/src/models/provider-adapter.mjs
  </read_first>
  <files>governance/manifests/ai/pi-baseline-preregistration.json, tools/supported/evaluate_pi_kernel.py, tests/e2e/test_pi_legacy_baseline.py</files>
  <action>Define frozen authorized case IDs, input/evidence/snapshot checksums, identical model/route, timeout, token/cost ceiling, one attempt per arm and separate metrics for schema, grounding, Tool selection, latency, usage/cost, recovery and human usefulness. Evaluator validates prerequisites before calls, records paired receipts and outputs PASS/FAIL/INCONCLUSIVE without aggregate intelligence score. Replay fixtures exercise the full evaluator while marking evidence_class=synthetic_replay.</action>
  <acceptance_criteria>
    - Any post-run preregistration change invalidates receipts.
    - Unequal model/input/budget or attempts >1 forces INCONCLUSIVE/FAIL.
    - Synthetic replay cannot satisfy real baseline completion.
    - Baseline tests exit 0 with provider_calls=0.</acceptance_criteria>
  <verify><automated>python -m pytest tests/e2e/test_pi_legacy_baseline.py -q</automated></verify>
</task>
<task type="execute">
  <name>53-01-02 — Execute deterministic cross-process fault and privacy matrix</name>
  <read_first>
    - tests/integration/test_pi_task_recovery.py
    - tests/ops/test_pi_kernel_stack.py
    - .planning/phases/PDA-53-real-baseline-fault-injection-and-uat/53-VALIDATION.md
    - .planning/spikes/pi-embedded-personal-kernel/005-streaming-control/README.md
  </read_first>
  <files>tests/e2e/test_pi_kernel_fault_matrix.py, ops/reports/evidence/pi-kernel-fault-matrix.json</files>
  <action>Build fixtures for Provider timeout/rate-limit/outcome_unknown, Node/Python kill at dispatch boundaries, SSE disconnect/replay, DB busy/corrupt-copy, cancel race, supervisor restart exhaustion and oversized/malicious Tool result. For each assert terminal/recovery task state, no hidden retry, safe UI/API/log output and protected authority fingerprints. Generate metadata-only report with exact command, case, expected/observed safe code and pass status.</action>
  <acceptance_criteria>
    - Every D-04 fault case has automated expected/observed assertions.
    - No case duplicates provider/domain effect or advances protected state.
    - Sensitive fixture values are absent from evidence report and runtime surfaces.
    - Fault matrix test exits 0.</acceptance_criteria>
  <verify><automated>python -m pytest tests/e2e/test_pi_kernel_fault_matrix.py -q</automated></verify>
</task>
</tasks>
<verification>Run preregistration and fault suites; no real Provider calls are permitted in this plan.</verification>
<success_criteria>Real comparison is preregistered and the complete failure boundary is proven before spending or UAT.</success_criteria>
