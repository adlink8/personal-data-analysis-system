---
phase: 28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personal_knowledge/core/project_paths.py
  - src/personal_knowledge/external_context/schema.py
  - src/personal_knowledge/external_context/registry.py
  - src/personal_knowledge/external_context/migrate.py
  - src/personal_knowledge/external_context/cli.py
  - governance/policies/external_sources.yaml
  - governance/policies/artifact_layers.yaml
  - tests/unit/test_external_source_registry.py
  - tests/integration/test_external_authority_schema.py
  - tests/contract/test_external_authority_interfaces.py
autonomous: true
requirements: [PDI-01, PDI-02, PDI-04]
must_haves:
  truths:
    - External authority is physically and semantically separate from personal KU and serving data.
    - Source metadata is allowlisted, checksum-bound and contains no raw copyrighted or private body.
    - Migration is dry-run by default, idempotent and append-only after publication.
  artifacts:
    - path: src/personal_knowledge/external_context/schema.py
      provides: Frozen source, observation, fact and lifecycle contracts
    - path: governance/policies/external_sources.yaml
      provides: Two-source project/technology allowlist
    - path: tests/integration/test_external_authority_schema.py
      provides: FK, integrity, idempotency and immutable-history proof
  key_links:
    - from: governance/policies/external_sources.yaml
      to: src/personal_knowledge/external_context/registry.py
      via: canonical validation and definition checksum
    - from: src/personal_knowledge/external_context/migrate.py
      to: var/db/external_context.sqlite
      via: explicit --write migration only
---

<objective>
Create the minimal independent External Context authority contract, registry,
append-only schema and metadata-only local interface.
</objective>

<tasks>
<task type="execute">
  <name>Define source policy, types and independent database path</name>
  <files>governance/policies/external_sources.yaml; governance/policies/artifact_layers.yaml; src/personal_knowledge/core/project_paths.py; src/personal_knowledge/external_context/schema.py; src/personal_knowledge/external_context/registry.py</files>
  <action>Add exactly two allowlisted project/technology public sources and strict metadata validation. Reject duplicate authority, missing license/provenance/region/time policy, body-like keys and secret-like values. Register independent D/S artifacts without adding them to personal required serving roles.</action>
  <verify>python -m pytest tests/unit/test_external_source_registry.py tests/unit/test_artifact_registry.py -q</verify>
  <done>Registry checksum is stable, policy changes alter it, invalid/private/body-bearing definitions fail closed and the DB path differs from UNIFIED_DB.</done>
</task>
<task type="execute">
  <name>Add explicit migration and append-only authority schema</name>
  <files>src/personal_knowledge/external_context/migrate.py; tests/integration/test_external_authority_schema.py</files>
  <action>Create source projection, import run, observation, fact, support and lifecycle-event tables with FK/check constraints, stable IDs/checksums and UPDATE/DELETE rejection triggers. Default migration is inspection only; --write is explicit and idempotent.</action>
  <verify>python -m pytest tests/integration/test_external_authority_schema.py -q</verify>
  <done>Dry-run preserves bytes/tables, write migration is idempotent, FK/integrity pass, replay does not duplicate and authoritative UPDATE/DELETE fails.</done>
</task>
<task type="execute">
  <name>Expose metadata-only source and schema status interface</name>
  <files>src/personal_knowledge/external_context/cli.py; tests/contract/test_external_authority_interfaces.py</files>
  <action>Add local list/get/schema-status commands backed by one service contract. Reads never mutate; output excludes content/body/raw text and secrets. Migration remains explicit --write.</action>
  <verify>python -m pytest tests/contract/test_external_authority_interfaces.py -q</verify>
  <done>CLI/service contracts match, stable errors are returned, privacy metadata reports zero bodies and read fingerprints remain unchanged.</done>
</task>
</tasks>

<verification>
python -m pytest tests/unit/test_external_source_registry.py tests/integration/test_external_authority_schema.py tests/contract/test_external_authority_interfaces.py -q
python -m personal_knowledge.governance.preflight --ci
</verification>

<success_criteria>
PDI-01 has a real independent contract and PDI-02/PDI-04 have the schema and
separation foundation needed for bounded ingest without expanding scope.
</success_criteria>
