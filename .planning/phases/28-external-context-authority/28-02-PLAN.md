---
phase: 28
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - src/personal_knowledge/external_context/ingest.py
  - src/personal_knowledge/external_context/lifecycle.py
  - tests/integration/test_external_context_runs.py
  - tests/integration/test_external_context_privacy.py
autonomous: true
requirements: [PDI-01, PDI-02]
must_haves:
  truths:
    - Bounded source imports are manifest-bound, replayable and never store raw pages.
    - External fact lifecycle is append-only and conflict-aware.
  artifacts:
    - path: src/personal_knowledge/external_context/ingest.py
      provides: File-first bounded observation/fact publication
    - path: src/personal_knowledge/external_context/lifecycle.py
      provides: Current/stale/superseded/conflict/invalid projection
  key_links:
    - from: external source definition checksum
      to: external import run
      via: exact registry binding
---

<objective>Publish a bounded real-source cohort into canonical external facts with freshness, region and conflict lifecycle.</objective>
<tasks>
<task type="execute"><name>Build manifest-bound file-first ingest</name><files>src/personal_knowledge/external_context/ingest.py; tests/integration/test_external_context_runs.py</files><action>Accept only normalized bounded inputs from allowlisted sources, validate exact source/version/checksum/time/region, publish atomically and replay idempotently.</action><verify>python -m pytest tests/integration/test_external_context_runs.py -q</verify><done>Valid cohort commits exactly once; tamper, stale registry or fault injection leaves fingerprint unchanged.</done></task>
<task type="execute"><name>Add lifecycle and privacy gates</name><files>src/personal_knowledge/external_context/lifecycle.py; tests/integration/test_external_context_privacy.py</files><action>Project append-only fact events and reject raw body, secret, invalid time, unsupported region and unresolved provenance.</action><verify>python -m pytest tests/integration/test_external_context_privacy.py -q</verify><done>Lifecycle is reconstructable and canonical payloads contain no raw public/private body.</done></task>
</tasks>
<verification>python -m pytest tests/integration/test_external_context_runs.py tests/integration/test_external_context_privacy.py -q</verification>
<success_criteria>A real bounded cohort can be imported safely without network crawling or personal-authority writes.</success_criteria>
