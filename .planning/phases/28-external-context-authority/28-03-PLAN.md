---
phase: 28
plan: 03
type: execute
wave: 3
depends_on: [28-02]
files_modified:
  - src/personal_knowledge/external_context/snapshots.py
  - src/personal_knowledge/external_context/service.py
  - src/personal_knowledge/intelligence/decision/context_binding.py
  - tests/integration/test_external_snapshot_lifecycle.py
  - tests/contract/test_decision_context_binding.py
autonomous: true
requirements: [PDI-03, PDI-04]
must_haves:
  truths:
    - External authority switches independently and reversibly.
    - Decision context validates exact Personal and External snapshot hashes on every create/read.
  artifacts:
    - path: src/personal_knowledge/external_context/snapshots.py
      provides: Prepare, validate, activate, rollback and forward restore
    - path: src/personal_knowledge/intelligence/decision/context_binding.py
      provides: Strongly typed dual-snapshot binding
  key_links:
    - from: decision context binding
      to: personal and external authorities
      via: two read-only exact hash validations
---

<objective>Create independent External Snapshot authority and fail-closed dual-snapshot DecisionContextBinding.</objective>
<tasks>
<task type="execute"><name>Implement external snapshot lifecycle</name><files>src/personal_knowledge/external_context/snapshots.py; tests/integration/test_external_snapshot_lifecycle.py</files><action>Use independent immutable snapshot/member/watermark/authority/event tables with prepare→validate→activate and reversible event history.</action><verify>python -m pytest tests/integration/test_external_snapshot_lifecycle.py -q</verify><done>Failure/dry-run preserves authority; activation, rollback and forward restore maintain exact manifest parity.</done></task>
<task type="execute"><name>Add typed dual-snapshot binding and read APIs</name><files>src/personal_knowledge/external_context/service.py; src/personal_knowledge/intelligence/decision/context_binding.py; tests/contract/test_decision_context_binding.py</files><action>Bind Personal/External IDs and hashes plus region/freshness/conflict policy. Validate both DBs read-only; reject drift, stale, region mismatch or unresolved conflict.</action><verify>python -m pytest tests/contract/test_decision_context_binding.py -q</verify><done>Bindings cannot be created or read against mismatched authorities and never copy external facts into personal storage.</done></task>
</tasks>
<verification>python -m pytest tests/integration/test_external_snapshot_lifecycle.py tests/contract/test_decision_context_binding.py -q</verification>
<success_criteria>PDI-03/04 are enforceable and Phase 29 can consume one exact dual-state decision context.</success_criteria>
