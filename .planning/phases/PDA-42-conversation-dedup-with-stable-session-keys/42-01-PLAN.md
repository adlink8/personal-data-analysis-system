# Plan 42-01: 稳定键 crosswalk + supersede 语义 + 确定性重建（builder 改造）

```yaml
phase: 42
plan: 42-01
wave: 1
depends_on: []
autonomous: true
requirements: [DED-01, DED-02]
files_modified:
  - src/personal_knowledge/application/conversation/build_canonical_agent_conversations.py
  - tests/integration/test_canonical_dedup_stable_keys.py    # 新建
  - var/reports/phase42_baseline.json                        # 新建，task 1 运行产物（基线固化）
  - var/backups/agent_conversations_pre42_<ts>.sqlite        # 新建，task 6 收尾产物（改键前旧代 canonical 固化副本，42-02 --old-canonical-db 指定输入）
```

<objective>
把 canonical 会话去重身份从 file_hash（`cs|merged|<fh>` 随内容漂移）改为 (source, source_session_id) 复合稳定键（D-01）：补全 schema 早已预留、从未实现的 source_mapping pass；file_hash 降级为变更检测信号；supersede 由 builder 每轮全量重建时确定性生成（D-03，canonical 是 DROP+重建，一次性 UPDATE 会被下次 sync 冲掉）；三处无 ORDER BY 补确定性排序（D-04）；所有降级/跳过路径带 stats 计数（D-05）。完成后同一会话的 jsonl 追加被识别为"同一 canonical session 的更新"，全量重建幂等且跨运行 byte-stable。
</objective>

<context>
- 锁定决策：**D-01**（身份 = (source, source_session_id)，hash 仅变更检测）、**D-02**（消息级 (session_key, ordinal) 只作对齐/去重键——**不重铸 cm| id**）、**D-03**（双份标 superseded 不硬删）、**D-04**（代表行确定性 ORDER BY）、**D-05**（失败不静默）。
- 实测现状（RESEARCH §1）：去重键唯一现状是 file_hash（Pass 1，builder :298-344）；现存双份 6 组/12 行（全 codex，AV twin 均可由原生 id 唯一定位）；潜在双份池 = 275 个 merged 会话（csid 含 fh）；`match_method='source_mapping'`（:96）与 `merged_by_source_mapping`（:135）早已预留、从未实现。
- 改键安全性（RESEARCH §1.3/§5.1）：`cm|` id 铸造（:541/:604）不含 canonical_session_id，**重铸会话键零 evidence ref 破坏**；EvidenceResolver 动态列探测，加列安全。
- **顺序硬约束（RESEARCH §3.3 / R7）**：normalized 滞后 12 天（920 vs AV live 1,159 会话）。必须先用旧代码常规 sync 消化积压（本 plan task 1），再动改键（task 2 起）——否则改键 delta 与积压 delta 混在一起无法归因验收。
- 红线（全部任务适用）：AgentsView live 只读（原生 id 一律取自 normalized `sessions.source_session_id`）；不硬删；不重铸 cm| id；不改 eligibility.py 的 SQL（`s.evidence_eligible=1` 天然排除 superseded）；`source_session_ref` 不作身份键（1 ref 挂 19 会话，R6）。
</context>

<tasks>

<task id="1" name="前置：旧代码消化 12 天 normalized 积压并固化基线">
<read_first>
- docs/runbooks/product-sync.md（sync→inspect→prepare 流程段）
- src/personal_knowledge/application/sync.py:60-73（pk-sync conversations 编排：inventory→normalized→canonical 串行）
- .planning/phases/PDA-42-conversation-dedup-with-stable-session-keys/42-RESEARCH.md §1.2-1.3、§3.3（基线数值：926 sessions / 6 双份组 / 23,009 refs / 756 存量孤儿）
</read_first>
<action>
在改任何代码之前，用当前 main 代码完成积压消化并固化基线：
1. 执行 `pk-sync conversations --write`，消化 AgentsView→normalized→canonical 的 12 天积压（normalized 会话预期 920→约 1,159）。
2. `pk-ku inspect` 查看两轨 delta；如有 delta 按常规 `pk-ku prepare` 周期消化（此步目的是把"数据积压 delta"在改键前清账，使 task 6 之后的 delta 可全部归因于改键）。
3. 用只读 sqlite 查询（scratchpad 内联脚本即可，不新增 src 代码）固化基线到 `var/reports/phase42_baseline.json`，必含键：`canonical_sessions_total`、`canonical_messages_total`、`dup_groups_legacy_av`（legacy-primary eligible 会话中内嵌 uuid 可在 AV 侧命中的组数，旧键口径）、`evidence_refs_total`（knowledge_unit_evidence distinct refs）、`evidence_refs_unresolved_baseline`（不可解析到当前 canonical 的存量孤儿数，≈756，R4 存量债基线）、`units_source_ref_unresolved_baseline`、`watermark_committed`、`watermark_committed_assistant`（checksum 值）。
</action>
<acceptance_criteria>
- `pk-sync conversations --write` exit 0；canonical 库 import_runs（或 builder 输出）最新一次运行时间为执行当日。
- `var/reports/phase42_baseline.json` 存在且含上列 8 个键，值均为非空数值/字符串。
- `pk-ku doctor --skip-ports` exit 0。
</acceptance_criteria>
</task>

<task id="2" name="source_mapping pass + 稳定 csid 铸法（取消 cs|merged|<fh>）">
<read_first>
- src/personal_knowledge/application/conversation/build_canonical_agent_conversations.py:125-127（_norm_id 铸键惯例）、:276-281（av_by_hash 后写覆盖反模式）、:298-344（Pass 1 file_hash merge + :305 `cs|merged|<fh>` 铸法 + :326-341 source_links 写法）、:351/:386（Pass 2/3 稳定铸法——目标形态样板）、:96（match_method CHECK 已含 'source_mapping'，零 schema 改动）
- src/personal_knowledge/application/conversation/build_agentsview_normalized.py:98-107（normalized sessions.source_session_id 原样保存 AV sessions.id——原生 id 唯一取用点）
- .planning/phases/PDA-42-conversation-dedup-with-stable-session-keys/42-PATTERNS.md「文件 1」段（惯例/反模式速查）
- .planning/phases/PDA-42-conversation-dedup-with-stable-session-keys/42-RESEARCH.md §2（键评估：AV id 唯一性 0 重复、legacy `rollout-<ts>-<uuid>` ⇔ AV `codex:<uuid>` 归一规则、R6 反例）
</read_first>
<action>
改 `build_canonical_agent_conversations.py` 的 crosswalk：
1. 新增模块级函数 `_native_session_uuid(source: str, session_id: str) -> str | None`：legacy 侧从 `rollout-<ts>-<uuid>` stem 用正则提取尾部 uuid；AV 侧对 `codex:<uuid>` 去 `codex:` 前缀；其余形态返回 None（当前只有 codex 存在跨源对齐需求）。
2. 匹配顺序重排：**Pass 1 = source_mapping**（`_native_session_uuid` 归一相等 → merge，link 的 `match_method="source_mapping"`、`match_confidence="strong"`，结构照抄 :326-341 现有写法）；**Pass 2 = file_hash 相等**作兜底与确认信号（沿用 `match_method="file_hash"`）；单源 pass 保持。对 source_mapping 已配对的组同时比对 file_hash：相等计 `file_hash_confirmed`，不等计 `file_hash_divergent`（= 检测到内容增长，D-01 的变更检测语义），**不改变归属**。
3. csid 铸法统一：AV 在场（含 merged）一律 `_norm_id("cs", "agentsview", av_sid)`；真 legacy-only 一律 `_norm_id("cs", "legacy", leg_sid)`；**删除 `_norm_id("cs", "merged", fh)` 铸法**。merged 状态只落 `merged` 列与 session_source_links，不进身份键。
4. AV 会话主索引从 `av_by_hash[fh]` 换成按 `source_session_id` 建（实测 0 重复）；file_hash 只建辅助索引。若出现意外重复稳定键：不静默覆盖，按 (started_at, session_id) 排序保留首个并计 `unexpected_duplicate_stable_key`（D-05）。
5. 红线：不新增对 AgentsView live 库的任何连接；`source_session_ref` 不参与身份判定；cm| id 铸造（:541/:604）一个字符不动。
</action>
<acceptance_criteria>
- 源码断言：`grep -n '_norm_id("cs", "merged"' src/personal_knowledge/application/conversation/build_canonical_agent_conversations.py` 0 命中；`grep -n '"match_method": "source_mapping"' 同文件` ≥1 命中；`grep -n '_norm_id("cm"' 同文件` 恰好 2 处且实参与现状一致（"av" / "legacy"）。
- 单测（task 5 用例 3）：fixture 中 legacy 快照 sha256 ≠ AV file_hash 但 uuid 相同 → 重建后 canonical_sessions 仅 1 行、session_source_links 含 match_method='source_mapping' 的 legacy link、`stats.merged_by_source_mapping == 1`、`stats.file_hash_divergent == 1`。
- `grep -n "agentsview.*mode=ro\|sessions.db" src/personal_knowledge/application/conversation/build_canonical_agent_conversations.py` 不出现任何指向 `~/.agentsview` live 库的新路径（源库仍只有 normalized/legacy 两个）。
</acceptance_criteria>
</task>

<task id="3" name="lifecycle/superseded_by 列 + supersede 确定性判定 + INSERT 占位耦合同步">
<read_first>
- src/personal_knowledge/application/conversation/build_canonical_agent_conversations.py:42-60（CANONICAL_SCHEMA dict 字面量——全量重建，追加列即可无需 ALTER）、:452-463（canonical_sessions INSERT **写死 16 个 ? 占位——本文件最易漏的耦合点**）、:436-439 与 :641-650（DROP+staging+os.replace 全量重建：supersede 必须每轮由 builder 重算）
- src/personal_knowledge/retrieval/evidence.py:68-81（EvidenceResolver 动态列探测 `_columns`——加列安全依据）
- src/personal_knowledge/application/knowledge/eligibility.py:150-160（`s.evidence_eligible=1` session 门——**不改此 SQL** 的依据：superseded 置 0 即自然退出 eligible 集）
- .planning/phases/PDA-42-conversation-dedup-with-stable-session-keys/42-PATTERNS.md「文件 1」第 5 点（schema 加列类比）
</read_first>
<action>
1. `CANONICAL_SCHEMA["canonical_sessions"]` 列表尾部追加 `("lifecycle", "TEXT NOT NULL DEFAULT 'active'")` 与 `("superseded_by_canonical_id", "TEXT")`；`_write_canonical_store` 的 INSERT 列名清单与 `?` 占位从 16 同步扩到 18，行 tuple 同步补值。
2. supersede 判定写进 build_crosswalk（每轮重建确定性重算，**禁止做成一次性 UPDATE 脚本**）：同一稳定键 (source, source_session_id) 组内、或跨源归一键相同但无法安全合并（弱匹配/同源双份）时——保留消息数最多的一份为 `lifecycle='active'`；平局按 started_at 最新，再平局按 csid 字典序最小。其余行 `lifecycle='superseded'`、`superseded_by_canonical_id=<保留者 csid>`、`evidence_eligible=0`（复用既有 ineligible 语义），计 `superseded_marked`（D-05）。
3. 强匹配（native id 归一命中，task 2 的 Pass 1）仍是**合并为一个 session**，不产生 superseded 行；superseded 仅用于不能安全合并的情形。被 supersede 行的消息照常写入 canonical_messages（**不硬删**，lineage 由 session_source_links + superseded_by 双向可查）。
4. 红线：不改 eligibility.py；不 DELETE 任何 canonical 行。
</action>
<acceptance_criteria>
- fixture 重建后 `PRAGMA table_info(canonical_sessions)` 含 `lifecycle`（dflt_value 'active'、notnull=1）与 `superseded_by_canonical_id` 两列。
- 单测（task 5 用例 5）：弱匹配双份 fixture → superseded 行 `evidence_eligible=0` 且 `superseded_by_canonical_id` 等于 active 行 csid；两个会话的 canonical_messages 行都仍在库中（COUNT 断言，不硬删）。
- `git diff --name-only` 不含 `src/personal_knowledge/application/knowledge/eligibility.py`。
- fixture 全流程构建无 sqlite OperationalError（INSERT 占位与列数不同步会在此立刻失败——task 5 任一用例通过即证明耦合已同步）。
</acceptance_criteria>
</task>

<task id="4" name="D-04 确定性 ORDER BY（三处）+ CrosswalkStats 扩展与打印">
<read_first>
- src/personal_knowledge/application/conversation/build_canonical_agent_conversations.py:152-158（_load_agentsview_sessions 无 ORDER BY）、:164-190（_load_legacy_sessions 无 ORDER BY + 去重"保留第一条"由物理扫描序决定）、:303-304（Pass 1 代表行 `leg_sessions[0]` 组内无排序）、:130-143（CrosswalkStats dataclass，:135 `merged_by_source_mapping` 已声明未累加）、:674-694（run() 打印段 + `duplicate_source_links` 的 "(must be 0)" gate 与 exit code）
- .planning/phases/PDA-42-conversation-dedup-with-stable-session-keys/42-RESEARCH.md §3.2（实测：同 session_id 双行 timestamp 不同 `12:00:33` vs `14:13:55`，代表行漂移是真实风险）
</read_first>
<action>
1. `_load_legacy_sessions` 的 SQL 加 `ORDER BY timestamp ASC, raw_file ASC, rowid ASC`（去重"保首条"语义不变，但首条从此确定性）。
2. `_load_agentsview_sessions` 的 SQL 加 `ORDER BY started_at, session_id`。
3. Pass 1 组内 legacy 代表行：组内按 (timestamp ASC, raw_file ASC) 排序后取首，不再取列表物理序首元素。
4. `CrosswalkStats` 新增 int 字段（default 0）：`stable_key_matched`、`file_hash_confirmed`、`file_hash_divergent`、`superseded_marked`、`unexpected_duplicate_stable_key`；`merged_by_source_mapping` 开始真实累加（task 2/3 的计数落点在此收口）。run() 打印段逐行输出全部新字段；`duplicate_source_links` 的 gate 打印与 `return 0 if ... else 1` 语义保持不变。
</action>
<acceptance_criteria>
- `grep -n "ORDER BY" src/personal_knowledge/application/conversation/build_canonical_agent_conversations.py` ≥2 命中，且分别位于 `_load_legacy_sessions` 与 `_load_agentsview_sessions` 的 SQL 字符串内（含 `timestamp ASC, raw_file ASC, rowid ASC` 与 `started_at, session_id` 两个精确子串）。
- `python -c "from personal_knowledge.application.conversation.build_canonical_agent_conversations import CrosswalkStats; s=CrosswalkStats(); print(s.stable_key_matched, s.file_hash_confirmed, s.file_hash_divergent, s.superseded_marked, s.unexpected_duplicate_stable_key, s.merged_by_source_mapping)"` 输出 `0 0 0 0 0 0`。
- fixture 构建的 stdout 含 `stable_key_matched`、`file_hash_divergent`、`superseded_marked` 三个字段名（run() 打印段生效）。
</acceptance_criteria>
</task>

<task id="5" name="fixture 集成测试：增长≠新会话 / 双跑 byte-stable / R6 负向 / supersede">
<read_first>
- tests/integration/test_agentsview_normalization.py:497-541（_make_normalized_fixture 手工建表模式——不 import builder schema，防御 schema 漂移被静默吸收）、:544-572（build_canonical_run 调用 + sqlite 直连断言范式）、:333-352（同输入双跑幂等模板）、:355-375（dry-run 不落盘 / staging 不残留用例）
- .planning/phases/PDA-42-conversation-dedup-with-stable-session-keys/42-PATTERNS.md「文件 3」段
- .planning/phases/PDA-42-conversation-dedup-with-stable-session-keys/42-RESEARCH.md §4.1/§4.3（幂等比对口径：全表 sorted dump sha256 + compute_source_checksum 双等值）
</read_first>
<action>
新建 `tests/integration/test_canonical_dedup_stable_keys.py`（tmp_path 全隔离，fixture 手工 CREATE TABLE，legacy 路径一律指向 tmp，不触真实 data/），5 个用例：
1. `test_growth_is_same_session`：会话 X（file_hash=H1，n 条消息）构建 → fixture 追加 2 条消息、file_hash 改 H2 → 重建：canonical_session_id 不变、消息集合为原集合超集、canonical_sessions 行数不变。**不断言 `file_hash_divergent`**——该计数仅在 source_mapping 配对组内（legacy 冻结 sha256 vs AV 现值）比对时产生，纯 AV-only fixture 无比对锚，无法命中；csid 不变 + 消息超集 + 会话行数不变已足证"增长≠新会话"，divergent 断言由用例 3 承载。
2. `test_double_build_byte_stable`（DED-02）：同输入构建到 canon_a/canon_b 两个 dest：两库全表 sorted dump 的 sha256 相等，且 `compute_source_checksum(canon_a) == compute_source_checksum(canon_b)`。
3. `test_source_mapping_merges_legacy_twin`：legacy sha256 ≠ AV file_hash、内嵌 uuid 相同 → 1 个 canonical session + source_mapping link + `merged_by_source_mapping == 1`（task 2 验收载体）。
4. `test_shared_source_session_ref_not_merged`（R6 负向）：两个 AV 会话（1 主 + 1 subagent）共享 `source_session_ref` → 仍是 2 个 canonical session。
5. `test_superseded_lifecycle`：弱匹配双份 → superseded 行断言（task 3 验收载体：lifecycle/superseded_by/evidence_eligible=0/消息不删）。
断言带中文失败消息（沿用既有 `assert h1 == h2, f"幂等失败: ..."` 风格）。
</action>
<acceptance_criteria>
- `pytest tests/integration/test_canonical_dedup_stable_keys.py -v` 5 用例全绿。
- `pytest tests/integration/test_agentsview_normalization.py -q` 既有测试**零修改**通过（旧行为不回归；若 canonical 相关既有用例因 csid 铸法变化需调整期望值，调整仅限期望值且在 commit message 说明）。
</acceptance_criteria>
</task>

<task id="6" name="首轮改键重建（真实库）+ 零重复验收 SQL">
<read_first>
- .planning/phases/PDA-42-conversation-dedup-with-stable-session-keys/42-RESEARCH.md §4.2（验收 SQL A-D 原文）、§5.2（watermark 双 key 首轮真实口径 delta 属预期——**本 task 不消化**）
- src/personal_knowledge/application/sync.py:60-73
- src/personal_knowledge/application/conversation/build_canonical_agent_conversations.py:646-648（单代滚动 backup：每次 --write 都会换代——步骤 1 收尾复制的依据）
</read_first>
<action>
1. `pk-sync conversations --write`（新代码首轮重建真实 canonical 库）；把 run 输出的 stats（stable_key_matched / merged_by_source_mapping / file_hash_divergent / superseded_marked / unexpected_duplicate_stable_key / duplicate_source_links）记录进执行笔记。**收尾（紧接本步立即执行，不得插入其他 sync）**：builder 单代滚动 backup（AGENT_CONVERSATIONS_DB 同目录的 `agent_conversations.backup.sqlite`，builder :646-648，每次 --write 换代）此刻正是改键前最后一代旧库，立即复制固化为 `var/backups/agent_conversations_pre42_<ts>.sqlite`（shutil.copy2 / Copy-Item），路径记入执行笔记，作为 42-02 `--old-canonical-db` 的指定输入——否则 42-01 与 42-02 之间任何一次 `pk-sync conversations --write` 都会把 backup 换代，旧代 legacy 消息 content 无处可取。
2. 对重建后的真实 canonical 库执行 RESEARCH §4.2 验收 SQL：A（一个源会话只归属一个 canonical session）、B（active 会话稳定键唯一）、C（(canonical_session_id, ordinal) 无重复）——三条结果必须为 0；D（codex 双份归零）：legacy-primary eligible 会话中内嵌 uuid 可在 AV 链接中找到的个数，用 python 提取 uuid 核对，必须为 0（预期基线中的 6 组存量双份全部经 source_mapping 合一）。
3. **明示不做**：本 task 之后 `pk-ku inspect` 将出现 deleted_refs 突增（superseded/被合并副本的 eligible 消息退出口径，RESEARCH §5.2）——这是**预期的真实口径修正**，不在本 plan 消化、不跑 prepare、不触发 Gate B STOP 反射；受控消化在 42-03。
</action>
<acceptance_criteria>
- `pk-sync conversations --write` exit 0，stdout 含 `duplicate_source_links` 为 0 的 gate 行。
- SQL A、B、C 对真实库执行结果均为 0（命令与输出原样记录进执行笔记）。
- D 检查输出 0（uuid 核对脚本与输出记录进执行笔记）。
- `var/reports/phase42_baseline.json` 的 `dup_groups_legacy_av`（改键前 6）与本次 D=0 形成前后对照，写进执行笔记。
- `var/backups/agent_conversations_pre42_<ts>.sqlite` 存在，且只读查询其 `SELECT COUNT(*) FROM canonical_sessions` 等于 baseline json 的 `canonical_sessions_total`（证明确为改键前旧代而非新键重建产物）；文件完整路径记入执行笔记（42-02 task 1/3/4 引用）。
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `pytest tests/integration/test_canonical_dedup_stable_keys.py tests/integration/test_agentsview_normalization.py -q` 全绿
2. 真实库零重复 SQL A/B/C/D 全 0（task 6 记录）
3. `python -c "...CrosswalkStats..."` 新字段默认 0（task 4）
4. `pk-ku doctor --skip-ports` exit 0（dedup 检查项接线在 42-03，此处仅回归）
5. `var/reports/phase42_baseline.json` 存在且 8 键齐全（42-02 对账依赖）
6. `var/backups/agent_conversations_pre42_<ts>.sqlite` 已固化且 canonical_sessions 行数与基线一致（task 6 收尾，42-02 --old-canonical-db 指定输入）
</verification>

<must_haves>
- 会话身份 = (source, source_session_id) 稳定键，file_hash 仅作变更检测（D-01）：源码中 `cs|merged|<fh>` 铸法不存在
- 同一会话内容增长被识别为同一 canonical session 的更新而非新会话（success criterion 1 前半；`test_growth_is_same_session`）
- 旧副本有明确 supersede 语义：lifecycle='superseded' + superseded_by_canonical_id + evidence_eligible=0，不硬删（success criterion 1 后半，D-03）
- 全量重建幂等且跨运行确定性：双跑 byte-stable 测试 + 真实库 SQL A/B/C 全 0 + D-04 三处确定性 ORDER BY（success criterion 2）
- 所有降级/跳过/映射失败路径带 stats 计数并打印（D-05）
- 红线守住：cm| id 铸法未动、AgentsView live 零新增依赖、eligibility.py SQL 未改、source_session_ref 未作身份键（R6 负向单测在案）
</must_haves>

<threat_model>
- 真实库重建风险：builder 既有 staging + os.replace + backup 机制承担回退；task 1 基线 json 提供改键前对照。
- 275 个 merged 会话 csid 换值：已实测无下游 ref 耦合（cm| id 不含 csid，RESEARCH §5.1），serving snapshot 以 DB checksum 为 version 本就随重建换版。
- delta 突增误判：task 6 明示"不跑 prepare、留 42-03 受控消化"，防止执行者把预期口径修正当数据事故回滚（AGENTS.md KU 硬规则 3 的 STOP 语义仅针对"inspect 有 delta 而 prepare no_op"的真异常）。
- 隐私面：不引入 file_path 等 live 库字段（RESEARCH §2 已否决）；测试与基线报告只含计数/hash/id，不回显消息原文。
</threat_model>
