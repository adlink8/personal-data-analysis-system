---
phase: 35
plan: 02
wave: 2
depends_on: [35-01]
requirements: [LIVE-03]
autonomous: true
---

<objective>固定并验证 ChatGPT MCP descriptor snapshot 和 live contract。</objective>
<tasks>
<task type="execute"><name>生成 canonical descriptor snapshot</name><action>记录 44 tools 的 name/input/output/annotations canonical hash 和 protocol version，并提供 check/update CLI。</action><acceptance_criteria>离线 server 与 live `/mcp` snapshot 完全一致，变更需显式 update。</acceptance_criteria></task>
<task type="execute"><name>本地 runtime MCP smoke</name><action>initialize、tools/list、read/explain 和 compact boundary 冒烟，记录 JSON evidence。</action><acceptance_criteria>协议、工具数、hash、预算和 typed errors 全通过。</acceptance_criteria></task>
</tasks>
