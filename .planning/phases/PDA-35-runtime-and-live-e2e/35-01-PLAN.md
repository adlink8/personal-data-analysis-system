---
phase: 35
plan: 01
wave: 1
depends_on: []
requirements: [LIVE-01]
autonomous: true
---

<objective>重写为 production-safe foreground supervisor，并完成静态/故障注入审计。</objective>
<tasks>
<task type="execute"><name>实现 ops runtime supervisor 和薄 wrapper</name><action>参数化三服务、owned PID、NoProxy health、bounded backoff、secret preflight、ephemeral orchestration secret、direct tunnel doctor、structured logs/state/readiness。</action><acceptance_criteria>无任意杀进程、无硬编码凭据/用户路径、Check/DryRun 零写入。</acceptance_criteria></task>
<task type="execute"><name>生产脚本场景测试与审计</name><action>覆盖 parser、missing secret/profile/executable、port conflict、dependency down、log dir absence、idempotency/healthy reuse 和 cleanup ownership；运行 bundled auditor 并生成中文报告。</action><acceptance_criteria>Critical/High=0 且 runtime verification evidence 完整。</acceptance_criteria></task>
</tasks>
