---
phase: 38
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - apps/personal_decision_cockpit/src/api/orchestration.ts
  - apps/personal_decision_cockpit/src/components/decision/ConfirmDrawer.tsx
  - apps/personal_decision_cockpit/src/components/decision/NewSessionDialog.tsx
  - apps/personal_decision_cockpit/src/components/decision/NewSessionFlow.tsx
  - apps/personal_decision_cockpit/src/pages/sessions/SessionPage.tsx
  - apps/personal_decision_cockpit/src/test/ConfirmDrawer.test.tsx
  - apps/personal_decision_cockpit/src/test/NewSessionFlow.test.tsx
  - apps/personal_decision_cockpit/src/test/SessionPage.test.tsx
  - apps/personal_decision_cockpit/src/test/orchestration.test.ts
autonomous: true
requirements: [DEC-02]
must_haves:
  truths:
    - "D-38-03：浏览器只暴露既有 project + low 的 prepare → exact preview → explicit confirm → commit/replay 路径；每次写入都显示操作专属确认文案。"
    - "D-38-04：Confirm Drawer 显示服务端原样 Preview、将创建事件、不会执行的动作、checksum、expected sequence、idempotency key 和风险提示；Preview 变化或过期必须重新 prepare。"
    - "D-38-05 D-38-06：同一确认的 exact replay 显示原 event 且不新增写入；actor identity 只在当前运行期存在，刷新后的会话只能 resume/explain 只读。"
  artifacts:
    - path: apps/personal_decision_cockpit/src/components/decision/ConfirmDrawer.tsx
      provides: 精确 Preview、具体确认文案、焦点管理和零副作用取消的确认抽屉
    - path: apps/personal_decision_cockpit/src/pages/sessions/SessionPage.tsx
      provides: 由服务端状态机限定的线性会话推进、receipt 与 exact replay 呈现
    - path: apps/personal_decision_cockpit/src/test/NewSessionFlow.test.tsx
      provides: project/low、纯 prepare、volatile actor 与不允许一键执行的回归
  key_links:
    - from: browser orchestration client
      to: /agent/session/prepare and /agent/session/* guarded REST routes
      via: 服务端原样 Preview、显式 confirmation、expected sequence 和 caller-supplied idempotency key
---

<objective>复用 v1.3 Guarded Orchestration，把已通过资格门的低风险 project 决策连接为逐步、精确、可重放的浏览器确认工作流；不改变任何 authority 的生命周期、风险策略或 Provider 行为。</objective>

<threat_model>
- T-38-05 隐式写入：浏览器为了“顺滑”自动 confirm/advance，或一次点击运行完整 session。
- T-38-06 Preview 替换：客户端重排、补写、过期后重用或改动服务端 exact Preview。
- T-38-07 双写：网络重试生成新 idempotency key，或把 replay 误报成第二次新增事件。
- T-38-08 身份伪续写：刷新后持久化 actor hash、伪称旧会话仍可继续写入。
- 阻断条件：任何 session/confirmation/sequence/binding 不一致，或前置 truth gate 未通过，必须停止在只读状态并交给 typed recovery。
</threat_model>

<tasks>
<task type="execute"><name>收口浏览器编排 client 与 volatile actor 边界</name><files>apps/personal_decision_cockpit/src/api/orchestration.ts; apps/personal_decision_cockpit/src/test/orchestration.test.ts</files><read_first>.planning/phases/PDA-38-guarded-decision-workspace/38-CONTEXT.md; apps/personal_decision_cockpit/docs/write-flow.md; src/personal_knowledge/intelligence/orchestration/service.py; tests/contract/test_orchestration_interfaces.py</read_first><action>审计并收口相对同源 `/agent/session/*` client：prepare 固定传递 `domain=project`、`risk_budget=low` 和当前运行期的 actor hash；确认/推进只原样发送服务端 Preview、指定 operation、Z 时间戳与调用方生成的 idempotency key。actor seed/hash 只能驻留模块运行期内存，不得写入 localStorage、sessionStorage、URL、Query cache persistence、DOM 或日志。client 负责安全规范化 compact typed error，但不得铸造 confirmation/HMAC、替换 payload、代替服务端检查 next transition 或在 fetch 失败时自动重发。</action><acceptance_criteria>prepare 是单次零写入请求；confirm/execute body 与服务端 Preview 深相等；刷新后会得到新的 actor identity，旧 session 不能伪装为可写；前端没有浏览器端 Provider 请求或直接 Pilot/Analysis/Calibration 写入路径。</acceptance_criteria><verify>cd apps/personal_decision_cockpit; npm run test -- --run src/test/orchestration.test.ts</verify></task>

<task type="execute"><name>实现 exact preview、具体确认与无副作用取消的 Confirm Drawer</name><files>apps/personal_decision_cockpit/src/components/decision/ConfirmDrawer.tsx; apps/personal_decision_cockpit/src/components/decision/NewSessionDialog.tsx; apps/personal_decision_cockpit/src/components/decision/NewSessionFlow.tsx; apps/personal_decision_cockpit/src/test/ConfirmDrawer.test.tsx; apps/personal_decision_cockpit/src/test/NewSessionFlow.test.tsx</files><read_first>apps/personal_decision_cockpit/src/components/decision/ConfirmDrawer.tsx; apps/personal_decision_cockpit/src/components/decision/NewSessionFlow.tsx; apps/personal_decision_cockpit/src/components/feedback/TypedRecoveryPanel.tsx</read_first><action>让 New Session Flow 只提供 project/low 的明确人工输入并将 prepare 标示为“不会写入”；收到 Preview 后以不可编辑、原样对象进入 Confirm Drawer。抽屉必须显示 operation、将新增的 append-only event、明确不会执行的动作、preview checksum、expected sequence、idempotency key、风险/限制和操作专属确认文案，避免“继续/确定”等模糊按钮。实现可访问 dialog 的初始焦点、焦点回收、Esc/关闭只取消 UI、busy 防双击；关闭或取消不得请求 confirm/execute。任何新 payload、preview 失效或 sequence/binding 变化都丢弃本地确认意图并要求用户从服务端重新 prepare。</action><acceptance_criteria>用户无法从 UI 选择高风险域、编辑 exact Preview 或确认模糊操作；Esc/取消/关闭均零 POST；同一 busy 状态不发两次；确认页面只展示脱敏且允许显示的 Preview 字段，不渲染 confirmation/HMAC/Provider body。</acceptance_criteria><verify>cd apps/personal_decision_cockpit; npm run test -- --run src/test/ConfirmDrawer.test.tsx src/test/NewSessionFlow.test.tsx src/test/orchestration.test.ts</verify></task>

<task type="execute"><name>按服务端合法状态推进会话并呈现 receipt 与 exact replay</name><files>apps/personal_decision_cockpit/src/pages/sessions/SessionPage.tsx; apps/personal_decision_cockpit/src/test/SessionPage.test.tsx; apps/personal_decision_cockpit/src/test/orchestration.test.ts</files><read_first>apps/personal_decision_cockpit/src/pages/sessions/SessionPage.tsx; src/personal_knowledge/intelligence/orchestration/models.py; src/personal_knowledge/intelligence/orchestration/service.py; .planning/phases/PDA-33-guarded-decision-orchestration/33-VERIFICATION.md</read_first><action>使 Session Page 每次以服务端 resume/get 的 verified chain 和 legal next transition 为准渲染；每一步都走独立 prepare → preview → explicit confirm，不实现“一键跑完”。成功后仅从服务端 OperationResult 显示 event_id、event_checksum、sequence、state 与 references；当 `replayed=true` 时明确显示“已返回原事件，未重复写入”，且不可再生成第二张 receipt。刷新后的 actor mismatch 会保留 session 的只读 resume/explain 和证据，不显示继续写入的伪入口；Provider availability 只呈现服务端状态，不替服务端补调。</action><acceptance_criteria>所有合法 transition 都需各自用户确认；非法/跳跃状态无可执行按钮；exact replay 复用原 receipt；刷新后的 actor mismatch 可审阅但不能 mutation；接收者能区分“确认决策”“记录行动”“记录结果”和“校准”，不会把其中任一项等同外部行动或因果结论。</acceptance_criteria><verify>cd apps/personal_decision_cockpit; npm run test -- --run src/test/SessionPage.test.tsx src/test/orchestration.test.ts</verify></task>
</tasks>

<verification>cd apps/personal_decision_cockpit; npm run test -- --run src/test/orchestration.test.ts src/test/ConfirmDrawer.test.tsx src/test/NewSessionFlow.test.tsx src/test/SessionPage.test.tsx</verification>

<success_criteria>DEC-02 的浏览器写入严格限制为既有 project/low 的逐步确认路径：Preview 精确、确认具体、取消无副作用、receipt 可审计，重放不重复写入。</success_criteria>
