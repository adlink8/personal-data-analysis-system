---
phase: 40
plan: 03
type: execute
wave: 3
depends_on: [40-01, 40-02]
files_modified:
  - apps/personal_decision_cockpit/docs/browser-e2e-dependency-review.md
  - apps/personal_decision_cockpit/docs/live-uat.md
  - apps/personal_decision_cockpit/README.md
  - apps/personal_decision_cockpit/package.json
  - apps/personal_decision_cockpit/package-lock.json
  - apps/personal_decision_cockpit/playwright.config.ts
  - apps/personal_decision_cockpit/e2e/cockpit.spec.ts
  - apps/personal_decision_cockpit/.gitignore
  - .planning/phases/PDA-40-product-hardening-and-live-uat/40-UAT.md
  - .planning/phases/PDA-40-product-hardening-and-live-uat/40-VERIFICATION.md
autonomous: false
requirements: [UX-01, UX-02, QA-01, QA-02]
must_haves:
  truths:
    - "D-40-05：发布结论必须同时引用 build、前端/Projection/orchestration/replay/privacy 回归和至少一次真实同源 project+low prepare/confirm/exact-replay UAT；缺一不可。"
    - "D-40-06：任何失败都保持 authority 数据不变；恢复仅为前端/配置回退或 typed runtime recovery，禁止数据库 reset、删除 append-only event、lifecycle rewrite 或 silent cache fallback。"
    - "D-40-07：若新增 `@playwright/test` 或任何浏览器 runner，先完成明确依赖审查；未批准时只能进行记录化的手动浏览器 UAT，不能宣称自动化 E2E 已完成。"
    - "真实写入仅限现有 project + low、prepare → exact preview → explicit confirm → same payload exact replay；使用 disposable authority，或在每次 live 写入前获得独立用户授权并保留 append-only 记录。"
  artifacts:
    - path: apps/personal_decision_cockpit/docs/browser-e2e-dependency-review.md
      provides: 浏览器 harness 复用/新增依赖、版本、锁文件、local-only、产物和 redaction 的显式审查结论
    - path: .planning/phases/PDA-40-product-hardening-and-live-uat/40-UAT.md
      provides: 真实浏览器场景、环境、红acted snapshot binding、观察、零副作用/append-only proof、恢复与用户验收记录
    - path: .planning/phases/PDA-40-product-hardening-and-live-uat/40-VERIFICATION.md
      provides: 需求到命令、证据、UAT 和 release verdict 的最终可审计映射
  key_links:
    - from: production http://127.0.0.1:8000/app/
      to: secured /ui/* and /agent/session/* routes
      via: 浏览器以同源相对 URL 读取和既有 Guarded Orchestration prepare/confirm/exact replay
    - from: 40-UAT.md
      to: 40-VERIFICATION.md
      via: 每一个 UX/QA requirement 的通过/失败、恢复、fixture 身份、红acted authority binding 和 release verdict
---

<objective>在不偷换依赖、不污染真实个人 authority 的前提下，以真实同源浏览器完成最终验收、记录可复现证据与恢复结论；只有全部硬门通过才可更新 README/Verification 为 accepted。</objective>

<threat_model>
- T-40-09 依赖偷渡：未经审查引入浏览器 runner、cloud reporter、全局服务或其他生产依赖，再把其结果当作理所当然的验收。
- T-40-10 路径错觉：只测试 Vite proxy 或 mock server，却称 production `/app`、Origin、CORS 和静态托管已经验证。
- T-40-11 重复写入/越权：浏览器重试产生多个 append-only event，或测试绕过 HMAC/sequence/idempotency、改用非 project/low 域。
- T-40-12 隐私泄露：截图、trace、HAR、console、UAT Markdown 或 CI artifact 保存私人证据、preview、confirmation、HMAC、provider body、凭据或完整本地路径。
- T-40-13 假收口：缺服务、缺 secret、generation unavailable 或任一关键场景失败时仍将 Phase 40/README 标为 passed。
- 阻断条件：依赖审查未批准、无法使用 disposable authority 且没有单独用户授权、真实同源 UAT/隐私检查缺失、或 UAT 仍有未恢复的 blocker 时，保持 Phase 40 blocked/gaps_found。
</threat_model>

<tasks>
<task type="execute"><name>完成浏览器 harness 的依赖与产物审查，再决定自动化或手动路径</name><files>apps/personal_decision_cockpit/docs/browser-e2e-dependency-review.md; apps/personal_decision_cockpit/package.json; apps/personal_decision_cockpit/package-lock.json; apps/personal_decision_cockpit/playwright.config.ts; apps/personal_decision_cockpit/.gitignore</files><read_first>apps/personal_decision_cockpit/package.json; package-lock.json; .planning/phases/PDA-40-product-hardening-and-live-uat/40-RESEARCH.md; .planning/phases/PDA-40-product-hardening-and-live-uat/40-CONTEXT.md; apps/personal_decision_cockpit/docs/live-uat.md</read_first><action>先检查仓库是否已有可复用、local-only 的浏览器 runner。若不存在，撰写依赖审查：为什么需要 `@playwright/test`、精确版本/lockfile 影响、Chromium-only、本机 `webServer`、无 cloud upload、无活 authority/tunnel/provider、raw trace/HAR/screenshot 默认不入库且被 `.gitignore` 覆盖、失败 artifact 的 redaction/删除策略和回退方案。只有审查获批后才添加最小开发依赖、local config 和 e2e 文件；若未获批，保留 package/lockfile 不变，按 `docs/live-uat.md` 执行记录化手动浏览器 UAT。无论选哪条路径，都不得新增生产 Node 进程、Service Worker、浏览器业务缓存或测试旁路。</action><acceptance_criteria>依赖审查结论明确为“复用”“批准新增”或“拒绝/手动”；新增 runner 时每项新增文件、版本、scope 与产物保留策略可审计，拒绝时没有隐性 package/lockfile 变更；两条路径都不会把自动化存在与真实 UAT 通过混淆。</acceptance_criteria><verify>cd apps/personal_decision_cockpit; npm ls @playwright/test; Get-Content docs/browser-e2e-dependency-review.md; git diff -- package.json package-lock.json .gitignore playwright.config.ts</verify></task>

<task type="execute"><name>运行完整自动化矩阵并准备隔离的同源浏览器验收环境</name><files>apps/personal_decision_cockpit/docs/live-uat.md; apps/personal_decision_cockpit/e2e/cockpit.spec.ts; .planning/phases/PDA-40-product-hardening-and-live-uat/40-UAT.md</files><read_first>apps/personal_decision_cockpit/docs/live-uat.md; apps/personal_decision_cockpit/docs/browser-e2e-dependency-review.md; apps/personal_decision_cockpit/vite.config.ts; src/personal_knowledge/services/api_server.py; tests/contract/test_orchestration_interfaces.py; tests/integration/test_orchestration_replay.py; tests/e2e/test_orchestration_acceptance.py</read_first><action>在已批准的 local-only/fixture 环境中，运行并记录 build、Vitest、Projection contracts、orchestration/replay/privacy 定向测试。浏览器覆盖（自动或记录化手动）必须打开 REST owner 提供的 production same-origin `/app/`，而不是仅 Vite proxy；覆盖 320/768/1024/1440、Tab/Shift+Tab、可见 focus、Esc/focus restore、reduced motion、真实 200% zoom、长中文/ID 与文本替代。故障场景分别使用可控 fixture/seam 验证 REST offline、MCP Widget unavailable、Chroma unavailable、single-authority partial，并记录正确恢复文案。所有目标服务/authority 都必须是 disposable 或只读；不得停止用户已有进程或修改 live DB/vector/pointer/watermark。</action><acceptance_criteria>`40-UAT.md` 记录每一场景的 source revision、build、schema/fixture 身份、红acted binding、预期/实际状态、pass/fail、任何 write 前后 ledger fingerprint、恢复结论和 artifact sensitivity decision；命令或浏览器失败不会被省略或改写为通过。200% zoom 以真实浏览器手工观察记录，不以 DPR 模拟替代。</acceptance_criteria><verify>cd apps/personal_decision_cockpit; npm run build; npm run test; Set-Location D:\ADLINK\数据分析; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection.py tests/contract/test_ui_projection_state_external.py tests/contract/test_ui_projection_decision.py tests/contract/test_ui_projection_actions_proactive.py tests/contract/test_orchestration_interfaces.py tests/integration/test_orchestration_replay.py tests/e2e/test_orchestration_acceptance.py -q</verify></task>

<task type="checkpoint" gate="blocking"><name>完成真实低风险同源写入验收并决定发布/恢复</name><checkpoint_type>human-verify</checkpoint_type><files>.planning/phases/PDA-40-product-hardening-and-live-uat/40-UAT.md; .planning/phases/PDA-40-product-hardening-and-live-uat/40-VERIFICATION.md; apps/personal_decision_cockpit/README.md</files><action>由用户在真实浏览器审查同源 read、evidence drill-down、一个 `project + low` session 的 prepare → exact preview → explicit confirm → 相同 payload exact replay、响应式/无障碍、各类离线/partial、storage/console/DOM/URL/artifact 隐私检查及恢复结果。实际写入优先使用 disposable authority；如必须写入 live authority，先取得该一次写入的独立授权，保留 append-only event，不得删除/重置。将所有 requirement 映射到命令与证据，记录 release verdict 为 pass、blocked、gaps_found 或 fixed-and-retested。仅当所有必需场景和隐私门通过时，才将 README 的 UAT 状态更新为已验收并将 Verification 标为 passed。</action><verify>用户显式接受附有 redacted evidence、零外部动作、零自动 promotion、零未授权 authority mutation 和 exact-replay identity 的 UAT 报告；自动化测试或截图不能替代该确认。</verify><done>用户接受且全部 hard gates 通过时 Phase 40 可标记 passed；任一 blocker 则保留真实失败/恢复记录，README 不得宣称完成。</done></task>
</tasks>

<verification>Phase 40 的最终验证只有在 blocking UAT checkpoint 通过后成立：`npm run build`、`npm run test`、Python UI Projection + orchestration/replay/privacy 定向矩阵通过，真实同源浏览器 UAT 报告无 blocker，且用户明确验收。若任何条件不成立，`40-VERIFICATION.md` 必须为 blocked/gaps_found，而非 passed。</verification>

<success_criteria>UX-01、UX-02、QA-01、QA-02 均有非敏感、可复现且由用户确认的真实证据；失败只能触发前端/配置/typed recovery，不会污染个人 authority，也不会把未完成 UAT 写成已发布。</success_criteria>
