---
phase: 39
plan: 03
type: execute
wave: 3
depends_on: [39-02]
files_modified:
  - src/personal_knowledge/services/ui_projection.py
  - src/personal_knowledge/services/api_server.py
  - apps/personal_decision_cockpit/src/api/schemas.ts
  - apps/personal_decision_cockpit/src/pages/system/SystemPage.tsx
  - apps/personal_decision_cockpit/src/components/system/SystemHealthStrip.tsx
  - tests/contract/test_ui_projection_actions_proactive.py
  - apps/personal_decision_cockpit/src/test/SystemPage.test.tsx
autonomous: true
requirements: [RUN-01]
must_haves:
  truths:
    - "System 页面把 REST、MCP、Tunnel、Chroma、authority readability/freshness 和 supervisor saved state 当作独立、有来源和时间的 observations。"
    - "REST 当前响应、TCP listener、fresh endpoint readiness、saved supervisor observation、authority readable 和 authority fresh 不被混用。"
    - "System 页面只有只读状态和安全恢复说明，没有 start、stop、restart、kill、tunnel 配置或泄露 PID、路径、原始 endpoint/异常的操作面。"
  artifacts:
    - path: src/personal_knowledge/services/ui_projection.py
      provides: Provenance-aware runtime observation matrix with safe partial/stale/unknown degradation
    - path: apps/personal_decision_cockpit/src/components/system/SystemHealthStrip.tsx
      provides: Independent timestamped health observations rather than a global green badge
    - path: tests/contract/test_ui_projection_actions_proactive.py
      provides: Runtime source/time/state/recovery and zero-control contract cases
  key_links:
    - from: CockpitProjectionService._system_status_get
      to: get_knowledge_status(probe_chroma=True) and safe endpoint probes
      via: per-component current observation DTO
    - from: ops/state/agent-stack.json
      to: SystemPage saved-state panel
      via: validated last_observed-only record, never current ownership proof
---

<objective>将运行状态从“一个绿色健康条”改为具有来源、观测时间、精确范围和恢复说明的独立观测矩阵。</objective>

<threat_model>
- T-39-09 伪全绿：处理当前页面请求成功或一个 TCP listener 存在，被表述为 MCP、Tunnel、Chroma 和所有 authority 健康。
- T-39-10 过期状态：过去 supervisor 写入的 PID/health 记录被当作当前进程所有权或实时服务状态。
- T-39-11 敏感运维泄露：运行卡片或错误暴露 PID、绝对路径、完整 endpoint、原始 connection/provider body 或密钥。
- T-39-12 越权运维：Cockpit 通过 UI/API 增加启停/重启/杀进程/隧道配置或任何服务副作用。
- Severity: 任何跨组件健康推断、saved-state 伪装为 current、敏感泄露或服务控制能力都阻塞完成。
</threat_model>

<tasks>
<task type="execute"><name>定义来源可追溯的 runtime observation contract</name><files>src/personal_knowledge/services/ui_projection.py; src/personal_knowledge/services/api_server.py; apps/personal_decision_cockpit/src/api/schemas.ts; tests/contract/test_ui_projection_actions_proactive.py</files><action>在已有 `decision_cockpit_projection_v1` 中增加最小的 server-owned observation DTO：state（healthy/reachable_only/unavailable/stale_observation/unknown/partial）、source、observed_at、safe recovery_hint、scope。分别探测当前 REST 请求、可验证的 MCP/Tunnel readiness、Chroma `probe_chroma=True`、authority readability 与 snapshot/as-of freshness。若没有 endpoint-specific readiness，只能返回 reachable_only/unknown；保留 API 的只读路由，不添加控制路由。所有错误经过 Phase 36 safe mapping。</action><verify>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection_actions_proactive.py tests/contract/test_ui_projection.py -q</verify><done>每项运行状态都能回答“观察的是什么、何时、由谁观察、它没有证明什么”，且没有把 REST 成功扩大成整栈健康。</done></task>
<task type="execute"><name>将 supervisor state 限制为 last-observed 证据</name><files>src/personal_knowledge/services/ui_projection.py; tests/contract/test_ui_projection_actions_proactive.py</files><action>仅读取并严格校验 `ops/state/agent-stack.json` 的有限公开字段；将其展示为 supervisor `last_observed`，附带保存时间、过期判断和安全恢复提示。缺失、损坏、过期或不匹配的文件必须是 stale_observation/unknown，不得阻止其他 fresh probes。禁止根据 PID、端口或保存 URL 认定当前 ownership/readiness；默认 DTO/DOM 不含 PID、绝对路径、完整内部 URL 或原始异常。</action><verify>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection_actions_proactive.py -q</verify><done>saved state 只作为历史观测出现；不存在、失效或损坏时 UI 仍能显示其它独立观测并给出安全恢复说明。</done></task>
<task type="execute"><name>渲染独立且无控制面的 System 页面</name><files>apps/personal_decision_cockpit/src/pages/system/SystemPage.tsx; apps/personal_decision_cockpit/src/components/system/SystemHealthStrip.tsx; apps/personal_decision_cockpit/src/test/SystemPage.test.tsx</files><action>以独立行/卡片展示 REST、MCP、Tunnel、Chroma、authority readability/freshness 与 supervisor last observation；显示 source、observed_at、scope、state 和 recovery，而不是单个全局绿标。默认隐藏技术标识，任何高级详情继续使用安全截断。明确声明 Cockpit 不能启动、停止、重启或配置服务；不要加入任何按钮、POST/PUT/DELETE 或 process-management 文案。</action><verify>Set-Location <repo-root>\apps\personal_decision_cockpit; npm run test -- --run src/test/SystemPage.test.tsx; npm run build</verify><done>用户可辨别“当前可达”“仅端口可达”“上次被 supervisor 观察到”“不可用/未知/部分可用”，但不能从页面控制本机进程或读取敏感运行详情。</done></task>
</tasks>

<verification>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection_actions_proactive.py tests/contract/test_ui_projection.py -q; Set-Location <repo-root>\apps\personal_decision_cockpit; npm run test -- --run src/test/SystemPage.test.tsx; npm run build</verification>
<success_criteria>RUN-01 的状态展示按组件独立、带时间/来源/范围、诚实处理 partial 和 stale，且 Cockpit 对服务运行仍完全只读。</success_criteria>
