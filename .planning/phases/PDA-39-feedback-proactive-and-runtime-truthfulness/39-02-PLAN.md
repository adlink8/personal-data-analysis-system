---
phase: 39
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - src/personal_knowledge/services/ui_projection.py
  - apps/personal_decision_cockpit/src/api/schemas.ts
  - apps/personal_decision_cockpit/src/pages/proactive/ProactivePage.tsx
  - apps/personal_decision_cockpit/src/components/proactive/ProactiveCard.tsx
  - apps/personal_decision_cockpit/src/components/action/CalibrationPanel.tsx
  - tests/contract/test_ui_projection_actions_proactive.py
  - tests/contract/test_proactive_interfaces.py
  - tests/contract/test_proactive_boundaries.py
  - apps/personal_decision_cockpit/src/test/ProactivePage.test.tsx
  - apps/personal_decision_cockpit/src/test/ActionsPage.test.tsx
autonomous: true
requirements: [FDB-02]
must_haves:
  truths:
    - "Proactive 页面只显示既有 authority 的候选、协调、control history、eligibility 和 limitations；候选排序字段是 importance.final_score。"
    - "没有已授权 guarded REST 写入的 Snooze/Suppress/Restore 等操作保持禁用，说明原因和可用的 MCP/CLI 路径，不产生 optimistic state、事件或假成功。"
    - "Calibration 忠实显示 frozen protocol、verdict、causal_claim=false、样本、INCONCLUSIVE 原因、限制、no-promotion 和 no-external-action。"
  artifacts:
    - path: src/personal_knowledge/services/ui_projection.py
      provides: Read-only proactive and calibration projection using authority vocabulary
    - path: apps/personal_decision_cockpit/src/pages/proactive/ProactivePage.tsx
      provides: Inbox/coordination/control state view with explicit unavailable-control recovery
    - path: apps/personal_decision_cockpit/src/components/action/CalibrationPanel.tsx
      provides: Frozen non-promoting calibration result presentation
    - path: tests/contract/test_proactive_boundaries.py
      provides: GET-only and zero-mutation boundary regression proof
  key_links:
    - from: ProactiveIntelligenceService controls.status and inbox
      to: CockpitProjectionService._proactive_summary_get
      via: stable candidate ID, final_score, control eligibility/reason and as-of metadata
    - from: calibration.service.explain
      to: CalibrationPanel
      via: frozen verdict/causal/limitation fields without client recomputation
---

<objective>展示主动提醒与校准的真实、有限和非自动化状态，绝不为了界面“可操作感”伪造控制、promotion 或外部行动。</objective>

<threat_model>
- T-39-05 排名字段漂移：UI 读取 display-only `importance.score` 而不是 authority 的 `importance.final_score`，导致误排序或空值伪装。
- T-39-06 假控制：禁用按钮仍有 client mutation、optimistic state 或成功 toast，使用户误以为 append-only control history 已更新。
- T-39-07 校准宣传：INCONCLUSIVE、样本不足或 no-promotion 被弱化成看似有效的个性化策略结论。
- T-39-08 authority 旁路：页面直接联库、客户端合并候选或新增 REST 写路由，改变既有 control/promotion 边界。
- Severity: 任意错误 field binding、浏览器写入、自动 promotion/external action 或掩盖不确定性都阻塞完成。
</threat_model>

<tasks>
<task type="execute"><name>固定 proactive authority 词汇和只读投影</name><files>src/personal_knowledge/services/ui_projection.py; apps/personal_decision_cockpit/src/api/schemas.ts; tests/contract/test_ui_projection_actions_proactive.py</files><action>让 `_proactive_summary_get`、inbox、metrics 与 candidate explain 仅通过 `ProactiveIntelligenceService` 读取，传递 `importance.final_score`、stable candidate ID、policy/reason codes、current_control_eligible、control reason/as-of、coordination 与 partial/limitations。空 inbox、suppressed/cooldown、authority unavailable 和 detail read failure 使用不同状态，不得通过零值或客户端 ranking 表示健康。不得增加新的 browser state authority。</action><verify>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection_actions_proactive.py tests/contract/test_proactive_interfaces.py -q</verify><done>Projection 的 now/deferrable 分类及页面输入使用 final_score，并可解释当前 control eligibility、原因和 authority 限制。</done></task>
<task type="execute"><name>实现诚实的 Proactive 与 Calibration 展示</name><files>apps/personal_decision_cockpit/src/pages/proactive/ProactivePage.tsx; apps/personal_decision_cockpit/src/components/proactive/ProactiveCard.tsx; apps/personal_decision_cockpit/src/components/action/CalibrationPanel.tsx; apps/personal_decision_cockpit/src/test/ProactivePage.test.tsx; apps/personal_decision_cockpit/src/test/ActionsPage.test.tsx</files><action>渲染 candidate priority、coordination、control history 与 as-of 状态；无 REST 写路径的控制必须 disabled，带精确解释和到既有 MCP/CLI/guarded 流程的恢复指引。Calibration 面板保留 frozen protocol、PASS/FAIL/INCONCLUSIVE、sample/cohort、reason code、limitations、`causal_claim=false`、`promotion_available=false` 与 `external_action_available=false`。不得添加 POST/PUT/DELETE、optimistic update、自动刷新写入、自动 promotion、外部动作或“已应用”假提示。</action><verify>Set-Location <repo-root>\apps\personal_decision_cockpit; npm run test -- --run src/test/ProactivePage.test.tsx src/test/ActionsPage.test.tsx; npm run build</verify><done>用户可以理解候选为何出现、控制为何不可用、校准为何 INCONCLUSIVE，但不能从这些页面执行未授权行为。</done></task>
<task type="execute"><name>证明主动页面没有越过只读/自动化边界</name><files>tests/contract/test_proactive_interfaces.py; tests/contract/test_proactive_boundaries.py; tests/contract/test_ui_projection_actions_proactive.py; apps/personal_decision_cockpit/src/test/ProactivePage.test.tsx</files><action>添加 route/client 静态与运行时回归：`/proactive/*` 保持 GET-only；Projection、页面读取前后 authority fingerprints 不变；provider/network/paid/external actions/promotion 均为零。覆盖 final_score、control unavailable、suppressed/cooldown、authority partial、Calibration INCONCLUSIVE/PASS 和 disabled action explanation。检查 Cockpit 源码不存在对 proactive control/calibration promotion/external action 的 fetch mutation。</action><verify>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_proactive_interfaces.py tests/contract/test_proactive_boundaries.py tests/contract/test_ui_projection_actions_proactive.py -q</verify><done>FDB-02 有机器可验证的只读边界和诚实降级证据，UI 不会以任何方式改变 control history、calibration policy 或外部世界。</done></task>
</tasks>

<verification>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection_actions_proactive.py tests/contract/test_proactive_interfaces.py tests/contract/test_proactive_boundaries.py -q; Set-Location <repo-root>\apps\personal_decision_cockpit; npm run test -- --run src/test/ProactivePage.test.tsx src/test/ActionsPage.test.tsx; npm run build</verification>
<success_criteria>FDB-02 的 proactive/calibration 视图完全来自既有 authority、能说明限制和控制路径，且没有新增 browser REST 写入、自动 promotion 或 external action。</success_criteria>
