---
phase: 39
plan: 04
type: execute
wave: 4
depends_on: [39-01, 39-02, 39-03]
files_modified:
  - tests/contract/test_ui_projection_actions_proactive.py
  - tests/contract/test_ui_projection.py
  - tests/contract/test_proactive_interfaces.py
  - tests/contract/test_proactive_boundaries.py
  - tests/contract/test_decision_interfaces.py
  - tests/unit/test_decision_effectiveness.py
  - apps/personal_decision_cockpit/src/test/ActionsPage.test.tsx
  - apps/personal_decision_cockpit/src/test/ProactivePage.test.tsx
  - apps/personal_decision_cockpit/src/test/SystemPage.test.tsx
autonomous: true
requirements: [FDB-01, FDB-02, RUN-01]
must_haves:
  truths:
    - "任何 feedback/proactive/runtime 的 authority 或依赖失败只使对应 section partial/unavailable/stale；其他独立 section 仍可读，且不泄露异常文本。"
    - "Phase 39 的所有浏览器入口保持读取；不会改变 Personal/External facts、Serving Snapshot、lifecycle、Calibration policy、control history、服务或外部世界。"
    - "实现阶段的验证有 Python/TypeScript 可复现证据；真实浏览器、响应式与端到端 UAT 继续留给 Phase 40。"
  artifacts:
    - path: tests/contract/test_ui_projection_actions_proactive.py
      provides: Fault-injection and zero-mutation cross-section contract suite
    - path: apps/personal_decision_cockpit/src/test/SystemPage.test.tsx
      provides: UI semantic test for independent runtime observations and no control surface
    - path: apps/personal_decision_cockpit/src/test/ProactivePage.test.tsx
      provides: UI semantic test for final_score, disabled controls and recovery copy
  key_links:
    - from: Phase 39 UI Projection endpoints
      to: authority fingerprints and route registry
      via: before/after zero-write and GET-only regression assertions
    - from: partial/stale runtime and authority fixtures
      to: Cockpit semantic components
      via: shared typed envelope instead of local fallback truth
---

<objective>以故障注入和零副作用回归把 Phase 39 的“诚实只读”边界固化下来，并为 Phase 40 的真实浏览器验收留下可复现基础。</objective>

<threat_model>
- T-39-13 降级伪装：单 authority、Chroma、saved runtime state 或详情读取失败后，页面显示空白、零值或全绿。
- T-39-14 读路径漂移：实现中意外增加 POST、写数据库、改变 snapshot/lifecycle/control/calibration 或调用 provider/external service。
- T-39-15 回归遗漏：只测试 happy path，遗漏 chronological ties、INCONCLUSIVE、disabled controls、listener-not-ready 与 stale saved state。
- T-39-16 过早产品宣称：把组件/contract 通过描述成 Phase 40 才能证明的浏览器 UAT、无障碍和端到端发布验收。
- Severity: 任一未隔离故障、隐藏的写副作用或错误验收声明都阻塞完成。
</threat_model>

<tasks>
<task type="execute"><name>建立跨 authority 的 partial 与安全错误故障注入</name><files>tests/contract/test_ui_projection_actions_proactive.py; tests/contract/test_ui_projection.py; tests/unit/test_decision_effectiveness.py</files><action>为 feedback、proactive、calibration、Chroma、authority readable/freshness、MCP/Tunnel readiness、missing/corrupt/stale supervisor state 和单条 detail read 设计独立 fault fixtures。断言结果是范围明确的 partial/unavailable/stale_observation/unknown + safe recovery，而非原始 `str(exc)`、零值、空白或全局失败；未受影响 section 继续返回 snapshot-bound 数据。覆盖 chronological ID ties、truncated list、missing outcome、causal_claim=false、PASS/FAIL/INCONCLUSIVE 与 unknown sample。</action><verify>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection_actions_proactive.py tests/contract/test_ui_projection.py tests/unit/test_decision_effectiveness.py -q</verify><done>每类失败都具有安全且可恢复的产品语义，不能让局部故障变成不真实的整体状态。</done></task>
<task type="execute"><name>证明 read-only、GET-only 与零自动化边界</name><files>tests/contract/test_proactive_interfaces.py; tests/contract/test_proactive_boundaries.py; tests/contract/test_decision_interfaces.py; tests/contract/test_ui_projection_actions_proactive.py</files><action>检查 Phase 39 projection/runtime/proactive/calibration 路由和客户端调用：除 Phase 38 已定义的 guarded session workflow 外均为 GET/只读。针对所有 Phase-39 reads 比较 Personal、External、Decision、Proactive、Calibration、Serving Snapshot 与 relevant control-history fingerprints；断言 database writes、provider/network/paid calls、external actions、promotions、service-management invocations 为零。禁止新增 direct Outcome、control、promotion 或 runtime mutation endpoint。</action><verify>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_proactive_interfaces.py tests/contract/test_proactive_boundaries.py tests/contract/test_decision_interfaces.py tests/contract/test_ui_projection_actions_proactive.py -q</verify><done>有自动化证据证明 Phase 39 读取不改变任何 authority、策略、服务或外部环境。</done></task>
<task type="execute"><name>完成语义 UI 回归与阶段验收边界记录</name><files>apps/personal_decision_cockpit/src/test/ActionsPage.test.tsx; apps/personal_decision_cockpit/src/test/ProactivePage.test.tsx; apps/personal_decision_cockpit/src/test/SystemPage.test.tsx</files><action>以 typed fixtures 验证 Actions 的非因果/截断/缺失阶段，Proactive 的 final_score/控制禁用与恢复，System 的独立 observation/source/time/recovery/no-control surface。执行构建及定向测试后，将实际命令和结果仅写入 Phase 39 后续 VERIFICATION；不得在代码、README 或 Roadmap 中宣称完成 browser E2E、响应式、键盘、隐私/UAT，这些仍属于 Phase 40。</action><verify>Set-Location <repo-root>\apps\personal_decision_cockpit; npm run test -- --run src/test/ActionsPage.test.tsx src/test/ProactivePage.test.tsx src/test/SystemPage.test.tsx; npm run build</verify><done>三类页面的局部/不确定/禁用状态均有 UI 语义测试；Phase 39 和 Phase 40 的验收责任没有混淆。</done></task>
</tasks>

<verification>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection_actions_proactive.py tests/contract/test_ui_projection.py tests/contract/test_proactive_interfaces.py tests/contract/test_proactive_boundaries.py tests/contract/test_decision_interfaces.py tests/unit/test_decision_effectiveness.py -q; Set-Location <repo-root>\apps\personal_decision_cockpit; npm run test -- --run src/test/ActionsPage.test.tsx src/test/ProactivePage.test.tsx src/test/SystemPage.test.tsx; npm run build</verification>
<success_criteria>反馈、主动提醒和运行状态在正常与故障条件下均有可复现的只读/安全/诚实契约证据；Phase 39 不取代 Phase 40 的浏览器 UAT。</success_criteria>
