---
phase: 39
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personal_knowledge/intelligence/decision/service.py
  - src/personal_knowledge/services/ui_projection.py
  - apps/personal_decision_cockpit/src/api/schemas.ts
  - apps/personal_decision_cockpit/src/pages/actions/ActionsPage.tsx
  - apps/personal_decision_cockpit/src/components/action/OutcomeTimeline.tsx
  - apps/personal_decision_cockpit/src/components/action/CalibrationPanel.tsx
  - tests/contract/test_ui_projection_actions_proactive.py
  - apps/personal_decision_cockpit/src/test/ActionsPage.test.tsx
autonomous: true
requirements: [FDB-01]
must_haves:
  truths:
    - "反馈页以 Recommendation → Decision → Action → Outcome → Effectiveness → Calibration 的固定 append-only 顺序展示事件；缺失阶段保持缺失，不能由 UI 补造。"
    - "每个 Outcome、Effectiveness 与 Calibration 结论相邻显示 causal_claim=false、可用的样本/队列信息与 authority limitations，不能把 PASS 或正面结果表现为因果或自动 promotion。"
    - "反馈历史可通过 authority-owned cursor 完整浏览：每页 newest-first、稳定 ID tie-breaker、next_cursor 与明确的 total/unknown-total 语义；窗口截断、单项读取失败和已无更多记录都被明确表达，浏览器不得自行拼接、重排或跳过事件。"
  artifacts:
    - path: src/personal_knowledge/intelligence/decision/service.py
      provides: Stable cursor-paginated latest-first recommendation read without changing the append-only authority
    - path: src/personal_knowledge/services/ui_projection.py
      provides: Snapshot-bound feedback timeline projection with truthful absence, history-window and non-causal limitations
    - path: apps/personal_decision_cockpit/src/pages/actions/ActionsPage.tsx
      provides: Accessible feedback timeline and outcome/calibration limitation rendering
    - path: tests/contract/test_ui_projection_actions_proactive.py
      provides: Chronology, checksum passthrough, non-causal and zero-write projection regression proof
  key_links:
    - from: DecisionFeedbackService.recommendations_list
      to: CockpitProjectionService._actions_recent_get
      via: newest-first bounded, checksum-verifying authority read
    - from: CockpitProjectionService._build_timeline
      to: OutcomeTimeline and CalibrationPanel
      via: fixed six-stage DTO carrying present/event_id/sequence/checksum/limitations
---

<objective>让反馈历史成为可信的事件追踪视图，而不是把有限样本、正向结果或存在的时间线节点包装成因果成功。</objective>

<threat_model>
- T-39-01 过期窗口误导：升序后截断的 recommendation list 被叫作“recent”，隐藏真正的新记录。
- T-39-02 因果夸大：Outcome、Effectiveness 或 Calibration 的 PASS/正向值被渲染成“建议导致成功”或“可自动 promotion”。
- T-39-03 事件伪造：浏览器为视觉完整性合成一个缺失阶段、时间、事件 ID、checksum 或样本量。
- T-39-04 局部故障泄露/扩散：一个详情读取失败泄露原始异常或让其余可信历史不可读。
- Severity: 任一错误排序、因果暗示、合成 authority 数据、未安全处理的错误或读路径写入都阻塞完成。
</threat_model>

<tasks>
<task type="execute"><name>收口 recommendation 的稳定游标式完整历史读取</name><files>src/personal_knowledge/intelligence/decision/service.py; src/personal_knowledge/services/ui_projection.py; tests/contract/test_ui_projection_actions_proactive.py</files><action>将 Cockpit 使用的 recommendation list 收口为 authority 侧 cursor-paginated 的 newest-first read：稳定排序必须含不可变 event timestamp 与唯一 ID tie-breaker，Projection 原样透传 opaque `next_cursor`、shown count、是否还有更早记录，以及 authority 能准确提供时的 total_available；没有准确总数时显式标记 unknown，而不是估算。cursor 必须由服务端解释和验证；非法、过期或与 snapshot/filter 不匹配的 cursor fail closed 为 typed read-only recovery。保持现有 read-only/query_only、checksum 与 append-only 状态机权威；浏览器只能按服务端 cursor 请求“加载更早记录”，不得计算 offset、重排、丢弃、去重后伪造序列或建立本地事件账本。</action><verify>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection_actions_proactive.py -q</verify><done>超过单页窗口的 fixture 可从首尾 cursor 连续浏览到 authority 已知的最旧记录；跨页无重复、无遗漏、不重排，非法/过期/mismatched cursor 无写入且返回 typed recovery，读取前后 authority fingerprint 不变。</done></task>
<task type="execute"><name>投影六阶段 append-only 反馈追踪</name><files>src/personal_knowledge/services/ui_projection.py; tests/contract/test_ui_projection_actions_proactive.py</files><action>扩展 `_actions_recent_get`、`_actions_recent_section` 与 `_build_timeline`，仅传递既有 Decision/Outcome/Effectiveness/Calibration authority 的字段。固定输出六阶段；未到达的阶段显式 `present=false`，不生成 event、timestamp、checksum 或 sample 值。保留 authority/snapshot binding、history-window、per-item partial 与安全 limitation；依赖 Phase 36 的公共错误映射，不返回 `str(exc)`、路径、PII、provider body 或 confirmation/HMAC。</action><verify>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection_actions_proactive.py tests/contract/test_ui_projection.py -q</verify><done>Projection 能在缺 Outcome、未知 cohort、截断历史与单项失败时准确输出 partial/limitations，且无任何写入、provider 调用、external action 或 promotion。</done></task>
<task type="execute"><name>把非因果、样本、限制与“加载更早记录”变成可访问的反馈语义</name><files>apps/personal_decision_cockpit/src/api/schemas.ts; apps/personal_decision_cockpit/src/pages/actions/ActionsPage.tsx; apps/personal_decision_cockpit/src/components/action/OutcomeTimeline.tsx; apps/personal_decision_cockpit/src/components/action/CalibrationPanel.tsx; apps/personal_decision_cockpit/src/test/ActionsPage.test.tsx</files><action>为 feedback DTO 增加严格 Zod 解析，保留 authority 提供的 `causal_claim=false`、verdict、sample/cohort、reason codes、limitations、event_id/sequence/checksum、partial、opaque next_cursor 与 unknown-total 状态。页面以文本与语义状态组件展示“非因果评估”、样本未知/不足、INCONCLUSIVE 与历史范围；“加载更早记录”只能发送服务端给出的 cursor 并在失败时保留已读页面和 typed read-only recovery。不得制作成功漏斗、个性化胜率、promote 按钮或直接记录 Outcome 的 POST；不得以浏览器去重/排序替代 authority 历史顺序。任何继续操作仅链接到 Phase 38 已有的 guarded prepare workflow。</action><verify>Set-Location <repo-root>\apps\personal_decision_cockpit; npm run test -- --run src/test/ActionsPage.test.tsx; npm run build</verify><done>Actions UI 在 PASS、FAIL、INCONCLUSIVE、无 Outcome、partial、长限制文本、多页历史和 cursor 失败下都保留非因果语义与 authority 固定顺序；跨页既不重复也不遗漏，不出现虚构动作或写入请求。</done></task>
</tasks>

<verification>Set-Location <repo-root>; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection_actions_proactive.py tests/contract/test_ui_projection.py tests/unit/test_decision_effectiveness.py -q; Set-Location <repo-root>\apps\personal_decision_cockpit; npm run test -- --run src/test/ActionsPage.test.tsx; npm run build</verification>
<success_criteria>FDB-01 的反馈历史在有限、缺失和不确定条件下仍可审计、可追溯、明确非因果，并且读取不会改变任何决策、结果、校准或权威状态。</success_criteria>
