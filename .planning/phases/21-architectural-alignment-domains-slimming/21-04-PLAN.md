---
plan: 21-04
title: "Memory domain migration + retrieval legacy + finalization (D-08 triple gate)"
wave: 4
depends_on: [21-01, 21-02, 21-03]
autonomous: true
phase: 21
requirements: [ARCH-21-04, ARCH-21-06, ARCH-21-07, ARCH-21-08]
files_modified:
  - src/personal_knowledge/application/memory/__init__.py
  - src/personal_knowledge/application/memory/build_capability_memory.py
  - src/personal_knowledge/application/memory/build_context_memory.py
  - src/personal_knowledge/application/memory/build_deep_memory_profile.py
  - src/personal_knowledge/application/memory/build_mem0_candidate_memory.py
  - src/personal_knowledge/application/memory/build_memory_evidence_bundles.py
  - src/personal_knowledge/application/memory/build_memory_graph.py
  - src/personal_knowledge/application/memory/build_memory_promotion_candidates.py
  - src/personal_knowledge/application/memory/build_memory_relation_candidates.py
  - src/personal_knowledge/application/memory/build_memory_store.py
  - src/personal_knowledge/application/memory/build_preference_memory.py
  - src/personal_knowledge/application/memory/build_profile_from_memory.py
  - src/personal_knowledge/application/memory/extract_memory_candidates_from_bundles.py
  - src/personal_knowledge/application/memory/mine_deep_memory_graph.py
  - src/personal_knowledge/application/memory/repair_memory_promotion_candidates.py
  - src/personal_knowledge/application/memory/sync_memory_lifecycle.py
  - src/personal_knowledge/application/memory/apply_memory_promotions.py
  - src/personal_knowledge/evaluation/memory/__init__.py
  - src/personal_knowledge/evaluation/memory/evaluate_memory_depth.py
  - src/personal_knowledge/evaluation/memory/evaluate_memory_promotion_candidates.py
  - src/personal_knowledge/evaluation/memory/evaluate_memory_relation_candidates.py
  - src/personal_knowledge/evaluation/memory/compare_memory_experiments.py
  - src/personal_knowledge/evaluation/memory/analyze_memory_mechanisms.py
  - src/personal_knowledge/evaluation/memory/audit_memory_experiments.py
  - src/personal_knowledge/evaluation/vector/__init__.py
  - src/personal_knowledge/evaluation/vector/compare_sqlite_generations.py
  - src/personal_knowledge/evaluation/vector/compare_vector_generations.py
  - src/personal_knowledge/evaluation/vector/evaluate_vector_collections.py
  - src/personal_knowledge/evaluation/vector/evaluate_vector_retrieval.py
  - src/personal_knowledge/retrieval/compare_sqlite_generations.py
  - src/personal_knowledge/retrieval/compare_vector_generations.py
  - src/personal_knowledge/retrieval/evaluate_vector_collections.py
  - src/personal_knowledge/retrieval/evaluate_vector_retrieval.py
  - src/personal_knowledge/domains/memory/build_capability_memory.py
  - src/personal_knowledge/domains/memory/build_context_memory.py
  - src/personal_knowledge/domains/memory/build_deep_memory_profile.py
  - src/personal_knowledge/domains/memory/build_mem0_candidate_memory.py
  - src/personal_knowledge/domains/memory/build_memory_evidence_bundles.py
  - src/personal_knowledge/domains/memory/build_memory_graph.py
  - src/personal_knowledge/domains/memory/build_memory_promotion_candidates.py
  - src/personal_knowledge/domains/memory/build_memory_relation_candidates.py
  - src/personal_knowledge/domains/memory/build_memory_store.py
  - src/personal_knowledge/domains/memory/build_preference_memory.py
  - src/personal_knowledge/domains/memory/build_profile_from_memory.py
  - src/personal_knowledge/domains/memory/extract_memory_candidates_from_bundles.py
  - src/personal_knowledge/domains/memory/mine_deep_memory_graph.py
  - src/personal_knowledge/domains/memory/repair_memory_promotion_candidates.py
  - src/personal_knowledge/domains/memory/sync_memory_lifecycle.py
  - src/personal_knowledge/domains/memory/apply_memory_promotions.py
  - src/personal_knowledge/domains/memory/evaluate_memory_depth.py
  - src/personal_knowledge/domains/memory/evaluate_memory_promotion_candidates.py
  - src/personal_knowledge/domains/memory/evaluate_memory_relation_candidates.py
  - src/personal_knowledge/domains/memory/compare_memory_experiments.py
  - src/personal_knowledge/domains/memory/analyze_memory_mechanisms.py
  - src/personal_knowledge/domains/memory/audit_memory_experiments.py
  - src/personal_knowledge/domains/memory/__init__.py
  - src/personal_knowledge/application/run_pipeline.py
  - tools/supported/build_generation_gap_analysis.py
  - tools/compat/v1_1/evaluate_vector_collections.py
  - tools/compat/v1_1/evaluate_vector_retrieval.py
  - src/personal_knowledge/governance/source_manifest.py
  - governance/manifests/source_migration.json
---

# Plan 21-04: Memory Domain Migration + Retrieval Legacy + Finalization

## Goal

Final batch. Relocate the 16 memory build/lifecycle scripts to `application/memory/`, the 6 memory evaluate/compare/analyze/audit scripts to `evaluation/memory/`, and the 4 retrieval-layer legacy eval/compare scripts to `evaluation/vector/` — leaving re-export facades everywhere (D-06/D-07). Then run the full D-08 triple gate (pytest + governance preflight + REST :8000 + MCP :8789 health), rebuild `source_migration.json`, and confirm the phase success criterion: `domains/` contains no `build_/evaluate_` scripts (only rules/models/constants + facades).

## Context references

- Facade template: `src/personal_knowledge/retrieval/unified_search.py` L18-58
- Layer rule: `governance/policies/architecture.yaml` L19 (`memory: layer: domain`), L22 (`pipeline` may_import memory), L24 (`evaluation` may_import NOT memory — note: evaluation may_import is `[core, conversation, knowledge, vector, evaluation]`; evaluation/memory/ modules that import domains.memory are fine because evaluation may_import conversation/knowledge but NOT memory — verify evaluation/memory/ files do not import domains.memory, only core/knowledge/application)
- Health endpoints: REST `GET http://127.0.0.1:8000/health` → `{"status":"ok",...}` (src/personal_knowledge/services/api_server.py L174); MCP `GET http://127.0.0.1:8789/health` → `{"ok":true,"status":"ok"}` (apps/personal_data_chatgpt/server.mjs L1569-1570)
- Service start: `pwsh -NoProfile -ExecutionPolicy Bypass -File apps/personal_data_chatgpt/scripts/start-services.ps1` (per AGENTS.md); or the batch file `启动服务.bat`
- source_manifest rebuild: `python -m personal_knowledge.governance.source_manifest --cohort all --output governance/manifests/source_migration.json` (existing manifest at governance/manifests/source_migration.json)

## Memory script classification (22 files total, excluding `__init__.py`)

**Move to `application/memory/` (16 — build/lifecycle/orchestration):**
build_capability_memory, build_context_memory, build_deep_memory_profile, build_mem0_candidate_memory, build_memory_evidence_bundles, build_memory_graph, build_memory_promotion_candidates, build_memory_relation_candidates, build_memory_store, build_preference_memory, build_profile_from_memory, extract_memory_candidates_from_bundles, mine_deep_memory_graph, repair_memory_promotion_candidates, sync_memory_lifecycle, apply_memory_promotions

**Move to `evaluation/memory/` (6 — evaluate/compare/analyze/audit):**
evaluate_memory_depth, evaluate_memory_promotion_candidates, evaluate_memory_relation_candidates, compare_memory_experiments, analyze_memory_mechanisms, audit_memory_experiments

## Retrieval legacy (4 — move to `evaluation/vector/`)

compare_sqlite_generations, compare_vector_generations, evaluate_vector_collections, evaluate_vector_retrieval — currently in `src/personal_knowledge/retrieval/`. Per CONTEXT, these are evaluation scripts misplaced in the retrieval (infrastructure/vector) layer; relocate to `evaluation/vector/` and leave facades in `retrieval/` so the existing shims/tests/audit refs keep working.

## Verified callers (selection)

- `run_pipeline.py` STEP_MODULES L52-59: 6 memory build entries (`build_memory_store`, `build_capability_memory`, `build_context_memory`, `build_preference_memory`, `build_memory_graph`, `build_profile_from_memory`) — update to `application.memory.*`
- `tools/compat/v1_1/`: `repair_memory_promotion_candidates`, `sync_memory_lifecycle` shims point at `domains.memory.*` (facades keep working); `evaluate_vector_collections`, `evaluate_vector_retrieval` shims point at `personal_knowledge.retrieval.*` (retrieval/ facades keep working — no shim edit needed)
- `tools/supported/build_generation_gap_analysis.py` L431-432: hint strings naming `retrieval.compare_vector_generations` / `retrieval.compare_sqlite_generations` — update to `evaluation.vector.*`
- `tests/unit/test_memory_*.py` (9 files): import memory modules via `domains.memory.*` — facades keep working
- `tests/unit/test_vector_collection_eval.py`, `test_vector_retrieval_eval.py`: import `retrieval.evaluate_vector_*` — facades keep working
- `tests/governance/test_phase19_default_paths.py:11`: imports `retrieval.compare_vector_generations` — facade keeps working (this test is in the 13-fail baseline; facade preserves its import)
- `domains/memory/audit_memory_experiments.py` L247,254,344,351: string refs to `evaluate_vector_collections.py` / `evaluate_vector_retrieval.py` (display strings, not imports — leave as bare filenames, they are report labels)

---

## Tasks

### Task 1: Create application/memory/, evaluation/memory/, evaluation/vector/ packages

<action>
Create three `__init__.py` files:
- `src/personal_knowledge/application/memory/__init__.py` — docstring `"""memory build/lifecycle orchestration (application layer)."""`
- `src/personal_knowledge/evaluation/memory/__init__.py` — docstring `"""memory evaluation suites (evaluation layer)."""`
- `src/personal_knowledge/evaluation/vector/__init__.py` — docstring `"""vector retrieval evaluation suites (evaluation layer)."""`
</action>

<read_first>
- src/personal_knowledge/application/__init__.py (convention)
- src/personal_knowledge/evaluation/__init__.py (convention)
- src/personal_knowledge/application/conversation/__init__.py, application/graph/__init__.py (21-01/02 outputs — match style)
</read_first>

<acceptance_criteria>
- `python -c "import personal_knowledge.application.memory, personal_knowledge.evaluation.memory, personal_knowledge.evaluation.vector"` succeeds
</acceptance_criteria>

---

### Task 2: Migrate the 16 memory build/lifecycle scripts to `application/memory/` + leave facades (D-04/D-06/D-07)

<action>
For each of these 16 files, MOVE content to `application/memory/<module>.py` and replace the `domains/memory/<module>.py` original with a re-export facade:

build_capability_memory, build_context_memory, build_deep_memory_profile, build_mem0_candidate_memory, build_memory_evidence_bundles, build_memory_graph, build_memory_promotion_candidates, build_memory_relation_candidates, build_memory_store, build_preference_memory, build_profile_from_memory, extract_memory_candidates_from_bundles, mine_deep_memory_graph, repair_memory_promotion_candidates, sync_memory_lifecycle, apply_memory_promotions

Facade contract:
```
"""Re-export facade — retained for backward compatibility during the 2026-08-13 cleanup window.
Canonical location: application/memory/<module>.py
"""
from personal_knowledge.application.memory.<module> import *  # noqa: F401,F403
```

Note on peer imports: `build_memory_relation_candidates`, `compare_memory_experiments`, `extract_memory_candidates_from_bundles`, `repair_memory_promotion_candidates` were repointed to `from personal_knowledge.core import llm as llm_mod` in 21-01 Task 3 — those imports stay unchanged in the moved files (core import is location-independent). The LLM hub decoupling from 21-01 is what makes this batch safe.
</action>

<read_first>
- All 16 source files in src/personal_knowledge/domains/memory/ (public symbols for facade completeness)
- src/personal_knowledge/application/memory/__init__.py (Task 1 output)
- src/personal_knowledge/retrieval/unified_search.py L18-58 (facade style)
</read_first>

<acceptance_criteria>
- `ls src/personal_knowledge/application/memory/*.py | wc -l` returns 17 (16 modules + `__init__.py`)
- `python -c "import personal_knowledge.application.memory.build_memory_store, personal_knowledge.application.memory.build_memory_relation_candidates, personal_knowledge.application.memory.sync_memory_lifecycle, personal_knowledge.application.memory.apply_memory_promotions"` succeeds
- `python -c "import personal_knowledge.domains.memory.build_memory_store, personal_knowledge.domains.memory.build_memory_relation_candidates"` succeeds (facades)
- `grep -L "2026-08-13" src/personal_knowledge/domains/memory/{build_*,extract_memory_candidates_from_bundles,mine_deep_memory_graph,repair_memory_promotion_candidates,sync_memory_lifecycle,apply_memory_promotions}.py` returns no files
</acceptance_criteria>

---

### Task 3: Migrate the 6 memory evaluate/compare/analyze/audit scripts to `evaluation/memory/` + leave facades (D-04/D-06/D-07)

<action>
Relocate (move content; leave facade):
- `domains/memory/evaluate_memory_depth.py` → `evaluation/memory/evaluate_memory_depth.py`
- `domains/memory/evaluate_memory_promotion_candidates.py` → `evaluation/memory/evaluate_memory_promotion_candidates.py`
- `domains/memory/evaluate_memory_relation_candidates.py` → `evaluation/memory/evaluate_memory_relation_candidates.py`
- `domains/memory/compare_memory_experiments.py` → `evaluation/memory/compare_memory_experiments.py`
- `domains/memory/analyze_memory_mechanisms.py` → `evaluation/memory/analyze_memory_mechanisms.py`
- `domains/memory/audit_memory_experiments.py` → `evaluation/memory/audit_memory_experiments.py`

Facade contract:
```
"""Re-export facade — retained for backward compatibility during the 2026-08-13 cleanup window.
Canonical location: evaluation/memory/<module>.py
"""
from personal_knowledge.evaluation.memory.<module> import *  # noqa: F401,F403
```

Layer check: `evaluation` may_import is `[core, conversation, knowledge, vector, evaluation]` — NOT memory. If any of these 6 files imports `personal_knowledge.domains.memory.*` or `personal_knowledge.application.memory.*`, that becomes an evaluation-layer violation. Audit each moved file: if it imports a memory build module, either (a) the import is to a peer that also moved to `evaluation/memory/` (fine — repoint to `evaluation.memory.<peer>`), or (b) it imports a build module from application/memory — in which case leave the import pointing at the `domains.memory.*` facade (evaluation→domains.memory is NOT in may_import, but the preflight AST scan only checks `integration.scripts.*`, so it will not trip; document any such import as a known facade-mediated dependency). Prefer repointing to `evaluation.memory.<peer>` where the peer co-migrated.
</action>

<read_first>
- The 6 source files (public symbols + their imports — especially cross-imports to other memory modules)
- src/personal_knowledge/evaluation/memory/__init__.py (Task 1 output)
- governance/policies/architecture.yaml L24 (evaluation may_import list)
- tests/unit/test_memory_experiment_comparison.py, test_memory_mechanism_analysis.py, test_memory_experiment_inventory.py (imports to preserve via facade)
</read_first>

<acceptance_criteria>
- `ls src/personal_knowledge/evaluation/memory/*.py | wc -l` returns 7 (6 modules + `__init__.py`)
- `python -c "import personal_knowledge.evaluation.memory.compare_memory_experiments, personal_knowledge.evaluation.memory.analyze_memory_mechanisms, personal_knowledge.evaluation.memory.audit_memory_experiments"` succeeds
- `python -c "import personal_knowledge.domains.memory.compare_memory_experiments, personal_knowledge.domains.memory.analyze_memory_mechanisms, personal_knowledge.domains.memory.audit_memory_experiments"` succeeds (facades)
- `grep -c "2026-08-13" src/personal_knowledge/domains/memory/evaluate_memory_depth.py src/personal_knowledge/domains/memory/evaluate_memory_promotion_candidates.py src/personal_knowledge/domains/memory/evaluate_memory_relation_candidates.py src/personal_knowledge/domains/memory/compare_memory_experiments.py src/personal_knowledge/domains/memory/analyze_memory_mechanisms.py src/personal_knowledge/domains/memory/audit_memory_experiments.py` returns `1` for each
</acceptance_criteria>

---

### Task 4: Migrate the 4 retrieval legacy eval/compare scripts to `evaluation/vector/` + leave facades in retrieval/

<action>
Relocate (move content; leave facade in retrieval/ so shims/tests keep working):
- `retrieval/compare_sqlite_generations.py` → `evaluation/vector/compare_sqlite_generations.py`
- `retrieval/compare_vector_generations.py` → `evaluation/vector/compare_vector_generations.py`
- `retrieval/evaluate_vector_collections.py` → `evaluation/vector/evaluate_vector_collections.py`
- `retrieval/evaluate_vector_retrieval.py` → `evaluation/vector/evaluate_vector_retrieval.py`

Facade in `retrieval/` for each:
```
"""Re-export facade — retained for backward compatibility during the 2026-08-13 cleanup window.
Canonical location: evaluation/vector/<module>.py
"""
from personal_knowledge.evaluation.vector.<module> import *  # noqa: F401,F403
```

Caller wiring:
- `src/personal_knowledge/application/run_pipeline.py` — no entries for these 4 (not in STEP_MODULES); skip.
- `tools/supported/build_generation_gap_analysis.py` L431-432: update hint strings from `python -m personal_knowledge.retrieval.compare_vector_generations` / `compare_sqlite_generations` to `python -m personal_knowledge.evaluation.vector.compare_vector_generations` / `compare_sqlite_generations`.
- `tools/compat/v1_1/evaluate_vector_collections.py` L11 and `evaluate_vector_retrieval.py` L11: these shims point at `personal_knowledge.retrieval.evaluate_vector_*` — the retrieval/ facades keep them working, so NO edit is strictly required. Optionally update to `personal_knowledge.evaluation.vector.*` for canonical correctness; either is acceptable since both paths resolve.
- The `domains/memory/audit_memory_experiments.py` string refs to `evaluate_vector_collections.py` / `evaluate_vector_retrieval.py` (L247,254,344,351) are bare report labels, not imports — leave unchanged.

Note: `evaluate_vector_collections.py` imports `build_conversation_vector_store as turns_mod` (repointed to `application.conversation` in 21-01 Task 6). That import is unchanged and valid from the new `evaluation/vector/` location.
</action>

<read_first>
- The 4 source files in src/personal_knowledge/retrieval/ (public symbols; evaluate_vector_collections.py L25 import of build_conversation_vector_store)
- src/personal_knowledge/evaluation/vector/__init__.py (Task 1 output)
- tools/supported/build_generation_gap_analysis.py (L428-435 hint strings)
- tools/compat/v1_1/evaluate_vector_collections.py, evaluate_vector_retrieval.py (shim targets)
- tests/unit/test_vector_collection_eval.py, test_vector_retrieval_eval.py (imports via retrieval facade)
</read_first>

<acceptance_criteria>
- `ls src/personal_knowledge/evaluation/vector/*.py | wc -l` returns 5 (4 modules + `__init__.py`)
- `python -c "import personal_knowledge.evaluation.vector.evaluate_vector_collections, personal_knowledge.evaluation.vector.evaluate_vector_retrieval, personal_knowledge.evaluation.vector.compare_vector_generations, personal_knowledge.evaluation.vector.compare_sqlite_generations"` succeeds
- `python -c "import personal_knowledge.retrieval.evaluate_vector_collections, personal_knowledge.retrieval.evaluate_vector_retrieval, personal_knowledge.retrieval.compare_vector_generations, personal_knowledge.retrieval.compare_sqlite_generations"` succeeds (retrieval facades)
- `grep -c "2026-08-13" src/personal_knowledge/retrieval/evaluate_vector_collections.py src/personal_knowledge/retrieval/evaluate_vector_retrieval.py src/personal_knowledge/retrieval/compare_vector_generations.py src/personal_knowledge/retrieval/compare_sqlite_generations.py` returns `1` for each
- `grep -n "retrieval.compare_vector_generations\|retrieval.compare_sqlite_generations" tools/supported/build_generation_gap_analysis.py` returns 0 matches (hint strings updated to evaluation.vector.*)
</acceptance_criteria>

---

### Task 5: Update run_pipeline.py STEP_MODULES memory entries + memory __init__ docstring

<action>
1. In `src/personal_knowledge/application/run_pipeline.py` STEP_MODULES (L52-59), update these 6 entries (value side only):
   - `build_memory_store`: `personal_knowledge.domains.memory.build_memory_store` → `personal_knowledge.application.memory.build_memory_store`
   - `build_capability_memory`: → `personal_knowledge.application.memory.build_capability_memory`
   - `build_context_memory`: → `personal_knowledge.application.memory.build_context_memory`
   - `build_preference_memory`: → `personal_knowledge.application.memory.build_preference_memory`
   - `build_memory_graph`: → `personal_knowledge.application.memory.build_memory_graph`
   - `build_profile_from_memory`: → `personal_knowledge.application.memory.build_profile_from_memory`
2. Update `src/personal_knowledge/domains/memory/__init__.py` docstring from `"""memory package — integration scripts."""` to `"""memory domain — rules, models, and constants for long-term memory. Build/eval orchestration lives in application/memory/ and evaluation/memory/."""`.
3. Verify NO `domains.conversation` / `domains.graph` / `domains.knowledge` / `domains.memory` strings remain in run_pipeline.py STEP_MODULES (all should now point at application.*).
</action>

<read_first>
- src/personal_knowledge/application/run_pipeline.py (L45-65 STEP_MODULES — after 21-01/02 edits, only memory entries remain as domains.*)
- src/personal_knowledge/domains/memory/__init__.py (docstring to update)
</read_first>

<acceptance_criteria>
- `grep -n "domains.memory\|domains.conversation\|domains.graph\|domains.knowledge" src/personal_knowledge/application/run_pipeline.py` returns 0 matches (all STEP_MODULES now use application.* canonical paths)
- `grep "integration scripts" src/personal_knowledge/domains/memory/__init__.py` returns 0 matches
- `python -c "import personal_knowledge.application.run_pipeline"` succeeds
</acceptance_criteria>

---

### Task 6: Rebuild source_migration.json (governance manifest refresh)

<action>
The physical source manifest must reflect the new file locations. Run:
`python -m personal_knowledge.governance.source_manifest --cohort all --output governance/manifests/source_migration.json`

This regenerates the canonical-src cohort preview with the migrated module paths (application/{conversation,graph,knowledge,memory}/, evaluation/{conversation,graph,knowledge,memory,vector}/). Confirm the manifest writes without error and the new paths appear in the `consumers`/`modules` mapping.

If the rebuild reports a diff against the previously-approved preview (phase 19/20 approved bytes), record the diff in the execution log — the new locations are the intended end-state per this phase. Do NOT rollback; the manifest is now authoritative for the post-phase-21 layout.
</action>

<read_first>
- src/personal_knowledge/governance/source_manifest.py (L739-775 main(); confirm --cohort all --output invocation)
- governance/manifests/source_migration.json (existing manifest — will be overwritten with refreshed paths)
</read_first>

<acceptance_criteria>
- `python -m personal_knowledge.governance.source_manifest --cohort all --output governance/manifests/source_migration.json` exits 0
- `python -c "import json; m=json.load(open('governance/manifests/source_migration.json',encoding='utf-8')); s=json.dumps(m); assert 'application/conversation' in s or 'application\\\\conversation' in s or 'application.memory' in s or 'application/knowledge' in s, 'new paths missing'; print('manifest refreshed')"` prints `manifest refreshed`
- The manifest file mtime is newer than before the command ran
</acceptance_criteria>

---

### Task 7: D-08 triple gate — pytest + governance preflight + REST/MCP health (FINAL)

<action>
This is the phase-level D-08 gate. All three must pass.

**Gate 1 — pytest:**
`python -m pytest tests/ -q --tb=short -p no:cacheprovider`
Compare the FAILED set to the pre-phase baseline (13 known fails, all in tests/governance/* and tests/unit/test_memory_decomplexity_plan — none related to domains build/eval). The post-phase FAILED set must be identical (no new failures). Record the final FAILED list.

**Gate 2 — governance preflight:**
`python -m personal_knowledge.governance.preflight --ci`
Must exit 0 / PASS. Confirms no new layer violation across all 4 migrated subdomains + retrieval facades.

**Gate 3 — REST + MCP health endpoints:**
Start the services (they are not always-running per AGENTS.md):
`pwsh -NoProfile -ExecutionPolicy Bypass -File apps/personal_data_chatgpt/scripts/start-services.ps1`
(or double-click `apps/personal_data_chatgpt/scripts/启动服务.bat`). Wait for the status panel to show REST + MCP up.
Then verify both endpoints return HTTP 200:
- REST: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/health` → `200` (body `{"status":"ok",...}`)
- MCP: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8789/health` → `200` (body `{"ok":true,"status":"ok"}`)

If services cannot be started in the execution environment, document the blocker and verify health via import smoke instead: `python -c "import personal_knowledge.services.api_server; print('api_server imports ok')"` — but the canonical D-08 gate is the live HTTP 200 check; prefer running the services.

**Phase success criterion check:**
`find src/personal_knowledge/domains -name "build_*.py" -o -name "evaluate_*.py" -o -name "compare_*.py" -o -name "analyze_*.py" -o -name "audit_*.py"` — every file returned must be a ≤6-line re-export facade (no real logic). `domains/` now contains only rules/models/constants (`migrate_add_knowledge_unit_tables.py` with `SCHEMA_SQL`) + facades + the domain `__init__.py` files.
</action>

<read_first>
- The pre-phase baseline FAILED list (13 fails captured before 21-01)
- src/personal_knowledge/governance/preflight.py (L47-68 architecture scan scope; L102-130 evaluate())
- src/personal_knowledge/services/api_server.py (L174 /health route)
- apps/personal_data_chatgpt/server.mjs (L1569-1570 /health route)
- AGENTS.md (service start instructions)
</read_first>

<acceptance_criteria>
- `python -m pytest tests/ -q --tb=short -p no:cacheprovider` FAILED set is identical to the pre-phase 13-fail baseline (no new failures); exit code is 1 only because of the known baseline fails
- `python -m personal_knowledge.governance.preflight --ci` exits 0 / reports PASS (all gates ok)
- `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/health` returns `200` (or, if services not startable in-env, `python -c "import personal_knowledge.services.api_server"` succeeds and the blocker is documented)
- `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8789/health` returns `200` (or documented blocker)
- `find src/personal_knowledge/domains -name "build_*.py" -o -name "evaluate_*.py" -o -name "compare_*.py" -o -name "analyze_*.py" -o -name "audit_*.py"` — every result file is ≤6 lines AND contains `2026-08-13` (pure facade); `wc -l` on any result is ≤6
- `find src/personal_knowledge/domains -name "*.py" ! -name "__init__.py" | xargs grep -lE "^def |^class " | grep -v facade` returns only `migrate_add_knowledge_unit_tables.py` (the sole remaining non-facade logic file, which is the schema constant — correct)
</acceptance_criteria>

---

## Verification (phase-level, supersedes batch gates)

1. **Pytest:** FAILED set == pre-phase 13-fail baseline. No new failures introduced across all 4 subdomain batches.
2. **Governance preflight --ci:** PASS. No architecture-boundary violation from the facades (AST scan scope is `integration.scripts.*` only).
3. **REST :8000 /health:** HTTP 200.
4. **MCP :8789 /health:** HTTP 200.
5. **domains/ slimmed:** no `build_/evaluate_/compare_/analyze_/audit_` logic remains — only facades + `migrate_add_knowledge_unit_tables.py` (schema constant) + `__init__.py` files.
6. **Cross-domain hub decoupled:** `grep -rn "domains.conversation import build_conversation_summary\|domains.conversation.build_conversation_summary" src/personal_knowledge/domains/` returns 0 (D-01/D-02 complete).
7. **source_migration.json refreshed:** reflects new application/evaluation locations.

## must_haves (goal-backward — phase success criteria)

- [ ] All 16 memory build scripts live in `application/memory/`; all 6 memory eval/compare/analyze/audit scripts live in `evaluation/memory/`
- [ ] All 4 retrieval legacy eval/compare scripts live in `evaluation/vector/` with facades in `retrieval/`
- [ ] Every moved `domains/memory/*.py` and `retrieval/{evaluate,compare}_vector*.py` is a re-export facade with `2026-08-13` marker (D-06/D-07)
- [ ] `run_pipeline.py` STEP_MODULES has zero `domains.*` values (all application.* canonical)
- [ ] `governance/manifests/source_migration.json` rebuilt with new paths
- [ ] D-08 triple gate passes: pytest (no new fails) + preflight --ci PASS + REST :8000 + MCP :8789 health 200
- [ ] `domains/` contains no build_/evaluate_ logic — only rules/models/constants (`SCHEMA_SQL`) + facades (phase success criterion)
