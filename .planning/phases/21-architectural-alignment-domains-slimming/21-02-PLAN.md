---
plan: 21-02
title: "Graph domain migration + delete v2 dead code (D-05)"
wave: 2
depends_on: [21-01]
autonomous: true
phase: 21
requirements: [ARCH-21-03, ARCH-21-05, ARCH-21-06, ARCH-21-07]
files_modified:
  - src/personal_knowledge/application/graph/__init__.py
  - src/personal_knowledge/application/graph/build_graph_relation_candidates.py
  - src/personal_knowledge/application/graph/build_merge_layer.py
  - src/personal_knowledge/application/graph/build_triple_store.py
  - src/personal_knowledge/application/graph/judge_graph_relations.py
  - src/personal_knowledge/application/graph/query_graph.py
  - src/personal_knowledge/evaluation/graph/__init__.py
  - src/personal_knowledge/evaluation/graph/evaluate_graph_relation_judgments.py
  - src/personal_knowledge/domains/graph/build_graph_relation_candidates.py
  - src/personal_knowledge/domains/graph/build_merge_layer.py
  - src/personal_knowledge/domains/graph/build_triple_store.py
  - src/personal_knowledge/domains/graph/judge_graph_relations.py
  - src/personal_knowledge/domains/graph/query_graph.py
  - src/personal_knowledge/domains/graph/evaluate_graph_relation_judgments.py
  - src/personal_knowledge/domains/graph/__init__.py
  - src/personal_knowledge/application/run_pipeline.py
  - src/personal_knowledge/retrieval/merge_cluster.py
  - tools/forensics/_audit_test_gaps.py
  - tools/_reorganize_scripts.py
---

# Plan 21-02: Graph Domain Migration + Delete v2 Dead Code

## Goal

Second batch. Delete the confirmed-dead `build_graph_relation_candidates_v2.py` (D-05), then relocate the 5 graph build scripts to `application/graph/` and the 1 evaluate script to `evaluation/graph/`, leaving re-export facades at every original `domains/graph/` location (D-06/D-07). Depends on 21-01 because `judge_graph_relations` was repointed to `core.llm` in wave 1; its import is now stable and can move with the file.

## Context references

- Facade template: `src/personal_knowledge/retrieval/unified_search.py` L18-58
- Layer rule source: `governance/policies/architecture.yaml` L20 (`graph: layer: domain, may_import: [core, knowledge, memory, graph]`), L22 (`pipeline` = application may import graph), L31 (`graph forbidden→pipeline/services/evaluation`)
- v2 dead-code evidence: the `.py` source has no test (only stale `tests/__pycache__/test_graph_relation_candidates_v2.cpython-*.pyc` remains — no `.py`); its only live-tree references are two registry strings (`tools/forensics/_audit_test_gaps.py:122`, `tools/_reorganize_scripts.py:99`); its internal import was written wrong (`from conversation` not `from personal_knowledge.domains.conversation`), confirming it was never wired correctly
- Preflight boundary scan (`governance/preflight.py` L47-68) only AST-scans `integration.scripts.*` imports — the domains facade `from personal_knowledge.application.graph import *` will not trip architecture-boundary (consistent with phase-1 unified_search facade)

## Verified graph callers (after 21-01)

| Caller | File:line | Reference |
|--------|-----------|-----------|
| run_pipeline STEP_MODULES | application/run_pipeline.py:50 | `"build_merge_layer": "personal_knowledge.domains.graph.build_merge_layer"` |
| retrieval/memory.py lazy import | retrieval/memory.py:638,707,802 | `import personal_knowledge.domains.graph.query_graph as query_graph` (3 lazy imports — phase-1 isolated violation, deferred per CONTEXT) |
| retrieval/merge_cluster hint | retrieval/merge_cluster.py:98 | user-facing hint string naming `build_merge_layer` module |
| retrieval/evaluate_vector_collections hint | retrieval/evaluate_vector_collections.py:198 | hint string naming `build_triple_store` module |
| judge_graph_relations intra-graph import | domains/graph/judge_graph_relations.py:15 | `from personal_knowledge.domains.graph.build_graph_relation_candidates import SQLITE_DB, SUMMARIES_JSON, canonical_pair` |
| tests/unit/test_graph_relation_candidates.py:15 | imports `build_graph_relation_candidates as mod` (facade keeps working) |
| tests/unit/test_graph_relation_judgments.py:15-16 | imports `judge_graph_relations`, `evaluate_graph_relation_judgments` |
| tests/unit/test_memory_graph_visualization.py:16 | imports `query_graph as mod` |
| tools/compat/v1_1/*.py (6 shims) | build_graph_relation_candidates, build_merge_layer, build_triple_store, evaluate_graph_relation_judgments, judge_graph_relations, query_graph | string module paths — facade keeps working (D-06) |
| tools/forensics/_audit_test_gaps.py:121-123 | registry strings for build_graph_relation_candidates, build_graph_relation_candidates_v2 (DELETE v2 entry — D-05), judge_graph_relations |
| tools/_reorganize_scripts.py:99 | `"build_graph_relation_candidates_v2"` string (DELETE — D-05) |

---

## Tasks

### Task 1: Delete `build_graph_relation_candidates_v2.py` + remove its registry entries (D-05)

<action>
1. Delete the file `src/personal_knowledge/domains/graph/build_graph_relation_candidates_v2.py` (889 LOC). Do NOT leave a facade (D-05: hard delete, no shim).
2. Remove the v2 entry from `tools/forensics/_audit_test_gaps.py` L122: delete the line `"personal_knowledge.domains.graph.build_graph_relation_candidates_v2",` from its module list.
3. Remove the v2 entry from `tools/_reorganize_scripts.py` L99: delete the line `"build_graph_relation_candidates_v2",` from its script list.
4. Delete stale bytecode caches: `tests/__pycache__/test_graph_relation_candidates_v2.cpython-*.pyc` and `tests/unit/__pycache__/test_graph_relation_candidates_v2.cpython-*.pyc` (these reference a `.py` test that no longer exists; they are orphaned and will be regenerated/cleaned automatically, but remove them explicitly to avoid confusion).
5. Verify no remaining reference: grep the live tree for `build_graph_relation_candidates_v2` — must return 0 matches in `src/`, `tests/`, `apps/`, `tools/`, `integration/`.
</action>

<read_first>
- src/personal_knowledge/domains/graph/build_graph_relation_candidates_v2.py (confirm it is the dead file; confirm its broken `from conversation import ...` at the top)
- tools/forensics/_audit_test_gaps.py (L118-126 region — the module list containing the v2 entry)
- tools/_reorganize_scripts.py (L95-103 region — the script list containing the v2 entry)
</read_first>

<acceptance_criteria>
- `test ! -f src/personal_knowledge/domains/graph/build_graph_relation_candidates_v2.py` succeeds (file gone)
- `grep -rn "build_graph_relation_candidates_v2" src/ tests/ apps/ tools/ integration/ 2>/dev/null | grep -v __pycache__` returns 0 matches
- `find tests/ -name "test_graph_relation_candidates_v2.py"` returns nothing (no orphaned test source)
- `python -c "import ast; ast.parse(open('tools/forensics/_audit_test_gaps.py',encoding='utf-8').read())"` succeeds (registry file still valid Python)
- `python -c "import ast; ast.parse(open('tools/_reorganize_scripts.py',encoding='utf-8').read())"` succeeds
</acceptance_criteria>

---

### Task 2: Migrate the 5 graph build scripts to `application/graph/` + leave facades (D-04/D-06/D-07)

<action>
1. Create `src/personal_knowledge/application/graph/__init__.py` (empty, docstring `"""graph build orchestration (application layer)."""`).
2. Relocate each file (move content; leave facade):
   - `domains/graph/build_graph_relation_candidates.py` → `application/graph/build_graph_relation_candidates.py`
   - `domains/graph/build_merge_layer.py` → `application/graph/build_merge_layer.py`
   - `domains/graph/build_triple_store.py` → `application/graph/build_triple_store.py`
   - `domains/graph/judge_graph_relations.py` → `application/graph/judge_graph_relations.py`
   - `domains/graph/query_graph.py` → `application/graph/query_graph.py`
3. In `application/graph/judge_graph_relations.py` (the moved file), update its intra-graph import L15:
   - OLD: `from personal_knowledge.domains.graph.build_graph_relation_candidates import SQLITE_DB, SUMMARIES_JSON, canonical_pair`
   - NEW: `from personal_knowledge.application.graph.build_graph_relation_candidates import SQLITE_DB, SUMMARIES_JSON, canonical_pair`
   (Both files are now peers under application/graph/; the domains.graph path would now hit the facade which also works, but canonical peer import is cleaner. The L14 `from personal_knowledge.core import llm as llm_mod` import from 21-01 Task 3 stays unchanged.)
4. Replace each of the 5 original `domains/graph/*.py` files with the facade:
   ```
   """Re-export facade — retained for backward compatibility during the 2026-08-13 cleanup window.
   Canonical location: application/graph/<module>.py
   """
   from personal_knowledge.application.graph.<module> import *  # noqa: F401,F403
   ```
   If a module exposes a `_`-prefixed symbol that callers import, add an explicit re-export line below the wildcard (run Task 5 audit to detect).
</action>

<read_first>
- The 5 source files in src/personal_knowledge/domains/graph/ (public symbols for facade completeness)
- src/personal_knowledge/application/graph/__init__.py (Task 2 step 1 output)
- src/personal_knowledge/domains/graph/judge_graph_relations.py L14-15 (the two imports — L14 from 21-01 stays, L15 updates)
- src/personal_knowledge/retrieval/unified_search.py L18-58 (facade style — absolute imports)
</read_first>

<acceptance_criteria>
- `ls src/personal_knowledge/application/graph/*.py` lists 5 modules + `__init__.py`
- `python -c "import personal_knowledge.application.graph.judge_graph_relations, personal_knowledge.application.graph.build_graph_relation_candidates, personal_knowledge.application.graph.build_merge_layer, personal_knowledge.application.graph.build_triple_store, personal_knowledge.application.graph.query_graph"` succeeds
- `python -c "import personal_knowledge.domains.graph.judge_graph_relations, personal_knowledge.domains.graph.build_graph_relation_candidates, personal_knowledge.domains.graph.build_merge_layer, personal_knowledge.domains.graph.build_triple_store, personal_knowledge.domains.graph.query_graph"` succeeds (facades)
- `grep -c "2026-08-13" src/personal_knowledge/domains/graph/build_graph_relation_candidates.py src/personal_knowledge/domains/graph/build_merge_layer.py src/personal_knowledge/domains/graph/build_triple_store.py src/personal_knowledge/domains/graph/judge_graph_relations.py src/personal_knowledge/domains/graph/query_graph.py` returns `1` for each
- `grep -n "from personal_knowledge.application.graph.build_graph_relation_candidates import" src/personal_knowledge/application/graph/judge_graph_relations.py` returns 1 match (peer import updated)
</acceptance_criteria>

---

### Task 3: Migrate `evaluate_graph_relation_judgments.py` to `evaluation/graph/` + leave facade (D-04/D-06/D-07)

<action>
1. Create `src/personal_knowledge/evaluation/graph/__init__.py` (empty, docstring `"""graph evaluation suites (evaluation layer)."""`).
2. Relocate `domains/graph/evaluate_graph_relation_judgments.py` → `evaluation/graph/evaluate_graph_relation_judgments.py`.
3. Replace the original with the facade:
   ```
   """Re-export facade — retained for backward compatibility during the 2026-08-13 cleanup window.
   Canonical location: evaluation/graph/evaluate_graph_relation_judgments.py
   """
   from personal_knowledge.evaluation.graph.evaluate_graph_relation_judgments import *  # noqa: F401,F403
   ```
</action>

<read_first>
- src/personal_knowledge/domains/graph/evaluate_graph_relation_judgments.py (public symbols)
- src/personal_knowledge/evaluation/__init__.py (existing evaluation package)
- tests/unit/test_graph_relation_judgments.py L16 (imports `evaluate_graph_relation_judgments as eval_mod` — facade must preserve)
</read_first>

<acceptance_criteria>
- `ls src/personal_knowledge/evaluation/graph/*.py` lists the module + `__init__.py`
- `python -c "import personal_knowledge.evaluation.graph.evaluate_graph_relation_judgments; import personal_knowledge.domains.graph.evaluate_graph_relation_judgments"` succeeds (both paths)
- `grep -c "2026-08-13" src/personal_knowledge/domains/graph/evaluate_graph_relation_judgments.py` returns `1`
</acceptance_criteria>

---

### Task 4: Update caller wiring — run_pipeline + retrieval hint strings

<action>
1. In `src/personal_knowledge/application/run_pipeline.py` STEP_MODULES (L50):
   - `"build_merge_layer"`: value `personal_knowledge.domains.graph.build_merge_layer` → `personal_knowledge.application.graph.build_merge_layer`
2. In `src/personal_knowledge/retrieval/merge_cluster.py` L98, update the user-facing hint string from `python -m personal_knowledge.domains.graph.build_merge_layer` to `python -m personal_knowledge.application.graph.build_merge_layer`.
3. In `src/personal_knowledge/retrieval/evaluate_vector_collections.py` L198, update the hint string from `python -m personal_knowledge.domains.graph.build_triple_store --write --only sqlite` to `python -m personal_knowledge.application.graph.build_triple_store --write --only sqlite`.

Do NOT change `retrieval/memory.py` L638/707/802 lazy imports of `domains.graph.query_graph` — those resolve through the facade and the full elimination is deferred (CONTEXT `<deferred>`). Do NOT change `tools/compat/v1_1/*.py` shims (facade keeps them working).
</action>

<read_first>
- src/personal_knowledge/application/run_pipeline.py (L45-65 STEP_MODULES)
- src/personal_knowledge/retrieval/merge_cluster.py (L95-100)
- src/personal_knowledge/retrieval/evaluate_vector_collections.py (L195-200)
- src/personal_knowledge/retrieval/memory.py (L630-810 — the 3 lazy query_graph imports; confirm they still resolve via facade, no edit)
</read_first>

<acceptance_criteria>
- `grep -n "build_merge_layer" src/personal_knowledge/application/run_pipeline.py` shows value `personal_knowledge.application.graph.build_merge_layer`
- `grep -rn "domains.graph.build_merge_layer\|domains.graph.build_triple_store" src/personal_knowledge/retrieval/` returns 0 matches
- `grep -rn "domains.graph.query_graph" src/personal_knowledge/retrieval/memory.py` still returns 3 matches (deferred lazy imports intentionally untouched)
- `python -c "import personal_knowledge.application.run_pipeline, personal_knowledge.retrieval.merge_cluster, personal_knowledge.retrieval.evaluate_vector_collections"` succeeds
</acceptance_criteria>

---

### Task 5: Graph-batch test gate + governance preflight + docstring (D-08 partial)

<action>
1. Update `src/personal_knowledge/domains/graph/__init__.py` docstring from `"""graph package — integration scripts."""` to `"""graph domain — rules, models, and constants for the memory/relation graph. Build/eval orchestration lives in application/graph/ and evaluation/graph/."""`.
2. Run the gates:
   - `python -m pytest tests/ -q --tb=short -p no:cacheprovider` — compare FAILED set to the post-21-01 baseline (still the original 13 known fails). Any NEW failure must be fixed.
   - `python -m personal_knowledge.governance.preflight --ci` — must PASS. The domains/graph facades contain `from personal_knowledge.application.graph import *`; confirm preflight's architecture-boundary gate (`governance/preflight.py` L47-68) does NOT flag it (it only scans `integration.scripts.*` imports).
3. Audit facade completeness: for each moved module, `python -c "from personal_knowledge.domains.graph.<module> import *"` then attempt to import each public name the tests use (`SQLITE_DB`, `SUMMARIES_JSON`, `canonical_pair`, `judge`, etc.). If a name is missing from the wildcard re-export, add an explicit `from ... import <name>` line to that facade.
</action>

<read_first>
- src/personal_knowledge/domains/graph/__init__.py (docstring to update)
- The post-21-01 baseline FAILED list (from 21-01 Task 7 execution log)
- governance/preflight.py L47-68 (confirm architecture-boundary scan scope is `integration.scripts.*` only)
</read_first>

<acceptance_criteria>
- `python -m pytest tests/ -q --tb=short -p no:cacheprovider` FAILED set is identical to post-21-01 baseline (no new failures)
- `python -m personal_knowledge.governance.preflight --ci` exits 0 / PASS (architecture-boundary gate not tripped by facades)
- `grep "integration scripts" src/personal_knowledge/domains/graph/__init__.py` returns 0 matches
- `python -c "from personal_knowledge.domains.graph.build_graph_relation_candidates import SQLITE_DB, SUMMARIES_JSON, canonical_pair"` succeeds (facade preserves intra-graph symbols used by judge_graph_relations and tests)
- Every file under `domains/graph/` (except `__init__.py` and the deleted v2) is a ≤6-line facade
</acceptance_criteria>

---

## Verification (batch-level)

1. **Dead code gone:** `grep -rn "build_graph_relation_candidates_v2" src/ tests/ apps/ tools/ integration/` (excluding `__pycache__`) returns 0.
2. **Pytest gate:** no new failures beyond baseline.
3. **Layer gate:** `governance preflight --ci` PASS.
4. **Facade completeness:** every moved graph module imports via both canonical and domains/ paths.

## must_haves (goal-backward)

- [ ] `domains/graph/build_graph_relation_candidates_v2.py` is deleted and all its registry/string references removed (D-05)
- [ ] All 5 graph build scripts live in `application/graph/`; `evaluate_graph_relation_judgments` lives in `evaluation/graph/`
- [ ] Every original `domains/graph/{build,evaluate}_*.py` and `query_graph.py` is a re-export facade with `2026-08-13` marker (D-06/D-07)
- [ ] `judge_graph_relations` peer-imports `build_graph_relation_candidates` via the `application.graph` canonical path
- [ ] `run_pipeline.py` graph step module points at application/graph; retrieval hint strings updated
- [ ] retrieval/memory.py 3 lazy `domains.graph.query_graph` imports intentionally retained (deferred per CONTEXT)
- [ ] Full pytest no new failures; governance preflight PASS
