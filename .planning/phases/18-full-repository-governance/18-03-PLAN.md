---
phase: 18
plan: "03"
title: Source portability, package boundaries and shim lifecycle
wave: 3
depends_on: ["18-01", "18-02"]
requirements: [GOV-05, GOV-06, GOV-08]
autonomous: false
---

# Plan 18-03: Source and Compatibility Governance

**Decisions:** D-18-03, D-18-05, D-18-07.

<task id="18-03-01" type="auto">
  <name>Inventory hard-coded paths, sys.path injections and entrypoints</name>
  <files>governance/baselines/path_hits.yaml, integration/scripts/governance/check_path_policy.py, tests/test_governance_paths.py</files>
  <action>区分生产违规、文档模板、private source locator 和 historical fixture exception；新增命中阻断。</action>
  <verify>python -m pytest -q tests/test_governance_paths.py</verify>
  <acceptance_criteria>所有当前命中分类；用户名/Desktop/fixed-drive 新增=0。</acceptance_criteria>
  <done>可移植性债务有 owner 和归零路径。</done>
</task>

<task id="18-03-02" type="auto">
  <name>Centralize configuration and environment discovery</name>
  <files>integration/scripts/core/project_paths.py, integration/scripts/core/runtime_config.py, integration/scripts/knowledge/build_knowledge_units.py, integration/scripts/services/mcp_server.py</files>
  <action>用 env/CLI/discovery 替换 GCP SDK、embedding、MCP/Desktop 固定路径；缺依赖输出可操作错误。</action>
  <verify>python -m pytest -q tests -k 'project_paths or runtime_config or mcp or knowledge'</verify>
  <acceptance_criteria>生产源码硬编码机器路径=0；fixture exceptions 有 policy ID。</acceptance_criteria>
  <done>同一源码可在支持环境重建。</done>
</task>

<task id="18-03-03" type="checkpoint:human-verify">
  <name>Register and retire shims/probes by cohorts</name>
  <files>governance/manifests/entrypoints.yaml, integration/scripts/governance/check_shim_budget.py</files>
  <action>登记 86 shim、2 非标准入口、22 tools：target/consumer/owner/deprecated/remove_after；先迁移消费者，不直接删除。</action>
  <verify>人工批准每一退役 cohort；运行旧/新 CLI parity tests。</verify>
  <acceptance_criteria>shim 新增=0；被移除项 consumer=0 且 rollback manifest 存在。</acceptance_criteria>
  <done>兼容面只减不增且不破坏入口。</done>
</task>

