---
phase: 18
plan: "01"
title: Governance policy engine and complete inventory
wave: 1
depends_on: []
requirements: [GOV-01, GOV-02, GOV-03, GOV-07]
autonomous: true
---

# Plan 18-01: Policy Engine and Complete Inventory

**Decisions:** D-18-01, D-18-04, D-18-07.

<task id="18-01-01" type="auto">
  <name>Define governance schema and ordered path policies</name>
  <files>governance/schema/file_inventory.schema.json, governance/policies/paths.yaml, tests/test_governance_inventory.py</files>
  <action>实现 node_type=file|directory|symlink|reparse、zone/kind/owner/privacy/git/source-of-truth/lifecycle/lineage/retention/validation schema、kind×required-fields matrix 与明确优先级；private deny 优先。</action>
  <verify>python -m pytest -q tests/test_governance_inventory.py -k schema</verify>
  <acceptance_criteria>file/directory/empty/deep/excluded/reparse/case-collision fixtures exactly one policy；required metadata 空值或无理由 N/A fail closed。</acceptance_criteria>
  <done>治理规则可机器解析且字段完整。</done>
</task>

<task id="18-01-02" type="auto">
  <name>Build metadata-only full-tree scanner</name>
  <files>integration/scripts/governance/build_project_inventory.py, tests/test_governance_inventory.py</files>
  <action>扫描所有文件和目录；`.git` internal 只登记根 exclusion，其他 excluded zone 继续枚举 descendant/depth/count；默认 R3/R4 禁止 open/read，仅 filesystem metadata。</action>
  <verify>python integration/scripts/governance/build_project_inventory.py --check --private-output integration/runtime/governance/file_inventory.json</verify>
  <acceptance_criteria>file coverage=100%、directory coverage=100%、metadata completeness=100%、generated lineage completeness=100%；报告 deepest depth/excluded descendants；monkeypatch open/read 证明 R3/R4 默认不读内容。</acceptance_criteria>
  <done>11k+ 文件均有唯一策略且 private inventory 被忽略。</done>
</task>

<task id="18-01-03" type="auto">
  <name>Generate sanitized inventory summary and drift baseline</name>
  <files>integration/scripts/governance/render_governance_report.py, governance/baselines/inventory_summary.json, tests/test_governance_report.py</files>
  <action>仅输出按 zone/kind/privacy/owner/status 聚合的 tracked summary，不含 R3/R4 路径正文。</action>
  <verify>python -m pytest -q tests/test_governance_inventory.py tests/test_governance_report.py</verify>
  <acceptance_criteria>报告 secret/private-content scan=0；orphan residual 全部有 owner/deadline；可比较目录/文件覆盖、存储增长和趋势。</acceptance_criteria>
  <done>形成持续治理的机器基线。</done>
</task>
