---
phase: 18
plan: "05"
title: Reproducible dependencies, tests and CI governance gates
wave: 4
depends_on: ["18-03", "18-04"]
requirements: [GOV-08, GOV-09, GOV-10, GOV-12]
autonomous: true
---

# Plan 18-05: Reproducibility and CI Gates

**Decisions:** D-18-02, D-18-05, D-18-07, D-18-08.

<task id="18-05-01" type="auto">
  <name>Make Python and Node environments reproducible</name>
  <files>requirements.txt, requirements-dev.txt, requirements-optional.txt, constraints.txt, integration/apps/personal_data_chatgpt/package-lock.json</files>
  <action>声明 core/AI/vector/UI/dev 可选依赖，生成受审 constraints；CI 覆盖 Python 3.12/3.14 与 Node 20。</action>
  <verify>fresh environment install smoke + python -m pytest --collect-only -q + npm test</verify>
  <acceptance_criteria>隐式依赖有 extra/文档；lock drift 可检测；无联网/private fixture 默认测试。</acceptance_criteria>
  <done>环境可重复安装。</done>
</task>

<task id="18-05-02" type="auto">
  <name>Integrate governance checks into local preflight and CI</name>
  <files>integration/scripts/governance/preflight.py, .github/workflows/ci.yml, tests/test_governance_preflight.py</files>
  <action>串联 inventory/privacy/path/shim/dependency/docs/artifact/planning/architecture/secret/test gates；旧债用 baseline，只阻断新增。</action>
  <verify>python integration/scripts/governance/preflight.py --ci</verify>
  <acceptance_criteria>本地与 CI 同门；P0 无 baseline 豁免；失败非零且指出 owner/policy。</acceptance_criteria>
  <done>治理成为持续系统而非一次性文档。</done>
</task>

<task id="18-05-03" type="auto">
  <name>Build governance dashboard and trend registry</name>
  <files>integration/scripts/governance/render_governance_report.py, integration/runtime/governance/governance_history.sqlite, tests/test_governance_report.py</files>
  <action>展示分类覆盖、privacy violations、path hits、shim、docs、deps、tests、planning drift、storage/orphans；只输出脱敏聚合。</action>
  <verify>python -m pytest -q tests/test_governance_report.py</verify>
  <acceptance_criteria>每项指标可追溯 run/policy；R3/R4 内容不进入 HTML/PNG。</acceptance_criteria>
  <done>项目治理可观测并持续改进。</done>
</task>

