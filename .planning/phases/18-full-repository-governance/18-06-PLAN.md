---
phase: 18
plan: "06"
title: Controlled physical migration and governance closeout
wave: 5
depends_on: ["18-03", "18-04", "18-05"]
requirements: [GOV-01, GOV-03, GOV-04, GOV-05, GOV-06, GOV-10, GOV-11, GOV-12]
autonomous: false
---

# Plan 18-06: Controlled Migration and Closeout

**Decisions:** D-18-01, D-18-02, D-18-03, D-18-04, D-18-05, D-18-06, D-18-07, D-18-08.

<task id="18-06-01" type="auto">
  <name>Generate ordered migration manifest and shadow tree</name>
  <files>integration/scripts/governance/plan_repository_migration.py, integration/runtime/governance/migration_preview.json, tests/test_governance_migration.py</files>
  <action>根据 current→target mapping 生成 mkdir/move/shim/update-import/update-doc 操作及逆操作；默认 preview，检测 dirty overlap。</action>
  <verify>python -m pytest -q tests/test_governance_migration.py</verify>
  <acceptance_criteria>每项操作有 reason/owner/deps/precheck/postcheck/rollback；未授权删除=0。</acceptance_criteria>
  <done>迁移可审计、可分 cohort、可逆。</done>
</task>

<task id="18-06-02" type="checkpoint:human-verify">
  <name>Approve and execute migration cohorts</name>
  <files>integration/scripts/governance/apply_repository_migration.py, integration/runtime/governance/migration_preview.json, integration/runtime/governance/migration_journal.jsonl, .planning/phases/18-full-repository-governance/18-MIGRATION-UAT.md</files>
  <action>用户批准 exact manifest 后，用受控 executor 按 docs/config → source imports → shims → artifacts/archive cohort 执行；每步记录 prestate hash、dirty overlap、operation、poststate hash 和 inverse。dirty overlap 或 prestate drift fail closed。</action>
  <verify>python integration/scripts/governance/apply_repository_migration.py --manifest integration/runtime/governance/migration_preview.json --dry-run；批准后 `--apply --cohort <id>`；失败或演练用 `--rollback --journal integration/runtime/governance/migration_journal.jsonl --cohort <id>`，随后 preflight + 关键 smoke。</verify>
  <acceptance_criteria>checkpoint 只批准 manifest；executor/journal 实际执行；部分失败从 journal 逆序恢复并先验证当前 state；Phase17 相关 cohort 等 UAT 完成；现有用户改动不覆盖。</acceptance_criteria>
  <done>目标物理架构逐步落地，不做 big-bang。</done>
</task>

<task id="18-06-03" type="auto">
  <name>Final reconcile and governance verification</name>
  <files>.planning/phases/18-full-repository-governance/18-VERIFICATION.md, governance/baselines/inventory_summary.json</files>
  <action>运行全量 inventory/preflight/tests/docs/planning/storage/rollback reconcile，生成最终差异和 residual ledger。</action>
  <verify>python integration/scripts/governance/preflight.py --full; python -m pytest -q</verify>
  <acceptance_criteria>coverage=100%；unknown/conflict/private tracked=0；new path/shim=0；planning一致；所有 residual 有 owner/deadline。</acceptance_criteria>
  <done>整个仓库包括最深叶文件进入持续治理闭环。</done>
</task>
