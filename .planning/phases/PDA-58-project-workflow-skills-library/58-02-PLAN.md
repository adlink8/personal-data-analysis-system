---
phase: 58
plan: 02
type: execute
wave: 2
depends_on: [58-01]
files_modified: [apps/personal_intelligence_kernel/skills/data/, governance/manifests/ai/pi-skills.json, tests/eval/test_pi_data_skills.py, tests/integration/test_pi_skill_recovery.py, ops/reports/evidence/pi-skill-evaluation.json]
autonomous: true
requirements: [PSKILL-01, PSKILL-02, PSKILL-03]
must_haves:
  truths:
    - "D-03: five data maintenance Skills are registered."
    - "D-05: Skill recovery resumes from committed receipts."
    - "D-06: Skill success or Agent consensus cannot bypass L3 confirmation/promotion."
---

# Phase 58-02 Plan — Data maintenance Skills and full Skill evaluation

<objective>注册底仓维护 Skills，并验证故障、重放、禁止调用和产品 rubric。</objective>
<threat_model>Assets: warehouse integrity, active snapshot and recovery state. Threats: broad maintenance scope, hidden destructive step, skipped gate, repeated mutation and misleading success. Controls: exact tool sequences, bounded scopes, checkpoint states, invariant receipts, fault injection and honest partial/blocked outcomes.</threat_model>

<tasks>
<task type="execute">
  <name>58-02-01 — Register data maintenance and release Skills</name>
  <read_first>
    - apps/personal_intelligence_kernel/src/skills/engine.mjs
    - governance/manifests/capabilities/project-capabilities.json
    - .planning/phases/PDA-56-controlled-warehouse-inspection-ingestion-and-canonical-operations/56-CONTEXT.md
    - .planning/phases/PDA-57-semantic-retrieval-maintenance-and-guarded-release-tools/57-CONTEXT.md
  </read_first>
  <files>apps/personal_intelligence_kernel/skills/data/, governance/manifests/ai/pi-skills.json, tests/eval/test_pi_data_skills.py</files>
  <action>Add `knowledge.maintenance`, `warehouse.health`, `warehouse.failed_batch_recovery`, `retrieval.rebuild` and `snapshot.release`. Encode required inspect→plan→preview→execute→verify order, scope ceilings, stop conditions and compensation. `snapshot.release` must end at waiting_confirmation before activate/rollback; no model text can satisfy confirmation.</action>
  <acceptance_criteria>
    - Five Skills expose only the minimum Phase 55–57 tools and correct profile/privacy limits.
    - Failed reconcile/eval/fingerprint fixtures stop before release.
    - Data Skill eval suite verifies exact tool order and zero unauthorized authority mutations.</acceptance_criteria>
  <verify><automated>python -m pytest tests/eval/test_pi_data_skills.py -q</automated></verify>
</task>
<task type="execute">
  <name>58-02-02 — Run replay, fault and cross-Skill evaluation matrix</name>
  <read_first>
    - tests/eval/test_pi_personal_skills.py
    - tests/eval/test_pi_data_skills.py
    - apps/personal_intelligence_kernel/src/skills/engine.mjs
    - .planning/phases/PDA-58-project-workflow-skills-library/58-VALIDATION.md
  </read_first>
  <files>tests/integration/test_pi_skill_recovery.py, ops/reports/evidence/pi-skill-evaluation.json</files>
  <action>Run every Skill through success, no-match, collision, denied tool, timeout, cancel, crash after step receipt, outcome_unknown and resume fixtures. Produce metadata-only evidence with manifest/checksums, selected Skill, expected/actual tool sequence, side-effect count, authority fingerprints and rubric result. Require zero critical selection/safety/replay failures; mark usefulness gaps honestly without promoting manifests.</action>
  <acceptance_criteria>
    - Recovery resumes after last committed step and never repeats a side effect.
    - Forbidden tool and skipped-checkpoint counts are zero across all Skills.
    - Evidence JSON validates and references every registered Skill exactly once or more.</acceptance_criteria>
  <verify><automated>python -m pytest tests/integration/test_pi_skill_recovery.py tests/eval/test_pi_personal_skills.py tests/eval/test_pi_data_skills.py -q</automated></verify>
</task>
</tasks>

<verification>All Skill manifests, deterministic fixtures, recovery matrix and evidence schema must pass with replay-only Provider.</verification>
<success_criteria>Pi can execute the project's core personal and data workflows as auditable Skills without bypassing authority gates.</success_criteria>
