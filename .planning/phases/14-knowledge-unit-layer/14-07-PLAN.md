---
phase: 14-knowledge-unit-layer
plan: "07"
type: gap_closure
wave: 6
depends_on: ["14-06"]
files_modified:
  - integration/scripts/migrate_add_knowledge_unit_tables.py
  - integration/scripts/refresh_knowledge_units.py
  - integration/scripts/build_knowledge_units_prod.py
  - integration/scripts/build_canonical_knowledge_units.py
  - integration/scripts/build_knowledge_unit_vector_store.py
  - integration/scripts/evaluate_knowledge_unit_rag.py
  - integration/scripts/reconcile_knowledge_index.py
  - integration/scripts/promote_knowledge_index.py
  - integration/scripts/rollback_knowledge_checkpoint.py
  - tests/test_knowledge_incremental_refresh.py
  - tests/test_knowledge_incremental_pipeline.py
  - tests/test_knowledge_index_promotion.py
  - .planning/phases/14-knowledge-unit-layer/14-VALIDATION.md
  - .planning/phases/14-knowledge-unit-layer/14-UAT.md
  - .planning/phases/14-knowledge-unit-layer/14-VERIFICATION.md
  - integration/analysis/ai_context/knowledge_incremental_delta.json
  - integration/analysis/ai_context/knowledge_incremental_candidate.json
  - integration/analysis/ai_context/knowledge_incremental_eval.json
  - integration/analysis/ai_context/knowledge_incremental_promotion_journal.json
  - integration/analysis/ai_context/phase14_incremental_final_reconcile.json
autonomous: false
requirements: [KU-08]
user_setup: []
must_haves:
  truths:
    - "增量 refresh 比较两个可复现 source checkpoint，识别 new、modified、deleted refs，并冻结 immutable delta inventory；同一 source checksum 为真正 no-op。"
    - "每个非空 delta 创建新的 extraction run 和 knowledge_run_items；绝不 resume 不含本次 refs 的旧 validated run。"
    - "模型由 CLI/config 显式注入并写入 manifest；缺失、未授权或失败时阻断，禁止硬编码或静默 fallback。"
    - "start/resume 由持久化 ledger 和 response cache 恢复；崩溃重启不重复已成功 LLM 调用或重复发布。"
    - "canonicalization 只重算 affected subjects，并通过版本化 replacement set 保留未受影响 canonical IDs/hash。"
    - "增量结果只进入 immutable candidate；actual Chroma ID/checksum、frozen test、affected-subject eval 和删除残留 gate 全部通过后才可申请 promote。"
    - "只有 exact candidate 获人工批准且 journal promote 成功后才推进 source watermark；任一步失败时旧 active 与旧 watermark 均不变。"
    - "自动测试以 fake LLM + fake Chroma 证明真实数据流，不以命令字符串或 argparse/main smoke 代替端到端证据。"
  artifacts:
    - "refresh_knowledge_units.py creates immutable delta inventory/run ledgers and orchestrates a resumable incremental build"
    - "test_knowledge_incremental_pipeline.py proves new/modified/deleted/no-op/crash-resume/cache/failure-isolation behavior end to end"
    - "knowledge_incremental_candidate.json binds source-before/source-after, delta inventory, extraction, canonical and actual index checksums"
    - "phase14_incremental_final_reconcile.json proves promoted lineage, zero residue, watermark advancement and reversible rollback"
  key_links:
    - "Phase 13.5 canonical message content hashes -> ordered delta inventory -> fresh extraction run items"
    - "fresh extraction run -> affected-subject canonical replacement set -> immutable candidate collection"
    - "actual candidate IDs/checksum + frozen/affected eval -> human checkpoint -> journal promote"
    - "journal commit -> watermark advance; abort/rollback -> old active and old watermark"
threat_model:
  - id: T-14-09
    threat: "旧 validated run 不含新增 refs，却被 resume 后错误声称完成增量刷新"
    mitigation: "delta inventory 与 fresh run FK/hash 绑定；resume 验证 inventory/source/model/config lineage"
  - id: T-14-10
    threat: "部分增量写入、错误 merge 或删除残留污染 active knowledge surface"
    mitigation: "affected-subject replacement set、immutable candidate、actual-ID reconcile、zero-residue gate、journal promote"
  - id: T-14-11
    threat: "模型漂移或缓存误用产生不可复现知识"
    mitigation: "CLI/config required model、manifest hashes、cache hit 重跑 schema/evidence/privacy gate、禁止 fallback"
  - id: T-14-12
    threat: "提前推进 watermark 导致失败 delta 永久漏处理"
    mitigation: "watermark 仅在 post-promote reconcile 成功后事务推进，rollback 恢复旧 watermark"
---

<objective>
关闭 Phase 14 增量刷新验收缺口：把当前只检测差异并输出命令的脚手架，改成可冻结、可恢复、可评估、可回滚的真实 incremental knowledge pipeline。

Purpose: 证明新增、修改和删除的 canonical evidence 能安全进入下一版 knowledge index，同时任一失败都不影响当前 2,393-item active checkpoint。
Output: delta inventory/run ledger、affected-subject canonical build、immutable candidate/eval、人工批准后的 journal promote、水位推进与完整端到端验收证据。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-knowledge-unit-layer/14-CONTEXT.md
@.planning/phases/14-knowledge-unit-layer/14-AI-SPEC.md
@.planning/phases/14-knowledge-unit-layer/14-RESEARCH.md
@.planning/phases/14-knowledge-unit-layer/14-PATTERNS.md
@.planning/phases/14-knowledge-unit-layer/14-VALIDATION.md
@.planning/phases/14-knowledge-unit-layer/14-06-SUMMARY.md
@integration/scripts/refresh_knowledge_units.py
@integration/scripts/build_knowledge_units_prod.py
@integration/scripts/build_canonical_knowledge_units.py
@integration/scripts/build_knowledge_unit_vector_store.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 先建立真实 incremental E2E 合同测试</name>
  <files>tests/test_knowledge_incremental_refresh.py, tests/test_knowledge_incremental_pipeline.py, .planning/phases/14-knowledge-unit-layer/14-VALIDATION.md</files>
  <read_first>tests/test_knowledge_incremental_refresh.py, tests/test_knowledge_unit_backfill.py, tests/test_canonical_knowledge_units.py, tests/test_knowledge_unit_eval_dataset.py, tests/test_knowledge_unit_gate.py, tests/test_knowledge_index_promotion.py, integration/scripts/refresh_knowledge_units.py, .planning/phases/14-knowledge-unit-layer/14-VALIDATION.md</read_first>
  <!-- This task creates tests/test_knowledge_incremental_pipeline.py before running verification. -->
  <action>创建可导入、可 collect 的 `tests/test_knowledge_incremental_pipeline.py` shared fixtures/harness：临时 canonical/unified SQLite builders、稳定 ref/content/eligibility/content-hash factory、可计数 fake LLM transport、可枚举 actual IDs 的 fake Chroma、active/pointer/watermark snapshot helper、failure injection 和 fixture contract table。这里只实现 harness 与 `test_incremental_harness_smoke`，不提前写要求尚未实现领域能力通过的完整 E2E；delta ledger、provider adapter、resume/canonical、candidate/reconcile 和 promotion 的 red→green 用例分别在对应实现任务中先写失败测试再实现。更新 14-VALIDATION.md 的 test ownership 表。</action>
  <verify>python -m pytest --collect-only -q tests/test_knowledge_incremental_pipeline.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; python -m pytest -q tests/test_knowledge_incremental_pipeline.py -k incremental_harness_smoke</verify>
  <acceptance_criteria>
    - 新测试模块可导入、collect，shared fixture smoke exit 0，不要求尚未实现的完整 E2E 在 Task 1 通过。
    - fixture 可观测 LLM call count、cache hits、DB lineage、actual Chroma IDs、active pointer 和 watermark，并支持后续任务故障注入。
    - contract table 明确每组 red→green 用例的 owner task 与最终 Task 8 全量执行责任。
    - 14-VALIDATION.md 明确 runtime 重新计算所得 exact eligible delta 只允许在独立人工 checkpoint 后付费执行，不采用历史估计数量。
  </acceptance_criteria>
  <done>共享 incremental harness 可稳定收集和运行 smoke，为后续同任务 red→green 实现提供基础。</done>
</task>

<task type="auto">
  <name>Task 2: 冻结 immutable delta inventory 并创建 fresh extraction run</name>
  <files>integration/scripts/migrate_add_knowledge_unit_tables.py, integration/scripts/refresh_knowledge_units.py, integration/scripts/build_knowledge_units_prod.py, tests/test_knowledge_incremental_refresh.py, tests/test_knowledge_incremental_pipeline.py</files>
  <!-- Red→green ownership: first add failing delta-ledger tests for new/modified/deleted/no-op, generated_at, ordered hash, fresh run/items and idempotent preflight; then implement until this task verify is fully green. -->
  <read_first>integration/scripts/migrate_add_knowledge_unit_tables.py, integration/scripts/build_knowledge_inventory.py, integration/scripts/refresh_knowledge_units.py, integration/scripts/build_knowledge_units_prod.py, integration/scripts/knowledge_unit_pipeline.py</read_first>
  <action>幂等迁移增量 lineage 所需字段/表：source_before_checksum、source_after_checksum、ordered_dataset_hash、ref change_type/content_hash_before/content_hash_after，以及只在发布后推进的 committed watermark；沿用现有 `knowledge_inventory.generated_at`，移除错误的 `created_at` 查询。复用全量 inventory 的 eligibility、清洗、去重与稳定排序合同，对完整 ref 集合比较 content hash，识别 new/modified/deleted（不得只比较 ref 存在性），生成内容寻址、不可修改的 delta inventory 和脱敏 artifact。空 delta 返回 no-op 且不创建 run。非空 delta 调用 production runner 的 fresh start 路径创建新 extraction manifest 和逐项 `knowledge_run_items`; run_id 绑定 delta inventory、source before/after、ordered dataset hash、prompt/schema/model/config hashes。禁止查找或 resume 旧 validated run。model 必须由 `--model` 或明确配置提供，无值/不可用即 fail closed；移除 `gemini-3.5-flash` 默认值和 refresh 中的硬编码，实际 model/config 写 manifest 且禁止 fallback。</action>
  <verify>python -m pytest -q tests/test_knowledge_incremental_refresh.py tests/test_knowledge_incremental_pipeline.py tests/test_knowledge_unit_backfill.py</verify>
  <acceptance_criteria>
    - SQL 使用真实 `generated_at` schema，迁移可重跑且历史 inventory/run 不被改写。
    - 相同 source before/after 与 ordered delta 产生相同 inventory/dataset hash；任何 content 修改都会改变 hash。
    - 每个非空 delta 只创建绑定本次 inventory 的 fresh run/items；旧 validated run 的 item set 不变且从不被 resume。
    - model 缺失、静默 fallback 或 manifest model/config/hash 不一致均非零阻断。
  </acceptance_criteria>
  <done>增量输入与 fresh extraction run 形成不可变、可审计且模型显式的 lineage。</done>
</task>

<task type="auto">
  <name>Task 2B: 实现 provider-aware LLM adapter 与模型合同</name>
  <files>integration/scripts/build_knowledge_units_prod.py, integration/scripts/refresh_knowledge_units.py, tests/test_knowledge_unit_backfill.py, tests/test_knowledge_incremental_pipeline.py</files>
  <!-- Red→green ownership: first add failing provider/endpoint/auth/model/cache-identity and no-fallback tests, then implement the adapter until this task verify is fully green. -->
  <read_first>integration/scripts/build_knowledge_units_prod.py, .planning/phases/14-knowledge-unit-layer/14-AI-SPEC.md, tests/test_knowledge_unit_backfill.py</read_first>
  <action>实现 provider-aware LLM adapter，要求 provider、endpoint、auth mode、model 与 generation config 由 CLI/config 显式注入；全部进入 run manifest、run identity 和 response cache key。建立 provider/model allowlist 和 endpoint validator：`gpt-5.6-luna` 绝不能发往 Vertex Google endpoint，provider/model 不匹配、缺失 auth config 或 silent fallback 一律 fail closed。若生产仍选择 Vertex，后续 immutable preflight 必须展示实际 provider/endpoint/model，并由付费执行 checkpoint 显式批准；不得暗中偏离 AI-SPEC。用 fake transports 证明各 provider 只收到匹配模型且 fallback call count=0。</action>
  <verify>python -m pytest -q tests/test_knowledge_unit_backfill.py tests/test_knowledge_incremental_pipeline.py</verify>
  <acceptance_criteria>
    - provider/endpoint/auth/model/config 均写入 manifest、config hash 与 cache key，任一变化产生不同 run/cache identity。
    - Luna→Vertex、Vertex model→非 Vertex endpoint、缺配置和 fallback 尝试均非零阻断且无网络调用。
    - production 实际 provider/model 只能来自后续 exact preflight 与人工批准。
  </acceptance_criteria>
  <done>LLM 路由与模型合同显式、可审计，不会把 Luna ID 误发 Vertex 或静默换模。</done>
</task>

<task type="auto">
  <name>Task 3: 实现可恢复抽取与 candidate-scoped canonical replacement</name>
  <files>integration/scripts/refresh_knowledge_units.py, integration/scripts/build_knowledge_units_prod.py, integration/scripts/build_canonical_knowledge_units.py, tests/test_knowledge_incremental_pipeline.py</files>
  <!-- Red→green ownership: first add failing crash/resume/cache-revalidation and candidate-scoped affected-canonical isolation tests, then implement until this task verify is fully green. -->
  <!-- Candidate isolation invariant: this task writes only an immutable candidate canonical build/replacement set. It must not update production canonical current or its pointer. -->
  <read_first>integration/scripts/build_knowledge_units_prod.py, integration/scripts/build_knowledge_units.py, integration/scripts/knowledge_unit_pipeline.py, integration/scripts/build_canonical_knowledge_units.py, tests/test_canonical_knowledge_units.py</read_first>
  <action>把 refresh 编排为领域 API：`prepare` 只写 delta/run ledger，`execute RUN_ID` 处理 pending/retryable items，`resume RUN_ID` 校验其 delta inventory/source/model/config lineage 后恢复；start 与 resume 互斥。每个 item 的 validated units、evidence links、cache metadata 和 terminal state 同事务提交；cache hit 必须重新执行当前 Pydantic/evidence/privacy gate。根据新/修改抽取结果及删除 refs 求 affected subjects；canonical builder 为每个 affected subject 同时加载现有 current canonical/member units 与本次 delta units，生成 versioned replacement set，显式 supersede/deprecate 旧成员；未受影响 canonical IDs、rows 与 hash 必须字节级不变。canonical publish 失败时整批 replacement 回滚，不改变 current surface。</action>
  <verify>python -m pytest -q tests/test_knowledge_incremental_pipeline.py tests/test_knowledge_unit_backfill.py tests/test_canonical_knowledge_units.py</verify>
  <acceptance_criteria>
    - crash/resume 的最终 units/dataset hash 与 uninterrupted run 相同，已成功 item 的 fake LLM call count 不增加。
    - modified ref 产生可追溯 supersede；deleted/excluded evidence 在 candidate-scoped canonical build 中 membership=0，同时 production canonical current hash 保持不变。
    - affected subjects 使用 old+delta 全量重并，未受影响 canonical ID-set/hash 完全不变。
    - 任一 schema/evidence/privacy/canonical failure 都停留 staging/blocked，旧 active 不变。
    - canonical build 成功而 index/eval 失败时，production canonical current、active index 与 pointer 三者 hash 均不变；三者只能由 promotion journal 联合切换。
  </acceptance_criteria>
  <done>fresh delta run 可安全恢复，并只替换真正受影响的 canonical subjects。</done>
</task>

<task type="auto">
  <name>Task 4: 构建 immutable candidate 并执行真实 reconcile/eval gate</name>
  <files>integration/scripts/refresh_knowledge_units.py, integration/scripts/build_knowledge_unit_vector_store.py, integration/scripts/reconcile_knowledge_index.py, integration/scripts/evaluate_knowledge_unit_rag.py, tests/test_knowledge_incremental_pipeline.py, tests/test_knowledge_index_promotion.py, integration/analysis/ai_context/knowledge_incremental_candidate.json, integration/analysis/ai_context/knowledge_incremental_eval.json</files>
  <!-- Red→green ownership: first add failing actual-ID/checksum, six-residue, affected-eval and old-active isolation tests, then implement candidate/reconcile/eval until this task verify is fully green. -->
  <read_first>integration/scripts/build_knowledge_unit_vector_store.py, integration/scripts/reconcile_knowledge_index.py, integration/scripts/evaluate_knowledge_unit_rag.py, integration/scripts/promote_knowledge_index.py, tests/test_knowledge_index_promotion.py</read_first>
  <!-- Extend ReconcileReport and its JSON schema with missing_count, orphan_count, duplicate_count, deleted_residue_count, deprecated_residue_count, excluded_residue_count, and actual_id_checksum; all six residue counts must be zero. -->
  <action>从 Task 3 的 versioned canonical replacement checkpoint 构建全量 immutable candidate collection，名称/hash 绑定 source before/after、delta inventory、extraction run、canonical build 与 expected ordered IDs；builder 接收显式 canonical build/candidate name，拒绝删除或复用 active/existing collection。写入后必须从 fake/real Chroma 分页读取 actual IDs 和 metadata，计算 actual ordered ID checksum；禁止再用输入 IDs 与自身比较。reconcile 要求 missing/orphan/duplicate/deleted/deprecated/excluded residue 全为 0。evaluator 接收 exact `--candidate`，跑 frozen hybrid regression、privacy/lifecycle scan 和 affected-subject new/modified/deleted queries，并输出机器可读非零 gate；不得自动选择“最新 collection”。任何 gate failure 保留 candidate 非 active并保持旧 pointer/watermark。</action>
  <verify>python -m pytest -q tests/test_knowledge_incremental_pipeline.py tests/test_knowledge_index_promotion.py tests/test_knowledge_unit_eval_dataset.py tests/test_knowledge_unit_gate.py</verify>
  <acceptance_criteria>
    - candidate artifact 的 expected/actual ID count 与 checksum 一致，六类 residue 全为 0。
    - frozen Recall@5/MRR@5 不低于当前批准阈值，affected-subject cases 全部命中正确版本，secret/deprecated hit=0。
    - candidate/eval artifacts 精确绑定同一 source/delta/extraction/canonical/index lineage。
    - fake Chroma E2E 明确证明 new ref 对应 unit/canonical ID 实际存在于 candidate，而非只生成命令或 manifest。
  </acceptance_criteria>
  <done>增量 candidate 通过 actual-ID/checksum、frozen 与 affected-subject gate，仍未改变 active。</done>
</task>

<task type="auto">
  <name>Task 5A: 生成 immutable production delta preflight</name>
  <files>integration/scripts/refresh_knowledge_units.py, integration/analysis/ai_context/knowledge_incremental_delta.json</files>
  <read_first>integration/scripts/refresh_knowledge_units.py, integration/scripts/build_knowledge_units_prod.py, .planning/phases/14-knowledge-unit-layer/14-VALIDATION.md</read_first>
  <action>在 fake E2E 与定向测试通过后执行非付费 production preflight，按 runtime source 重新计算 exact eligible new/modified/deleted delta，不采用任何历史估计数量。该 preflight 不是纯只读：只允许幂等写入 metadata staging（immutable delta inventory、fresh extraction run manifest、knowledge_run_items）和脱敏 artifact；重复执行相同 source/provider/model/config 必须复用相同 identity，row diff=0。用真实可执行 CLI `python integration/scripts/refresh_knowledge_units.py --prepare --provider PROVIDER --endpoint ENDPOINT --auth-mode AUTH --model MODEL --artifact integration/analysis/ai_context/knowledge_incremental_delta.json` 生成 artifact。artifact 必须包含 schema_version、artifact_hash、delta_inventory_id、fresh_run_id、source_before_checksum、source_after_checksum、ordered_dataset_hash、eligible counts、provider/endpoint/auth_mode/model/config_hash、calls/tokens/cost/time/cache 估计、active checkpoint、watermark before/after、rollback target，以及显式 `active_changed=false`、`watermark_changed=false`、`production_llm_calls=0`、`candidate_writes=0`。prepare 禁止 LLM 调用、Chroma 写、candidate canonical/current canonical 写、active index/version/pointer 写和 watermark 写。</action>
  <verify>$d = Get-Content -LiteralPath 'integration/analysis/ai_context/knowledge_incremental_delta.json' -Raw | ConvertFrom-Json; $required=@('artifact_hash','delta_inventory_id','fresh_run_id','source_before_checksum','source_after_checksum','ordered_dataset_hash','provider','endpoint','auth_mode','model','config_hash','active_changed','watermark_changed','production_llm_calls','chroma_writes','candidate_canonical_writes','canonical_current_writes','active_index_writes','pointer_writes','watermark_writes','candidate_writes'); foreach($n in $required){if(-not $d.PSObject.Properties.Name.Contains($n)){throw "missing $n"}}; $zero=@('production_llm_calls','chroma_writes','candidate_canonical_writes','canonical_current_writes','active_index_writes','pointer_writes','watermark_writes','candidate_writes'); foreach($n in $zero){if([int]$d.$n -ne 0){throw "$n must be zero"}}; if($d.active_changed -ne $false -or $d.watermark_changed -ne $false){throw 'preflight mutated production state'}</verify>
  <acceptance_criteria>
    - runtime exact eligible delta 与 artifact hash 可重复，数量不写死且无原始正文。
    - provider/model/cost/cache/rollback 与 active/watermark before/after 齐全。
    - 所有安全字段真实存在并为 false/0；缺字段不能以 PowerShell null 假通过。
    - 重复相同 preflight 的 delta inventory/fresh run/run items identity 相同且 metadata row diff=0；Chroma/canonical-current/active/pointer/watermark writes=0。
  </acceptance_criteria>
  <done>可供人审的 immutable、非付费 production delta artifact 已生成，production state 零变化。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5B: 审核 exact production delta 与付费执行授权</name>
  <files>integration/analysis/ai_context/knowledge_incremental_delta.json</files>
  <read_first>integration/analysis/ai_context/knowledge_incremental_delta.json, .planning/phases/14-knowledge-unit-layer/14-VALIDATION.md</read_first>
  <action>只审核 Task 5A 已冻结的 artifact，不在 checkpoint 内运行 prepare 或修改文件。展示 exact artifact/inventory/dataset hash、runtime eligible new/modified/deleted 数、provider/endpoint/auth/model、预计 calls/tokens/cost/time/cache hits、旧 active/watermark 与 rollback target。source 或配置漂移须回 Task 5A 重建新 artifact；用户明确批准前不调用真实 LLM。</action>
  <verify>$d = Get-Content -LiteralPath 'integration/analysis/ai_context/knowledge_incremental_delta.json' -Raw | ConvertFrom-Json; if(-not $d.artifact_hash -or $d.active_changed -ne $false -or $d.watermark_changed -ne $false -or [int]$d.production_llm_calls -ne 0 -or [int]$d.candidate_writes -ne 0){throw 'unsafe artifact'}</verify>
  <acceptance_criteria>
    - 用户批准 exact artifact hash、provider/endpoint/model 与预算，不授权任何漂移后的 run。
    - checkpoint 前 production LLM/candidate/active/watermark 变化均为 0。
  </acceptance_criteria>
  <instructions>核对 exact delta、provider/model、预算和旧 checkpoint；确认后回复“批准执行此 incremental delta”。</instructions>
  <resume-signal>Type “批准执行此 incremental delta” to authorize only this artifact hash/provider/model, or describe a blocker.</resume-signal>
  <done>exact production delta artifact 的付费抽取获得一次性明确授权。</done>
</task>

<task type="auto">
  <name>Task 6: 执行获批 delta 并停在 promotion review 边界</name>
  <files>integration/scripts/refresh_knowledge_units.py, integration/analysis/ai_context/knowledge_incremental_delta.json, integration/analysis/ai_context/knowledge_incremental_candidate.json, integration/analysis/ai_context/knowledge_incremental_eval.json</files>
  <read_first>integration/analysis/ai_context/knowledge_incremental_delta.json, integration/scripts/refresh_knowledge_units.py, integration/scripts/build_knowledge_units_prod.py, integration/scripts/build_canonical_knowledge_units.py, integration/scripts/build_knowledge_unit_vector_store.py, integration/scripts/evaluate_knowledge_unit_rag.py</read_first>
  <action>仅对 Task 5B 获批且 artifact/source/delta/provider/endpoint/model/config hash 未漂移的 fresh run 执行 extraction；中断时只 resume 同一 run。完成后执行 candidate-scoped affected-subject canonical replacement、immutable candidate build、actual-ID reconcile、frozen hybrid 与 affected-subject eval。每步持久化 lineage 和状态；任何 terminal extraction、canonical、index、reconcile 或 eval failure 都将 run/candidate 标 blocked，保留 production canonical current、旧 active 与旧 watermark，并停止。通过后输出 exact candidate/eval artifacts，但不得在本 task promote。</action>
  <verify>python -m pytest -q tests/test_knowledge_incremental_refresh.py tests/test_knowledge_incremental_pipeline.py tests/test_knowledge_index_promotion.py tests/test_knowledge_search_contracts.py</verify>
  <acceptance_criteria>
    - processed+abstained+terminal_failed 等于获批 delta item count，terminal failure 触发配置阈值时 gate FAIL。
    - candidate actual-ID checksum 和 lineage 与获批 delta、fresh run、canonical build 完全一致，residue=0。
    - frozen/affected/privacy gates PASS，默认 active 和 committed watermark 仍保持执行前值。
  </acceptance_criteria>
  <done>获批 production delta 已形成通过 gate 的独立 candidate，等待单独 promotion 授权。</done>
</task>

<task type="auto">
  <name>Task 7A: 实现并验证联合 promotion preflight 与 prepare/commit journal</name>
  <files>integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py, integration/scripts/reconcile_knowledge_index.py, tests/test_knowledge_index_promotion.py, tests/test_knowledge_incremental_pipeline.py</files>
  <!-- Red→green ownership: first add failing preflight, durable prepare/commit, exact journal-id, atomic canonical/index/pointer and rollback fault tests, then implement until this task verify is fully green. -->
  <read_first>integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py, integration/scripts/reconcile_knowledge_index.py, tests/test_knowledge_index_promotion.py, integration/analysis/ai_context/knowledge_incremental_candidate.json, integration/analysis/ai_context/knowledge_incremental_eval.json</read_first>
  <action>在人工 promotion checkpoint 前实现真实可执行 CLI：`python integration/scripts/promote_knowledge_index.py --candidate COLLECTION --expected-checksum SHA --preflight --candidate-artifact PATH --eval-artifact PATH` 只读验证 candidate status、actual checksum、六类 residue、eval gate、source/delta/extraction/canonical lineage、current canonical/index/pointer hashes 与 journal clean；`--prepare` 写 durable intent 但不切生产状态；`--commit JOURNAL_ID` 在单 writer/transaction protocol 中联合切换 production canonical checkpoint、DB index version 和 active pointer，post-reconcile 成功后才标 committed；rollback CLI 用 journal 恢复三者与 watermark。不得沿用当前直接 mutation 的 `--promote` 路径绕过 preflight。用 temp DB/fake Chroma 测试 preflight 无 diff、prepare 可恢复、commit 三者原子可见、每个故障注入三者 hash 不变或完整回滚。</action>
  <verify>python -m pytest -q tests/test_knowledge_index_promotion.py tests/test_knowledge_incremental_pipeline.py</verify>
  <acceptance_criteria>
    - checkpoint 将调用的 `--candidate/--expected-checksum/--preflight` CLI 已由实现和测试真实提供，命令可执行。
    - prepare/commit journal 绑定 exact canonical build/index checksum/pointer/source watermark，重复 prepare/commit 幂等。
    - canonical/index/pointer 任一切换或 post-check 失败时三者 production hash 均恢复旧值，watermark 不推进。
  </acceptance_criteria>
  <done>人工批准前，联合 promotion preflight 和可恢复 journal 已实现并通过故障注入测试。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7B: 单独批准 exact incremental candidate promotion</name>
  <files>integration/analysis/ai_context/knowledge_incremental_candidate.json, integration/analysis/ai_context/knowledge_incremental_eval.json</files>
  <read_first>integration/analysis/ai_context/knowledge_incremental_candidate.json, integration/analysis/ai_context/knowledge_incremental_eval.json, integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py</read_first>
  <action>展示 exact candidate collection/build/actual checksum、frozen/affected metrics、zero-residue、当前与目标 active lineage、source watermark before/after、journal 状态及联合 rollback target。付费抽取授权不等于 promotion 授权。任何 hash mismatch、gate failure、source drift 或 unresolved journal 阻断；用户只批准本次 exact candidate checksum。</action>
  <verify>$c = Get-Content -LiteralPath 'integration/analysis/ai_context/knowledge_incremental_candidate.json' -Raw | ConvertFrom-Json; $e = Get-Content -LiteralPath 'integration/analysis/ai_context/knowledge_incremental_eval.json' -Raw | ConvertFrom-Json; if ($e.gate.status -ne 'PASS' -or $c.actual_id_checksum -ne $e.candidate_checksum -or $c.active_changed -or $c.watermark_changed) { throw 'candidate not safe to promote' }; python integration/scripts/promote_knowledge_index.py --candidate $c.collection_name --expected-checksum $c.actual_id_checksum --preflight --candidate-artifact integration/analysis/ai_context/knowledge_incremental_candidate.json --eval-artifact integration/analysis/ai_context/knowledge_incremental_eval.json</verify>
  <acceptance_criteria>
    - exact actual checksum、lineage、reconcile、frozen/affected/privacy gate 全部 PASS。
    - 旧 active/旧 watermark、目标 active/新 watermark 与 rollback journal 操作均明确。
    - 用户明确批准 exact collection/checksum，不授权未来自动 promotion。
  </acceptance_criteria>
  <instructions>核对 candidate/eval/reconcile、旧新 checkpoint 和 rollback；确认后回复“批准 promote 此 incremental candidate”。</instructions>
  <resume-signal>Type “批准 promote 此 incremental candidate” to authorize only this collection/checksum, or describe a blocker.</resume-signal>
  <done>exact incremental candidate 获得独立 journal promotion 授权。</done>
</task>

<task type="auto">
  <name>Task 8: Journal promote、推进 watermark、联合 rollback 与最终验收</name>
  <!-- Execute only the exact checksum approved in Task 7B, through the CLI and journal implemented/tested in Task 7A. -->
  <files>integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py, integration/scripts/reconcile_knowledge_index.py, tests/test_knowledge_incremental_pipeline.py, tests/test_knowledge_index_promotion.py, .planning/phases/14-knowledge-unit-layer/14-UAT.md, .planning/phases/14-knowledge-unit-layer/14-VERIFICATION.md, integration/analysis/ai_context/knowledge_incremental_promotion_journal.json, integration/analysis/ai_context/phase14_incremental_final_reconcile.json</files>
  <read_first>integration/analysis/ai_context/knowledge_incremental_candidate.json, integration/analysis/ai_context/knowledge_incremental_eval.json, integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py, integration/scripts/reconcile_knowledge_index.py, .planning/phases/14-knowledge-unit-layer/14-VALIDATION.md</read_first>
  <action>仅对 Task 7B 批准的 exact checksum 执行 Task 7A 已验证的 journal CLI。使用可复制链：`python integration/scripts/promote_knowledge_index.py --candidate COLLECTION --expected-checksum SHA --prepare --candidate-artifact integration/analysis/ai_context/knowledge_incremental_candidate.json --eval-artifact integration/analysis/ai_context/knowledge_incremental_eval.json --journal-artifact integration/analysis/ai_context/knowledge_incremental_promotion_journal.json`；随后 `$j = Get-Content -LiteralPath 'integration/analysis/ai_context/knowledge_incremental_promotion_journal.json' -Raw | ConvertFrom-Json; if (-not $j.journal_id -or $j.status -ne 'prepared') { throw 'prepared journal missing' }; python integration/scripts/promote_knowledge_index.py --commit $j.journal_id --journal-artifact integration/analysis/ai_context/knowledge_incremental_promotion_journal.json`。JOURNAL_ID 只能从这份 exact artifact 解析，禁止使用 latest、stdout 或猜测。post-promote 从实际 Chroma、SQLite index versions、candidate-scoped canonical checkpoint 与 pointer 做联合 reconcile；成功后才推进 committed watermark，失败则按同一 journal 恢复 canonical/index/pointer/manifest/watermark。随后完成 rollback→reconcile→restore→reconcile 演练、默认 ku1+raw4 smoke、定向与全量 pytest。Task 8 必须创建 `.planning/phases/14-knowledge-unit-layer/14-UAT.md` 与 `14-VERIFICATION.md`，记录实际命令、测试数、candidate/active/watermark hashes、未验证项和风险；完成状态只由这些实际证据决定，任何失败/缺证据均不得标记 Phase 14 complete。</action>
  <verify>python -m pytest -q tests/test_knowledge_incremental_refresh.py tests/test_knowledge_incremental_pipeline.py tests/test_knowledge_unit_backfill.py tests/test_canonical_knowledge_units.py tests/test_knowledge_unit_eval_dataset.py tests/test_knowledge_unit_gate.py tests/test_knowledge_index_promotion.py tests/test_knowledge_search_contracts.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; python -m pytest -q</verify>
  <acceptance_criteria>
    - post-promote active actual-ID checksum 等于 exact candidate，默认路由命中新 ref，deleted/deprecated/excluded residue=0。
    - committed watermark 仅在 successful journal commit + post-promote reconcile 后推进；rollback 恢复旧 watermark，restore 恢复新 watermark。
    - rollback/restore 两侧均用 stored expected ID-set/checksum 与 actual collection 精确验证，不以数量放宽 missing。
    - 定向与全量测试全部通过；14-UAT/14-VERIFICATION 如实记录证据，任何未通过项阻止 Phase 14 complete。
  </acceptance_criteria>
  <done>真实增量链路从 source delta 到 active/watermark 完整闭环，并具有可重复 E2E、失败隔离和联合回滚证据。</done>
</task>

</tasks>

<verification>
- [ ] `python -m pytest -q tests/test_knowledge_incremental_refresh.py tests/test_knowledge_incremental_pipeline.py tests/test_knowledge_unit_backfill.py tests/test_canonical_knowledge_units.py tests/test_knowledge_unit_eval_dataset.py tests/test_knowledge_unit_gate.py tests/test_knowledge_index_promotion.py tests/test_knowledge_search_contracts.py` 全部通过。
- [ ] `python -m pytest -q` 全量回归通过，且 14-UAT.md / 14-VERIFICATION.md 记录实际通过数与任何 pre-existing failure 的证据归属。
- [ ] Fake LLM + fake Chroma E2E 证明 new ref → unit → canonical → actual candidate ID，并覆盖 modified/deleted/no-op/crash-resume/cache/failure isolation。
- [ ] Production delta 的付费执行与 exact candidate promotion 分别获得独立人工批准。
- [ ] actual-ID/checksum reconcile、frozen+affected eval、zero residue、journal promote、watermark advance 与 rollback/restore hashes 完整一致。
</verification>

<success_criteria>
- KU-08 的“增量刷新”由真实数据流证明，不再只是差异检测或命令生成。
- new/modified/deleted evidence 使用 immutable delta inventory 和 fresh resumable run；模型与完整 lineage 可审计。
- affected-subject canonical replacement 保留未受影响 IDs，candidate 失败不影响旧 active。
- 只有 exact candidate 通过 reconcile/eval 并获批后才 journal promote，之后才推进 watermark；删除/过期残留为 0。
- Phase 14 最终验收和全量回归均有可复现证据，才能重新标记 complete。
</success_criteria>

<output>
After completion, create `.planning/phases/14-knowledge-unit-layer/14-07-SUMMARY.md`
</output>
