---
phase: 14-knowledge-unit-layer
plan: "02"
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - integration/scripts/migrate_add_knowledge_unit_tables.py
  - integration/scripts/knowledge_unit_pipeline.py
  - integration/scripts/build_knowledge_units.py
  - integration/scripts/evaluate_knowledge_unit_extraction.py
  - tests/test_knowledge_unit_backfill.py
  - tests/test_knowledge_unit_retry_cache.py
  - tests/test_knowledge_unit_checkpoint.py
  - integration/analysis/ai_context/knowledge_unit_inventory.json
  - integration/analysis/ai_context/knowledge_unit_inventory.md
autonomous: true
requirements: [KU-01, KU-02, KU-03, KU-05]
user_setup: []
must_haves:
  truths:
    - "抽取输入由冻结 inventory 的完整 hash 和 source checkpoint 定义，行数来自实际清单而不是硬编码 5,485。"
    - "同一 run 中断后按 item ledger 恢复；成功项不会重复调用、重复写入或被 begin_staging 清空。"
    - "terminal API error、未完成 item、零产出、低于预注册 yield、evidence/privacy critical error 中任一出现时 gate 必须失败。"
    - "模型只能由 CLI/config 注入并写入 manifest；禁止硬编码生产模型或静默 fallback。"
  artifacts:
    - "integration/scripts/build_knowledge_units.py exposes inventory/start/resume/model/batch controls"
    - "integration/scripts/evaluate_knowledge_unit_extraction.py emits a machine-readable fail-closed gate"
    - "tests/test_knowledge_unit_backfill.py covers drift, interruption, idempotency, and active isolation"
    - "tests/test_knowledge_unit_retry_cache.py covers classified retry and exact cache identity"
  key_links:
    - "frozen inventory -> knowledge_build_runs/source identity -> per-item ledger"
    - "cache hit -> current Pydantic/evidence/privacy validation -> transactional staging write"
    - "strict extraction report -> validated extraction checkpoint; never active index"
threat_model:
  - id: T-14-01
    threat: "输入漂移或错误的 5,485 假设造成漏抽、重复抽取或无法复现"
    mitigation: "冻结有序 inventory，记录完整 dataset hash/source checksum/count/time range，resume 前漂移即阻断"
  - id: T-14-02
    threat: "中断、限流或缓存混用造成重复计费和不一致 staging"
    mitigation: "durable item state machine、lease recovery、完整 cache key、单 SQLite writer"
  - id: T-14-03
    threat: "全 API 失败或空结果仍被旧 gate 判为 PASS"
    mitigation: "完成性、terminal error、nonzero/min-yield、privacy/evidence 的失败封闭 gate"
---

<objective>
交付 production backfill engine 的安全底座：先冻结真实输入清单并解释 5,485/2,237 差异，再实现可恢复、可缓存、可重试、失败封闭的 staging 抽取。

Purpose: 让后续 pilot 和全量付费抽取可审计、可暂停、可重跑，且任何失败都不影响现有 active checkpoint。
Output: inventory/source identity、run item/cache schema、start/resume engine、严格 extraction gate 与故障注入测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-knowledge-unit-layer/14-01-SUMMARY.md
@.planning/phases/14-knowledge-unit-layer/14-CONTEXT.md
@.planning/phases/14-knowledge-unit-layer/14-AI-SPEC.md
@.planning/phases/14-knowledge-unit-layer/14-RESEARCH.md
@.planning/phases/14-knowledge-unit-layer/14-PATTERNS.md
@.planning/phases/14-knowledge-unit-layer/14-VALIDATION.md
@integration/scripts/migrate_add_knowledge_unit_tables.py
@integration/scripts/knowledge_unit_pipeline.py
@integration/scripts/build_knowledge_units.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 冻结 production inventory 与持久化 work-item 合同</name>
  <files>integration/scripts/migrate_add_knowledge_unit_tables.py, integration/scripts/knowledge_unit_pipeline.py, integration/scripts/build_knowledge_units.py, tests/test_knowledge_unit_backfill.py, integration/analysis/ai_context/knowledge_unit_inventory.json, integration/analysis/ai_context/knowledge_unit_inventory.md</files>
  <read_first>integration/scripts/migrate_add_knowledge_unit_tables.py, integration/scripts/knowledge_unit_pipeline.py, integration/scripts/build_knowledge_units.py, tests/test_knowledge_unit_contracts.py, tests/test_knowledge_unit_checkpoint.py</read_first>
  <action>先写失败测试，再以幂等 migration 增加 frozen inventory、run items、response cache 与 gate decision 所需表/索引。inventory 每项保存 evidence_ref、content_hash、session/source/agent/time bucket、eligibility、position；source identity 保存 canonical DB checksum/schema/count/time range。run_id 由完整 inventory hash、source checkpoint、prompt/schema/model/generation config 派生。提供只读 inventory preflight/report，明确输出 coarse eligible、清洗去重后 authoritative count 及排除原因；报告只能含 count/hash，不含原文。若当前清单不是 5,485，不修改完成阈值去迎合旧数字；若 source 或 ordered item hash 在 resume 时漂移，非零退出。状态机固定为 pending/in_flight/retryable/succeeded/abstained/terminal_failed，记录 attempt、lease、error class、cache/response hash、unit count。所有 INSERT 使用显式列名，默认 inspect/dry-run。</action>
  <verify>python -m pytest -q tests/test_knowledge_unit_contracts.py tests/test_knowledge_unit_backfill.py -k "inventory or migration or drift or ledger"</verify>
  <acceptance_criteria>
    - inventory 同输入两次排序与 dataset hash 完全相同，新增/删改 evidence 会触发 drift abort。
    - authoritative count 从 inventory artifact 读取，代码和测试中不存在把 5485 作为成功常量。
    - privacy-safe report 不含 message content、evidence quote、token 或完整 prompt。
    - migration 重跑无 diff，inspect/dry-run 不修改 production DB。
  </acceptance_criteria>
  <done>冻结清单可解释当前实际输入范围，item ledger 可在不触碰 active/current 的前提下创建和恢复。</done>
</task>

<task type="auto">
  <name>Task 2: 实现可恢复 batch、分类 retry 与内容寻址 cache</name>
  <files>integration/scripts/build_knowledge_units.py, integration/scripts/knowledge_unit_pipeline.py, tests/test_knowledge_unit_backfill.py, tests/test_knowledge_unit_retry_cache.py</files>
  <read_first>integration/scripts/build_knowledge_units.py, integration/scripts/knowledge_unit_pipeline.py, integration/prompts/knowledge_unit_extractor/v1_main.md, integration/prompts/knowledge_unit_extractor/v1_schema.md, tests/test_knowledge_unit_extraction.py</read_first>
  <action>把新 run 与恢复 run 拆成显式 CLI：`--start --inventory PATH` 与 `--resume RUN_ID`，并增加 `--model`、`--batch-size`、`--concurrency`、retry/backoff/timeout/config 参数。禁止硬编码 Vertex/Gemini/Luna 作为实际生产 ID；model 必填或从项目配置解析，实际值与 prompt/schema/config hash 写 manifest，不可用时 blocked/abort 且不 fallback。worker 仅返回纯响应；SQLite validated units、evidence links 和 item terminal state 由主线程单 writer 在一个事务写入。补 429/500/503/timeout + Retry-After/jitter/token refresh，400/无效凭据 fail-fast。cache key 必含 model/prompt/schema/normalized input/generation config；cache hit 仍重跑当前 Pydantic/evidence/privacy gate。过期 in_flight lease 恢复为 retryable，resume 不调用会删除同 run 成果的 begin_staging。</action>
  <verify>python -m pytest -q tests/test_knowledge_unit_backfill.py tests/test_knowledge_unit_retry_cache.py</verify>
  <acceptance_criteria>
    - fake client 的 interrupted 与 uninterrupted run 产生相同 row set 和 dataset hash。
    - resume 对 succeeded/abstained item 的 LLM 调用数为 0，过期 lease 可恢复。
    - model/prompt/schema/config 任一变化均 cache miss；完全相同时 hit 且重新验证。
    - 429/500/503/timeout 按上限重试，400 与永久鉴权错误终止；attempts/usage/error class 进入 manifest/report。
    - SQLite production writer 单线程提交，测试不存在并行 DB publish。
  </acceptance_criteria>
  <done>backfill engine 可安全中断续跑、复用精确 cache，并能区分可重试与永久错误。</done>
</task>

<task type="auto">
  <name>Task 3: 建立严格 extraction gate 与 active 隔离回归</name>
  <files>integration/scripts/evaluate_knowledge_unit_extraction.py, integration/scripts/knowledge_unit_pipeline.py, tests/test_knowledge_unit_backfill.py, tests/test_knowledge_unit_checkpoint.py</files>
  <read_first>integration/scripts/evaluate_knowledge_unit_extraction.py, integration/scripts/knowledge_unit_pipeline.py, integration/scripts/promote_knowledge_index.py, tests/test_knowledge_unit_checkpoint.py, .planning/phases/14-knowledge-unit-layer/14-AI-SPEC.md</read_first>
  <action>实现机器可读 strict gate：inventory hash/count 完整一致，pending/in_flight/retryable=0，terminal_api_errors=0，units_total&gt;0，yield 不低于 manifest 中预注册阈值，schema valid≥95%，overall failure≤10%，foreign/missing evidence、speaker misattribution、secret/deleted/excluded/thinking/tool-raw hit 均为 0，cache replay dataset hash 一致。pilot 尚未预注册 yield 时 gate 状态必须是 `awaiting_pilot_threshold` 而不是 PASS。通过仅写 extraction `validated` checkpoint，禁止把 draft 标记 canonical current 或改 active pointer。测试在失败/崩溃/重跑前后对旧 current rows、active pointer 和 collection identity 做不变断言。</action>
  <verify>python -m pytest -q tests/test_knowledge_unit_backfill.py tests/test_knowledge_unit_checkpoint.py -k "gate or active or isolation or all_api or zero_yield"</verify>
  <acceptance_criteria>
    - all API error、零 output、未完成 inventory 或任何 critical violation 均返回非零且不能 PASS。
    - threshold 未由 pilot 固化时 production gate 不允许人为默认通过。
    - failed/new/resumed extraction 不改变既有 canonical current、active pointer 或 active collection。
    - gate JSON 绑定 run_id、inventory/model/prompt/schema/config hashes 与每项 gate 结果。
  </acceptance_criteria>
  <done>只有完整、可复现且无 critical violation 的 run 才能进入 validated extraction checkpoint，任何失败保留旧 active。</done>
</task>

</tasks>

<verification>
- [ ] `python -m pytest -q tests/test_knowledge_unit_backfill.py tests/test_knowledge_unit_retry_cache.py tests/test_knowledge_unit_checkpoint.py` 通过。
- [ ] inventory report 的 authoritative count/hash 可重建，且不硬编码 5,485。
- [ ] dry-run、失败注入和 resume 均未改变 active pointer/current checkpoint。
</verification>

<success_criteria>
- 生产抽取具备冻结输入、幂等恢复、精确 cache、分类 retry 和失败封闭 gate。
- 实际模型由 CLI/config 注入并记录，模型不可用只会 blocked/abort。
- 14-03 可以在同一合同上安全运行 300–500 分层 pilot。
</success_criteria>

<output>
After completion, create `.planning/phases/14-knowledge-unit-layer/14-02-SUMMARY.md`
</output>
