---
phase: 14-knowledge-unit-layer
plan: "06"
type: execute
wave: 5
depends_on: ["14-05"]
files_modified:
  - integration/scripts/refresh_knowledge_units.py
  - integration/scripts/reconcile_knowledge_index.py
  - integration/scripts/migrate_memory_lifecycle.py
  - integration/scripts/sync_memory_lifecycle.py
  - integration/scripts/rollback_knowledge_checkpoint.py
  - tests/test_knowledge_incremental_refresh.py
  - tests/test_memory_lifecycle_sync.py
  - tests/test_knowledge_index_promotion.py
  - integration/analysis/ai_context/knowledge_incremental_refresh_report.json
  - integration/analysis/ai_context/memory_lifecycle_preview.json
  - integration/analysis/ai_context/knowledge_incremental_candidate.json
  - integration/analysis/ai_context/knowledge_incremental_eval.json
  - integration/analysis/ai_context/phase14_final_reconcile.json
autonomous: false
requirements: [KU-02, KU-06, KU-07, KU-08]
user_setup: []
must_haves:
  truths:
    - "增量 refresh 以 Phase 13.5 source snapshot/checksum 或 delta watermark 定位 affected evidence/subjects，无变化时为 no-op。"
    - "新增/修改只重建受影响 subjects；deleted/excluded/deprecated 在一次 refresh 内从 draft/canonical/index 传播且 residue=0。"
    - "wrong/stale/missing 只进入 dev/hard-negative backlog，不自动修改 frozen test。"
    - "Memory lifecycle 默认 preview，只有显式批准 `--write` 才应用；deprecated 只标记不物理删除。"
    - "Lifecycle `--write` 授权不包含 incremental candidate promotion；后者在 reconcile/eval/smoke PASS 后单独授权。"
    - "最终联合 rollback 能恢复 canonical/extraction/index/pointer/manifest/lifecycle，并通过 frozen smoke 和 exact reconcile。"
  artifacts:
    - "integration/scripts/refresh_knowledge_units.py performs affected-subject delta rebuild with old-active isolation"
    - "integration/scripts/reconcile_knowledge_index.py proves zero missing/orphan/deprecated residue against actual Chroma IDs"
    - "integration/scripts/sync_memory_lifecycle.py is preview-first, idempotent, and write-gated"
    - "integration/analysis/ai_context/phase14_final_reconcile.json binds the final active lineage and rollback proof"
  key_links:
    - "Phase 13.5 source checkpoint/delta -> affected evidence/subjects -> incremental extraction/canonical candidate"
    - "deleted/excluded/deprecated -> unit/canonical lifecycle -> actual Chroma removal -> reconcile report"
    - "canonical lifecycle proposal -> memory preview -> explicit write -> joint rollback manifest"
    - "incremental candidate actual-ID reconcile + eval/smoke -> separate human authorization -> journal promote"
threat_model:
  - id: T-14-05
    threat: "Incremental candidate 或 rollback 造成 DB/index/pointer lineage 分裂"
    mitigation: "immutable candidate、serial publisher、promotion journal 与 final exact reconcile"
  - id: T-14-08
    threat: "删除/排除/过期证据仍残留索引，或 lifecycle write 错误覆盖历史"
    mitigation: "一次 refresh 传播、zero-residue gate、preview-first/deprecate-only/explicit write"
---

<objective>
完成 affected-subject 增量刷新、删除/过期传播、memory lifecycle preview/write 合同和最终联合回滚验证，收口 Phase 14。

Purpose: 让知识层从一次性 backfill 变成可持续更新且不会遗忘删除/版本边界的生产系统。
Output: incremental refresh/reconcile、lifecycle migration/sync、写入 checkpoint、全量回归与 final lineage/rollback report。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-knowledge-unit-layer/14-05-SUMMARY.md
@.planning/phases/14-knowledge-unit-layer/14-AI-SPEC.md
@.planning/phases/14-knowledge-unit-layer/14-RESEARCH.md
@integration/scripts/promote_knowledge_index.py
@integration/scripts/rollback_knowledge_checkpoint.py
@integration/scripts/build_memory_promotion_candidates.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 affected-subject incremental refresh 与 zero-residue reconcile</name>
  <files>integration/scripts/refresh_knowledge_units.py, integration/scripts/reconcile_knowledge_index.py, tests/test_knowledge_incremental_refresh.py, integration/analysis/ai_context/knowledge_incremental_refresh_report.json</files>
  <read_first>integration/scripts/build_knowledge_units.py, integration/scripts/build_canonical_knowledge_units.py, integration/scripts/build_knowledge_unit_vector_store.py, integration/scripts/promote_knowledge_index.py, .planning/phases/13.5-agentsview-session-integration/13.5-01-PLAN.md</read_first>
  <action>先用 temp SQLite/fake Chroma 写新增、修改、删除、excluded、deprecated、no-change、mid-refresh failure 测试。refresh 输入必须是稳定的 Phase 13.5 source snapshot/checksum 或显式 delta watermark；比较前后 evidence hashes 定位 affected refs/subjects，只对这些 subjects 运行 extraction/canonical rebuild，cache 复用仍重验证。新结果进入独立 staging/candidate，生产 SQLite writer、canonical publisher、index publisher 串行；gate/promotion 失败保留旧 active。deleted/excluded/deprecated 在同一 refresh lineage 中传播到 draft/canonical lifecycle，并从 candidate/active surface 移除。reconcile 从实际 Chroma IDs 检查 missing/orphan/duplicate/deprecated residue=0；周期 full reconcile 作为兜底。wrong/stale/missing 只追加 dev/hard-negative backlog，测试 frozen dataset hash 不变。</action>
  <verify>python -m pytest -q tests/test_knowledge_incremental_refresh.py tests/test_knowledge_index_promotion.py</verify>
  <acceptance_criteria>
    - no-change delta 的 LLM/index writes 为 0；新增/修改只改变 affected subjects 的 stable rows/IDs。
    - deleted/excluded/deprecated 一次 refresh 后 actual index residue=0，unaffected subjects hash 不变。
    - mid-refresh failure 保留旧 canonical/index/pointer，resume/retry 不重复付费或写入。
    - feedback backlog 更新不改变 frozen dataset 文件/hash。
  </acceptance_criteria>
  <done>增量 refresh 能最小重算并完整传播删除/生命周期，同时保持失败隔离。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 preview-first memory lifecycle migration/sync</name>
  <files>integration/scripts/migrate_memory_lifecycle.py, integration/scripts/sync_memory_lifecycle.py, tests/test_memory_lifecycle_sync.py, integration/analysis/ai_context/memory_lifecycle_preview.json</files>
  <read_first>integration/scripts/build_memory_promotion_candidates.py, integration/scripts/migrate_add_knowledge_unit_tables.py, integration/scripts/sync_memory_lifecycle.py, tests/test_memory_contracts.py</read_first>
  <action>实现幂等 lifecycle migration 和 `inspect/migrate/main` 只读默认接口，为 memory_items 增加所需 status/version/last_seen/canonical_unit_id 或单独映射表，保持历史可追溯。sync 先输出 stable-ID preview，分类 would-create/update/deprecate/conflict；上游 canonical/index/pointer 不完整时 fail closed。只有 `--write` 应用 preview hash 精确匹配的 proposal，deprecate/supersede 不物理删除。测试 preview 无 DB diff、write 幂等、conflict 不自动 approved、重复运行 diff=0、报告无原文/secret。本 task 只在临时 DB 验证，不执行 production `--write`。</action>
  <verify>python -m pytest -q tests/test_memory_lifecycle_sync.py tests/test_memory_contracts.py</verify>
  <acceptance_criteria>
    - migration 可重跑且默认 inspect/dry-run；preview 不修改 production DB。
    - proposal 使用 stable IDs 并绑定 canonical/index/version/checksum，过期 preview 拒绝 write。
    - deprecated/superseded 仅状态变更不 DELETE；conflict fail closed。
    - temp DB 上两次相同 `--write` 的第二次 diff=0。
  </acceptance_criteria>
  <done>Memory lifecycle 具备可审核 preview、精确写入合同、幂等和历史保留。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: 审核并批准 production lifecycle --write</name>
  <files>integration/analysis/ai_context/memory_lifecycle_preview.json, integration/analysis/ai_context/knowledge_incremental_refresh_report.json</files>
  <read_first>integration/analysis/ai_context/memory_lifecycle_preview.json, integration/analysis/ai_context/knowledge_incremental_refresh_report.json, integration/scripts/sync_memory_lifecycle.py, integration/scripts/rollback_knowledge_checkpoint.py</read_first>
  <action>生成 production dry-run preview，展示 create/update/deprecate/conflict 数量、affected subject/stable IDs、canonical/index/source hashes、预计 DB/index 变化和联合 rollback target。任何 conflict、stale preview、active mismatch、secret/deprecated residue 或 unresolved journal 阻断。用户明确批准前不得调用 `sync_memory_lifecycle.py --write` 或发布 refresh candidate。</action>
  <verify>python integration/scripts/sync_memory_lifecycle.py --preview integration/analysis/ai_context/memory_lifecycle_preview.json</verify>
  <acceptance_criteria>
    - preview 绑定当前 source/canonical/index/pointer 且 reconcile healthy。
    - conflict=0 或全部明确留在 review；不存在物理删除动作。
    - rollback target/command 和 affected stable IDs 已展示，批准前 production diff=0。
  </acceptance_criteria>
  <instructions>检查 preview 中的 create/update/deprecate/conflict、affected IDs 和 rollback target；确认后回复“批准 lifecycle write”。</instructions>
  <resume-signal>Type “批准 lifecycle write” to authorize this exact preview hash, or describe a blocker.</resume-signal>
  <done>此 preview hash 的 production lifecycle 写入已获一次性明确批准。</done>
</task>

<task type="auto">
  <name>Task 4: 仅应用获批 lifecycle preview</name>
  <files>integration/scripts/sync_memory_lifecycle.py, tests/test_memory_lifecycle_sync.py, integration/analysis/ai_context/memory_lifecycle_preview.json</files>
  <read_first>integration/analysis/ai_context/memory_lifecycle_preview.json, integration/scripts/sync_memory_lifecycle.py, integration/scripts/rollback_knowledge_checkpoint.py</read_first>
  <action>仅对 Task 3 获批且 source/canonical/index/preview hash 未漂移的 exact preview 执行 lifecycle `--write`；SQLite lifecycle writer 单独串行。写后重跑相同 preview 验证 no-op，并确认 deprecated/superseded 只改状态、不物理删除。本 task 不构建、发布或 promote incremental index candidate；Task 3 的 lifecycle 授权不得解释为 index promotion 授权。</action>
  <verify>$preview = Get-Content -LiteralPath 'integration/analysis/ai_context/memory_lifecycle_preview.json' -Raw | ConvertFrom-Json; if ([string]::IsNullOrWhiteSpace([string]$preview.preview_hash)) { throw 'preview missing hash' }; python integration/scripts/sync_memory_lifecycle.py --write --preview integration/analysis/ai_context/memory_lifecycle_preview.json --expected-hash $preview.preview_hash</verify>
  <acceptance_criteria>
    - write 使用获批 exact preview hash；漂移时非零拒绝且无部分写入。
    - 第二次相同 preview 为 no-op，deprecate/supersede 无 DELETE。
    - active pointer、DB active index version 与 Chroma collections 在本 task 前后不变。
  </acceptance_criteria>
  <done>Lifecycle preview 已按独立授权安全写入，默认 active 与索引发布状态未改变。</done>
</task>

<task type="auto">
  <name>Task 5: 构建并评估 incremental candidate，不改变 active</name>
  <files>integration/scripts/refresh_knowledge_units.py, integration/scripts/reconcile_knowledge_index.py, integration/analysis/ai_context/knowledge_incremental_refresh_report.json, integration/analysis/ai_context/knowledge_incremental_candidate.json, integration/analysis/ai_context/knowledge_incremental_eval.json</files>
  <read_first>integration/analysis/ai_context/memory_lifecycle_preview.json, integration/scripts/refresh_knowledge_units.py, integration/scripts/reconcile_knowledge_index.py, integration/scripts/evaluate_knowledge_unit_rag.py, integration/scripts/promote_knowledge_index.py</read_first>
  <action>从 persisted source delta 与 affected subjects 执行 production refresh 到独立 staging/canonical build，再构建 immutable incremental candidate；SQLite writer、canonical publisher、index publisher 串行。candidate artifact 必须记录 collection_name/build/checksum/canonical/extraction/source lineage。用实际 Chroma IDs 验证 missing/orphan/duplicate/deleted/deprecated residue=0，并通过 candidate override 运行 frozen smoke、knowledge contract 和受影响 subject queries；结果写 incremental eval artifact。任何 reconcile/eval/smoke failure 保留 candidate 非 active；默认 active 全程不变。</action>
  <verify>python -m pytest -q tests/test_knowledge_incremental_refresh.py tests/test_knowledge_index_promotion.py tests/test_knowledge_search_contracts.py</verify>
  <acceptance_criteria>
    - candidate actual-ID reconcile 的 missing/orphan/duplicate/deleted/deprecated residue 均为 0。
    - incremental eval/smoke PASS，unaffected subjects hash 不变，frozen smoke 无质量/隐私回归。
    - candidate/eval artifacts 的 collection/checksum/source/canonical/extraction lineage 精确一致。
    - 构建和评估前后默认 active pointer/version 不变，candidate 仍非 active。
  </acceptance_criteria>
  <done>Incremental candidate 已通过 reconcile/eval/smoke，停在独立 promotion review 边界。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: 单独批准 incremental candidate promotion</name>
  <files>integration/analysis/ai_context/knowledge_incremental_candidate.json, integration/analysis/ai_context/knowledge_incremental_eval.json</files>
  <read_first>integration/analysis/ai_context/knowledge_incremental_candidate.json, integration/analysis/ai_context/knowledge_incremental_eval.json, integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py</read_first>
  <action>从 candidate artifact 解析 exact collection/build/checksum，确认 actual-ID reconcile、frozen/affected-subject smoke、privacy/lifecycle gates 全部 PASS；展示当前/目标 active lineage、journal 状态、rollback target/command。该授权与 Task 3 lifecycle `--write` 授权严格分离，只覆盖此 incremental candidate。任何 mismatch、failed gate 或 unresolved journal 阻断。</action>
  <verify>$candidate = Get-Content -LiteralPath 'integration/analysis/ai_context/knowledge_incremental_candidate.json' -Raw | ConvertFrom-Json; $eval = Get-Content -LiteralPath 'integration/analysis/ai_context/knowledge_incremental_eval.json' -Raw | ConvertFrom-Json; if ($eval.gate.status -ne 'PASS' -or $candidate.collection_checksum -ne $eval.candidate_checksum) { throw 'incremental candidate gate/lineage failed' }; python integration/scripts/promote_knowledge_index.py --candidate $candidate.collection_name --preflight</verify>
  <acceptance_criteria>
    - reconcile/eval/smoke gates PASS 且 candidate/eval lineage 完全一致。
    - rollback target 可读、journal 无未解决 intent、默认 active 在批准前未变。
    - 用户明确批准的是 exact incremental collection/checksum，而非未来自动 promotion。
  </acceptance_criteria>
  <instructions>核对 candidate reconcile、eval/smoke、旧/新 checkpoint 与 rollback 命令；确认后回复“批准 incremental promote”。</instructions>
  <resume-signal>Type “批准 incremental promote” to authorize this exact candidate, or describe a blocker.</resume-signal>
  <done>通过 reconcile/eval/smoke 的 exact incremental candidate 已获独立 promotion 授权。</done>
</task>

<task type="auto">
  <name>Task 7: Journal promote、最终 reconcile 与联合 rollback/restore 演练</name>
  <files>integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py, integration/scripts/reconcile_knowledge_index.py, tests/test_memory_lifecycle_sync.py, tests/test_knowledge_index_promotion.py, integration/analysis/ai_context/phase14_final_reconcile.json</files>
  <read_first>integration/analysis/ai_context/knowledge_incremental_candidate.json, integration/analysis/ai_context/knowledge_incremental_eval.json, integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py, integration/scripts/reconcile_knowledge_index.py</read_first>
  <action>获批后通过 prepare/commit journal promote exact incremental candidate，publisher 串行；立即验证 DB/canonical/index/pointer/manifest/lifecycle、actual collection IDs 和默认-route smoke。任一 post-promote failure 联合 rollback 到 preflight target。随后用已知安全 checkpoint 做一次可逆 rollback→reconcile→restore 演练，记录每步 hashes 和 actual IDs。运行 Phase 14 定向测试及全量 pytest；失败时恢复最后通过 checkpoint，不把阶段标 complete。</action>
  <verify>python -m pytest -q tests/test_knowledge_incremental_refresh.py tests/test_memory_lifecycle_sync.py tests/test_knowledge_index_promotion.py tests/test_knowledge_search_contracts.py tests/test_rag_feedback_privacy.py</verify>
  <acceptance_criteria>
    - post-promote active lineage 与 exact incremental candidate 一致，deleted/deprecated actual residue=0。
    - 联合 rollback 与 restore 后 DB/canonical/index/pointer/manifest/lifecycle hashes 均回到预期且 smoke 通过。
    - Phase 14 定向测试与 `python -m pytest -q` 全部通过；未验证项不会被标 complete。
    - final reconcile JSON 不含原 query/evidence content，且绑定当前 active 与可用 rollback checkpoint。
  </acceptance_criteria>
  <done>Incremental candidate 已按独立授权 journal promote，联合 rollback/restore 和完整回归均有证据。</done>
</task>

</tasks>

<verification>
- [ ] `python -m pytest -q tests/test_knowledge_incremental_refresh.py tests/test_memory_lifecycle_sync.py tests/test_knowledge_index_promotion.py tests/test_knowledge_search_contracts.py tests/test_rag_feedback_privacy.py` 通过。
- [ ] `python -m pytest -q` 全量回归通过。
- [ ] Production lifecycle write 与 incremental candidate promotion 分别获得独立批准。
- [ ] Incremental candidate 在 promote 前通过 actual-ID reconcile、eval/smoke；final joint rollback/restore evidence 完整。
</verification>

<success_criteria>
- 新 canonical evidence 可按 affected subjects 增量刷新，无变化 no-op，失败保留旧 active。
- 删除/排除/过期在一次 refresh 内传播且实际索引 residue=0。
- Lifecycle 仅由获批 preview 写入且不物理删除；incremental candidate 仅在单独授权后 journal promote。
- KU-01..KU-08 的实现、评估、canary 和生命周期证据可追溯，Phase 14 全量回归通过。
</success_criteria>

<output>
After completion, create `.planning/phases/14-knowledge-unit-layer/14-06-SUMMARY.md`
</output>
