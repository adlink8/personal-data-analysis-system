---
phase: 14-knowledge-unit-layer
plan: "05"
type: execute
wave: 4
depends_on: ["14-04"]
files_modified:
  - integration/scripts/promote_knowledge_index.py
  - integration/scripts/rollback_knowledge_checkpoint.py
  - integration/scripts/search_vectors.py
  - integration/scripts/unified_search.py
  - integration/scripts/api_server.py
  - integration/scripts/mcp_server.py
  - integration/scripts/migrate_add_rag_feedback_tables.py
  - integration/scripts/evaluate_knowledge_canary.py
  - tests/test_knowledge_index_promotion.py
  - tests/test_knowledge_search_contracts.py
  - tests/test_rag_feedback_privacy.py
  - integration/analysis/ai_context/knowledge_unit_canary_report.json
autonomous: false
requirements: [KU-02, KU-04, KU-07]
user_setup: []
must_haves:
  truths:
    - "Promote 拒绝不存在、不可读、checksum 不符、gate 未通过或 lineage 不完整的 candidate。"
    - "DB index version、canonical checkpoint、active pointer 与 collection 的切换使用可恢复 journal；中断后可 reconcile。"
    - "CLI/REST/MCP 共用唯一 knowledge-first backend，citation/lifecycle/checkpoint 不合法时 raw fallback 或 abstain。"
    - "30-query canary 通过 candidate override/独立 canary route 运行，默认 active 在 canary 全程不变。"
    - "只有人工 labels 和 canary gate PASS 后才出现独立 production promote 授权；promote 后不一致会联合 rollback。"
    - "反馈默认只存 query hash、IDs、scores、route、latency、versions、label，不存 raw query/evidence text。"
  artifacts:
    - "integration/scripts/promote_knowledge_index.py validates and journals the complete promotion contract"
    - "integration/scripts/rollback_knowledge_checkpoint.py restores DB/index/pointer/canonical lineage jointly"
    - "integration/scripts/unified_search.py owns the shared knowledge-first/raw-fallback contract"
    - "integration/analysis/ai_context/knowledge_unit_canary_report.json records 30 privacy-safe labeled queries"
  key_links:
    - "passed frozen A/B + candidate checksum -> promotion prepare/commit journal -> active pointer"
    - "candidate override -> unified_search canary route -> REST/MCP adapters -> privacy-safe feedback; default active unchanged"
    - "passed canary + explicit authorization -> promotion journal -> active pointer; post-promote failure -> joint rollback"
threat_model:
  - id: T-14-05
    threat: "未通过或损坏 candidate 被提升，或回滚只改 pointer 不恢复 DB/canonical"
    mitigation: "完整 precondition validation、prepare/commit journal、联合 reconcile/rollback tests"
  - id: T-14-06
    threat: "REST/MCP 重复检索逻辑导致 citation、fallback 和版本合同分叉"
    mitigation: "唯一 unified_search backend 与参数化 contract tests"
  - id: T-14-07
    threat: "Canary/feedback 保存原 query，或未通过 canary 的 candidate 先污染默认 active"
    mitigation: "hash-only feedback schema、candidate override、30-query gate PASS 后独立 promote 授权"
---

<objective>
补强 promotion/rollback 合同，接入统一 knowledge retrieval 与隐私安全反馈，先以 candidate override 完成 30-query canary，再单独批准真实 promote。

Purpose: 只有 lineage、离线 gate 和实际 collection 全部可信时才切换 active；线上错误可立即回到稳定 raw/旧 checkpoint。
Output: 可恢复 promotion journal、联合 rollback、CLI/REST/MCP contract、feedback schema、canary report。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-knowledge-unit-layer/14-04-SUMMARY.md
@.planning/phases/14-knowledge-unit-layer/14-AI-SPEC.md
@.planning/phases/14-knowledge-unit-layer/14-PATTERNS.md
@integration/analysis/ai_context/knowledge_unit_candidate_ab.json
@integration/scripts/promote_knowledge_index.py
@integration/scripts/rollback_knowledge_checkpoint.py
@integration/scripts/unified_search.py
@integration/scripts/api_server.py
@integration/scripts/mcp_server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现可恢复 promotion journal 与联合 rollback</name>
  <files>integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py, tests/test_knowledge_index_promotion.py</files>
  <read_first>integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py, integration/scripts/build_knowledge_unit_vector_store.py, integration/analysis/ai_context/knowledge_unit_candidate_ab.json, tests/test_knowledge_index_promotion.py</read_first>
  <action>先扩展测试覆盖不存在/unpassed/过期/checksum/count 不符 candidate、active pointer 与 DB 不一致、prepare/commit 各中断点。promote preflight 必须读取实际 Chroma collection 并验证 candidate 状态、count/checksum、passed frozen report、canonical/extraction lineage 和当前 active 一致。实现 durable prepare/commit journal：记录 intent/old/new checkpoints，DB 更新、`os.replace` pointer、commit marker 每步可恢复；启动时 reconcile 未完成 journal。rollback 必须恢复 canonical current checkpoint、index version、pointer 和 manifest，再读实际 collection 做 exact reconcile/smoke。默认 `--inspect`/`--dry-run`，本 task 只用 temp DB/fake Chroma，不执行 production promote。</action>
  <verify>python -m pytest -q tests/test_knowledge_index_promotion.py</verify>
  <acceptance_criteria>
    - nonexistent、unpassed、stale、checksum/count mismatch candidate 全部非零拒绝且 active 不变。
    - prepare/DB/pointer/commit 任一点中断，下一次 reconcile 得到明确 old 或 new 完整状态，不存在半切换。
    - rollback 后 canonical/index/pointer/manifest lineage 一致，实际 collection missing/orphan/duplicate=0。
    - 默认命令不会写 production，JSONL audit log 不是唯一真相。
  </acceptance_criteria>
  <done>Promotion 和 rollback 具备完整前置条件、崩溃恢复与联合一致性测试。</done>
</task>

<task type="auto">
  <name>Task 2: 建立统一 knowledge retrieval 与隐私安全 feedback contract</name>
  <files>integration/scripts/search_vectors.py, integration/scripts/unified_search.py, integration/scripts/api_server.py, integration/scripts/mcp_server.py, integration/scripts/migrate_add_rag_feedback_tables.py, integration/scripts/evaluate_knowledge_canary.py, tests/test_knowledge_search_contracts.py, tests/test_rag_feedback_privacy.py</files>
  <read_first>integration/scripts/search_vectors.py, integration/scripts/unified_search.py, integration/scripts/api_server.py, integration/scripts/mcp_server.py, tests/test_data_access_contracts.py, tests/test_apps_sdk_data_contracts.py, tests/test_memory_contracts.py</read_first>
  <action>在 `unified_search.py` 实现唯一 `search_knowledge_units(query, top_k, filters, include_evidence)` backend，knowledge-first 成功结果带 route/reason、index/canonical/extraction versions、canonical unit IDs、scores、lifecycle 和 evidence refs；evidence 默认只给 refs 不给原文。active 缺失/不一致、citation 无法回查、非 current lifecycle 时过滤并 raw fallback/abstain。`top_k` 有界，空 query 合同固定。CLI/REST/MCP 仅适配同一 backend，增加参数化等价测试。以幂等 migration 增加 rag_runs/items/feedback，仅保存 query hash、IDs/scores、route、latency、versions、label；报告/schema 禁止 raw query、result text、evidence quote、token/secret。canary evaluator 生成本地 30-query label worksheet 和机器 gate。</action>
  <verify>python -m pytest -q tests/test_knowledge_search_contracts.py tests/test_rag_feedback_privacy.py tests/test_data_access_contracts.py tests/test_apps_sdk_data_contracts.py tests/test_memory_contracts.py</verify>
  <acceptance_criteria>
    - backend、REST、MCP 对同输入的核心字段/route/fallback 语义等价。
    - missing/broken active、invalid citation、deprecated/conflict unit 不返回为可信 KU，能 raw fallback/abstain。
    - feedback tables/reports 中不存在 raw query、evidence quote、完整 result text、credential/token 字段。
    - canary worksheet 固定 30 个 query hashes，并支持 helpful/wrong/stale/missing 人工 label。
  </acceptance_criteria>
  <done>统一检索接口和隐私安全 feedback/canary 合同通过自动 contract tests。</done>
</task>

<task type="auto">
  <name>Task 3: 通过 candidate override 生成 30-query canary worksheet</name>
  <files>integration/scripts/unified_search.py, integration/scripts/evaluate_knowledge_canary.py, tests/test_knowledge_search_contracts.py, tests/test_rag_feedback_privacy.py, integration/analysis/ai_context/knowledge_unit_canary_report.json</files>
  <read_first>integration/analysis/ai_context/knowledge_unit_candidate_build.json, integration/analysis/ai_context/knowledge_unit_candidate_ab.json, integration/scripts/evaluate_knowledge_canary.py, integration/scripts/unified_search.py, integration/scripts/promote_knowledge_index.py</read_first>
  <action>从 `knowledge_unit_candidate_build.json.collection_name` 解析 exact candidate，使用 `--candidate-override` 或独立只读 canary pointer 运行固定 30 个真实 queries；此路径必须在 response 中明确 `route=knowledge_canary_override`，且不得调用 promote、不得改 active pointer/DB active version。生成只含 query hash、route、latency、returned IDs/scores、candidate/canonical/extraction versions 和待填 label 的 worksheet，不保存 raw query/evidence/result text。记录 canary 前后 active pointer/version 并断言完全相同。本 task 不伪造人工 label，也不宣告 canary PASS。</action>
  <verify>$candidate = Get-Content -LiteralPath 'integration/analysis/ai_context/knowledge_unit_candidate_build.json' -Raw | ConvertFrom-Json; if ([string]::IsNullOrWhiteSpace([string]$candidate.collection_name)) { throw 'candidate build missing collection_name' }; python integration/scripts/evaluate_knowledge_canary.py --candidate-override $candidate.collection_name --queries 30 --report integration/analysis/ai_context/knowledge_unit_canary_report.json</verify>
  <acceptance_criteria>
    - worksheet 恰有 30 个 privacy-safe query records，且 label 初始为空而不是模型自填。
    - 每条 route 明确为 canary override；canary 前后默认 active pointer 与 DB active version 完全不变。
    - 30-query 执行期间 retrieval/feedback reports 不含 raw query/evidence/result text。
    - 未调用 promotion journal，candidate 仍是非 active 状态。
  </acceptance_criteria>
  <done>30-query canary 已通过隔离 override 完成，默认 active 未改变，人工标注 worksheet 已生成。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 完成 30-query 人工 canary labels</name>
  <files>integration/analysis/ai_context/knowledge_unit_canary_report.json</files>
  <read_first>integration/analysis/ai_context/knowledge_unit_canary_report.json, .planning/phases/14-knowledge-unit-layer/14-AI-SPEC.md</read_first>
  <action>用户逐项结合本地 query 对 worksheet 的 30 个记录标注 `helpful|wrong|stale|missing`；报告仍只保存 query hash 和 label，不补写 raw query。任何疑似 secret/citation/lifecycle 错误标为 wrong/stale 并说明本地审查编号，不能由模型自动覆盖。标注未满 30 条时不得运行最终 canary gate。</action>
  <verify>python integration/scripts/evaluate_knowledge_canary.py --report integration/analysis/ai_context/knowledge_unit_canary_report.json --check-label-completeness</verify>
  <acceptance_criteria>
    - 恰有 30 个允许枚举内的人工 labels，缺失/未知 label=0。
    - 报告中仍无 raw query、evidence quote、result text 或 secret。
    - critical wrong/stale 不被改标或忽略以满足门槛。
  </acceptance_criteria>
  <how-to-verify>使用本地 query 映射逐项判断 returned IDs/evidence refs 是否有帮助、错误、过期或缺失；完成全部 30 条后确认报告仅保存 hash/IDs/label。</how-to-verify>
  <resume-signal>Type “labels complete” when all 30 human labels are recorded, or describe a critical issue.</resume-signal>
  <done>30 个真实 query 已由用户完成隐私安全人工标注。</done>
</task>

<task type="auto">
  <name>Task 5: 计算隔离 canary gate 并保持默认 active 不变</name>
  <files>integration/scripts/evaluate_knowledge_canary.py, integration/analysis/ai_context/knowledge_unit_canary_report.json</files>
  <read_first>integration/analysis/ai_context/knowledge_unit_canary_report.json, integration/analysis/ai_context/knowledge_unit_raw_baseline.json, integration/analysis/ai_context/knowledge_unit_candidate_build.json, integration/scripts/evaluate_knowledge_canary.py</read_first>
  <action>对完成的 30 labels 计算 helpful≥80%、critical wrong/stale=0、fallback≤30%、warm p95≤raw baseline 2x，并核对 report 中的 candidate checksum/lineage 与 build/A-B artifact 一致。任一 critical/gate failure 返回非零并令 candidate 保持非 active；默认 active 无需回滚，因为 canary 从未切换它。全部 PASS 只写 `ready_for_promotion_review=true`，不得调用 promotion journal。wrong/stale critical case不可豁免。</action>
  <verify>python integration/scripts/evaluate_knowledge_canary.py --report integration/analysis/ai_context/knowledge_unit_canary_report.json --strict</verify>
  <acceptance_criteria>
    - helpful/fallback/latency/critical metrics 可由 30 records 重算，strict gate 有明确非零失败退出码。
    - PASS 只标记 ready_for_promotion_review；FAIL 保持 candidate 非 active，默认 active 前后不变。
    - canary report 的 collection/checksum/canonical/extraction lineage 与 14-04 artifacts 精确一致。
  </acceptance_criteria>
  <done>隔离 canary 已得到机器 gate；只有 PASS candidate 可进入独立 promote 授权。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: 在 canary PASS 后单独批准 production promote</name>
  <files>integration/analysis/ai_context/knowledge_unit_candidate_build.json, integration/analysis/ai_context/knowledge_unit_candidate_ab.json, integration/analysis/ai_context/knowledge_unit_canary_report.json</files>
  <read_first>integration/analysis/ai_context/knowledge_unit_candidate_build.json, integration/analysis/ai_context/knowledge_unit_candidate_ab.json, integration/analysis/ai_context/knowledge_unit_canary_report.json, integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py</read_first>
  <action>仅在 frozen A/B 与 30-query canary gate 均 PASS 后运行 promotion preflight。从 candidate build artifact 解析 exact collection，展示实际 checksum、当前/目标 canonical/index/pointer、journal 状态、rollback target/command；任何 lineage/checksum/active mismatch 或 unresolved journal 阻断。该 checkpoint 是 canary 之后独立的一次性 production promote 授权。</action>
  <verify>$candidate = Get-Content -LiteralPath 'integration/analysis/ai_context/knowledge_unit_candidate_build.json' -Raw | ConvertFrom-Json; $canary = Get-Content -LiteralPath 'integration/analysis/ai_context/knowledge_unit_canary_report.json' -Raw | ConvertFrom-Json; if ($canary.gate.status -ne 'PASS') { throw 'canary gate is not PASS' }; python integration/scripts/promote_knowledge_index.py --candidate $candidate.collection_name --preflight</verify>
  <acceptance_criteria>
    - A/B 和 canary 均 PASS，candidate/checksum/canonical/extraction lineage 一致。
    - rollback target 可读且 smoke/reconcile 通过，journal 无未解决 intent。
    - 用户批准前 active pointer/DB 状态保持 canary 前的默认值。
  </acceptance_criteria>
  <instructions>核对 A/B、30-query canary、exact candidate、旧/新 checkpoint 和 rollback 命令；确认后回复“批准 production promote”。</instructions>
  <resume-signal>Type “批准 production promote” to authorize this exact candidate, or describe a blocker.</resume-signal>
  <done>通过 canary 的 exact candidate 已获一次性 production promote 授权。</done>
</task>

<task type="auto">
  <name>Task 7: 通过 journal promote 并验证失败联合 rollback</name>
  <files>integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py, integration/analysis/ai_context/knowledge_unit_canary_report.json</files>
  <read_first>integration/analysis/ai_context/knowledge_unit_candidate_build.json, integration/analysis/ai_context/knowledge_unit_canary_report.json, integration/scripts/promote_knowledge_index.py, integration/scripts/rollback_knowledge_checkpoint.py</read_first>
  <action>获批后对 exact candidate 执行 prepare/commit journal promote，SQLite/index/pointer publisher 串行；立即从实际 collection reconcile DB/canonical/index/pointer/manifest，并用 canary smoke queries 走默认 route。任一不一致或 smoke critical failure 立即联合 rollback 到 preflight target，验证旧 active/raw route 与 actual IDs。只有 post-promote reconcile/smoke PASS 才将 promotion journal committed。</action>
  <verify>$candidate = Get-Content -LiteralPath 'integration/analysis/ai_context/knowledge_unit_candidate_build.json' -Raw | ConvertFrom-Json; python integration/scripts/promote_knowledge_index.py --candidate $candidate.collection_name --write</verify>
  <acceptance_criteria>
    - PASS 时默认 active/canonical/index/manifest 与 exact candidate lineage 一致且 journal committed。
    - post-promote 失败时联合 rollback 到旧 checkpoint，实际 collection reconcile 和 smoke 通过。
    - production SQLite writer、index publisher 与 pointer switch 串行，无半发布状态。
  </acceptance_criteria>
  <done>通过 canary 的 candidate 已安全 journal promote，或在 post-promote 故障时完成联合 rollback。</done>
</task>

</tasks>

<verification>
- [ ] `python -m pytest -q tests/test_knowledge_index_promotion.py tests/test_knowledge_search_contracts.py tests/test_rag_feedback_privacy.py` 通过。
- [ ] REST/MCP/CLI contract 等价，feedback 数据与报告无原 query/evidence text。
- [ ] Canary 通过 candidate override 完成且默认 active 未变；PASS 后才有独立 promote 授权。
- [ ] Post-promote reconcile/smoke PASS，或联合 rollback evidence 完整。
</verification>

<success_criteria>
- 只有 passed、可读且 checksum/lineage 一致的 candidate 可被 promote。
- Knowledge-first 检索能安全 raw fallback/abstain，CLI/REST/MCP 不分叉。
- 30-query canary 在不改变默认 active 的情况下满足质量/回退/延时门槛；只有随后获批才 promote。
</success_criteria>

<output>
After completion, create `.planning/phases/14-knowledge-unit-layer/14-05-SUMMARY.md`
</output>
