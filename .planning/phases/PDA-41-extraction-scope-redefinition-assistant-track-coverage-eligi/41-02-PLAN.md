# Plan 41-02: assistant 轨抽取器（prod 双轨参数化 + track config + as| 前缀 + 独立 watermark key + prompt 新建）

```yaml
wave: 2
depends_on: [41-01]
autonomous: true
requirements: [EXT-01]
files_modified:
  - src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py
  - src/personal_knowledge/application/knowledge/confirmation_signals.py  # 新建（D-03 信号检测）
  - src/personal_knowledge/application/knowledge/build_knowledge_units.py
  - src/personal_knowledge/application/knowledge/refresh_knowledge_units.py
  - src/personal_knowledge/application/ku.py
  - src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py
  - tools/migrations/rebuild_knowledge_unit_type_check.py               # 新建，默认 dry-run（阻断前置）
  - assets/prompts/knowledge_unit_extractor/v1_assistant.md            # 新建
  - assets/prompts/knowledge_unit_extractor/v1_assistant_schema.md     # 新建
  - tools/migrations/bootstrap_assistant_watermark.py                  # 新建，默认 dry-run
  - tests/unit/test_knowledge_unit_prod_assistant_track.py             # 新建
```

<objective>
把 prod 增量抽取引擎（build_knowledge_units_prod.py）从 user 单轨硬编码参数化为 run 级单轨的双轨引擎：track config 注入 prompt 文件/role_label/evidence_scope/unit_id 前缀/unit_type 校验集合；新建 assistant 轨 prompt（`v1_assistant.md`，speaker gate 平移为"只有 role=assistant 内容能证明解决方案/结论"）；输入为 QA 对（assistant 原文 + 前置 1 条 user 上下文）+ 12000 字符尾部截断；`as|` 前缀自动获得 StagingPublisher 的 pass 族隔离；`knowledge_source_watermark` 新增 `committed_assistant` key，首次 bootstrap 把存量 assistant 排除在 delta 之外（不触发 7 万条全量队列）。
</objective>

<context>
- 锁定决策：**D-01**（独立 unit_type 集合 `solution`/`decision_rationale`/`technical_conclusion`，独立 prompt）；**D-02**（evidence_scope='assistant' 承载信任分级，evidence_scope CHECK 已含无需变更（migrate_add_knowledge_unit_tables.py:109）——但 **unit_type CHECK 不含 3 新类型（:99 与 :129），需 task 0 表重建迁移**）；**D-03**（quote 回查必做，复用 `_evidence_supported` 锚 assistant 原文；确认信号只做修饰不做硬 gate——信号检测与 confidence 修饰由本 plan task 6 承接，lifecycle 自动路由留增量）；**D-04**（ku| 世代豁免，assistant 轨只覆盖增量 → 独立 watermark key 是本 plan 的 R4 关键）。
- 引擎硬编码点（RESEARCH §1 表）：prompt 路径模块常量（build_knowledge_units.py:60 `PROMPT_PATH`，prod:626 读取）；包装文案 "用户对话证据（role=user）："（prod:211）；`evidence_scope='user'` 写死（prod:591）；`unit_id = "v1|" + ...`（prod:576）；6 类型校验（build_knowledge_units.py:75-81 `KnowledgeUnit.valid_unit_type`）。
- **不新建独立 ledger 模块**：L2 的 `knowledge_l2_session_jobs` 模式绕开 Gate F watermark 保护与 skip_succeeded（RESEARCH §1 理由 1-2），禁止复刻。
- **硬约束 R2**：`StagingPublisher.promote` 取 `substr(unit_id,1,3)` 作 pass 族（knowledge_unit_pipeline.py:180），新前缀必须恰好 3 字符含 `|` → `as|`。
- cache 命名空间安全：`compute_cache_key` 含 prompt_hash（prod:97-101），两轨不串 cache，无需额外隔离。
- run 级单轨约束（RESEARCH §1 末）：一个 extraction run 只属于一条轨；`pk-ku prepare --roles assistant --track assistant` 产 assistant-only run；user 轨 prepare 默认 `--roles user`。
- 依赖 41-01：`compute_eligible_messages` 提供带 role 的轨道输入；`ref_roles` 已可从 eligible 结果直接构建。
</context>

<tasks>

<task id="0" name="前置阻断：unit_type CHECK 表重建迁移（knowledge_units + canonical_knowledge_units）">
<read_first>
- src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py:96-140（knowledge_units :99 与 canonical_knowledge_units :129 的 unit_type CHECK 现状，均只含 6 个 user 类型）
- tools/migrations/backfill_ku_data_debts.py（既有迁移脚本的 dry-run/计数/备份模式）
- docs/runbooks/ku-incremental.md（Gate F / promote 前的 schema 一致性语境）
</read_first>
<action>
**plan-checker 阻断项**：SQLite 不能 ALTER CHECK，`evidence_scope` 的 CHECK（:109）虽已含 'assistant'，但 `unit_type` 的 CHECK 不含 solution/decision_rationale/technical_conclusion——不迁移则 as| unit 的 INSERT 必然失败。新建 `tools/migrations/rebuild_knowledge_unit_type_check.py`（argparse：默认 dry-run，`--write` 落库）：
1. 幂等探测：从 sqlite_master 读 `knowledge_units` 与 `canonical_knowledge_units` 的建表 SQL，若 CHECK 已含 'solution' → 打印 `no_op` 退出 0。
2. `--write` 先备份：复制 unified DB 到 `var/backups/unified_db_pre_unit_type_check_<UTC 时间戳>.sqlite`，打印备份路径与大小，备份失败即中止。
3. 对两表分别执行标准 SQLite 表重建：`PRAGMA foreign_keys=OFF` → BEGIN → `CREATE TABLE <table>_new`（DDL 与现表逐列相同，仅 unit_type CHECK 扩为 `('preference','habit','personal_fact','project_decision','capability','tool_usage','solution','decision_rationale','technical_conclusion')`）→ `INSERT INTO <table>_new SELECT * FROM <table>` → **行数守恒断言**（前后 COUNT(*) 不等即 ROLLBACK 报错退出非零）→ DROP 旧表 → `ALTER TABLE <table>_new RENAME TO <table>` → 重建该表全部索引（事先从 sqlite_master 备份 index DDL）→ COMMIT → `PRAGMA foreign_keys=ON` → `PRAGMA foreign_key_check` 必须返回 0 行（knowledge_unit_evidence / canonical_unit_members 外键引用不受损）。
4. dry-run：打印两表当前 CHECK 定义、将重建的索引清单、两表行数，不落库。
5. 同步 `migrate_add_knowledge_unit_tables.py` 的 SCHEMA_SQL：:99 与 :129 两处 unit_type CHECK 加入同样 3 个新类型（新库直接含新 CHECK，旧库走迁移）。
</action>
<acceptance_criteria>
- 无 `--write`：stdout 含两表行数与 `dry_run` 标记，sqlite_master 中两表 DDL 不变。
- `--write` 后：向 knowledge_units 与 canonical_knowledge_units 各 INSERT 一行 unit_type='solution' 的测试行成功（随后 DELETE 清理）；脚本输出的行数守恒断言 PASS；`PRAGMA foreign_key_check` 返回 0 行；备份文件存在于 var/backups/；再跑一次输出 `no_op`。
- 源码断言：`grep -c "solution" src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py` ≥2（两处 CHECK 同步）。
- **顺序硬约束**：本迁移 `--write` 完成前，禁止执行 task 4/5 的 `pk-ku prepare --track assistant` 与任何 as| extract。
</acceptance_criteria>
</task>

<task id="1" name="track config 与 assistant Pydantic 模型">
<read_first>
- src/personal_knowledge/application/knowledge/build_knowledge_units.py:58-112（PROMPT_VERSION/PROMPT_PATH/KnowledgeUnit/ExtractionResult）
- src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py:44-73（import 区与常量区）
- src/personal_knowledge/application/knowledge/knowledge_unit_pipeline.py:163-211（promote 前缀族逻辑）
</read_first>
<action>
1. `build_knowledge_units.py` 新增：
   - `class AssistantKnowledgeUnit(BaseModel)`（extra="forbid"）：字段与 KnowledgeUnit 相同（unit_type/subject/question/answer/confidence/evidence_quote/lifecycle），`valid_unit_type` 的 allowed 改为 `{"solution", "decision_rationale", "technical_conclusion"}`。
   - `class AssistantExtractionResult(BaseModel)`（extra="forbid"）：`units: list[AssistantKnowledgeUnit]`、`abstain: bool`、`abstain_reason: str`。
   - 常量 `ASSISTANT_PROMPT_VERSION = "v1_assistant"`；`ASSISTANT_PROMPT_PATH = <repo>/assets/prompts/knowledge_unit_extractor/v1_assistant.md`（与 :60 PROMPT_PATH 同款 Path 推导）。
2. `build_knowledge_units_prod.py` 新增 `@dataclass(frozen=True) class TrackConfig`，字段：`name: str`（"user"/"assistant"）、`prompt_path: Path`、`prompt_version: str`、`role_label: str`（注入包装文案，如 "用户对话证据（role=user）：" / "助手回答证据（role=assistant）："）、`evidence_scope: str`、`unit_id_prefix: str`、`result_model: type`（ExtractionResult 或 AssistantExtractionResult）、`roles: tuple[str, ...]`。模块级两个实例 `USER_TRACK` / `ASSISTANT_TRACK`；`__post_init__` 断言 `len(unit_id_prefix) == 3 and unit_id_prefix.endswith("|")`（R2 硬约束，构造期 fail closed）。
</action>
<acceptance_criteria>
- 单测：`AssistantExtractionResult` 接受 unit_type='solution'/'decision_rationale'/'technical_conclusion'，对 'preference' 等 6 旧类型抛 ValidationError；`ExtractionResult` 反之（旧 6 类型过、3 新类型拒）。
- 单测：`TrackConfig(name="x", unit_id_prefix="asst|", ...)` 构造抛 AssertionError；`ASSISTANT_TRACK.unit_id_prefix == "as|"` 且 len==3。
</acceptance_criteria>
</task>

<task id="2" name="prod 引擎按 track 注入（prompt/包装/scope/前缀/QA 输入/截断）">
<read_first>
- src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py:199-240（call_llm_with_retry）、:474-488（_evidence_supported）、:491-607（_commit_item_result）、:609-760（process_run 主循环与 content 加载）
- src/personal_knowledge/application/knowledge/extract_knowledge_units_l2_session.py:54,190-197（MAX_WINDOW_CHARS=12000 尾部硬截先例）
- src/personal_knowledge/application/knowledge/build_knowledge_unit_vector_store.py:102-187（_load_user_contexts 前置 user 解析先例）
</read_first>
<action>
1. `call_llm_with_retry` 增加关键字参数 `role_label: str = "用户对话证据（role=user）："`，:211 的包装 f-string 用该参数替换硬编码文案（默认参数保持 user 轨现有 cache key 兼容：user 轨 prompt_hash 不变）。
2. `process_run` 增加参数 `track: TrackConfig = USER_TRACK`；:626 的 prompt 读取改为 `track.prompt_path.read_text(...)`；`_llm_worker` 调用传 `role_label=track.role_label`；schema_hash 由 `"v1_extra_forbid"` 改为 `f"{track.prompt_version}_extra_forbid"`（assistant 轨独立 cache 命名空间，双保险）。
3. `_commit_item_result` 增加 `track` 参数：`unit_id = track.unit_id_prefix + hashlib.sha256(...)`（:576）；`evidence_scope` 写 `track.evidence_scope`（:591，替换 "user" 字面量）；result 解析用 `track.result_model.model_validate(...)`。
4. content 加载（:720-726）按 track 分支：assistant 轨时
   - 读取锚消息 content + role（断言 role=='assistant'，不符则 status='terminal_failed' last_error_class='role_mismatch' 并计数——run 级单轨防混入）；
   - 查同 session 最近前置 1 条 eligible user 消息（`canonical_messages` 同 canonical_session_id、role='user'、timestamp/rowid 早于锚消息，取最近一条），清洗后拼入 LLM 输入为"用户问题上下文（仅供理解，不作证据）："段；无前置 user 时该段为空串（不 fail）；
   - `strip_system_injections` 后对 assistant 原文做尾部硬截：`MAX_CHARS = 12000`，`cleaned = cleaned[:12000]`，截断发生数计入 stats['truncated']（跳过/降级带计数）；**quote 回查 `_evidence_supported(unit.evidence_quote, cleaned)` 对截断后文本执行**（现有调用点已用 work["cleaned"]，保证截断语义一致）。
5. user 轨行为逐字节不变（现有测试全绿即证）。
</action>
<acceptance_criteria>
- 单测：模拟 LLM 返回（monkeypatch call_llm_with_retry），assistant track 的 run 产出的 knowledge_units 行满足：`unit_id LIKE 'as|%'`、`evidence_scope='assistant'`、unit_type ∈ 3 新类型；quote 不在截断后文本中的 unit 被丢弃且 stats['units_dropped_no_evidence'] ≥1。
- 单测：assistant run 的 run_items 混入 user ref → 该行 status='terminal_failed'、last_error_class='role_mismatch'，run 其余 items 正常。
- `pytest tests/unit/test_knowledge_unit_prod_evidence.py tests/unit/test_knowledge_unit_retry_cache.py tests/unit/test_knowledge_unit_pilot.py` 通过（user 轨零回归）。
</acceptance_criteria>
</task>

<task id="3" name="v1_assistant prompt 与 schema 文件新建">
<read_first>
- assets/prompts/knowledge_unit_extractor/v1_main.md（结构参照，**不修改本文件**——CONTEXT canonical_refs 明令平级新建）
- assets/prompts/knowledge_unit_extractor/v1_schema.md
- assets/prompts/knowledge_unit_extractor/v1_session_window.md（QA 上下文段写法参照）
</read_first>
<action>
1. 新建 `assets/prompts/knowledge_unit_extractor/v1_assistant.md`：结构对齐 v1_main.md（角色/任务/输出 JSON/禁区），关键差异：
   - speaker gate 平移（D-03）：明示"只有 role=assistant 的回答内容能证明解决方案/结论；前置用户问题仅为理解上下文，禁止作为 evidence_quote 来源、禁止抽取用户画像事实"；
   - unit_type 枚举限定 solution / decision_rationale / technical_conclusion，各给 1 行定义与 1 个例子；
   - abstain 条件：纯工具输出、临时性回答、无沉淀价值的客套；
   - evidence_quote 必须逐字摘自回答正文（10 字以上连续片段，对齐 `_evidence_supported` 口径）。
2. 新建 `assets/prompts/knowledge_unit_extractor/v1_assistant_schema.md`：照 v1_schema.md 结构，unit_type enum 换 3 新类型，extra=forbid 说明保留。
</action>
<acceptance_criteria>
- 源码断言：`grep -c "solution" assets/prompts/knowledge_unit_extractor/v1_assistant.md` ≥2 且 `grep -E "preference|personal_fact" assets/prompts/knowledge_unit_extractor/v1_assistant.md` 无命中（旧类型不进新 prompt）。
- `git diff --stat assets/prompts/knowledge_unit_extractor/v1_main.md` 为空（v1_main 未被触碰）。
- 单测：task 1 的 ASSISTANT_PROMPT_PATH.exists() 为真。
</acceptance_criteria>
</task>

<task id="4" name="prepare/extract/watermark 的 track 接线和独立 watermark key">
<read_first>
- src/personal_knowledge/application/knowledge/refresh_knowledge_units.py:1632-1790（prepare_production_delta 全体，watermark 读取 :1689-1693）、:1222-1252（get_committed_watermark / advance_watermark(key 参数已存在)）
- src/personal_knowledge/application/ku.py:100-130（prepare flags）、:258-280（watermark 子命令）、:505-530（prepare 透传）、:843-935（_cmd_watermark）
</read_first>
<action>
1. `prepare_production_delta` 增加参数 `track: str = "user"`（校验 ∈ {"user","assistant"}）；track='assistant' 时：
   - watermark 读取 key 改为 `committed_assistant`（:1690 的 `WHERE key='committed'` 参数化）；
   - roles 缺省从 `["user"]` 变为 `["assistant"]`（track 与 roles 冲突时 fail closed：roles 显式给出且不含 track 对应 role → raise ValueError）；
   - artifact dict 增加 `"track"` 字段。
2. `ku.py` prepare 子命令加 `--track {user,assistant}`（default "user"），透传到 prepare_production_delta；usage 文档串（:430-451）补一行 track 说明。
3. watermark 子命令加 `--track {user,assistant}`（default "user" → key 'committed'，assistant → key 'committed_assistant'）；`_cmd_watermark` 的 key 选择集中一处；`check_watermark_advance_preconditions`（:1255+）复用不变（fail-closed 检查读 knowledge_run_items，与 key 无关）；advance 走既有 `advance_watermark(..., key=...)`。
4. `execute_run`/`process_run` 的 track 来源：run 创建时（_materialize_delta_run 或 start_run）把 track 写入 run 行/manifest（knowledge_build_runs 已有 prompt_version 列——assistant run 写 `v1_assistant`）；`pk-ku extract --run <id>` 按 run 的 prompt_version 反查 TrackConfig（'v1_assistant'→ASSISTANT_TRACK，否则 USER_TRACK），禁止 CLI 另传 track 造成双轨混跑。
</action>
<acceptance_criteria>
- 单测（Nyquist 用例 5）：`prepare_production_delta(track="assistant", ...)` 返回 artifact 含 `"track": "assistant"`，seeded run_items 全部解析为 assistant refs（join canonical_messages.role 断言无 'user'）；run manifest 的 prompt_version=='v1_assistant'。
- 单测：track='assistant' 且 roles=['user'] 显式冲突 → ValueError。
- 单测：track='assistant' 时读取的 watermark key 为 'committed_assistant'（mock/注入 DB 预置两 key 不同值，断言 diff 基线取自 assistant key）。
- `pk-ku prepare --track assistant`（真实库 dry 路径）输出的 artifact JSON 含 track 字段且 `active_changed=false` 等安全字段全 false。
</acceptance_criteria>
</task>

<task id="5" name="assistant watermark bootstrap 迁移（R4 防全量队列）">
<read_first>
- tools/migrations/backfill_ku_data_debts.py（dry-run/计数模式）
- src/personal_knowledge/application/knowledge/refresh_knowledge_units.py:1238-1252（advance_watermark）
- docs/runbooks/ku-incremental.md（Gate A/F 语义）
</read_first>
<action>
新建 `tools/migrations/bootstrap_assistant_watermark.py`（默认 dry-run，`--write` 落库）：
1. 读 `knowledge_source_watermark` 的 `committed` 值；若 `committed_assistant` 已存在 → 打印 `no_op` 退出 0（幂等）。
2. dry-run 打印：将写入 key='committed_assistant'、value=<committed 当前值>、以及"若跳过本步骤首次 prepare --track assistant 将入队的存量 assistant 消息估算数"（用 41-01 的 `compute_eligible_messages(canonical_db, roles=("assistant",))` 计数，只输出 count）。
3. `--write` 调 `advance_watermark(db_path, committed_value, key="committed_assistant")` 落库，打印 before/after。
4. 语义注释写清：bootstrap 即 D-04 豁免的 operational 落地——存量 assistant 消息视为 ku|/v1| 世代已覆盖，assistant 轨只抽增量。
</action>
<acceptance_criteria>
- 无 `--write`：stdout 含估算 count，DB 无 `committed_assistant` 行。
- `--write` 后：`SELECT value FROM knowledge_source_watermark WHERE key='committed_assistant'` == 执行时的 committed 值；再跑一次输出 `no_op`。
- bootstrap 后跑 `pk-ku prepare --track assistant`：delta 不含 bootstrap 时刻之前的存量 assistant refs（extract_item_count 仅为 watermark 后新增量）。
</acceptance_criteria>
</task>

<task id="6" name="D-03 确认信号检测 + confidence 修饰（修饰非硬 gate）">
<read_first>
- src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py:609-760（process_run 主循环，canon_con 可用点）、:491-607（_commit_item_result）
- docs/runbooks/ku-incremental.md §3F（reconcile 的 lifecycle 路由——本 task 不接入，仅注释指向）
- 41-RESEARCH.md §2「确认信号修饰（D-03 落地形态）」
</read_first>
<action>
1. 新建 `src/personal_knowledge/application/knowledge/confirmation_signals.py`：
   - 模块顶部常量 `ADOPTION_PATTERNS` / `CORRECTION_PATTERNS` 两组编译正则（中英双语初版词表，采纳侧如 "解决了"/"可以了"/"就这样"/"谢谢"/"works"/"thanks"，纠正侧如 "不对"/"错了"/"应该是"/"其实不是"/"no,"/"wrong"——词表集中一处便于后续调）。
   - `def detect_confirmation_signal(canonical_db: Path, *, session_id: str, anchor_message_ref: str) -> str`：在 canonical_messages 中取同 session、时序上紧随锚 assistant 消息之后的第一条 role='user' 消息；无后续 user 消息 → 返回 `"none"`；命中纠正模式 → `"corrected"`；命中采纳模式 → `"adopted"`；均不命中 → `"none"`。同条消息双命中时**纠正优先**（保守）。
2. 接线（仅 assistant track）：process_run 的 assistant 分支构造 payload 时调用检测函数，结果放入 `work["confirmation_signal"]`；`_commit_item_result` 写 unit 前修饰 confidence：`adopted → confidence = min(1.0, confidence + 0.05)`；`corrected → confidence = max(0.0, confidence - 0.2)`；`none → 不变`。**不做任何丢弃/skip**（D-03：修饰非硬 gate）。stats 增加计数键 `confirmation_adopted` / `confirmation_corrected` / `confirmation_none`（跳过路径带计数，失败不静默）。
3. user 轨不调用检测函数，行为零变化。
4. 代码注释指明：corrected 行是未来 lifecycle supersede 候选，自动路由属 deferred（CONTEXT deferred 与 runbook §3F），本 task 不做。
</action>
<acceptance_criteria>
- 单测：fixture 三种后续 user 消息（采纳/纠正/无后续）→ `detect_confirmation_signal` 分别返回 adopted/corrected/none；双命中消息返回 corrected。
- 单测：assistant track 模拟抽取，adopted unit 的 confidence 相对 LLM 原值 +0.05（封顶 1.0）、corrected -0.2（封底 0.0），unit 均正常 INSERT 不被 gate 丢弃；stats 三个计数键齐全且与实际相符。
- 单测：user track 的 stats 无 confirmation 计数键（零回归）。
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `python tools/migrations/rebuild_knowledge_unit_type_check.py`（dry-run）→ `--write` → 再跑确认 `no_op`（task 0，阻断前置，先于一切 as| 写入）
2. `pytest tests/unit/test_knowledge_unit_prod_assistant_track.py -v`（task 1/2/4/6 全部单测）
3. `pytest tests/unit/test_knowledge_unit_prod_evidence.py tests/unit/test_knowledge_unit_retry_cache.py tests/unit/test_knowledge_unit_pilot.py tests/unit/test_knowledge_unit_gate.py -q`（user 轨零回归）
4. `python tools/migrations/bootstrap_assistant_watermark.py`（dry-run 计数）→ `--write`
5. `pk-ku prepare --track assistant` → artifact 检查（track 字段、extract_item_count、安全字段全 false）
6. `pk-ku extract --run <ir_*> --max-items 20`（pilot smoke，**Vertex 付费步骤，执行时需人工批准**）→ `pk-ku status` 查看 succeeded/abstained/terminal_failed 与 confirmation_* 计数分布
7. `pk-ku doctor --skip-ports` exit 0
</verification>

<must_haves>
- unit_type CHECK 表重建迁移完成（knowledge_units + canonical_knowledge_units 两表），行数守恒 + 外键完好 + 幂等 no_op，SCHEMA_SQL 同步（task 0，阻断前置）
- run 级单轨成立：一个 run 的 items 全为同一 role，manifest prompt_version 标识轨身份（'v1' / 'v1_assistant'）
- `as|` 前缀恰好 3 字符（构造期断言），promote 后 v1|/l2|/ku| current 行零触碰（单测 + 41-04 端到端双保险）
- assistant 轨产出 `evidence_scope='assistant'`，quote 回查锚截断后 assistant 原文（D-03）
- 确认信号检测落地且只做 confidence 修饰（adopted +0.05 / corrected -0.2，封顶封底），无任何硬 gate 丢弃；confirmation_* 三计数齐全（D-03，task 6）
- `committed_assistant` watermark key 存在且 bootstrap 后首次 prepare 不产全量队列（R4 闭环）
- user 轨行为与 cache 命名空间不变：既有 prod 测试零修改全绿
- 不新建 ledger 模块：assistant 轨走 knowledge_run_items + Gate F fail-closed 全套保护
</must_haves>

<threat_model>
- 隐私面：assistant 内容是模型输出，可能复述用户敏感信息；prompt 必须禁止抽取用户画像事实（speaker gate 平移），产出 unit 的 evidence_quote 只锚 assistant 原文。QA 对构造中前置 user 消息进入 LLM 输入（付费 API 出域）——与 user 轨现有出域同级，但 stats/日志不得记录该上下文原文，只计 `truncated` 等计数。
- 误伤面：`role_mismatch` 的 terminal_failed 走 knowledge_run_items 既有 ledger，watermark advance 的 fail-closed 前置检查（terminal_failed 需 --acknowledge-failures）会拦住未处理行——不得为绕过该检查而改 status 语义。
- 成本面：assistant 存量 ~73k 条；缺 bootstrap 时首次 prepare 即全量队列撞 Gate A——bootstrap 迁移是本 plan 的硬性前置，verification 第 3 步不可跳过。
</threat_model>
