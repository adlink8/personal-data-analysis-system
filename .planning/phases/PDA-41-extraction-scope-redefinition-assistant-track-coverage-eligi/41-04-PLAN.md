# Plan 41-04: eval 集 + 下游贯通（EvidenceResolver scope 修复 + 端到端 promote 演练）

```yaml
wave: 3
depends_on: [41-01, 41-02, 41-03]
autonomous: true
requirements: [EXT-01, EXT-02, EXT-03]
files_modified:
  - src/personal_knowledge/retrieval/evidence.py
  - src/personal_knowledge/evaluation/knowledge/evaluate_knowledge_unit_rag.py
  - src/personal_knowledge/evaluation/knowledge/evaluate_knowledge_unit_extraction.py
  - src/personal_knowledge/application/knowledge/build_knowledge_unit_vector_store.py
  - var/eval/frozen_test_assistant.private.jsonl                          # 新建，20 条（路径以 EVAL_DIR 实际为准）
  - tests/unit/test_knowledge_unit_rag_eval.py
  - tests/unit/test_evidence_resolver_scope.py                            # 新建
```

<objective>
打通 assistant 轨的"最后一公里"：新建 20 条 assistant 轨 eval 集（gold refs 锚 assistant 消息），接入 evaluate CLI；extract gate 的 Gate 8 补 assistant 方向对称检查；修复 EvidenceResolver 对 assistant scope 消息的 veto（retrieval/evidence.py:80-81——RESEARCH R3 唯一必改下游）；vector store metadata 补 evidence_scope 供检索层按轨过滤；merge 阈值随 eval 基线记录校准建议；最后做一次完整 prepare→extract(pilot)→gate→canonical→publish→vector→evaluate→canary --strict→promote→watermark(assistant key)→doctor 的端到端演练，证明 EXT-01/02/03 在真实链路上同时成立。
</objective>

<context>
- 锁定决策：**D-01**（eval 集独立，20 条起步——CONTEXT discretion 明示沿用 20 条级）；**D-02**（检索层按 scope/unit_type 过滤的承载点：vector metadata）；**D-03**（eval 的 expected_abstain 行验证"无确认信号不硬杀"）；**D-04**（ku| 豁免行不进入 eval gold）。
- R3 影响面表（RESEARCH §6）结论：evidence.py:80-81 是**唯一必改**；relevance.py 黑名单不含 'assistant' 不会误伤（不改）；review_packets.py:482-483 只取 scope='user'（本 phase 不改，记 follow-up）；evaluate 的 ref 并集匹配（evaluate_knowledge_unit_rag.py:496-505）不关心 role（不改）。
- eval 集格式照抄 frozen_test_queries.private.jsonl；`_load_eval_dataset`（:49-59）else 分支已支持任意 `{name}.private.jsonl`，CLI `--dataset` choices（:580）需加 'frozen-test-assistant'（1-2 行）。
- snippet 兜底风险（RESEARCH §5）：`content[:200]` 对 5100 字符 assistant 消息命中率低——标注时保证 gold ref 匹配为主路径，不改评估代码。
- 依赖 41-02（抽取器产出 as| units 供 pilot）、41-03（doctor coverage check 在端到端最后一步验收）。
</context>

<tasks>

<task id="1" name="EvidenceResolver scope 放宽（必改点）">
<read_first>
- src/personal_knowledge/retrieval/evidence.py:66-91（_message 的 eligible 判定链）
- src/personal_knowledge/application/knowledge/doctor_ku.py:340-370（evidence_resolver check 对 ineligible 的容忍语义）
- tests/unit/ 下既有 evidence resolver 相关测试（grep "EvidenceResolver" 定位）
</read_first>
<action>
改 `src/personal_knowledge/retrieval/evidence.py:80-81`：message 级证据的 scope 判定从 `str(row["evidence_scope"] or "user") == "user"` 放宽为 `in ("user", "assistant")`——assistant scope 消息的 resolve 结果从 ineligible 变为 ok。**红线不动**：session 级 `evidence_eligible=0`（:75-79）与 `is_system`（:82-83）的 veto 保持原样——证据下钻放行不能越过 ineligible session 红线。
新增 `tests/unit/test_evidence_resolver_scope.py`（Nyquist 用例 10）：fixture canonical DB 造 4 条消息（user scope eligible session、assistant scope eligible session、assistant scope ineligible session、is_system=1），断言 resolve 结果分别为 ok / ok / ineligible / ineligible。
</action>
<acceptance_criteria>
- `pytest tests/unit/test_evidence_resolver_scope.py -v` 4 断言全过。
- 既有 evidence resolver 测试与 `pytest tests/unit/test_doctor_ku.py` 通过（doctor 的 evidence_resolver check 不误报）。
- 源码断言：`grep -n "in (\"user\", \"assistant\")" src/personal_knowledge/retrieval/evidence.py` 有命中。
</acceptance_criteria>
</task>

<task id="2" name="assistant eval 集（20 条）+ CLI 接入">
<read_first>
- src/personal_knowledge/evaluation/knowledge/evaluate_knowledge_unit_rag.py:40-60（EvalQuery 结构 + _load_eval_dataset）、:578-584（CLI choices）
- 现有 eval 数据文件（EVAL_DIR 下 frozen_test_queries.private.jsonl 前 3 行实测结构）
- RESEARCH §5（标注策略与字段）
</read_first>
<action>
1. 定位 EVAL_DIR 实际路径（读 evaluate_knowledge_unit_rag.py 顶部常量），新建 `frozen_test_assistant.private.jsonl`，20 行，字段对齐 frozen 集并扩展：`id`（asst-001..asst-020）、`split: "frozen_test_assistant"`、`query`（用户会提的检索问题）、`gold_evidence_refs`（assistant 消息的 canonical_message_id，**逐条在 canonical DB 中核实存在**）、`allowed_unit_types: ["solution","decision_rationale","technical_conclusion"]`、`expected_abstain`、`expected_conflict: false`、`group: "assistant_track"`、`agent`、`started_at`。构成：15 正例（ku| 世代来源会话优先，内容已实测 ~90% 为真）+ 3 应 abstain（纯工具输出/临时回答，expected_abstain=true，驱动 no_answer_false_positive 指标）+ 2 跨源对照（不同 agent 的同类问题）。
2. `evaluate_knowledge_unit_rag.py:580` 的 `--dataset` choices 加 `"frozen-test-assistant"`；`_load_eval_dataset` 无需改（else 分支按 `{name}.private.jsonl` 解析，确认 name 'frozen-test-assistant' 命中正确文件名，必要时在函数内加一行显式映射）。
</action>
<acceptance_criteria>
- 单测（Nyquist 用例 9，加入 tests/unit/test_knowledge_unit_rag_eval.py）：`_load_eval_dataset("frozen-test-assistant")` 返回 20 条；每条 9 个字段齐全；`allowed_unit_types` 与 6 旧类型无交集；expected_abstain=true 恰 3 条；gold refs 在真实 canonical DB `SELECT canonical_message_id FROM canonical_messages` 全部存在（该用例可标 integration/skip-if-missing）。
- `python -m personal_knowledge.evaluation.knowledge.evaluate_knowledge_unit_rag --help` 的 choices 含 frozen-test-assistant。
</acceptance_criteria>
</task>

<task id="3" name="Gate 8 对称检查 + vector metadata 补 scope">
<read_first>
- src/personal_knowledge/evaluation/knowledge/evaluate_knowledge_unit_extraction.py:216-229（Gate 8 speaker_attribution）
- src/personal_knowledge/application/knowledge/build_knowledge_unit_vector_store.py:330-341（metadata 构造）、:214-232（load_eligible_units 不按 scope 过滤——期望行为，不改）
- tests/unit/test_knowledge_unit_gate.py（gate 测试模式）
</read_first>
<action>
1. `evaluate_knowledge_unit_extraction.py` Gate 8 区块后追加对称检查：`SELECT COUNT(*) FROM knowledge_units WHERE run_id=? AND unit_type IN ('solution','decision_rationale','technical_conclusion') AND evidence_scope != 'assistant'`，`GateCheck(name="speaker_attribution_assistant", passed=count==0, ...)`；原 speaker_attribution 保持不动。
2. `build_knowledge_unit_vector_store.py` metadata 构造（:330-341）增加 `"evidence_scope"` 字段（取 knowledge_units 同名列，缺省 "user"）——D-02 的检索层按轨过滤承载点；unit_type 已在 metadata（确认，若无则一并补）。`load_eligible_units` 不按 scope 过滤的行为**不改**（as| units 进候选索引是期望行为）。
</action>
<acceptance_criteria>
- 单测：fixture 造 1 行 unit_type='solution' 且 evidence_scope='user' 的 unit → 新 GateCheck failed；evidence_scope='assistant' → passed。user 类型 6 种错配仍被原 Gate 8 拦（回归）。
- `pytest tests/unit/test_knowledge_unit_gate.py tests/unit/test_build_knowledge_unit_vector_store.py tests/unit/test_knowledge_unit_vector_store.py -q` 通过。
- 单测：vector metadata dict 含 evidence_scope 键。
</acceptance_criteria>
</task>

<task id="4" name="端到端演练 + merge 阈值基线记录">
<read_first>
- docs/runbooks/ku-incremental.md（Gate A/B/E/F、canary/promote/watermark 顺序）
- .planning/phases/PDA-41-.../41-02-PLAN.md（track 接线与 bootstrap）
- var/reports/analysis/ai_context/ku_canary_gate_salvage_20260726.json（现役 canary gate 基线格式，Recall@5 0.65）
</read_first>
<action>
按 runbook 顺序执行一次 assistant 轨完整链路（**LLM/付费步骤需人工批准**，autonomous 执行到付费门前暂停汇报）：
1. `pk-ku inspect`——41-01 切换后首次，delta 解读为口径修正（R6），记录数值。
2. `python tools/migrations/bootstrap_assistant_watermark.py --write`（若 41-02 未做）。
3. `pk-ku prepare --roles assistant --track assistant`——artifact 的 extract_item_count 符合预期（增量而非全量），active_changed=false 等安全字段全 false。
4. `pk-ku extract --run <ir_*> --max-items 20`（pilot smoke）→ `pk-ku status`：记录 succeeded/abstained/terminal_failed/units_dropped_no_evidence 分布；role_mismatch 应为 0。
5. `pk-ku extract-gate`（含对称 Gate 8）全过 → `pk-ku canonical --write` → `pk-ku publish --write` → `pk-ku vector --write`。
6. promote 前缀隔离验收（Nyquist 用例 6 的端到端版）：promote 前后 `SELECT substr(unit_id,1,3), status, COUNT(*) FROM knowledge_units GROUP BY 1,2` 对比——v1|/l2|/ku| 的 current 计数不变，as| 由 staging→current。
7. 跑 assistant eval 集 `evaluate_candidate`：记录 Recall@5 等指标到 `var/reports/analysis/ai_context/ku_canary_gate_assistant_track_<date>.json`（格式对齐 20260726 基线），**首轮只记录不设阈值**（校准用途）；同时记录 4-gram 相似度分布观察，给出 `MERGE_SIMILARITY_THRESHOLD` 是否需调的结论（R5，只出结论不改值）。
8. `pk-ku canary … --strict` PASS → `pk-ku promote --require-eval-pass` → `pk-ku watermark --advance --track assistant --from-canonical --write`（写 committed_assistant，**不是** committed）。
9. `pk-ku doctor --skip-ports`：coverage_matrix check 出现、as| pass 族在矩阵中显形、整体 exit 0。
10. 演练报告（count-only）附到 phase 目录执行笔记。
</action>
<acceptance_criteria>
- 步骤 6 的前后 SQL 对比：v1|/l2|/ku| current 计数差为 0（promote 隔离铁证）。
- 步骤 8 后 `SELECT key FROM knowledge_source_watermark` 同时存在 committed 与 committed_assistant，且 committed 值未被本次演练改变。
- 步骤 9 doctor exit 0 且 coverage 矩阵含 as| 覆盖行。
- eval 报告 JSON 存在于 var/reports/analysis/ai_context/，含 Recall@5 数值。
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `pytest tests/unit/test_evidence_resolver_scope.py tests/unit/test_knowledge_unit_rag_eval.py tests/unit/test_knowledge_unit_gate.py tests/unit/test_build_knowledge_unit_vector_store.py tests/unit/test_knowledge_unit_vector_store.py tests/unit/test_doctor_ku.py -q`
2. `pk-ku doctor --skip-ports` exit 0
3. 端到端演练 10 步（task 4）全部完成，报告落盘
</verification>

<must_haves>
- assistant scope 消息的证据下钻 resolve → ok（retrieval/evidence.py 是唯一改动点；relevance/review_packets 等其余下游确认不动）
- ineligible session / is_system 的 resolve veto 红线不变（单测锁死）
- 20 条 assistant eval 集可被 CLI 加载，gold refs 全部真实存在，3 条 expected_abstain 驱动 no_answer_false_positive 指标
- Gate 8 双向对称：user 类型必须 scope='user'（旧）、assistant 3 类型必须 scope='assistant'（新）
- vector metadata 含 evidence_scope（D-02 过滤承载点）
- 端到端 promote 演练证明：as| 族隔离（v1|/l2|/ku| 零触碰）、watermark 双 key 分立、doctor coverage 显形 as|
- eval 首轮只记录基线不预设阈值（校准语义写进报告）
</must_haves>

<threat_model>
- 隐私面：evidence.py 的 scope 放行是本 phase 最大的下钻面扩张——assistant 内容（模型输出）自此可被证据链下钻展示。放行边界严格限定在 `evidence_scope in ('user','assistant')` 一行：session 级 evidence_eligible=0 与 is_system=1 的 veto 一字不动，单测 4 象限锁死；'system'/'sidechain'/'subagent' 等其他 scope 值依然 ineligible。
- 误伤面：eval 集 gold refs 若指向已消失消息会让 Recall 指标失真——验收强制逐条核实存在；snippet 兜底（content[:200]）对长 assistant 消息天然低命中，首轮指标只作校准基线，不得据此判定回归。
- 运营面：端到端演练含付费 LLM 调用与 promote/watermark 写操作——autonomous 模式下执行到 extract（付费门）前必须暂停并请求人工批准；watermark 只动 committed_assistant，committed（user 轨）被演练改变的任何迹象都是阻断性 bug。
</threat_model>
