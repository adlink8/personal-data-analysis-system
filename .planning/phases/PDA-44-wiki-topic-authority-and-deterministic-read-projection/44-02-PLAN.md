---
phase: 44
plan: 02
status: complete
---

# Phase 44-02 Plan — Authority-bound read service

将 v1.5 预规划 Phase 41 映射为当前活动 Phase 44：在既有 Personal State、Decision、External read authority 之上实现 TopicKey 驱动的 `topic.list`、`topic.get`、`topic.backlinks` 与 `topic.resolve`。本批次禁止 provider、检索、写入、浏览器端重建和第二事实库。

## Verification

- Python unit/contract tests cover valid/invalid keys, typed errors, snapshot/freshness/partial envelope and explicit-only backlinks。
- HTTP contract tests verify GET-only transport and 405 for POST/PUT/PATCH/DELETE。
- Real local preflight records authority truth separately：Decision 1 条可投影；Personal State 因 `run_missing` 不发布 Project/Goal。
