---
phase: 44
plan: 01
type: execute
wave: 1
depends_on: ["44-00"]
files_modified:
  - src/personal_knowledge/services/topic_projection.py
  - tests/unit/test_topic_projection_keys.py
autonomous: true
---

# Phase 44-01: Wiki contract foundation

## Objective

建立与 v1.4 权威服务隔离的 Wiki 投影契约基础，供后续只读适配器复用。

## Scope

- 固定 `personal_wiki_projection_v1`、三个只读 operation 和安全 reason code。
- 严格解析并规范化 `project`、`goal`、`decision` TopicKey。
- 提供不查询任何权威的 Wiki read envelope builder。
- 用单元测试覆盖 URL 解码、重复解码、分隔符、空段、控制字符和错误信封。

## Hard boundaries

本计划不修改数据库、provider、Chroma、REST 路由、前端或 v1.4 权威逻辑；不创建新的 SSOT。

## Verification

`python -m pytest tests/unit/test_topic_projection_keys.py -q`
