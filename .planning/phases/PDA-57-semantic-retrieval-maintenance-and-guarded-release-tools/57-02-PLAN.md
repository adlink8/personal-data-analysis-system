---
phase: 57
plan: 02
type: execute
wave: 2
depends_on: [57-01]
files_modified: [src/personal_knowledge/services/snapshot_release_tools.py, governance/manifests/capabilities/project-capabilities.json, tests/contract/test_pi_guarded_write_tools.py, tests/e2e/test_pi_snapshot_release.py, .planning/phases/PDA-57-semantic-retrieval-maintenance-and-guarded-release-tools/57-RELEASE-DRILL.md]
autonomous: false
requirements: [WARE-04, PTOOL-02]
must_haves:
  truths:
    - "D-04: release preview freezes manifest/checksum/eval/fingerprints and L3 confirmation."
    - "D-05: active pointer switch is atomic and exactly reversible."
    - "D-06: activate/rollback use one guarded transaction protocol."
---

# Phase 57-02 Plan — Guarded snapshot release and exact rollback

<objective>提供 snapshot prepare/activate/rollback Tool，并以临时环境和授权 live drill 证明原子恢复。</objective>
<threat_model>Assets: serving snapshot, active pointer and rollback target. Threats: stale/tampered preview, failed eval promotion, split pointer, wrong rollback and automatic L3 action. Controls: frozen manifest, explicit confirmation, CAS pointer, atomic replace, fingerprints, append-only ledger and manual live checkpoint.</threat_model>

<tasks>
<task type="execute">
  <name>57-02-01 — Implement shared guarded-write and snapshot release contracts</name>
  <read_first>
    - src/personal_knowledge/services/warehouse_mutations.py
    - src/personal_knowledge/services/retrieval_maintenance_tools.py
    - src/personal_knowledge/application/serving/
    - .planning/research/v2.0-pi-capability-os/ARCHITECTURE.md
  </read_first>
  <files>src/personal_knowledge/services/snapshot_release_tools.py, governance/manifests/capabilities/project-capabilities.json, tests/contract/test_pi_guarded_write_tools.py</files>
  <action>Factor one `plan/dry_run/preview/confirm/execute/verify/receipt/compensate` contract and add `snapshot.prepare`, `snapshot.activate`, `snapshot.rollback`. Prepare freezes generation, manifest checksum, reconcile zeros, accepted evaluation checksum, current/target pointer and protected fingerprints. Activate/rollback require unexpired exact preview, actor confirmation phrase, CAS current pointer and idempotency; automatic code may only downgrade to the frozen rollback target.</action>
  <acceptance_criteria>
    - Tampered/stale/failed-eval/wrong-pointer preview writes zero state.
    - Duplicate activation/rollback returns original receipt.
    - Guarded-write contract tests cover every state transition and exit 0.</acceptance_criteria>
  <verify><automated>python -m pytest tests/contract/test_pi_guarded_write_tools.py -q</automated></verify>
</task>
<task type="checkpoint:human-action">
  <name>57-02-02 — Prove forced failure and exact snapshot rollback</name>
  <read_first>
    - src/personal_knowledge/services/snapshot_release_tools.py
    - tests/contract/test_pi_guarded_write_tools.py
    - .planning/phases/PDA-57-semantic-retrieval-maintenance-and-guarded-release-tools/57-VALIDATION.md
  </read_first>
  <files>tests/e2e/test_pi_snapshot_release.py, .planning/phases/PDA-57-semantic-retrieval-maintenance-and-guarded-release-tools/57-RELEASE-DRILL.md</files>
  <action>First run activate/failure/rollback against a temporary serving root, injecting failure before pointer write, after temporary file, and immediately after atomic replace. Verify restart reconciliation and exact prior active restoration. Present a live-pointer dry-run with exact current/target IDs and fingerprints; execute the bounded live drill only after explicit user authorization, otherwise record blocked without changing live state.</action>
  <acceptance_criteria>
    - Temporary E2E suite passes every crash window with one active pointer and preserved authority fingerprints.
    - Live drill cannot start without explicit authorization and exact preview checksum.
    - RELEASE-DRILL records temporary evidence and either signed live result or honest blocked status.</acceptance_criteria>
  <verify><automated>python -m pytest tests/e2e/test_pi_snapshot_release.py -q</automated></verify>
</task>
</tasks>

<verification>Automated fixture release must pass; live pointer operation remains a separate explicit checkpoint.</verification>
<success_criteria>Pi can prepare and execute governed releases without acquiring direct promotion authority, and exact rollback is proven.</success_criteria>
