---
phase: 57
plan: 01
type: execute
wave: 1
depends_on: [56-02]
files_modified: [src/personal_knowledge/services/semantic_maintenance_tools.py, src/personal_knowledge/services/retrieval_maintenance_tools.py, governance/manifests/capabilities/project-capabilities.json, tests/integration/test_pi_semantic_maintenance.py, tests/integration/test_pi_retrieval_maintenance.py]
autonomous: true
requirements: [WARE-03, PTOOL-02]
must_haves:
  truths:
    - "D-01: Pi may build derived artifacts but cannot bypass evaluation/promotion."
    - "D-02: semantic writes are evidence/model/schema-bound Candidates."
    - "D-03: retrieval reconcile is zero missing/orphan/duplicate."
    - "D-06: all writes use the shared guarded transaction state machine."
---

# Phase 57-01 Plan — Semantic and retrieval maintenance Tools

<objective>将抽取、修复、backfill、索引构建、reconcile 和 evaluation 接入统一受控 Tool 面。</objective>
<threat_model>Assets: semantic inventory, retrieval generation and evaluation truth. Threats: unsupported Candidate, cross-snapshot mixing, partial index, silent gate bypass and duplicate model/build side effects. Controls: evidence/model receipts, generation isolation, operation ledger, reconcile/eval typed gates and idempotency.</threat_model>

<tasks>
<task type="execute">
  <name>57-01-01 — Wrap semantic extraction, repair and backfill as staged Tools</name>
  <read_first>
    - src/personal_knowledge/application/knowledge/build_knowledge_units.py
    - src/personal_knowledge/application/knowledge/extract_knowledge_units_l2_session.py
    - src/personal_knowledge/services/warehouse_mutations.py
    - governance/manifests/capabilities/project-capabilities.json
  </read_first>
  <files>src/personal_knowledge/services/semantic_maintenance_tools.py, governance/manifests/capabilities/project-capabilities.json, tests/integration/test_pi_semantic_maintenance.py</files>
  <action>Add `knowledge.extract_l1`, `knowledge.extract_l2`, `knowledge.repair_candidates`, `knowledge.detect_conflicts` and `knowledge.backfill` operations. Require logical source scope, snapshot/watermark, bounded batch, extractor/model route and evidence refs. Write only Candidate/staging through operation ledger; reject serving/lifecycle/promotion fields and require schema/privacy/evidence receipts before success.</action>
  <acceptance_criteria>
    - Every generated record is staged and has source evidence, model/extractor receipt and schema version.
    - Unsupported, cross-snapshot or promotion-shaped output is rejected with zero inventory/active change.
    - Duplicate idempotency returns the same Candidate IDs and no second model call.</acceptance_criteria>
  <verify><automated>python -m pytest tests/integration/test_pi_semantic_maintenance.py -q</automated></verify>
</task>
<task type="execute">
  <name>57-01-02 — Wrap index build, reconcile and evaluation as isolated generation Tools</name>
  <read_first>
    - src/personal_knowledge/application/serving/
    - src/personal_knowledge/retrieval/
    - src/personal_knowledge/evaluation/
    - src/personal_knowledge/services/semantic_maintenance_tools.py
  </read_first>
  <files>src/personal_knowledge/services/retrieval_maintenance_tools.py, governance/manifests/capabilities/project-capabilities.json, tests/integration/test_pi_retrieval_maintenance.py</files>
  <action>Add `index.build`, `index.reconcile` and `index.evaluate` operations. Build always targets a new generation ID bound to one approved semantic snapshot. Reconcile emits exact missing/orphan/duplicate counts and blocks evaluation unless all are zero. Evaluation invokes frozen policy and returns pass/fail plus evidence checksum; no operation writes active pointer.</action>
  <acceptance_criteria>
    - Failed/interrupted build cannot alter active generation.
    - Nonzero reconcile counts block evaluation/release readiness.
    - Test suite proves deterministic generation receipt and unchanged canonical/watermark/active fingerprints.</acceptance_criteria>
  <verify><automated>python -m pytest tests/integration/test_pi_retrieval_maintenance.py -q</automated></verify>
</task>
</tasks>

<verification>Run semantic and retrieval suites through Pi gateway against isolated fixture stores.</verification>
<success_criteria>Pi can maintain semantic and retrieval artifacts while every promotion boundary remains deterministic and closed.</success_criteria>
