---
phase: 36
plan: 04
type: execute
wave: 3
depends_on: [36-01, 36-02, 36-03]
files_modified:
  - apps/personal_decision_cockpit/.gitignore
  - apps/personal_decision_cockpit/README.md
  - docs/runbooks/decision-cockpit.md
  - .planning/phases/PDA-36-secure-projection-and-cockpit-baseline/36-VERIFICATION.md
autonomous: true
requirements: [CCK-04]
must_haves:
  truths:
    - "D-36-07：Cockpit、Projection 与其契约测试在通过实际 build/test 前均是 WIP；文档不会把 Phase 36–40 或 v1.4 标成已交付。"
    - "审计基线只纳入 Cockpit 源码、锁文件、受控 fixtures、Projection、相关 contracts 与运行文档；不纳入 `node_modules`、`dist`、运行状态、数据库、凭据或无关工作区改动。"
    - "构建/测试说明可在 Windows PowerShell 从项目根复现，且明确 `/app` 由现有 Python REST 托管、浏览器不拥有事实 authority。"
    - "Phase 36 验证记录仅报告实际执行的命令、结果、失败/限制与文件范围，不伪造 Phase 40 浏览器 UAT 或发布结论。"
  artifacts:
    - path: apps/personal_decision_cockpit/README.md
      provides: Cockpit 本地构建、测试、REST `/app` 托管与安全边界说明
    - path: docs/runbooks/decision-cockpit.md
      provides: 可审计的运行、故障检查与非破坏性恢复指引
    - path: .planning/phases/PDA-36-secure-projection-and-cockpit-baseline/36-VERIFICATION.md
      provides: Phase 36 实测证据、需求追踪和未完成范围
  key_links:
    - from: apps/personal_decision_cockpit/README.md
      to: src/personal_knowledge/services/api_server.py `/app`
      via: Vite build artifact 由既有 REST 进程托管而非新增 Node 生产服务
    - from: docs/runbooks/decision-cockpit.md
      to: 36-VERIFICATION.md
      via: 每次验证记录对应的命令、边界与恢复事实
---

<objective>把已修复的 Cockpit、Projection 与契约验证收口为可审计但不夸大完成度的 Phase 36 基线。</objective>

<threat_model>
- T-36-10：未跟踪的 Cockpit WIP、生成的 build artifact 或 node_modules 被误纳入基线，导致不可复现或污染仓库。缓解：最小文件清单与 `.gitignore` 复核。
- T-36-11：README/规划文档将通过局部测试的代码宣称为整套 v1.4 或浏览器 UAT 已交付。缓解：明确 Phase 36 范围、真实命令结果和后续验收边界。
- T-36-12：运行说明诱导用户通过前端启动/停止服务或将 `dist` 当成个人数据 authority。缓解：runbook 明确只读展示、REST `/app` 托管和现有 supervisor 所有权边界。
- 阻断条件：验证记录缺少真实命令/失败信息、将 Phase 40 UAT 伪报为完成，或基线包含 `node_modules`、数据库、凭据、运行 PID/state 文件，则本计划不得通过。
</threat_model>

<tasks>
<task type="execute"><name>界定可跟踪 Cockpit 基线与忽略边界</name><files>apps/personal_decision_cockpit/.gitignore; apps/personal_decision_cockpit/README.md</files><read_first>36-CONTEXT.md; apps/personal_decision_cockpit/.gitignore; apps/personal_decision_cockpit/package.json; apps/personal_decision_cockpit/README.md; .gitignore; git status --short</read_first><action>审计 `apps/personal_decision_cockpit/` 的文件清单：纳入 source、tests、受控 fixtures、package.json、package-lock.json、Vite/TypeScript/Tailwind 配置和 README；保持 `node_modules/`、`dist/`、coverage、临时浏览器输出与本机状态被忽略。只对 Cockpit 自己的 `.gitignore` 做最小必要调整，不覆盖根 `.gitignore` 的既有用户改动，不触碰数据库、`ops/state/`、凭据或其他未跟踪目录。README 需明确这些文件在本计划开始前是 implementation candidate，只有 Phase 36 验证通过后才构成基线。</action><acceptance_criteria>`git check-ignore -v` 证明 `node_modules/`、`dist/` 与 coverage 被忽略；`git status --short -- apps/personal_decision_cockpit` 可区分应审计的源文件与忽略的生成物；没有把数据库、PID、secret、运行日志或不相关 worktree 文件加入 Cockpit 基线。</acceptance_criteria><verify>git check-ignore -v apps/personal_decision_cockpit/node_modules apps/personal_decision_cockpit/dist; git status --short -- apps/personal_decision_cockpit</verify></task>

<task type="execute"><name>编写可复现且不越权的 Cockpit 运行说明</name><files>apps/personal_decision_cockpit/README.md; docs/runbooks/decision-cockpit.md</files><read_first>apps/personal_decision_cockpit/package.json; apps/personal_decision_cockpit/vite.config.ts; src/personal_knowledge/services/api_server.py; docs/runbooks/product-sync.md; docs/AGENTS.md; 36-RESEARCH.md</read_first><action>在 Cockpit README 与新增 `docs/runbooks/decision-cockpit.md` 写明 Windows PowerShell 的依赖安装、`npm run test`、`npm run build`、由现有 Python REST 提供 `/app` 的访问前提，以及 transport/projection 的安全边界。记录：浏览器只读消费 server-owned Projection；唯一的 UI 写意图仍走既有 `project + low` guarded session contract；前端不启动/停止 REST/MCP/Tunnel/Chroma，不直接访问 SQLite/Chroma，不持久化个人 payload；CORS 开发 allowlist 的配置方式与安全故障提示。不要记录真实 token、个人数据、snapshot 全量内容、运行 PID 或把未来页面/UAT 写成完成。</action><acceptance_criteria>两份文档均给出可复制的 PowerShell test/build 命令、`/app` 托管责任和同源边界；runbook 不包含密钥/个人 payload/具体本机 PID；文本明确 Phase 36 不等于 v1.4 shipped，Phase 37–40 和 Wiki 仍未验收。</acceptance_criteria><verify>rg -n "npm run test|npm run build|/app|same-origin|project \+ low|not shipped|Phase 40|Wiki" apps/personal_decision_cockpit/README.md docs/runbooks/decision-cockpit.md</verify></task>

<task type="execute"><name>执行受限基线验证并记录事实证据</name><files>.planning/phases/PDA-36-secure-projection-and-cockpit-baseline/36-VERIFICATION.md; apps/personal_decision_cockpit/README.md; docs/runbooks/decision-cockpit.md</files><read_first>36-CONTEXT.md; 36-RESEARCH.md; 36-01-PLAN.md; 36-02-PLAN.md; 36-03-PLAN.md; .planning/REQUIREMENTS.md; .planning/ROADMAP.md</read_first><action>在完成前三份计划后，按实际运行顺序执行 Cockpit Vitest、Vite build、Projection Python contracts、transport security contracts 与 `git diff --check`。把命令、退出状态、覆盖的 CCK-01..04、authority/Provider/external/promotion 零副作用断言、失败（如有）及恢复方式写入 `36-VERIFICATION.md`；未运行的浏览器 UAT、响应式/无障碍、实时服务故障和后续页面功能必须明确标为 Phase 40/后续事项。验证文件只引用稳定 IDs/安全摘要，不复制个人 response body、token、HMAC、路径或秘密。</action><acceptance_criteria>`36-VERIFICATION.md` 对 CCK-01..04 逐项列出可复现命令与真实 PASS/FAIL/未运行状态；至少包含 `npm run test`、`npm run build`、四个 UI Projection Python contract 文件、transport security test、orchestration interface test 和 `git diff --check` 的结果；没有把未执行项写成 passed，也不泄露敏感内容。</acceptance_criteria><verify>Set-Location apps/personal_decision_cockpit; npm run test; npm run build; Set-Location ../..; $env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_cockpit_transport_security.py tests/contract/test_ui_projection.py tests/contract/test_ui_projection_state_external.py tests/contract/test_ui_projection_decision.py tests/contract/test_ui_projection_actions_proactive.py tests/contract/test_orchestration_interfaces.py -q; git diff --check</verify></task>
</tasks>

<verification>Get-Content .planning/phases/PDA-36-secure-projection-and-cockpit-baseline/36-VERIFICATION.md; git check-ignore -v apps/personal_decision_cockpit/node_modules apps/personal_decision_cockpit/dist; git diff --check</verification>

<success_criteria>CCK-04 的 Cockpit 基线可被审计和复现：只包含有意纳入的源/测试/文档，准确记录 Phase 36 验证，而不声称后续 v1.4、Wiki 或真实浏览器 UAT 已完成。</success_criteria>
