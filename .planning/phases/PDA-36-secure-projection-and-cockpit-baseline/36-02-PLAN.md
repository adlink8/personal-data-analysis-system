---
phase: 36
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personal_knowledge/services/ui_projection.py
  - tests/contract/test_ui_projection.py
  - tests/contract/test_ui_projection_state_external.py
  - tests/contract/test_ui_projection_decision.py
  - tests/contract/test_ui_projection_actions_proactive.py
autonomous: true
requirements: [CCK-01, CCK-03]
must_haves:
  truths:
    - "D-36-01/D-36-02：所有 `/ui/*` 聚合仍由 `CockpitProjectionService` 以 `mode=ro` 与 `query_only` 完成；Projection 不调用 Provider、promotion、lifecycle mutation 或外部动作。"
    - "D-36-05：所有 `decision_cockpit_projection_v1` envelope 都含版本、operation、snapshot bindings、freshness、authorities、partial、limitations 与 data 的稳定语义。"
    - "D-36-06：authority 失败时公开结果只含 allowlisted safe code/message；路径、PII、密钥、provider body、confirmation/HMAC 与异常原文不进入 API envelope。"
    - "真实 authority vocabulary 保持 `confirmation_state ∈ {proposed, accepted, rejected, deferred, revoked}` 与 proactive `importance.final_score`，不由 Projection 另造状态。"
  artifacts:
    - path: src/personal_knowledge/services/ui_projection.py
      provides: 物理只读、版本化且安全限制信息的 Cockpit Projection v1
    - path: tests/contract/test_ui_projection.py
      provides: 基础 envelope、只读和公开错误回归
    - path: tests/contract/test_ui_projection_state_external.py
      provides: Personal/External partial 与安全降级回归
  key_links:
    - from: CockpitProjectionService._collect
      to: 公共 limitation mapper
      via: authority 异常只生成 stable safe code/message 和 partial metadata
    - from: CockpitProjectionService._envelope
      to: 所有 `/ui/*` REST operation
      via: Python v1 envelope 是浏览器 DTO 的唯一事实来源
---

<objective>把 Cockpit Projection 固化为物理只读、版本化且不泄露异常细节的服务端 DTO 权威。</objective>

<threat_model>
- T-36-04：浏览器 Projection 在聚合时绕过 authority、触发 Provider/生命周期/promotion 或变更事实。缓解：保持只读连接与测试中的调用/指纹断言。
- T-36-05：下游 authority 异常经 `str(exc)` 泄露路径、PII、密钥或 provider body。缓解：公共 limitation catalog 与私有诊断分离。
- T-36-06：partial 响应缺 snapshot/freshness/authority 语义，页面误把未知或过期数据当事实。缓解：所有 envelope 保留稳定 metadata 与逐 authority 状态。
- 阻断条件：公开 Projection 包含 poisoned 异常内容，或任何 read operation 改变 Personal/External/Decision/Proactive/Calibration/Serving authority 指纹，则本计划不得通过。
</threat_model>

<tasks>
<task type="execute"><name>定义安全的 Projection public limitation/error 目录</name><files>src/personal_knowledge/services/ui_projection.py; tests/contract/test_ui_projection.py; tests/contract/test_ui_projection_state_external.py</files><read_first>36-CONTEXT.md; 36-RESEARCH.md; src/personal_knowledge/services/ui_projection.py; src/personal_knowledge/services/agent_contract.py; tests/contract/test_ui_projection.py; tests/contract/test_ui_projection_state_external.py</read_first><action>在 `ui_projection.py` 内集中定义公开 limitation/error 的 allowlisted code 与用户安全 message；将 `_error`、`_collect`、`_personal_state_detail`、calibration isolation 及同类异常分支改为使用该 mapper。保留内部异常的受控日志/诊断能力，但不在返回 `limitations`、`error.detail` 或 `data` 中插入 `str(exc)`。未知 operation 同样只返回固定 `unknown_operation` contract，不回显 operation 以外的未可信输入。</action><acceptance_criteria>注入包含 `C:\secret\x`、`sk-test`、`Bearer`、provider JSON 与 confirmation/HMAC 字样的异常时，所有公开 JSON 字段均不包含这些片段；受影响 authority 标记为 error、`partial=true`，并提供可枚举 safe code/message；正常 partial/成功响应继续包含 snapshot/freshness/authorities。</acceptance_criteria><verify>$env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection.py tests/contract/test_ui_projection_state_external.py -q</verify></task>

<task type="execute"><name>保持 Projection v1 的物理只读与 authority 边界</name><files>src/personal_knowledge/services/ui_projection.py; tests/contract/test_ui_projection.py; tests/contract/test_ui_projection_decision.py; tests/contract/test_ui_projection_actions_proactive.py</files><read_first>src/personal_knowledge/services/ui_projection.py; src/personal_knowledge/services/api_server.py; tests/contract/test_ui_projection.py; tests/contract/test_ui_projection_decision.py; tests/contract/test_ui_projection_actions_proactive.py; .planning/phases/PDA-23-composite-ssot-snapshot-integrity-close-target-a-with-typed-/23-01-PLAN.md</read_first><action>审查并收口 `CockpitProjectionService` 的每条 public operation，使 SQLite 打开保持 `mode=ro` 和 `PRAGMA query_only=ON`，只调用现有 read surfaces，并通过 `_envelope` 返回固定 `decision_cockpit_projection_v1` 字段集合。禁止为浏览器加入 SQLite/Chroma client、Provider invocation、promotion、lifecycle write、External write 或外部动作；保持 REST adapter 仅为 service 调用的薄映射。</action><acceptance_criteria>所有既有 `/ui/*` operation 在 direct service 与 `ui_rest_contract` 下有相同 schema_version/operation 与只读结果语义；针对每个 authority 的读前后 fingerprint 不变，Provider/external/promotion 调用计数为 0；attempted SQL write 在 Projection connection 上失败。</acceptance_criteria><verify>$env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection.py tests/contract/test_ui_projection_decision.py tests/contract/test_ui_projection_actions_proactive.py -q</verify></task>

<task type="execute"><name>锁定状态与主动评分的 authority vocabulary</name><files>src/personal_knowledge/services/ui_projection.py; tests/contract/test_ui_projection_decision.py; tests/contract/test_ui_projection_actions_proactive.py</files><read_first>src/personal_knowledge/services/ui_projection.py; tests/contract/test_ui_projection_decision.py; tests/contract/test_ui_projection_actions_proactive.py; .planning/REQUIREMENTS.md</read_first><action>将 Projection 的 decision/proactive 映射限定为 authority 已发布 vocabulary：confirmation 仅使用 `proposed`、`accepted`、`rejected`、`deferred`、`revoked`，未知值进入保守的 attention/limitation 路径；proactive 排序和展示只读取 `importance.final_score`，缺失或非数值时保守归入 deferrable 并产生安全 limitation。不要在 Projection 计算新的业务 score、确认状态或 lifecycle 裁决。</action><acceptance_criteria>Python contract fixture 证明 `accepted` 可被正确分类，`rejected/deferred/revoked` 进入 closed，未知 confirmation 不会被提升为可执行；每个 now card 都带可解析的 `importance.final_score`，缺分/坏分不会进入 now。</acceptance_criteria><verify>$env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection_decision.py tests/contract/test_ui_projection_actions_proactive.py -q</verify></task>
</tasks>

<verification>$env:PYTHONPATH = "$PWD\src"; python -m pytest tests/contract/test_ui_projection.py tests/contract/test_ui_projection_state_external.py tests/contract/test_ui_projection_decision.py tests/contract/test_ui_projection_actions_proactive.py -q</verification>

<success_criteria>CCK-01 与 CCK-03 的服务端 Projection 具备固定 v1 envelope、物理只读边界、真实 authority vocabulary 和可验证的安全 partial/degraded 语义。</success_criteria>
