---
phase: 36
plan: 03
type: execute
wave: 2
depends_on: [36-02]
files_modified:
  - apps/personal_decision_cockpit/src/api/client.ts
  - apps/personal_decision_cockpit/src/api/schemas.ts
  - apps/personal_decision_cockpit/src/pages/overview/OverviewPage.tsx
  - apps/personal_decision_cockpit/src/test/schemas.test.ts
  - apps/personal_decision_cockpit/src/test/liveContract.test.ts
  - apps/personal_decision_cockpit/src/test/appSmoke.test.tsx
autonomous: true
requirements: [CCK-01, CCK-03]
must_haves:
  truths:
    - "D-36-01/D-36-05：客户端只接受 `decision_cockpit_projection_v1` 与当前 endpoint 对应的 operation；错版本或错 operation 不能被渲染成有效 Cockpit 数据。"
    - "客户端请求保持相对 URL，不直接读取 SQLite/Chroma、拼接多个 authority、持久化个人 decision payload 或重新实现 lifecycle/confirmation/risk 判断。"
    - "Overview 使用 `accepted` 等真实 confirmation vocabulary 与 `importance.final_score`；展示修正不会生成新的 ranking authority。"
    - "解析失败与 API 错误只呈现安全 code/message，不向 console 记录 response body 或个人 payload。"
  artifacts:
    - path: apps/personal_decision_cockpit/src/api/schemas.ts
      provides: endpoint-bound Projection v1 Zod schemas
    - path: apps/personal_decision_cockpit/src/api/client.ts
      provides: 相对同源 fetch 与安全 ApiError 映射
    - path: apps/personal_decision_cockpit/src/test/liveContract.test.ts
      provides: 九个受控 `/ui/*` fixture 的 client contract 回归
  key_links:
    - from: client.ts:apiGet
      to: schemas.ts endpoint schema
      via: 相对 HTTP response 必须通过版本和 operation 绑定的 Zod parsing
    - from: OverviewPage.tsx
      to: CockpitProjectionService overview DTO
      via: confirmation_state 与 importance.final_score 仅用于显示既有 authority 语义
---

<objective>使 React Cockpit 只消费已验证的 Projection v1 DTO，并修正当前 Overview 对实际 authority 字段的错误假设。</objective>

<threat_model>
- T-36-07：服务端返回错误版本或错误 operation，宽松 Zod schema 仍让页面把不相关/旧数据当成当前 decision 状态。缓解：schema_version 与 endpoint operation 使用 literal/expected-value parsing。
- T-36-08：UI 为补齐显示在浏览器侧拼接 authority 或缓存个人数据，形成 shadow SSOT。缓解：保留相对 API client、内存查询状态和服务端 Projection 单入口。
- T-36-09：旧 `confirmed` / `importance.score` 词汇让真实 `accepted` 或 `importance.final_score` 数据错误分类。缓解：用 controlled fixtures 与组件测试锁定真实 vocabulary。
- 阻断条件：错误 schema/operation 通过 parsing，客户端使用绝对后端 URL/本地持久化，或测试中响应 payload 进入 console，则本计划不得通过。
</threat_model>

<tasks>
<task type="execute"><name>把每个 Projection schema 绑定到 v1 和预期 operation</name><files>apps/personal_decision_cockpit/src/api/schemas.ts; apps/personal_decision_cockpit/src/test/schemas.test.ts; apps/personal_decision_cockpit/src/test/liveContract.test.ts</files><read_first>36-RESEARCH.md; src/personal_knowledge/services/ui_projection.py; apps/personal_decision_cockpit/src/api/schemas.ts; apps/personal_decision_cockpit/src/test/fixtures/overview.json; apps/personal_decision_cockpit/src/test/liveContract.test.ts</read_first><action>重构 Zod common envelope factory，使所有 exported `/ui/*` DTO schema 固定接受 `schema_version: "decision_cockpit_projection_v1"`，并分别固定 `overview.get`、`personal_state.get`、`external_delta.get`、`decision_queue.get`、`decision_workspace.get`、`actions_recent.get`、`proactive_summary.get`、`calibration_overview.get`、`system.status.get` 的 operation。保留 endpoint 所需的 data 结构与受控 forward-compatible unknown fields，但不允许任意 schema_version/operation。更新九个 metadata-only fixture test，使 fixture 只能从受控 REST response 或隔离 contract fixture 更新。</action><acceptance_criteria>九个 fixture 都可由对应 schema parse；任一 fixture 的 schema_version 或 operation 被篡改时 parse 失败；错误 endpoint schema 不能接受其他 endpoint 的合法 payload；Snapshot/freshness/authorities/partial/limitations 的基础字段仍受检查。</acceptance_criteria><verify>Set-Location apps/personal_decision_cockpit; npm run test -- --run src/test/schemas.test.ts src/test/liveContract.test.ts</verify></task>

<task type="execute"><name>保持相对同源客户端与安全错误映射</name><files>apps/personal_decision_cockpit/src/api/client.ts; apps/personal_decision_cockpit/src/test/schemas.test.ts; apps/personal_decision_cockpit/src/test/appSmoke.test.tsx</files><read_first>apps/personal_decision_cockpit/src/api/client.ts; apps/personal_decision_cockpit/src/api/hooks.ts; apps/personal_decision_cockpit/src/api/orchestration.ts; apps/personal_decision_cockpit/src/test/appSmoke.test.tsx; 36-CONTEXT.md</read_first><action>保留 `apiGet` 与 guarded orchestration client 的相对路径请求方式；将非 2xx、非 JSON 与 Zod parse failure 归并为只含 allowlisted code、status 和安全用户消息的 `ApiError`。移除或禁止记录 response body、headers、个人 payload、confirmation token 和 HMAC 的 console 路径；不增加 `localStorage`、`sessionStorage`、IndexedDB、Service Worker 或新的浏览器 business-rule store。</action><acceptance_criteria>源码/测试断言所有 Cockpit API URL 以 `/ui/` 或既有 `/agent/session/` 相对路径调用；error fixtures 不能把 response body 显示或 console 输出；API parse failure 进入可处理的 typed error 而不是以 `any` 渲染；页面不新增浏览器个人数据持久化 API。</acceptance_criteria><verify>Set-Location apps/personal_decision_cockpit; npm run test -- --run src/test/schemas.test.ts src/test/appSmoke.test.tsx</verify></task>

<task type="execute"><name>修正 Overview 的 confirmation 与 proactive score 展示</name><files>apps/personal_decision_cockpit/src/pages/overview/OverviewPage.tsx; apps/personal_decision_cockpit/src/test/appSmoke.test.tsx; apps/personal_decision_cockpit/src/test/liveContract.test.ts</files><read_first>apps/personal_decision_cockpit/src/pages/overview/OverviewPage.tsx; apps/personal_decision_cockpit/src/api/schemas.ts; apps/personal_decision_cockpit/src/test/fixtures/overview.json; src/personal_knowledge/services/ui_projection.py; tests/contract/test_ui_projection_decision.py; tests/contract/test_ui_projection_actions_proactive.py</read_first><action>将 Overview 的 decision queue/Now Stack 逻辑从旧 `confirmed` 假设改为 authority 的 `proposed`、`accepted`、`rejected`、`deferred`、`revoked` vocabulary，并只读取 `importance.final_score`。缺少或非数值 final_score 的项目显示为保守的 deferrable/信息不足状态；未知 confirmation state 显示 needs-attention/limitation，而不是可执行或已确认。此改动只修正展示，不调用 ranking、lifecycle 或 confirmation write API。</action><acceptance_criteria>使用 `accepted` fixture 时 Overview 显示正确当前/已接受语义；`rejected`、`deferred`、`revoked` 不显示为待确认；`importance.final_score` 缺失/非法项目不会出现在 now/high priority；组件测试不需要真实 authority 或写操作。</acceptance_criteria><verify>Set-Location apps/personal_decision_cockpit; npm run test -- --run src/test/liveContract.test.ts src/test/appSmoke.test.tsx</verify></task>
</tasks>

<verification>Set-Location apps/personal_decision_cockpit; npm run test -- --run src/test/schemas.test.ts src/test/liveContract.test.ts src/test/appSmoke.test.tsx; npm run build</verification>

<success_criteria>浏览器端只接受服务端的精确 Projection v1 contract；当前状态与主动评分按真实 authority vocabulary 呈现，且没有新增 shadow state、个人缓存或安全信息泄露。</success_criteria>
