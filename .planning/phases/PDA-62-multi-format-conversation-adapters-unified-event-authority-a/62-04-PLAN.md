---
phase: 62-multi-format-conversation-adapters-unified-event-authority-and-replaceable-extraction-views
plan: 04
type: execute
wave: 3
depends_on: [62-02, 62-03]
files_modified:
  - src/personal_knowledge/application/conversation/compatibility_projection.py
  - src/personal_knowledge/application/conversation/event_generations.py
  - src/personal_knowledge/application/conversation/event_repository.py
  - src/personal_knowledge/core/conversation_repository.py
  - src/personal_knowledge/application/run_pipeline.py
  - src/personal_knowledge/application/sync.py
  - tests/contract/test_conversation_v2_compatibility.py
  - tests/integration/test_conversation_event_generations.py
  - tests/integration/test_conversation_v2_sync.py
autonomous: true
requirements: [CONV-03, CONV-04, CONV-05]
must_haves:
  truths:
    - "Canonical v2 reuses the existing database/publication command and does not create a second permanent product authority."
    - "Legacy canonical tables are deterministic projections bound to exactly one active event generation."
    - "Any activation or projection failure restores prior authority, projection, pointer/version, and fingerprints."
  artifacts:
    - path: src/personal_knowledge/application/conversation/compatibility_projection.py
      provides: deterministic event-to-current-canonical projection seam
    - path: src/personal_knowledge/application/conversation/event_generations.py
      provides: stage/validate/activate/rollback transaction owner
    - path: tests/integration/test_conversation_event_generations.py
      provides: activation fault injection and exact rollback evidence
  key_links:
    - from: src/personal_knowledge/application/sync.py
      to: src/personal_knowledge/application/conversation/event_generations.py
      via: `pk-sync conversations` shadow/v2 orchestration
    - from: active event generation
      to: canonical_sessions/canonical_messages/canonical_tool_events
      via: generation-bound compatibility projection
    - from: ConversationRepository
      to: compatibility projection and event-aware repository
      via: preserved old methods plus explicit event methods/provider
---

<objective>
Integrate all adapters into `pk-sync conversations`, build generation-bound compatibility projections, and implement fail-closed activation/rollback without yet touching live canonical data.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-CONTEXT.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-RESEARCH.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-02-SUMMARY.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-03-SUMMARY.md
@src/personal_knowledge/application/sync.py
@src/personal_knowledge/core/conversation_repository.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Red → Green deterministic compatibility projection and provider/consumer parity</name>
  <files>src/personal_knowledge/application/conversation/compatibility_projection.py; src/personal_knowledge/core/conversation_repository.py; tests/contract/test_conversation_v2_compatibility.py</files>
  <action>Declare two public seams: event-aware repository reads typed active-generation events/relations/fidelity; existing `ConversationRepository` methods continue reading the legacy table contract. RED tests construct one v2 generation with message/reasoning/tool/compaction/unknown events and assert the compatibility projection emits only documented lossy session/message/tool rows, stable source refs, no double counting, no compact summary as user fact, and exact generation lineage. Run provider/consumer contracts for current summary, eligibility, retrieval and delta consumers. Projection code owns only event-to-legacy mapping; it cannot activate generations.</action>
  <verify><automated>python -m pytest -q tests/contract/test_conversation_v2_compatibility.py tests/unit/test_conversation_repository.py tests/contract/test_agentsview_downstream_contracts.py</automated></verify>
  <done>Existing consumers remain compatible while event-aware consumers can access non-flattened semantics explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Red → Green staging, validation, atomic activation and exact rollback state machine</name>
  <files>src/personal_knowledge/application/conversation/event_generations.py; src/personal_knowledge/application/conversation/event_repository.py; tests/integration/test_conversation_event_generations.py</files>
  <action>Implement the sole generation lifecycle owner: prepare staging generation, validate schema/FK/digests/provenance/fidelity/adapter coverage, build and validate compatibility projection, activate event authority plus projection/version binding, and rollback. RED fault-injection cases must cover failures before commit, authority commit with projection/pointer/version failure, checksum mismatch, stale source manifest, unknown adapter result, and consumer parity failure. Treat every post-authority projection/pointer failure as activation failure and restore prior authority rows, compatibility tables, version/watermark and fingerprints. Preserve old generation rows and backups; do not delete.</action>
  <verify><automated>python -m pytest -q tests/integration/test_conversation_event_generations.py</automated></verify>
  <done>Activation is atomic from consumer perspective and every injected failure restores the exact prior canonical state.</done>
</task>

<task type="auto">
  <name>Task 3: Add dry-run/shadow v2 orchestration to the existing sync command</name>
  <files>src/personal_knowledge/application/run_pipeline.py; src/personal_knowledge/application/sync.py; tests/integration/test_conversation_v2_sync.py</files>
  <action>Extend `pk-sync conversations` without changing its default active behavior until Plan 62-08: dry-run probes all family capabilities and estimates snapshot/event counts; an explicit shadow mode captures artifacts and writes a non-active generation/report; an explicit activation mode delegates only to `event_generations`. Outputs are metadata-only and list per-family full/partial/blocked counts, fidelity, source/artifact/generation hashes, projection parity and rollback target. Post-commit delta fires only after successful activation and carries no bodies. Unknown family, source drift, missing family coverage or blocked privacy gate prevents activation.</action>
  <verify><automated>python -m pytest -q tests/integration/test_conversation_v2_sync.py tests/integration/test_agent_conversation_rollback.py</automated></verify>
  <done>The existing product command can build and inspect v2 shadow generations while current canonical service behavior remains unchanged.</done>
</task>
</tasks>

<verification>Run compatibility, generation, sync, existing AgentView/canonical rollback suites and `git diff --check`. Confirm no test or default command activates live v2 or calls a provider.</verification>
<success_criteria>One canonical authority supports v2 events and legacy consumers through generation-safe projections with proven exact rollback.</success_criteria>
<output>Create 62-04-SUMMARY.md when complete.</output>
