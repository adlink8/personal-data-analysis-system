---
phase: 62-multi-format-conversation-adapters-unified-event-authority-and-replaceable-extraction-views
plan: 05
type: execute
wave: 4
depends_on: [62-04]
files_modified:
  - src/personal_knowledge/application/conversation/extraction_views.py
  - src/personal_knowledge/application/conversation/extraction_policy.py
  - src/personal_knowledge/application/conversation/view_repository.py
  - tests/unit/test_conversation_extraction_views.py
  - tests/contract/test_conversation_extraction_policy.py
  - tests/integration/test_conversation_view_repository.py
autonomous: true
requirements: [CONV-06]
must_haves:
  truths:
    - "Trace is one versioned view and can be deprioritized or removed without re-adapting sources or changing event IDs."
    - "Every view is reproducible from one event generation and carries exact evidence-event lineage."
    - "Compaction summaries rank first initially as navigation signals, never as self-authenticating truth."
  artifacts:
    - path: src/personal_knowledge/application/conversation/extraction_views.py
      provides: deterministic Turn/NativeTrace/Episode/CompactionWindow/Session/Topic/CrossSession view builders
    - path: src/personal_knowledge/application/conversation/extraction_policy.py
      provides: versioned replaceable scheduling policy and digest
    - path: src/personal_knowledge/application/conversation/view_repository.py
      provides: generation/policy-bound view storage and lineage reads
  key_links:
    - from: canonical event IDs and relations
      to: view evidence_event_refs
      via: deterministic view builders
    - from: ExtractionPolicy
      to: candidate queue ordering
      via: policy digest, view type, fidelity, freshness and novelty metadata
---

<objective>
Build reproducible extraction views and a versioned priority policy so source semantics remain stable when extraction granularity changes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-CONTEXT.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-RESEARCH.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-04-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Red → Green deterministic view builders over event relations</name>
  <files>src/personal_knowledge/application/conversation/extraction_views.py; tests/unit/test_conversation_extraction_views.py</files>
  <action>Write RED fixtures for native turn/trace IDs, DAG branches, loop boundaries, Pi/Grok/Qoder compactions, incomplete call/results, session boundaries and cross-session topic links. Implement seven view builders with stable view IDs derived from generation, builder version and ordered evidence event set. `NativeTraceView` uses explicit source-native trace/turn/loop IDs only; `EpisodeView` may use versioned deterministic heuristics; `CompactionWindowView` relates summary event to compacted/retained event sets; Session/Topic/CrossSession views retain member view/event lineage and contradiction slots. Unknown or partial relations reduce fidelity and never cause adjacency guesses to be labeled native.</action>
  <verify><automated>python -m pytest -q tests/unit/test_conversation_extraction_views.py</automated></verify>
  <done>All required views rebuild deterministically and expose exactly which stable events they summarize.</done>
</task>

<task type="auto">
  <name>Task 2: Red → Green versioned ExtractionPolicy with trace-priority replacement tests</name>
  <files>src/personal_knowledge/application/conversation/extraction_policy.py; tests/contract/test_conversation_extraction_policy.py</files>
  <action>Define a data-driven policy contract containing allowed view types, priority bands, fidelity thresholds, freshness/novelty weighting, dedup/supersession, budgets and abstain/block reasons. Initial policy orders CompactionWindow, native trace/episode, session, topic, cross-session as locked. RED tests replace the policy with session-first and trace-disabled variants and assert raw artifact/event/view identities do not change; only queue ranks/policy digest change. Compaction priority cannot override missing evidence refs or low fidelity.</action>
  <verify><automated>python -m pytest -q tests/contract/test_conversation_extraction_policy.py</automated></verify>
  <done>Changing trace priority is a policy-only operation with deterministic new scheduling output.</done>
</task>

<task type="auto">
  <name>Task 3: Persist generation/policy-bound views without creating fact authority</name>
  <files>src/personal_knowledge/application/conversation/view_repository.py; tests/integration/test_conversation_view_repository.py</files>
  <action>Persist view headers, membership/lineage, fidelity, builder/policy versions and lifecycle in separate v2 tables or an additive companion schema. Enforce that views reference only one active/staged event generation, are replaceable/rebuildable, and cannot write canonical KU or compatibility messages. RED tests cover idempotent rebuild, policy revision, event-generation drift, stale marking, lineage resolution and no authority mutation.</action>
  <verify><automated>python -m pytest -q tests/integration/test_conversation_view_repository.py</automated></verify>
  <done>Views are queryable projections with exact lineage and lifecycle, not a competing evidence or knowledge SSOT.</done>
</task>
</tasks>

<verification>Run view unit/contract/integration suites, event repository regressions, and `git diff --check`.</verification>
<success_criteria>The system can prioritize compaction now and change away from trace later without re-ingesting evidence or losing provenance.</success_criteria>
<output>Create 62-05-SUMMARY.md when complete.</output>
