---
phase: 62-multi-format-conversation-adapters-unified-event-authority-and-replaceable-extraction-views
plan: 06
type: execute
wave: 5
depends_on: [62-05]
files_modified:
  - src/personal_knowledge/evaluation/conversation/semantic_admission.py
  - src/personal_knowledge/application/knowledge/view_candidate_prepare.py
  - src/personal_knowledge/application/knowledge/ku.py
  - tests/unit/test_semantic_admission_gate.py
  - tests/contract/test_view_candidate_prepare.py
  - tests/integration/test_view_candidate_ledger.py
autonomous: true
requirements: [CONV-07, CONV-08]
must_haves:
  truths:
    - "Deterministic privacy/structure/evidence rejection always runs before any model-visible semantic payload."
    - "The semantic gate can admit, reject, or abstain and cannot cite evidence outside the authorized view event set."
    - "Old message-level prepare runs remain audit history but are never executable under the new view-policy contract."
    - "All tests and prepare estimates in this plan use deterministic replay and make zero paid provider calls."
  artifacts:
    - path: src/personal_knowledge/evaluation/conversation/semantic_admission.py
      provides: structured deterministic-first, abstention-capable semantic gate seam
    - path: src/personal_knowledge/application/knowledge/view_candidate_prepare.py
      provides: view/policy/evidence-bound candidate ledger preparation and supersession rules
    - path: tests/contract/test_view_candidate_prepare.py
      provides: policy/evidence/cost/no-paid-call contract
  key_links:
    - from: view lineage and event generation
      to: semantic gate authorized evidence
      via: immutable view digest and evidence-event allowlist
    - from: semantic gate decision
      to: candidate ledger
      via: structured admit/reject/abstain result and reason codes
---

<objective>
Replace absorb-everything/message-regex preparation with a deterministic-first, evidence-bound semantic admission seam and a zero-cost view candidate prepare path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-CONTEXT.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-RESEARCH.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-05-SUMMARY.md
@src/personal_knowledge/application/knowledge/eligibility.py
@src/personal_knowledge/application/knowledge/ku.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Red → Green deterministic-first and replay-backed semantic admission gate</name>
  <files>src/personal_knowledge/evaluation/conversation/semantic_admission.py; tests/unit/test_semantic_admission_gate.py</files>
  <action>Declare a structured public result with `admit|reject|abstain`, supported claims, evidence event IDs, novelty/durability/specificity/future-usefulness/contamination/contradiction assessments, limitations and reason codes. RED tests prove secrets/injection/unsupported source/empty evidence/invalid lineage are rejected before provider invocation; replayed semantic cases cover durable fact, transient chatter, tool noise, unsupported summary claim, contradiction, duplicate knowledge and uncertain evidence. The provider receives only bounded redacted view content plus authorized event handles, returns structured data, and cannot add evidence outside the allowlist. Use only a deterministic replay provider in this plan; assert provider production-call counter remains zero.</action>
  <verify><automated>python -m pytest -q tests/unit/test_semantic_admission_gate.py</automated></verify>
  <done>The gate makes intelligent structured value judgments while deterministic safety remains non-bypassable and abstention is first-class.</done>
</task>

<task type="auto">
  <name>Task 2: Red → Green view/policy/evidence-bound prepare ledger and old-run supersession</name>
  <files>src/personal_knowledge/application/knowledge/view_candidate_prepare.py; tests/contract/test_view_candidate_prepare.py; tests/integration/test_view_candidate_ledger.py</files>
  <action>Create a new prepare seam keyed by active event generation, view builder version, extraction policy digest, semantic prompt/schema version and evidence-event digest. It writes estimates/ledger states only and never calls the provider. Old message-level runs are classified `superseded_policy`/non-executable through an append-only audit transition or guard; do not delete their 24,487 ledger rows or caches. RED tests reject extraction when generation/view/policy/gate versions mismatch, when evidence is unresolved, or when a legacy message-level run ID is supplied. Estimate calls/tokens/cost per view type and family without body logging.</action>
  <verify><automated>python -m pytest -q tests/contract/test_view_candidate_prepare.py tests/integration/test_view_candidate_ledger.py</automated></verify>
  <done>Only current view-policy runs can approach extraction, while the two old queues are preserved but technically non-executable.</done>
</task>

<task type="auto">
  <name>Task 3: Expose inspect/prepare/status commands without enabling paid extraction</name>
  <files>src/personal_knowledge/application/knowledge/ku.py; tests/contract/test_view_candidate_prepare.py</files>
  <action>Add explicit event/view-aware inspect and prepare/status flags or subcommands behind existing `pk-ku` product policy. Output active generation, policy/view counts, deterministic exclusions, semantic-gate replay coverage, pending estimates and `blocked_pending_user_cost_approval`. Do not wire `pk-ku extract` to the new run type in this phase; invoking extract on it must return a typed blocked error that names the separate approval/pilot requirement. Existing old run IDs must also refuse execution as superseded.</action>
  <verify><automated>python -m pytest -q tests/contract/test_view_candidate_prepare.py tests/unit/test_knowledge_unit_gate.py</automated></verify>
  <done>Operators can inspect and estimate the new extraction model, but no command path can spend provider quota yet.</done>
</task>
</tasks>

<verification>Run all semantic/prepare/ledger tests, existing KU gate tests, `pk-ku doctor --skip-ports`, and `git diff --check`. Assert test telemetry/provider ledger shows zero production/paid calls.</verification>
<success_criteria>Candidate preparation is view/evidence/policy-bound, semantically selective, auditable, and incapable of executing old or unapproved paid queues.</success_criteria>
<output>Create 62-06-SUMMARY.md when complete.</output>
