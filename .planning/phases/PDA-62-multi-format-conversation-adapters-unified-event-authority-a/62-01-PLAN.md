---
phase: 62-multi-format-conversation-adapters-unified-event-authority-and-replaceable-extraction-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personal_knowledge/core/conversation_events.py
  - src/personal_knowledge/adapters/conversation_sources/__init__.py
  - src/personal_knowledge/adapters/conversation_sources/contracts.py
  - src/personal_knowledge/adapters/conversation_sources/snapshots.py
  - src/personal_knowledge/application/conversation/event_schema.py
  - src/personal_knowledge/application/conversation/event_repository.py
  - tests/unit/test_conversation_event_contracts.py
  - tests/integration/test_conversation_source_snapshots.py
  - tests/integration/test_conversation_event_repository.py
autonomous: true
requirements: [CONV-02, CONV-03, CONV-04, CONV-05]
must_haves:
  truths:
    - "Raw evidence, canonical semantic events, derived views, and compatibility messages are distinct authority layers."
    - "Every safe event is resolvable to an immutable source artifact and native locator; unmodeled fields are explicit rather than silently dropped."
    - "Live SQLite capture is WAL-safe and forbidden credential/account/token/auth data is unreachable by construction."
  artifacts:
    - path: src/personal_knowledge/core/conversation_events.py
      provides: typed event, relation, fidelity, provenance, and field-disposition contracts
    - path: src/personal_knowledge/adapters/conversation_sources/contracts.py
      provides: family adapter capability/probe/result public seam
    - path: src/personal_knowledge/adapters/conversation_sources/snapshots.py
      provides: allowlisted content-addressed file/directory/SQLite capture seam
    - path: src/personal_knowledge/application/conversation/event_repository.py
      provides: generation-bound v2 event persistence and read contract
  key_links:
    - from: src/personal_knowledge/adapters/conversation_sources/snapshots.py
      to: src/personal_knowledge/application/conversation/event_repository.py
      via: immutable artifact manifest and source artifact IDs
    - from: src/personal_knowledge/core/conversation_events.py
      to: src/personal_knowledge/application/conversation/event_schema.py
      via: schema constraints for event kinds, fidelity and relations
---

<objective>
Establish the loss-aware public contracts, immutable source capture, and generation-bound canonical v2 event store that every Phase 62 adapter and view will use.

Purpose: create one stable evidence/event seam before implementing any family parser, and keep capture, semantic modeling, and persistence as separate responsibilities.
Output: tested event/fidelity/provenance types, snapshot manifests, v2 schema, and repository behavior with no live source mutation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/AGENTS.md
@docs/architecture/engineering-and-testing-contract.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-CONTEXT.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-RESEARCH.md
@src/personal_knowledge/adapters/agentsview.py
@src/personal_knowledge/application/conversation/build_canonical_agent_conversations.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Red → Green typed event, relation, provenance and fidelity contracts</name>
  <files>src/personal_knowledge/core/conversation_events.py; src/personal_knowledge/adapters/conversation_sources/contracts.py; tests/unit/test_conversation_event_contracts.py</files>
  <action>Declare the public seam before implementation: a family adapter accepts one immutable `SourceArtifactSet` plus a versioned capability descriptor and returns an `AdaptationResult` containing sessions, typed events, first-class relations, fidelity dimensions, field dispositions, warnings and a deterministic dataset digest. Write RED tests for stable identity independent of ordinal movement, typed unknown-native preservation, relation endpoint validation, source locator resolvability, explicit partial/unknown fidelity, and rejection of events without an artifact/native locator. Implement frozen types/enums and validation only after RED. Event IDs must prefer native identity and include family/artifact/contract version collision domains; order fields never become the sole identity.</action>
  <verify><automated>python -m pytest -q tests/unit/test_conversation_event_contracts.py</automated></verify>
  <done>The adapter/event contract can represent all locked event kinds and cannot emit complete-looking unprovenanced or implicitly lossy records.</done>
</task>

<task type="auto">
  <name>Task 2: Red → Green immutable allowlisted source snapshot seam</name>
  <files>src/personal_knowledge/adapters/conversation_sources/snapshots.py; tests/integration/test_conversation_source_snapshots.py</files>
  <action>Expose separate capture operations for immutable file sets/directories and mutable SQLite. RED tests must cover content-addressed deduplication, manifest replay, changed-source detection, symlink/reparse escape rejection, exact allowlisted relative paths, byte/count limits, SQLite online backup consistency under concurrent WAL writes, and failure before any formal artifact is published. SQLite capture accepts a declared table/column capability for validation but never enumerates/copies adjacent credential/account/token/auth tables into reports or fixtures. Manifests contain hashes, capture method, schema/capability digest and metadata-only privacy dispositions; no body/secret logging. Reuse the online-backup behavior of `agentsview.py`, but do not mix family parsing or canonical publication into this module.</action>
  <verify><automated>python -m pytest -q tests/integration/test_conversation_source_snapshots.py</automated></verify>
  <done>Every later adapter receives a consistent immutable evidence set, and unsafe/ambiguous/over-broad capture fails closed without source writes.</done>
</task>

<task type="auto">
  <name>Task 3: Red → Green generation-bound canonical v2 schema and repository</name>
  <files>src/personal_knowledge/application/conversation/event_schema.py; src/personal_knowledge/application/conversation/event_repository.py; tests/integration/test_conversation_event_repository.py</files>
  <action>Create cohesive v2 tables for source artifacts, adapter runs, event generations, sessions, events, event relations, field dispositions/fidelity, and generation authority without altering the existing compatibility tables. RED tests use temporary SQLite and assert FK/uniqueness, idempotent replay, generation isolation, native locator lookup, event/relation ordering, unknown-native preservation, no cross-generation relation, and read-only active-generation queries. The repository is the only persistence seam; it stages and validates generations but does not activate pointers or build views/projections. Keep schema migration additive and versioned.</action>
  <verify><automated>python -m pytest -q tests/integration/test_conversation_event_repository.py</automated></verify>
  <done>A complete v2 generation can be written, validated and replayed in the existing canonical database without becoming active or disturbing legacy consumers.</done>
</task>
</tasks>

<verification>Run all three focused suites together plus `git diff --check`. Inspect that fixtures and failures contain only synthetic/redacted content and no user paths beyond explicit temporary test roots.</verification>
<success_criteria>The phase has one enforceable raw-evidence and typed-event foundation with stable provenance, explicit fidelity, safe capture, and generation isolation.</success_criteria>
<output>Create 62-01-SUMMARY.md when complete.</output>
