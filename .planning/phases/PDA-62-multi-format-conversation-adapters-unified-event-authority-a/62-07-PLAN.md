---
phase: 62-multi-format-conversation-adapters-unified-event-authority-and-replaceable-extraction-views
plan: 07
type: execute
wave: 6
depends_on: [62-06]
files_modified:
  - src/personal_knowledge/evaluation/conversation/adapter_fidelity.py
  - tests/integration/test_conversation_v2_live_metadata.py
  - .planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-SHADOW-REPORT.json
  - .planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-VALIDATION.md
autonomous: false
requirements: [CONV-01, CONV-02, CONV-03, CONV-04, CONV-05, CONV-06, CONV-07, CONV-08]
must_haves:
  truths:
    - "A single captured live source cohort produces explicit full/partial/blocked coverage for all 17 observed families."
    - "Shadow v2 and compatibility projections are compared against the same immutable snapshot without activating live canonical authority."
    - "No report contains message bodies, secrets, credential-table data, or paid model output."
    - "Live activation remains blocked until a human reviews the shadow/fidelity/rollback evidence."
  artifacts:
    - path: src/personal_knowledge/evaluation/conversation/adapter_fidelity.py
      provides: per-family source-to-artifact-to-event-to-projection coverage evaluator
    - path: .planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-SHADOW-REPORT.json
      provides: metadata-only live shadow evidence and activation recommendation
    - path: .planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-VALIDATION.md
      provides: requirement/test/failure/paid-call closure matrix
  key_links:
    - from: live source snapshot manifest
      to: shadow event generation and compatibility projection
      via: identical source artifact set/digest
    - from: per-family fidelity report
      to: activation checkpoint
      via: explicit pass/partial/blocked thresholds and unresolved limitations
---

<objective>
Run a zero-paid-call live shadow build and produce auditable per-family fidelity, compatibility, privacy, view and rollback evidence before any canonical activation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-CONTEXT.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-RESEARCH.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-06-SUMMARY.md
@.planning/quick/260812-dug-canonical-canonical-llm/260812-dug-VERIFICATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build metadata-only fidelity evaluator and live-read negative tests</name>
  <files>src/personal_knowledge/evaluation/conversation/adapter_fidelity.py; tests/integration/test_conversation_v2_live_metadata.py</files>
  <action>Implement per-family metrics for discovered sessions, native artifacts available, captured artifacts, adapted sessions/events/relations, unknown/redacted/unavailable dispositions, source-ref resolution sample, replay digest stability, compatibility projection parity and view coverage. RED tests use temporary sources plus a read-only live-metadata smoke mode that never returns bodies. Define activation gates: 17/17 families have a capability result; every native-available session is captured or explicitly blocked; no unresolved provenance; forbidden-source access zero; replay digest stable; current consumers pass; partial ChatGPT/Cursor limitations disclosed rather than hidden.</action>
  <verify><automated>python -m pytest -q tests/integration/test_conversation_v2_live_metadata.py</automated></verify>
  <done>The evaluator can distinguish genuine preservation, honest partial coverage and unsafe/missing ingestion without inspecting reports manually.</done>
</task>

<task type="auto">
  <name>Task 2: Capture one immutable live cohort and build a non-active v2 shadow generation</name>
  <files>.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-SHADOW-REPORT.json</files>
  <action>Run `pk-sync conversations` dry-run first, then the explicit v2 shadow write mode against the live read-only agent sources. Use online backup for every mutable SQLite source and preserve canonical/AgentsView/source fingerprints before/after. Do not activate v2, replace compatibility tables, advance active watermarks, write live agent stores, run KU extract or call any provider. Generate a metadata-only report containing all 17 family counts/fidelity, artifact/generation digests, shadow compatibility parity, views by type, deterministic gate counts, cost estimate, old-run supersession readiness, source fingerprint invariants and exact rollback target.</action>
  <verify><automated>pk-sync conversations --event-v2-shadow --report .planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-SHADOW-REPORT.json --write</automated></verify>
  <done>A complete non-active shadow generation exists and the active canonical/KU/service state is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Produce requirement and security validation matrix</name>
  <files>.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-VALIDATION.md</files>
  <action>Aggregate focused tests, all-family coverage, schema/FK checks, source fingerprints, compatibility consumers, replay/idempotency, privacy negative tests, projection/activation fault injection, policy replacement, semantic replay rubric, old-run extraction refusal and provider call count. Map each CONV-01..08 and D-01..D-31 to evidence. Mark partial/unknown results honestly. Conclude `READY_FOR_HUMAN_ACTIVATION_REVIEW` only if every critical gate passes and paid_calls=0; otherwise record blockers and keep activation unavailable.</action>
  <verify><automated>python -m pytest -q tests/unit/test_conversation_event_contracts.py tests/contract/test_conversation_stream_adapters.py tests/contract/test_conversation_store_adapters.py tests/security/test_conversation_source_privacy.py tests/integration/test_conversation_event_generations.py tests/contract/test_conversation_extraction_policy.py tests/unit/test_semantic_admission_gate.py tests/contract/test_view_candidate_prepare.py</automated></verify>
  <done>Phase 62 has a complete evidence matrix and an explicit activation disposition without mutating live authority.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Human review of v2 live canonical activation</name>
  <files>.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-SHADOW-REPORT.json; .planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-VALIDATION.md</files>
  <action>Review all-family coverage, partial ChatGPT/Cursor disclosures, source fingerprints, compatibility parity, event/view lineage samples, privacy negatives, rollback fault injection, active KU empty state, old-run refusal and zero paid calls. Approve only the canonical v2 activation/rollback drill in Plan 62-08; this checkpoint does not authorize paid LLM extraction.</action>
  <what-built>A non-active live v2 shadow generation and complete validation evidence.</what-built>
  <how-to-verify>Confirm 17/17 explicit family results; every partial/blocked source is named; source and current canonical fingerprints are unchanged; forbidden-table access and body/secret logs are zero; compatibility consumers pass; activation fault tests restore exact prior state; active KU collection remains empty; old 24,487-call queues cannot execute; paid_calls=0.</how-to-verify>
  <resume-signal>Reply “批准 Phase 62 canonical v2 激活与回滚演练” to release Plan 62-08, or provide blockers. This does not approve any paid extraction.</resume-signal>
  <verify><automated>python -c "import json,pathlib; p=pathlib.Path('.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a'); r=json.loads((p/'62-SHADOW-REPORT.json').read_text(encoding='utf-8')); v=(p/'62-VALIDATION.md').read_text(encoding='utf-8'); assert r.get('paid_calls') == 0 and 'READY_FOR_HUMAN_ACTIVATION_REVIEW' in v"</automated></verify>
  <done>Canonical activation has an explicit human disposition; absent approval, Phase 62 remains safely shadow-only.</done>
</task>
</tasks>

<verification>The shadow report and validation matrix must remain metadata-only and independently reproducible from immutable manifests; no active pointer/watermark/provider call may change.</verification>
<success_criteria>Real data proves all-family ingestion quality and rollback readiness before the user decides whether to activate canonical v2.</success_criteria>
<output>Create 62-07-SUMMARY.md when complete, including the human disposition.</output>
