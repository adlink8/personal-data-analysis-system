---
phase: 62-multi-format-conversation-adapters-unified-event-authority-and-replaceable-extraction-views
plan: 08
type: execute
wave: 7
depends_on: [62-07]
files_modified:
  - data/canonical/agent/structured/db/agent_conversations.sqlite
  - var/db/personal_system.sqlite
  - .planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-ACTIVATION-REPORT.json
  - docs/runbooks/product-sync.md
autonomous: false
requirements: [CONV-05, CONV-08]
must_haves:
  truths:
    - "Only a recorded Plan 62-07 human approval permits live v2 activation."
    - "The active event generation, compatibility projection, publication version and watermark switch together or fully roll back."
    - "Existing canonical consumers and services remain healthy after activation, and old canonical generations remain recoverable."
    - "Active KU stays empty, quarantined KU stays isolated, old message queues remain audit-only/non-executable, and paid calls remain zero."
  artifacts:
    - path: .planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-ACTIVATION-REPORT.json
      provides: before/after/rollback/health/source-fingerprint and zero-paid-call evidence
    - path: docs/runbooks/product-sync.md
      provides: canonical v2 sync, fidelity, shadow, activation and recovery operator workflow
  key_links:
    - from: approved shadow generation
      to: active canonical event generation and compatibility projection
      via: event generation activation transaction
    - from: active canonical checksum
      to: publication registry/watermark/conversation delta
      via: existing post-commit sync seam
---

<objective>
After explicit human approval, atomically activate the validated v2 event generation, prove compatibility and rollback on live state, and leave paid knowledge extraction blocked.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-CONTEXT.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-VALIDATION.md
@.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-07-SUMMARY.md
@docs/runbooks/product-sync.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify approval, stop exact consumers, and capture a recovery manifest</name>
  <files>.planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-ACTIVATION-REPORT.json</files>
  <action>Fail closed unless 62-07-SUMMARY records the exact human approval phrase and 62-VALIDATION is ready. Resolve and stop only REST/MCP/Kernel/current canonical consumers, leaving unrelated processes and source agents untouched. Use SQLite online backup and a checksum manifest for the current canonical DB plus active generation/projection/publication/watermark state. Recheck the approved shadow digest, source snapshot digest, 17-family gate, old-run refusal, current empty KU collection and paid_calls=0 immediately before activation.</action>
  <verify><automated>pk-sync conversations --event-v2-activation-check --shadow-report .planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-SHADOW-REPORT.json --json</automated></verify>
  <done>Exact recovery state and approval are verified; any drift stops before authority mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Activate canonical v2, restart consumers, and verify live compatibility</name>
  <files>data/canonical/agent/structured/db/agent_conversations.sqlite; var/db/personal_system.sqlite; .planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-ACTIVATION-REPORT.json</files>
  <action>Activate only the approved shadow generation through `pk-sync conversations --event-v2-activate ... --write`. Treat projection/version/watermark/delta mismatch as failure and immediately restore the manifest. Restart exact consumers and verify SQLite quick_check/FK, active event generation, compatibility generation binding, publication/watermark, all current `ConversationRepository` consumers, REST/MCP/Kernel health, real canonical fallback search, active KU collection count=0 and no old KU hit. Mark the two old message-level prepare runs append-only `superseded_policy` only after v2 activation succeeds; never delete ledger/cache history.</action>
  <verify><automated>pk-sync status --json; pk-ku doctor --json; rag-search stats --json; python -m pytest -q tests/contract/test_conversation_v2_compatibility.py tests/integration/test_conversation_v2_sync.py</automated></verify>
  <done>Live canonical reads the v2 event authority through a compatible projection, services are healthy, and knowledge serving remains empty/non-paid.</done>
</task>

<task type="auto">
  <name>Task 3: Perform an exact rollback-and-reactivate drill and document operations</name>
  <files>data/canonical/agent/structured/db/agent_conversations.sqlite; .planning/phases/PDA-62-multi-format-conversation-adapters-unified-event-authority-a/62-ACTIVATION-REPORT.json; docs/runbooks/product-sync.md</files>
  <action>Using only the Task 1 manifest, roll back once to the prior canonical generation and verify logical fingerprints, compatibility tables, version/watermark, services and source invariants. Then re-activate the same approved v2 generation and repeat health/parity/event-view/source-ref checks. If either direction fails, restore the prior known-good state and stop; do not improvise another migration. Document dry-run, shadow, fidelity report, activation, status, rollback, partial-family interpretation, old-run supersession and the separate paid semantic-pilot checkpoint in the runbook.</action>
  <verify><automated>python -m pytest -q tests/integration/test_conversation_event_generations.py tests/integration/test_agent_conversation_rollback.py; pk-sync status --json; pk-ku doctor --json; git diff --check</automated></verify>
  <done>Both directions of the live canonical generation switch are proven recoverable and the operator workflow is current.</done>
</task>
</tasks>

<verification>Activation is complete only if source fingerprints are unchanged, v2/projection/version/watermark agree, services and canonical fallback work, active KU remains empty, old queues refuse extraction, paid_calls=0, and the exact rollback/reactivation drill passes.</verification>
<success_criteria>The existing canonical product authority is safely evolved to typed v2 events with backward-compatible reads and demonstrated live rollback, while knowledge extraction remains explicitly blocked.</success_criteria>
<output>Create 62-08-SUMMARY.md when complete.</output>
