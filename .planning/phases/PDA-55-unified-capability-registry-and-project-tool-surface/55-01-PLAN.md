---
phase: 55
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [governance/manifests/capabilities/project-capabilities.json, governance/schemas/project-capability-registry-v1.schema.json, src/personal_knowledge/services/capability_registry.py, tools/supported/generate_capability_descriptors.py, tests/contract/test_project_capability_registry.py]
autonomous: true
requirements: [CAP-01, CAP-02]
must_haves:
  truths:
    - "D-01: one registry is the REST/MCP/Pi capability SSOT."
    - "D-02: public tools are namespaced domain operations, not internal functions."
    - "D-03: every capability declares complete authority and execution metadata."
    - "D-04: profile/drift/escalation checks fail closed."
---

# Phase 55-01 Plan — Capability registry schema and deterministic generator

<objective>建立版本化 Project Capability Registry、Python validator 和确定性 descriptor generator。</objective>
<threat_model>Assets: capability identity, permission profiles and schema integrity. Threats: duplicate aliases, descriptor drift, profile escalation, hidden side effects and generated-code tampering. Controls: canonical checksum, strict schema, enum allowlists, duplicate rejection, deterministic snapshots and fail-closed load.</threat_model>

<tasks>
<task type="execute">
  <name>55-01-01 — Define project-capability-registry-v1</name>
  <read_first>
    - .planning/phases/PDA-55-unified-capability-registry-and-project-tool-surface/55-CONTEXT.md
    - .planning/research/v2.0-pi-capability-os/ARCHITECTURE.md
    - governance/manifests/ai/pi-domain-tools.json
    - apps/personal_data_chatgpt/server.mjs
  </read_first>
  <files>governance/schemas/project-capability-registry-v1.schema.json, governance/manifests/capabilities/project-capabilities.json, src/personal_knowledge/services/capability_registry.py</files>
  <action>Define registry fields `id`, `version`, `checksum`, `title`, `description`, `input_schema`, `output_schema`, `profiles`, `privacy_ceiling`, `authority_class`, `side_effect_class`, `timeout_ms`, `budget`, `idempotency`, `confirmation`, `receipt_schema`, `status` and optional compatibility aliases. Implement strict canonical checksum validation, natural deterministic ordering, unique id/alias enforcement and production/operator filtering. Seed read-only operations for knowledge, retrieval, state, external, decision, action/outcome, evidence, wiki, data quality and system health without exposing table/path/callable identifiers.</action>
  <acceptance_criteria>
    - Registry contains all required fields and only approved enum values.
    - Duplicate ID/alias, unknown profile, checksum drift or read tool with side effects is rejected before load.
    - `python -m pytest tests/contract/test_project_capability_registry.py -q` exits 0 with at least 15 negative fixtures.
  </acceptance_criteria>
  <verify><automated>python -m pytest tests/contract/test_project_capability_registry.py -q</automated></verify>
</task>
<task type="execute">
  <name>55-01-02 — Generate deterministic REST/MCP/Pi descriptor snapshots</name>
  <read_first>
    - governance/manifests/capabilities/project-capabilities.json
    - src/personal_knowledge/services/capability_registry.py
    - apps/personal_data_chatgpt/scripts/descriptor-snapshot.mjs
    - governance/manifests/ai/pi-tool-registry.json
  </read_first>
  <files>tools/supported/generate_capability_descriptors.py, tests/contract/test_project_capability_registry.py</files>
  <action>Implement `generate_capability_descriptors.py --write|--check` to produce stable REST/MCP/Pi descriptor JSON with source registry checksum and per-operation schema checksum. `--check` compares generated bytes to tracked snapshots and exits nonzero on drift; generator must not inspect runtime network, user home or ambient Pi configuration.</action>
  <acceptance_criteria>
    - Two generation runs produce byte-identical snapshots.
    - `--check` fails after a seeded descriptor edit and passes after regeneration.
    - Every generated operation points to one source capability ID and checksum.</acceptance_criteria>
  <verify><automated>python tools/supported/generate_capability_descriptors.py --check</automated></verify>
</task>
</tasks>

<verification>Run registry contract tests and deterministic generation check from a clean temporary output directory.</verification>
<success_criteria>One strict registry can deterministically describe the same approved capability surface for every consumer.</success_criteria>
