---
phase: 26
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - src/personal_knowledge/intelligence/decision/schema.py
  - src/personal_knowledge/intelligence/decision/runs.py
  - src/personal_knowledge/intelligence/decision/recommendations.py
  - src/personal_knowledge/intelligence/decision/state_machine.py
  - tests/unit/test_decision_state_machine.py
  - tests/contract/test_decision_cognition_boundaries.py
  - tests/integration/test_decision_feedback_runs.py
  - tests/integration/test_decision_feedback_concurrency.py
autonomous: true
requirements: [DEC-01, DEC-02]
must_haves:
  truths:
    - "Recommendation generation is deterministic, versioned and abstains on insufficient, stale, conflicting, uncertain or contraindicated Phase 25 inputs."
    - "Accept, reject, defer and revoke-before-action record a user decision only; acceptance never validates premises and never executes an action."
    - "Confirmation and action history is projected from append-only events with legal transitions, exact recommendation checksum, human actor validation and expiry checks."
    - "Every projected stream starts at the valid sequence=1 recommendation_published genesis; confirmation and action events continue its expected_sequence and previous_event_checksum chain rather than creating a partial history."
    - "Every write uses expected_sequence plus BEGIN IMMEDIATE and an idempotency key; retries are stable and concurrent stale writers fail closed."
  artifacts:
    - path: src/personal_knowledge/intelligence/decision/recommendations.py
      provides: "Versioned deterministic recommendation rule registry and abstention"
    - path: src/personal_knowledge/intelligence/decision/state_machine.py
      provides: "Append-only confirmation/action event writer and pure projector"
    - path: tests/integration/test_decision_feedback_concurrency.py
      provides: "Expected-sequence, idempotency, contention and rollback proof"
  key_links:
    - from: src/personal_knowledge/intelligence/decision/recommendations.py
      to: src/personal_knowledge/intelligence/decision/runs.py
      via: "Policy version and canonical recommendation payload bound into decision run"
    - from: src/personal_knowledge/intelligence/decision/state_machine.py
      to: decision_events
      via: "Validate the publication genesis, then continue one checksum-linked monotonic sequence per recommendation stream"
    - from: decision_events
      to: decision_recommendations
      via: "Sequence=1 recommendation_published binds the exact recommendation and decision/source checksums before later confirmation/action events hydrate"
    - from: decision_confirmations
      to: decision_recommendations
      via: "Exact recommendation ID and checksum without premise promotion"
---

<objective>
Implement deterministic recommendations and the permission-safe recommendation-to-confirmation-to-action state machine.

Purpose: make the first half of DEC-02 auditable while keeping user confirmation separate from truth and external execution.
Output: versioned recommendation rules, pure event projection and concurrency-safe append APIs.
</objective>

<execution_context>
Read before implementation:
- `26-01-PLAN.md` and its completed implementation
- `src/personal_knowledge/intelligence/changes.py`
- `src/personal_knowledge/intelligence/explanations.py`
- `src/personal_knowledge/application/knowledge/lifecycle_events.py`
- `tests/integration/test_personal_state_runs.py`
- `.planning/phases/PDA-24-evaluation-closure-and-lifecycle-adoption-close-target-b-c-q/24-03-CHECKPOINT.md`

Use only deterministic local rules and SQLite. No LLM, connector, subprocess, scheduler, network call, paid provider or live confirmation/action write is permitted.
</execution_context>

<tasks>
<task type="execute">
  <name>Task 1: Write failing recommendation, permission, replay and concurrency tests</name>
  <files>tests/unit/test_decision_state_machine.py; tests/contract/test_decision_cognition_boundaries.py; tests/integration/test_decision_feedback_runs.py; tests/integration/test_decision_feedback_concurrency.py</files>
  <action>Add tests first for deterministic rule replay and policy-version drift; minimum evidence, stale/conflict/uncertainty/contraindication abstention; expired recommendation rejection; non-human actor rejection; accept/reject/defer/revoke-before-action transitions; planned/started/completed/abandoned/not_taken action transitions; confirmation not changing any Phase 25 row; accept not creating an action; illegal transitions; history beginning with exactly one `recommendation_published` at sequence=1; confirmation/action continuing at expected sequence 2+ with the genesis/event `previous_event_checksum`; missing, duplicated or tampered genesis rejection during projection/hydration; same-key same-payload replay; same-key changed-payload conflict; two-writer expected-sequence contention; same-second ordering; checksum-chain tampering; and injected failures after each insert boundary rolling back all rows. Monkeypatch network, subprocess and connector entry points and require zero calls.</action>
  <verify>python -m pytest tests/unit/test_decision_state_machine.py tests/contract/test_decision_cognition_boundaries.py tests/integration/test_decision_feedback_runs.py tests/integration/test_decision_feedback_concurrency.py -q</verify>
  <done>Tests define the complete legal transition and permission envelope and fail before implementation without touching live data.</done>
</task>

<task type="execute">
  <name>Task 2: Add versioned deterministic recommendation rules</name>
  <files>src/personal_knowledge/intelligence/decision/schema.py; src/personal_knowledge/intelligence/decision/runs.py; src/personal_knowledge/intelligence/decision/recommendations.py</files>
  <action>Implement a small immutable rule registry. Each rule declares ID/version, eligible Phase 25 cognition types, evidence minimum, domain/kind, uncertainty behavior, contraindications and expiry. Produce structured recommendations containing target, horizon, rationale codes, expected benefit, costs/constraints, assumptions, confidence and uncertainty, with support refs only. Insufficient, stale, conflicting, abstained or cross-snapshot input yields an explicit reason code and no recommendation. Bind rule/policy version and canonical outputs into DecisionRun identity; do not use free-form LLM generation or copy source bodies.</action>
  <verify>python -m pytest tests/unit/test_decision_state_machine.py tests/contract/test_decision_cognition_boundaries.py tests/integration/test_decision_feedback_runs.py -q</verify>
  <done>Eligible fixtures replay byte-identically, policy changes version outputs, and every unsafe/insufficient fixture abstains without a committed recommendation.</done>
</task>

<task type="execute">
  <name>Task 3: Implement append-only confirmation and action streams</name>
  <files>src/personal_knowledge/intelligence/decision/state_machine.py; src/personal_knowledge/intelligence/decision/schema.py</files>
  <action>Implement a pure state projector over ordered decision_events and explicit append functions for confirmation and action records. Before projecting or appending, require exactly one legal sequence=1 `recommendation_published` genesis whose bindings match the recommendation ID/checksum, decision run checksum and Phase 25 source run/snapshot; reject missing, duplicated, reordered or checksum-tampered genesis. Validate exact recommendation checksum, expiry, legal transition, human actor class/hashed identity, caller idempotency key and `expected_sequence`. Within one `BEGIN IMMEDIATE`, compare the current sequence (already 1 immediately after publication), reserve the next immutable integer sequence, insert the typed row and an event whose `previous_event_checksum` equals the prior event checksum, then commit once. Same canonical replay returns the original receipt without another event; changed payload under the same key returns idempotency_conflict. Any typed-row/event failure rolls back both inserts. Action records are attestations/plans only and must reject executable commands, URLs, connector credentials and dispatch targets. Do not infer user identity from REST/MCP context.</action>
  <verify>python -m pytest tests/unit/test_decision_state_machine.py tests/integration/test_decision_feedback_concurrency.py tests/contract/test_decision_cognition_boundaries.py -q</verify>
  <done>Concurrent and retried writes extend one valid genesis-rooted checksum chain without duplicate events; missing/tampered genesis, stale writers, event failures and illegal transitions fail without rows, and no acceptance path triggers external execution or premise promotion.</done>
</task>
</tasks>

<verification>
- `python -m pytest tests/unit/test_decision_state_machine.py tests/contract/test_decision_cognition_boundaries.py tests/integration/test_decision_feedback_runs.py tests/integration/test_decision_feedback_concurrency.py -q`
- `python -m pytest tests/unit/test_personal_state_changes.py tests/contract/test_personal_state_explanations.py tests/integration/test_personal_state_runs.py -q`
- Static/runtime contract proves `external_actions=0`, `network_calls=0`, `paid_calls=0`.
- Projection and hydration fixtures prove sequence starts at the publication genesis and every later event preserves expected sequence and previous-event checksum continuity.
</verification>

<success_criteria>
- Recommendations are deterministic proposals and never facts or KUs.
- User decisions, action attestations and their ordering are append-only, checksum-bound and permission-safe.
- The full history is reconstructable from one valid publication genesis; missing, duplicate, reordered or tampered genesis/events fail closed.
- Idempotency, concurrency, replay, tamper and rollback behavior is directly tested.
- Phase 24 remains unresolved and no live decision/action is recorded.
</success_criteria>
