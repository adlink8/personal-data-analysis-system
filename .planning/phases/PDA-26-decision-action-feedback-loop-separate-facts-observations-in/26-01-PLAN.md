---
phase: 26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - governance/policies/artifact_layers.yaml
  - src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py
  - src/personal_knowledge/intelligence/decision/__init__.py
  - src/personal_knowledge/intelligence/decision/schema.py
  - src/personal_knowledge/intelligence/decision/runs.py
  - tests/unit/test_decision_schema.py
  - tests/contract/test_decision_cognition_boundaries.py
  - tests/integration/test_decision_feedback_runs.py
autonomous: true
requirements: [DEC-01, DEC-02]
must_haves:
  truths:
    - "a.decision_feedback is an independent non-serving A-layer authority whose evidence parent is a.personal_change."
    - "Facts, observations and inferences are typed Phase 25 references; recommendations and user confirmations cannot become assertions, facts or knowledge units."
    - "Every committed decision run and recommendation is bound to one exact Phase 25 run checksum/publication sequence and serving snapshot ID/hash."
    - "Each recommendation is atomically published with exactly one recommendation_published sequence=1 genesis event bound to the recommendation ID/checksum, decision run checksum and Phase 25 source run/snapshot; no committed recommendation can exist without that genesis."
    - "Committed rows are append-only, checksum verified and idempotent; cross-snapshot, unpublished, stale or tampered inputs fail without partial rows."
  artifacts:
    - path: governance/policies/artifact_layers.yaml
      provides: "Non-serving a.decision_feedback registry entry"
    - path: src/personal_knowledge/intelligence/decision/schema.py
      provides: "Frozen cognitive, recommendation, support and run records with canonical identity"
    - path: src/personal_knowledge/intelligence/decision/runs.py
      provides: "Snapshot/run-bound planning, validation and atomic publication"
    - path: tests/contract/test_decision_cognition_boundaries.py
      provides: "DEC-01 anti-reclassification and authority boundary proof"
  key_links:
    - from: src/personal_knowledge/intelligence/decision/runs.py
      to: src/personal_knowledge/intelligence/service.py
      via: "Read-only resolution of a committed Phase 25 run and its typed refs"
    - from: src/personal_knowledge/intelligence/decision/runs.py
      to: governance/policies/artifact_layers.yaml
      via: "Registry ID, layer, parent and non-serving validation"
    - from: decision_support_refs
      to: personal_state_runs
      via: "Exact source run/checksum/publication and snapshot binding"
    - from: src/personal_knowledge/intelligence/decision/runs.py
      to: decision_events
      via: "Same publication transaction inserts the unique sequence=1 recommendation_published genesis bound to recommendation, decision run and Phase 25 source checksums"
---

<objective>
Create the independent immutable decision-feedback authority and lock the cognitive boundary before recommendation workflow behavior is added.

Purpose: satisfy DEC-01 at storage and type level while providing the snapshot-bound foundation required by DEC-02.
Output: additive schema, typed canonical records, deterministic run publication and negative-path contracts.
</objective>

<execution_context>
Read before implementation:
- `.planning/phases/PDA-26-decision-action-feedback-loop-separate-facts-observations-in/26-RESEARCH.md`
- `.planning/phases/PDA-25-personal-state-and-change-intelligence-model-goals-constrain/25-VERIFICATION.md`
- `src/personal_knowledge/intelligence/schema.py`
- `src/personal_knowledge/intelligence/runs.py`
- `src/personal_knowledge/intelligence/service.py`
- `src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py`
- `governance/policies/artifact_layers.yaml`

All schema execution is against temporary test databases. Do not run the migration against the live project database and do not change serving roles, pointers, lifecycle state or watermarks.
</execution_context>

<tasks>
<task type="execute">
  <name>Task 1: Write failing authority, cognition and immutable-run contracts</name>
  <files>tests/unit/test_decision_schema.py; tests/contract/test_decision_cognition_boundaries.py; tests/integration/test_decision_feedback_runs.py</files>
  <action>Add tests first for: registry entry present but absent from required_serving_roles; exact discriminators fact/observation/inference/recommendation/user_confirmation; omitted discriminator and recommendation-as-fact/KU rejection; confirmation never changing Phase 25 provenance/evidence; additive table/FK/index/immutable-trigger idempotence; source Phase 25 run checksum/publication/snapshot binding; exact replay; cross-snapshot and unpublished-run rejection; row/payload/manifest tamper rejection; one `recommendation_published` sequence=1 genesis per recommendation bound to the recommendation ID/checksum, decision run checksum and Phase 25 source run/snapshot; replay producing no duplicate genesis; missing or tampered genesis rejection; sequence starting at one; fault injection after recommendation or genesis insertion rolling back the whole publication; and zero KU/lifecycle/serving/watermark changes. Build fixtures only in temporary SQLite files.</action>
  <verify>python -m pytest tests/unit/test_decision_schema.py tests/contract/test_decision_cognition_boundaries.py tests/integration/test_decision_feedback_runs.py -q</verify>
  <done>The new contracts fail for missing implementation and encode every DEC-01 authority boundary without requiring live schema or private bodies.</done>
</task>

<task type="execute">
  <name>Task 2: Add the non-serving decision authority and additive immutable tables</name>
  <files>governance/policies/artifact_layers.yaml; src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py</files>
  <action>Register `a.decision_feedback` as layer A, privacy R4, evidence parent `a.personal_change`, producer `intelligence.decision`, lifecycle immutable and version source run-manifest checksum. Do not add a serving role. Add normalized tables for decision_runs, decision_recommendations, decision_support_refs, decision_confirmations, decision_actions, decision_outcomes, decision_effectiveness and decision_events with foreign keys, constrained enums, canonical payload/checksum columns, stream sequence/idempotency uniqueness where applicable, indexes and UPDATE/DELETE rejection triggers. Constrain the event ledger so each recommendation stream has one immutable sequence=1 `recommendation_published` genesis and later events cannot occupy sequence one. Recommendation rows must have no fact/KU/approved/executed authority field. Preserve all existing tables and migration idempotence.</action>
  <verify>python -m pytest tests/unit/test_decision_schema.py tests/governance/test_artifact_layer_policy.py tests/integration/test_serving_snapshot_schema.py -q</verify>
  <done>Temporary migrations create the complete independent authority exactly once, enforce immutable typed rows, and leave required serving roles unchanged.</done>
</task>

<task type="execute">
  <name>Task 3: Implement typed references and snapshot-bound atomic run publication</name>
  <files>src/personal_knowledge/intelligence/decision/__init__.py; src/personal_knowledge/intelligence/decision/schema.py; src/personal_knowledge/intelligence/decision/runs.py</files>
  <action>Implement frozen records and canonical JSON/checksum helpers for CognitionReference, DecisionRun, Recommendation, support refs, publication manifests and the genesis event. Facts/observations/inferences must be referenced from Phase 25 by exact record/run/snapshot metadata and never copied as truth rows. Resolve one committed Phase 25 publication, verify its run/output checksum, immutable publication sequence, snapshot ID/hash and support refs, then deterministically plan IDs/checksums. Publish only with explicit `write=True` in one `BEGIN IMMEDIATE` transaction after revalidation; for every recommendation insert the recommendation row and exactly one `recommendation_published` event at `sequence=1` in that same transaction. Its canonical payload/checksum must bind the recommendation ID/checksum, decision run checksum, Phase 25 source run ID/checksum and snapshot ID/hash; its previous-event checksum is the explicit genesis sentinel. Exact replay returns the existing run and genesis without inserting a duplicate, while changed policy/input creates a new immutable run. Any recommendation/genesis insert or checksum failure rolls back all run, recommendation, support and event rows. Add fault-injection boundaries and fail-closed stable reason codes. This API must not generate an external action, migrate live data or alter Phase 25.</action>
  <verify>python -m pytest tests/unit/test_decision_schema.py tests/contract/test_decision_cognition_boundaries.py tests/integration/test_decision_feedback_runs.py tests/integration/test_personal_state_runs.py -q</verify>
  <done>Same source run/policy/input replays identically with one and only one valid sequence=1 publication genesis; drift, tampering, missing genesis, event failure and mixed snapshots publish nothing; every recommendation support remains a typed Phase 25 reference.</done>
</task>
</tasks>

<verification>
- `python -m pytest tests/unit/test_decision_schema.py tests/contract/test_decision_cognition_boundaries.py tests/integration/test_decision_feedback_runs.py -q`
- `python -m pytest tests/unit/test_personal_state_schema.py tests/integration/test_personal_state_runs.py tests/integration/test_serving_snapshot_schema.py tests/governance/test_artifact_layer_policy.py -q`
- `python -m personal_knowledge.governance.preflight --ci`
- Atomic rollback, replay and integrity fixtures prove no recommendation can commit without one valid checksum-bound sequence=1 `recommendation_published` genesis and retries cannot duplicate it.
</verification>

<success_criteria>
- DEC-01 cognitive types and permissions are machine-enforced, not documentation-only.
- `a.decision_feedback` exists independently and is not part of serving authority.
- Immutable decision runs bind exact Phase 25 and snapshot versions with deterministic replay and atomic rollback.
- Every recommendation stream is reconstructable from its atomic publication genesis; missing, duplicated or tampered genesis fails closed.
- No migration is applied to live data and Phase 24 remains release-blocked.
</success_criteria>
