---
phase: 45
plan: 02
status: complete
---

# Phase 45-02 Plan — Evidence drawer and no-write UX

把既有只读 `EvidenceDrawer` 接到 Wiki claim 的服务端 authority ref；缺少完整 stable binding 时不构造引用。检查键盘 Esc/焦点行为沿用既有抽屉契约，页面不新增 mutation 控件。
