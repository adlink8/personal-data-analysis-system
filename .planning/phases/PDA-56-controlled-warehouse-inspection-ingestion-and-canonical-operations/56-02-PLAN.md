---
phase: 56
plan: 02
type: execute
wave: 2
depends_on: [56-01]
files_modified: [src/personal_knowledge/services/warehouse_mutations.py, src/personal_knowledge/services/pi_domain_gateway.py, governance/manifests/capabilities/project-capabilities.json, tests/integration/test_pi_warehouse_mutations.py, tests/e2e/test_pi_warehouse_recovery.py]
autonomous: true
requirements: [WARE-02, SEC-03]
must_haves:
  truths:
    - "D-02: raw is immutable and canonical corrections are compensating events."
    - "D-03: mutations bind ledger, snapshot/watermark, idempotency and fingerprints."
    - "D-04: canonical L3 commit requires exact preview and confirmation."
    - "D-06: outcome_unknown reconciles before resume or compensation."
---

# Phase 56-02 Plan — Guarded ingestion and canonical maintenance

<objective>把 source ingestion 与 canonical reconcile/correction 封装成幂等、可恢复、可补偿的 Tool 事务。</objective>
<threat_model>Assets: raw history, canonical identity, watermark and import idempotency. Threats: duplicate import, wrong-source mutation, stale preview, partial commit, blind retry and history overwrite. Controls: immutable source manifest, exact preview checksum, transaction ledger, CAS binding, compensation records and recovery reconciliation.</threat_model>

<tasks>
<task type="execute">
  <name>56-02-01 — Implement operation ledger and ingestion/canonical preview protocol</name>
  <read_first>
    - src/personal_knowledge/services/orchestration_service.py
    - src/personal_knowledge/services/pi_domain_gateway.py
    - src/personal_knowledge/application/conversation/
    - .planning/research/v2.0-pi-capability-os/ARCHITECTURE.md
  </read_first>
  <files>src/personal_knowledge/services/warehouse_mutations.py, src/personal_knowledge/services/pi_domain_gateway.py, governance/manifests/capabilities/project-capabilities.json, tests/integration/test_pi_warehouse_mutations.py</files>
  <action>Create metadata-only `pi_data_operations` ledger with operation_id, capability_id, actor/profile, idempotency, authority/snapshot/watermark binding, plan/preview checksum, status, expected/actual counts, before/after fingerprints, receipt and compensation reference. Add `ingestion.discover|preview|commit|quarantine` and `canonical.reconcile|deduplicate|link|apply_correction|verify`; commit accepts only server-issued unexpired preview, exact checksum, `confirmed=true` and one idempotency identity. Corrections append compensating records linked to prior identity and never update/delete raw history.</action>
  <acceptance_criteria>
    - Preview lists exact logical authority, source checksum, operation/count ceilings and unchanged guarantees.
    - Duplicate commit returns original receipt; stale/tampered/binding-mismatch preview writes zero rows.
    - Mutation integration suite proves raw rows immutable and compensation linkage reconstructs current canonical state.</acceptance_criteria>
  <verify><automated>python -m pytest tests/integration/test_pi_warehouse_mutations.py -q</automated></verify>
</task>
<task type="execute">
  <name>56-02-02 — Add crash, outcome-unknown and compensation recovery</name>
  <read_first>
    - src/personal_knowledge/services/warehouse_mutations.py
    - apps/personal_intelligence_kernel/src/tasks/ledger.mjs
    - tests/integration/test_pi_warehouse_mutations.py
    - .planning/phases/PDA-56-controlled-warehouse-inspection-ingestion-and-canonical-operations/56-VALIDATION.md
  </read_first>
  <files>tests/e2e/test_pi_warehouse_recovery.py</files>
  <action>Inject crashes before transaction, after DB commit before receipt, and after receipt; reconcile operation ledger against deterministic store markers before allowing retry. Mark ambiguous states `outcome_unknown`; resume either returns committed receipt or executes one declared compensation. Verify no duplicate source/canonical rows, no accidental watermark advance and exact before/after fingerprint semantics.</action>
  <acceptance_criteria>
    - Every crash point converges to one committed or one compensated operation.
    - Blind retry is rejected while outcome is unknown.
    - E2E recovery suite exits 0 with zero duplicate canonical identity and unchanged unrelated authority fingerprints.</acceptance_criteria>
  <verify><automated>python -m pytest tests/e2e/test_pi_warehouse_recovery.py -q</automated></verify>
</task>
</tasks>

<verification>Run mutation and recovery suites against temporary authority copies; inspect operation ledger for one receipt per idempotency identity.</verification>
<success_criteria>Pi can safely drive ingestion and canonical maintenance while Python preserves immutable history and exact transaction authority.</success_criteria>
