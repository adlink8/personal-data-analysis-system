---
phase: 56
plan: 01
type: execute
wave: 1
depends_on: [55-02]
files_modified: [src/personal_knowledge/services/warehouse_tools.py, governance/manifests/capabilities/project-capabilities.json, tests/contract/test_pi_warehouse_read_tools.py, tests/security/test_pi_warehouse_tool_containment.py]
autonomous: true
requirements: [WARE-01, SEC-03]
must_haves:
  truths:
    - "D-01: Pi receives operations, never SQL/path/callable authority."
    - "D-05: results are bounded metadata and references."
    - "All forbidden capability fixtures leave authority fingerprints unchanged."
---

# Phase 56-01 Plan — Warehouse inspection and containment Tools

<objective>提供安全的底仓 schema/stat/lineage/freshness/integrity/anomaly/failed-batch Tool 面。</objective>
<threat_model>Assets: personal warehouse contents, topology, credentials and availability. Threats: SQL injection, path traversal, table enumeration, unbounded export, secret/error leakage and expensive scans. Controls: operation enums, logical authority IDs, parameter caps, query templates, timeout, safe envelopes and fingerprint checks.</threat_model>

<tasks>
<task type="execute">
  <name>56-01-01 — Implement parameterized warehouse read facade</name>
  <read_first>
    - .planning/phases/PDA-56-controlled-warehouse-inspection-ingestion-and-canonical-operations/56-CONTEXT.md
    - src/personal_knowledge/services/dashboard.py
    - src/personal_knowledge/services/agent_contract.py
    - governance/manifests/capabilities/project-capabilities.json
  </read_first>
  <files>src/personal_knowledge/services/warehouse_tools.py, governance/manifests/capabilities/project-capabilities.json, tests/contract/test_pi_warehouse_read_tools.py</files>
  <action>Add registry operations `warehouse.inspect`, `warehouse.lineage`, `warehouse.quality`, `warehouse.freshness`, `warehouse.integrity` and `warehouse.failed_batches`. Inputs use logical authority IDs, bounded date/filter enums and limit 1..100; Python maps IDs to fixed adapters and returns schema version, counts, checks, stable IDs, limitations and artifact refs. No input accepts SQL, path, table, callable or arbitrary order expression.</action>
  <acceptance_criteria>
    - Every operation returns a compact registry output schema and completes within configured timeout on fixtures.
    - Inputs containing SQL fragments, path separators, unknown authority or limit >100 are rejected before DB open.
    - Contract tests cover every operation and exit 0.</acceptance_criteria>
  <verify><automated>python -m pytest tests/contract/test_pi_warehouse_read_tools.py -q</automated></verify>
</task>
<task type="execute">
  <name>56-01-02 — Prove data-plane negative capability boundary</name>
  <read_first>
    - src/personal_knowledge/services/warehouse_tools.py
    - tests/contract/test_pi_runtime_containment.py
    - governance/manifests/ai/pi-network-allowlist.json
    - .planning/phases/PDA-56-controlled-warehouse-inspection-ingestion-and-canonical-operations/56-VALIDATION.md
  </read_first>
  <files>tests/security/test_pi_warehouse_tool_containment.py</files>
  <action>Create isolated fixtures for arbitrary SQL, ATTACH, PRAGMA mutation, traversal, symlink, executable/callable, DELETE/TRUNCATE, schema change, unknown network, oversized result and credential-shaped values. Snapshot canonical DB, watermark, active pointer and serving manifest metadata before/after; assert zero mutation and safe error codes with no seeded values.</action>
  <acceptance_criteria>
    - All forbidden fixtures fail closed with zero Python domain operation execution where preflight applies.
    - Protected fingerprints are byte-identical before and after the suite.
    - Security suite exits 0 without touching live `var/db` paths.</acceptance_criteria>
  <verify><automated>python -m pytest tests/security/test_pi_warehouse_tool_containment.py -q</automated></verify>
</task>
</tasks>

<verification>Run read contracts and containment suite against temporary fixture roots.</verification>
<success_criteria>Pi can inspect warehouse health and lineage without receiving general database or host capability.</success_criteria>
