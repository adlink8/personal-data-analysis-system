---
phase: 52
plan: 01
type: execute
wave: 1
depends_on: [51-02]
files_modified: [src/personal_knowledge/services/pi_runtime_projection.py, src/personal_knowledge/services/api_server.py, apps/personal_decision_cockpit/src/api/schemas.ts, apps/personal_decision_cockpit/src/api/hooks.ts, apps/personal_decision_cockpit/src/pages/system/PiRuntimePage.tsx, apps/personal_decision_cockpit/src/app/router.tsx, apps/personal_decision_cockpit/src/test/PiRuntimePage.test.tsx, tests/contract/test_pi_cockpit_transport.py]
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "D-01: browser uses same-origin 8000 proxy and never connects to 8790."
    - "D-02: pi_cockpit_event_v1 excludes raw AI bodies."
    - "D-03: cancel/resume states are versioned and truthful."
    - "D-04: UI reuses the existing Cockpit design system."
---

# Phase 52-01 Plan — Same-origin event projection and Pi Runtime UI

<objective>把安全 Pi event/task projection 接入现有 API/Cockpit，并实现可访问、可恢复、无正文的运行时页面。</objective>
<threat_model>Assets: browser privacy, control mutations and runtime truth. Threats: raw SDK/body leakage, cross-origin control, stale cancel, SSE duplicate and misleading terminal status. Controls: same-origin projection schema, origin gate, task version/idempotency, client de-dup and UI-SPEC states.</threat_model>
<tasks>
<task type="execute">
  <name>52-01-01 — Add safe same-origin Pi runtime/SSE/control contracts</name>
  <read_first>
    - .planning/phases/PDA-52-cockpit-streaming-supervision-and-observability/52-CONTEXT.md
    - .planning/phases/PDA-52-cockpit-streaming-supervision-and-observability/52-UI-SPEC.md
    - src/personal_knowledge/services/api_server.py
    - src/personal_knowledge/services/ui_projection.py
    - tests/contract/test_cockpit_transport_security.py
  </read_first>
  <files>src/personal_knowledge/services/pi_runtime_projection.py, src/personal_knowledge/services/api_server.py, tests/contract/test_pi_cockpit_transport.py</files>
  <action>Implement safe projection for Kernel status, task list/detail and `pi_cockpit_event_v1`. Add same-origin GET status/tasks, SSE stream and POST cancel/resume routes under `/api/pi/`; Python connects to loopback Kernel internally. Preserve existing Origin policy and static errors. POST requires task_id, expected_version and idempotency_key; stale version/cross-origin/unknown task has zero writes. Strip prompt/completion/Tool input/result/provider body/path/credential fields before serialization.</action>
  <acceptance_criteria>
    - Browser contract contains only UI-SPEC allowlisted metadata and safe recovery actions.
    - Cross-origin and stale-version POST are rejected with zero task changes.
    - SSE cursor replay is ordered/de-duplicable and raw Kernel errors are sealed.
    - Python transport contract test exits 0.</acceptance_criteria>
  <verify><automated>python -m pytest tests/contract/test_pi_cockpit_transport.py -q</automated></verify>
</task>
<task type="execute">
  <name>52-01-02 — Build Pi Runtime page, timeline and truthful controls</name>
  <read_first>
    - apps/personal_decision_cockpit/src/api/schemas.ts
    - apps/personal_decision_cockpit/src/api/hooks.ts
    - apps/personal_decision_cockpit/src/components/layout/AppShell.tsx
    - apps/personal_decision_cockpit/src/pages/system/SystemPage.tsx
    - .planning/phases/PDA-52-cockpit-streaming-supervision-and-observability/52-UI-SPEC.md
  </read_first>
  <files>apps/personal_decision_cockpit/src/api/schemas.ts, apps/personal_decision_cockpit/src/api/hooks.ts, apps/personal_decision_cockpit/src/pages/system/PiRuntimePage.tsx, apps/personal_decision_cockpit/src/app/router.tsx, apps/personal_decision_cockpit/src/test/PiRuntimePage.test.tsx</files>
  <action>Add Zod DTO and hooks for status/tasks/SSE/cancel/resume. Build AI Runtime route with KernelStatusCard, keyboard task list, task timeline, privacy notice and state-aware controls exactly per UI-SPEC. De-duplicate by event_id, reconnect from last cursor, throttle polite live region and show requested/acknowledged/terminal cancellation distinctly. Do not persist raw events in localStorage or log them.</action>
  <acceptance_criteria>
    - Component covers empty/loading/live/replay/stale/offline/error/outcome_unknown and every cancel state.
    - Keyboard selection, visible focus, Esc/focus return and reduced-motion assertions pass.
    - 320/768/1024/1440 fixtures have no clipped controls or horizontal page overflow.
    - Component tests and frontend build exit 0.</acceptance_criteria>
  <verify><automated>npm test --prefix apps/personal_decision_cockpit -- --run src/test/PiRuntimePage.test.tsx</automated></verify>
</task>
</tasks>
<verification>Run Python transport, React component tests and `npm run build --prefix apps/personal_decision_cockpit`.</verification>
<success_criteria>Cockpit presents truthful Pi runtime state and guarded controls without exposing Kernel/raw AI data.</success_criteria>
