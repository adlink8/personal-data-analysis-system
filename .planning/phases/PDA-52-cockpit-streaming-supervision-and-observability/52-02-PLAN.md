---
phase: 52
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified: [ops/runtime/start-agent-stack.ps1, ops/runtime/smoke-agent-stack.py, src/personal_knowledge/services/ui_projection.py, tests/ops/test_pi_kernel_stack.py, apps/personal_decision_cockpit/src/test/piRuntimeUatContracts.test.ts]
autonomous: true
requirements: [UI-01, OPS-01]
must_haves:
  truths:
    - "D-05: supervisor owns Kernel child and preserves unknown port owners."
    - "D-06: telemetry is metadata-only and privacy sealed."
---

# Phase 52-02 Plan — Kernel supervision, readiness and privacy-safe observability

<objective>把 Kernel 接入现有 foreground supervisor，建立独立 readiness/restart/telemetry，并完成自动 UI 安全契约。</objective>
<threat_model>Assets: process ownership, service availability and local diagnostics. Threats: killing foreign process, restart storm, listener-only readiness, secret/body logs and false UI health. Controls: command ownership match, bounded restart, `/ready`, structured field allowlist, stale observation semantics and dry-run zero writes.</threat_model>
<tasks>
<task type="execute">
  <name>52-02-01 — Add owned Kernel child to production supervisor and status projection</name>
  <read_first>
    - ops/runtime/start-agent-stack.ps1
    - tests/ops/test_agent_stack_script.py
    - apps/personal_intelligence_kernel/src/server.mjs
    - src/personal_knowledge/services/ui_projection.py
  </read_first>
  <files>ops/runtime/start-agent-stack.ps1, ops/runtime/smoke-agent-stack.py, src/personal_knowledge/services/ui_projection.py, tests/ops/test_pi_kernel_stack.py</files>
  <action>Add KernelPort 8790 and an explicit `pi-kernel` child spec after REST prerequisites and before consumers that require AI readiness. Check/Probe remain zero-write; Run adopts only a healthy matching process and never kills unknown port owners. Readiness uses `/ready`, restart budget remains bounded, stop verifies command ownership, and saved state includes only service/pid/adopted/healthy/restarts/health_url. UI projection distinguishes live REST observation from historical supervisor state and adds safe Kernel readiness metadata.</action>
  <acceptance_criteria>
    - Check/DryRun creates no ops state/log and reports exact missing prerequisites.
    - Unknown/unhealthy port owner is preserved and startup fails diagnostically.
    - Owned Kernel is stopped/restarted within budget; orphan count is zero.
    - Ops tests exit 0 and existing supervisor tests remain green.</acceptance_criteria>
  <verify><automated>python -m pytest tests/ops/test_agent_stack_script.py tests/ops/test_pi_kernel_stack.py -q</automated></verify>
</task>
<task type="execute">
  <name>52-02-02 — Close automated accessibility/privacy/degraded contracts</name>
  <read_first>
    - apps/personal_decision_cockpit/src/pages/system/PiRuntimePage.tsx
    - apps/personal_decision_cockpit/src/test/PiRuntimePage.test.tsx
    - tests/contract/test_pi_cockpit_transport.py
    - .planning/phases/PDA-52-cockpit-streaming-supervision-and-observability/52-VALIDATION.md
  </read_first>
  <files>apps/personal_decision_cockpit/src/test/piRuntimeUatContracts.test.ts, tests/contract/test_pi_cockpit_transport.py</files>
  <action>Add contract fixtures for reconnect duplicate, stale task version, Kernel offline, Python proxy offline, outcome_unknown, long IDs, 200% viewport, reduced motion, keyboard/Esc/focus return and seeded sensitive values in Kernel/provider failures. Assert DOM, URL, localStorage, console, SSE and supervisor logs contain none of the secret/path/prompt/result fixtures and that all mutation failures are zero-write.</action>
  <acceptance_criteria>
    - Automated UAT contract covers all six UI-SPEC verification dimensions.
    - No sensitive fixture appears in any browser/API/ops surface.
    - Degraded state always gives an observed timestamp and typed recovery action.
    - Frontend UAT contracts and Python transport contracts exit 0.</acceptance_criteria>
  <verify><automated>npm test --prefix apps/personal_decision_cockpit -- --run src/test/piRuntimeUatContracts.test.ts</automated></verify>
</task>
</tasks>
<verification>Run supervisor, transport, React tests and frontend build; real browser UAT remains Phase 53.</verification>
<success_criteria>Kernel is an owned, observable service and the Cockpit runtime surface is technically ready for real UAT.</success_criteria>
