---
phase: 37
plan: 03
type: execute
wave: 3
depends_on: [37-01, 37-02]
files_modified:
  - apps/personal_decision_cockpit/src/api/hooks.ts
  - apps/personal_decision_cockpit/src/components/evidence/EvidenceDrawer.tsx
  - apps/personal_decision_cockpit/src/components/evidence/WidgetDiagnosticCard.tsx
  - apps/personal_decision_cockpit/src/pages/overview/OverviewPage.tsx
  - apps/personal_decision_cockpit/src/pages/state/PersonalStatePage.tsx
  - apps/personal_decision_cockpit/src/pages/external/ExternalContextPage.tsx
  - apps/personal_decision_cockpit/src/pages/decisions/DecisionWorkspacePage.tsx
  - apps/personal_decision_cockpit/src/pages/evidence/EvidencePage.tsx
  - apps/personal_decision_cockpit/src/test/EvidenceDrawer.test.tsx
  - apps/personal_decision_cockpit/src/test/EvidencePage.test.tsx
  - apps/personal_decision_cockpit/src/test/PersonalStatePage.test.tsx
  - apps/personal_decision_cockpit/src/test/ExternalContextPage.test.tsx
  - apps/personal_decision_cockpit/src/test/DecisionWorkspacePage.test.tsx
autonomous: true
requirements: [STATE-03, EVID-01]
must_haves:
  truths:
    - 用户可从当前 Personal State、External Fact 和 Decision Support 打开同一个只读 Evidence Drawer；每次请求只携带卡片绑定的 stable reference，不会从浏览器重建或替换快照。
    - Evidence Drawer 将有效、mismatch、expired、abstain、无证据和 authority error 区分显示，并保留 authority、snapshot、checksum、freshness、uncertainty、limitations 和 safe recovery。
    - Phase 37 只补齐 Decision Workspace 的只读证据语义：不新增写入能力、不改变既有 guarded session/action/outcome/prepare/confirm/execute 流程或 guard policy；Phase 38 负责对现有入口增加 readiness/truth gate。
    - MCP Data Browser、Memory Graph、Relation Review 仅作为受限诊断 integration；Widget 不可用时页面始终非空，Memory Graph 明确为历史/诊断而非 Personal State SSOT。
  artifacts:
    - path: apps/personal_decision_cockpit/src/components/evidence/EvidenceDrawer.tsx
      provides: 基于 stable reference 的共用只读 evidence drill-down 与 typed recovery 呈现
    - path: apps/personal_decision_cockpit/src/components/evidence/WidgetDiagnosticCard.tsx
      provides: 带显式 origin、sandbox/referrer policy、状态与恢复路径的非权威 Widget 容器
    - path: apps/personal_decision_cockpit/src/pages/evidence/EvidencePage.tsx
      provides: 权威 evidence access 与遗留诊断 Widget 的清晰分层页面
    - path: apps/personal_decision_cockpit/src/test/EvidenceDrawer.test.tsx
      provides: stable binding、typed recovery、隐私安全内容和只读网络交互的 UI 回归测试
  key_links:
    - from: apps/personal_decision_cockpit/src/components/evidence/EvidenceDrawer.tsx
      to: apps/personal_decision_cockpit/src/api/hooks.ts
      via: useEvidenceResolve 仅调用 Plan 37-01 的同源 GET Projection route
    - from: apps/personal_decision_cockpit/src/pages/decisions/DecisionWorkspacePage.tsx
      to: src/personal_knowledge/services/ui_projection.py
      via: decision support 的 recommendation/support checksum 作为 evidence resolver 的稳定输入
    - from: apps/personal_decision_cockpit/src/components/evidence/WidgetDiagnosticCard.tsx
      to: apps/personal_decision_cockpit/src/pages/evidence/EvidencePage.tsx
      via: 非同源 MCP Widget 固定为受限、可恢复的诊断 surface，不能承担 authority 成功信号
---

<objective>
把 Phase 37 的稳定证据契约接到 Personal、External 与 Decision 页面，并将遗留 MCP
Widget 收口为明确的、受限的诊断集成，使任何证据或 Widget 失败都不会被浏览器表述为当前
权威结果；本阶段不新增或改写既有 guarded decision workflow，Phase 38 再对其实施 truth gate。
</objective>

<tasks>
<task type="execute">
  <name>实现通用只读 Evidence Drawer 与 stable-reference 调用链</name>
  <files>apps/personal_decision_cockpit/src/api/hooks.ts; apps/personal_decision_cockpit/src/components/evidence/EvidenceDrawer.tsx; apps/personal_decision_cockpit/src/test/EvidenceDrawer.test.tsx</files>
  <action>使用 Plan 37-01 的 Zod resolver envelope 增加 useEvidenceResolve，并实现可键盘操作、Esc 可关闭、焦点可恢复的 Evidence Drawer。调用参数只能来自已渲染对象的 subject_type、stable_id、snapshot_id、checksum 和允许的 state key；抽屉显示 authority、snapshot、checksum、freshness、evidence status、uncertainty、limitations 和 safe next step。对 mismatch、expired、abstain、无 evidence、partial 和 authority error 展示不同 recovery，不显示封存值、原始对话、路径、provider body、HMAC 或 confirmation material，也不提供 mutation 控件。</action>
  <verify>Push-Location apps/personal_decision_cockpit; npm run test -- --run src/test/EvidenceDrawer.test.tsx; Pop-Location</verify>
  <done>有效和失败的每一种 stable evidence reference 都有非空、可读、可关闭的结果；测试证明 hook 使用 GET/read-only contract，且不存在隐私正文或写入 API 调用。</done>
</task>

<task type="execute">
  <name>从状态、External 与决策工作区接入同一证据下钻</name>
  <files>apps/personal_decision_cockpit/src/pages/overview/OverviewPage.tsx; apps/personal_decision_cockpit/src/pages/state/PersonalStatePage.tsx; apps/personal_decision_cockpit/src/pages/external/ExternalContextPage.tsx; apps/personal_decision_cockpit/src/pages/decisions/DecisionWorkspacePage.tsx; apps/personal_decision_cockpit/src/test/PersonalStatePage.test.tsx; apps/personal_decision_cockpit/src/test/ExternalContextPage.test.tsx; apps/personal_decision_cockpit/src/test/DecisionWorkspacePage.test.tsx</files>
  <action>在可用 assertion、External fact 和 Decision support/recommendation 上提供“查看证据”入口，将卡片已经绑定的 stable reference 原样交给 Drawer。Overview 仅链接到有明确 stable reference 的项目，不为统计数或缺失 authority 构造伪 evidence；state/external/decision 的 stale、conflict、partial、mismatch 和 evidence insufficiency 仍可解释。Decision 页面只增加 support/checksum 的证据读取，不新增、替换、隐藏或绕过既有 session、记录行动、记录结果、prepare、confirm、execute 路径及其服务端 guard policy；Phase 38 负责对这些既有入口实施 readiness/truth gate。</action>
  <verify>Push-Location apps/personal_decision_cockpit; npm run test -- --run src/test/PersonalStatePage.test.tsx src/test/ExternalContextPage.test.tsx src/test/DecisionWorkspacePage.test.tsx; Pop-Location</verify>
  <done>三类对象均能在 snapshot 一致时下钻，并在 binding/authority 异常时保持原页面可读；浏览器不会用最新数据偷偷替代历史卡片的证据绑定；本计划不新增、删除或替换 guarded orchestration，测试证明证据下钻本身不发起写入；Phase 38 的专项测试证明既有写入口在 truth-gate 不满足时不可达。</done>
</task>

<task type="execute">
  <name>收口 Evidence 页面与跨源 MCP Widget 的诊断降级</name>
  <files>apps/personal_decision_cockpit/src/components/evidence/WidgetDiagnosticCard.tsx; apps/personal_decision_cockpit/src/pages/evidence/EvidencePage.tsx; apps/personal_decision_cockpit/src/test/EvidencePage.test.tsx</files>
  <action>把 Evidence 页面改为先说明 current-object Evidence Drawer 是权威读取路径，再将 Data Browser、Memory Graph、Relation Review 放入显式“诊断/历史集成”区域。Widget URL 只能来自明确配置的允许 origin；iframe 使用最小 sandbox 权限、严格 referrerPolicy 和描述性 title，且不允许 same-origin、top navigation、popup、download 或 form 权限。借助同源 system status/受控超时判断 Widget 可能不可用时，显示包含状态、限制、重试/新窗口诊断链接的非空 recovery card；iframe load 不得被当作 authoritative success，Memory Graph 必须可见标注为非 Personal State SSOT。</action>
  <verify>Push-Location apps/personal_decision_cockpit; npm run test -- --run src/test/EvidencePage.test.tsx; Pop-Location</verify>
  <done>Widget 上线、不可用和超时都不会留下空白成功卡；iframe 属性、诊断标签和 Memory Graph 历史定位受测试保护，且页面仍可通过 Drawer 访问服务端 evidence。</done>
</task>
</tasks>

<verification>
Push-Location apps/personal_decision_cockpit; npm run test -- --run src/test/EvidenceDrawer.test.tsx src/test/EvidencePage.test.tsx src/test/PersonalStatePage.test.tsx src/test/ExternalContextPage.test.tsx src/test/DecisionWorkspacePage.test.tsx; Pop-Location
python -m pytest tests/contract/test_ui_projection_evidence.py -q
</verification>

<success_criteria>
EVID-01 形成一个可验证的、只读的 current-object 证据路径：人可从 Personal、External 和
Decision 理解每个结论为何可信或为何不能行动；遗留 Widget 只能辅助诊断，不能越过
authority、snapshot、privacy 或 Phase 38 的确认边界。
</success_criteria>
