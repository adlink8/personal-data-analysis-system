---
phase: 37
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/personal_knowledge/services/ui_projection.py
  - src/personal_knowledge/services/api_server.py
  - tests/contract/test_ui_projection_state_external.py
  - tests/contract/test_ui_projection_evidence.py
  - apps/personal_decision_cockpit/src/api/schemas.ts
  - apps/personal_decision_cockpit/src/api/hooks.ts
  - apps/personal_decision_cockpit/src/test/fixtures/personal-state.json
  - apps/personal_decision_cockpit/src/test/fixtures/external-delta.json
  - apps/personal_decision_cockpit/src/test/fixtures/evidence-resolve.json
  - apps/personal_decision_cockpit/src/test/schemas.test.ts
  - apps/personal_decision_cockpit/src/test/liveContract.test.ts
autonomous: true
requirements: [STATE-02, STATE-03, EVID-01]
must_haves:
  truths:
    - Personal State、External Fact 与 Decision Support 的展示引用均由服务端以稳定 ID、snapshot 和 checksum 校验；浏览器不读取数据库、不比较快照也不裁定证据可用性。
    - External DTO 使用一组固定的来源、地区、有效期、lifecycle、conflict、freshness 和限制字段；生产者与 Zod/fixture 不再以宽松字段掩盖语义漂移。
    - 证据解析只能通过只读 allowlist 路由进入既有 state/external/decision read service；mismatch、expired、abstain 与 authority error 返回安全的 typed result，绝不回退到最新对象或泄露封存正文。
  artifacts:
    - path: src/personal_knowledge/services/ui_projection.py
      provides: 版本化的 state/external/evidence Projection DTO、服务端 freshness/readiness 元数据和有界证据解析器
    - path: src/personal_knowledge/services/api_server.py
      provides: 与 Projection 一致的只读 Cockpit evidence route，拒绝非 GET 及不受支持的引用类型
    - path: tests/contract/test_ui_projection_evidence.py
      provides: stable reference、snapshot/checksum mismatch、abstain、authority isolation 与零 mutation 的契约证明
    - path: apps/personal_decision_cockpit/src/api/schemas.ts
      provides: 与服务端严格同步的 state/external/evidence Zod envelope 解析契约
  key_links:
    - from: src/personal_knowledge/services/ui_projection.py
      to: src/personal_knowledge/intelligence/service.py
      via: personal_state stable reference 必须经 state.explain 的既有快照约束解析
    - from: src/personal_knowledge/services/ui_projection.py
      to: src/personal_knowledge/services/decision_intelligence_reads.py
      via: external fact 只能通过 external.explain 的独立 External authority 下钻
    - from: apps/personal_decision_cockpit/src/api/schemas.ts
      to: tests/contract/test_ui_projection_state_external.py
      via: 同一组 canonical DTO 字段由浏览器解析和 Python contract 共同锁定
---

<objective>
在任何 Phase 37 页面渲染前，收口服务端拥有的状态、External 与证据读取契约：
固定 DTO，提供 snapshot/checksum 绑定的只读 evidence resolver，并使前后端 fixture
能够拒绝影响真值语义的字段漂移。
</objective>

<tasks>
<task type="execute">
  <name>扩展 Projection 的权威元数据与 canonical External DTO</name>
  <files>src/personal_knowledge/services/ui_projection.py; tests/contract/test_ui_projection_state_external.py</files>
  <action>在不改变任何 authority 数据的前提下，为 personal assertion、External fact 和可读 decision support 投影稳定暴露 display-only stable reference、authority、snapshot binding、checksum（已有时）、record lifecycle、server-derived freshness/readiness、evidence availability 和 safe limitations。确定一套 External 事实字段（subject/predicate 或经服务端定义的 display label、source identity、region、valid window、source quality/confidence、lifecycle/conflict），更新 external_delta.get 以只输出该契约；保留 updated=[] 是 authority 不提供逐事实更新事件的显式 limitation。不要把生命周期状态、来源时效或展示分组合并成同一个客户端颜色字段。</action>
  <verify>python -m pytest tests/contract/test_ui_projection_state_external.py -q</verify>
  <done>Personal 八域和 External 独立 authority 仍可在单节失败时返回 partial；关键真值字段不再以 nullish/passthrough 掩盖 producer/consumer 不一致，且所有 freshness/readiness 判断由服务端给出原因。</done>
</task>

<task type="execute">
  <name>实现快照绑定的只读证据解析 Projection 与 REST 路由</name>
  <files>src/personal_knowledge/services/ui_projection.py; src/personal_knowledge/services/api_server.py; tests/contract/test_ui_projection_evidence.py</files>
  <action>新增一个 Cockpit-owned 的 GET evidence resolve operation（具体路径遵循现有 /ui 路由命名），输入仅接受受类型约束的 personal_state、external_fact 或 decision stable reference。服务端先逐项验证 stable_id、snapshot_id、checksum 及 state key，再仅调度至既有 IntelligenceService.state.explain、DecisionIntelligenceReadService.external.explain 或 DecisionFeedbackService 的只读读取路径。返回最小化 metadata、evidence reference/status、uncertainty、limitations、safe next steps 和 typed recovery；mismatch、expired、无证据、abstain 与单 authority 故障不得用最新记录替代。禁止原始消息正文、sealed assertion value、任意 URL/path、provider body、HMAC/confirmation 材料和任何 mutation/provider/promotion/external action。</action>
  <verify>python -m pytest tests/contract/test_ui_projection_state_external.py tests/contract/test_ui_projection_evidence.py -q</verify>
  <done>三种 subject 都能从有效稳定引用得到只读结果；篡改或过期 binding、未知 subject、authority 故障和无证据返回可区分安全错误；测试证明所有 resolver 请求均不改变 authority fingerprint 或产生写入。</done>
</task>

<task type="execute">
  <name>同步客户端 schema、hooks 与受控真实响应 fixtures</name>
  <files>apps/personal_decision_cockpit/src/api/schemas.ts; apps/personal_decision_cockpit/src/api/hooks.ts; apps/personal_decision_cockpit/src/test/fixtures/personal-state.json; apps/personal_decision_cockpit/src/test/fixtures/external-delta.json; apps/personal_decision_cockpit/src/test/fixtures/evidence-resolve.json; apps/personal_decision_cockpit/src/test/schemas.test.ts; apps/personal_decision_cockpit/src/test/liveContract.test.ts</files>
  <action>为 canonical state/external/evidence envelope 建立严格但可演进的 Zod discriminants，并新增只读 evidence hook。以经 Privacy Guard 处理的服务端真实元数据 fixture 固化完整、empty、partial、mismatch/abstain 场景；更新既有 fixture 的 External 字段以匹配服务端，不再保留 fact_type/observed_at/source_id 与 subject/predicate 两种互相冲突的真值解释。客户端仅能格式化服务端 metadata，不得使用本地时间阈值把数据判为可行动或伪造 evidence link。</action>
  <verify>Push-Location apps/personal_decision_cockpit; npm run test -- --run src/test/schemas.test.ts src/test/liveContract.test.ts; Pop-Location</verify>
  <done>Zod 对影响 authority、snapshot、freshness、readiness 或 evidence 的畸形载荷 fail closed；fixture 与 Python Projection 契约同源，evidence hook 只发 GET 且不携带可写 payload。</done>
</task>
</tasks>

<verification>
python -m pytest tests/contract/test_ui_projection_state_external.py tests/contract/test_ui_projection_evidence.py -q
Push-Location apps/personal_decision_cockpit; npm run test -- --run src/test/schemas.test.ts src/test/liveContract.test.ts; Pop-Location
</verification>

<success_criteria>
STATE-02 的 External 隔离和 STATE-03/EVID-01 的证据、快照、时效真值已由一个
服务端只读契约锁定，后续 UI 只能消费该契约，不能把字段漂移、最新对象回退或浏览器推理
伪装成权威结果。
</success_criteria>
