---
phase: 23
plan: 04
type: execute
wave: 3
depends_on: [23-02, 23-03]
files_modified:
  - src/personal_knowledge/application/sync.py
  - src/personal_knowledge/application/conversation/build_conversation_vector_store.py
  - src/personal_knowledge/application/google_structure_lifecycle.py
  - src/personal_knowledge/application/knowledge/doctor_ku.py
  - src/personal_knowledge/governance/preflight.py
  - docs/runbooks/product-sync.md
  - docs/runbooks/ku-incremental.md
  - tests/unit/test_product_sync_versions.py
  - tests/unit/test_doctor_ku.py
  - tests/governance/test_artifact_layer_policy.py
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05]
must_haves:
  truths:
    - D-23-10 Conversation, Turn, Google and KU versions/watermarks advance monotonically after publication.
    - D-23-11 pk-sync exposes dry-run-first status/Turn/Google product paths without breaking conversations.
    - D-23-12 doctor and preflight fail on registry, snapshot, pointer, evidence or watermark drift.
    - D-23-13 live bootstrap is read-only unless explicit write is separately authorized.
  artifacts:
    - version-aware pk-sync product surface
    - composite snapshot doctor and governance gates
    - updated operating runbooks
---

<objective>
Productize source versions/watermarks and enforce Target A integrity through sync, doctor, governance and documentation.
</objective>

<threat_model>
- Accidental live/paid writes: every new sync path is dry-run by default and explicit write never calls paid extraction.
- Watermark advance before publication or regression: bind watermarks to artifact versions and validate monotonic transitions.
- Health false positive: critical drift returns non-zero; network/service availability remains warn-only where appropriate.
- Severity: high findings block completion.
</threat_model>

<tasks>
<task type="execute">
  <name>Add versioned Conversation, Turn, Google and KU sync status</name>
  <read_first>src/personal_knowledge/application/sync.py; src/personal_knowledge/application/conversation/build_conversation_vector_store.py; src/personal_knowledge/application/google_structure_lifecycle.py; src/personal_knowledge/application/ku.py; docs/runbooks/product-sync.md</read_first>
  <action>Add reusable artifact-version/watermark recording after successful Conversation canonical publication, Turn vector publication, Google lifecycle promotion and KU publication. Extend `pk-sync` with `status`, `turns` and `google` commands, preserving `conversations`. All commands default to dry-run; `--write` requires their build/gate success and records versions/watermarks, but does not activate a composite snapshot or call paid extraction implicitly. Status emits JSON-capable current/published/watermark/drift data for all four sources.</action>
  <verify>python -m pytest tests/unit/test_product_sync_versions.py tests/unit/test_pk_ku_cli.py -q</verify>
  <acceptance_criteria>Repeated unchanged sync is a no-op; version and watermark never regress; failed builds/gates do not advance; existing `pk-sync conversations` and `pk-ku` tests still pass.</acceptance_criteria>
</task>
<task type="execute">
  <name>Enforce composite integrity in doctor and governance</name>
  <read_first>src/personal_knowledge/application/knowledge/doctor_ku.py; src/personal_knowledge/governance/preflight.py; governance/policies/artifact_layers.yaml; 23-VALIDATION.md</read_first>
  <action>Add critical doctor checks for registry validation, active snapshot/member integrity, active snapshot ↔ legacy pointer parity, member collection count/checksum, evidence resolver probe and source version/watermark drift. Add governance preflight coverage for tracked registry completeness and forbidden duplicate authority/dependency direction. Keep doctor/status read-only and JSON stable; critical failure returns non-zero.</action>
  <verify>python -m pytest tests/unit/test_doctor_ku.py tests/governance/test_artifact_layer_policy.py -q</verify>
  <acceptance_criteria>Each injected registry/snapshot/pointer/evidence/watermark defect fails the corresponding critical check and process exit; a healthy fixture passes; port health remains warn-only.</acceptance_criteria>
</task>
<task type="execute">
  <name>Document bootstrap, activation, rollback and recovery</name>
  <read_first>docs/runbooks/product-sync.md; docs/runbooks/ku-incremental.md; docs/architecture/retrieval-ssot.md; src/personal_knowledge/application/serving/snapshots.py</read_first>
  <action>Update runbooks with the registry/snapshot authority, safe current-state bootstrap, validation, explicit activation, rollback, pointer-projection recovery, source version/watermark ordering and drift troubleshooting. State that SQLite serving authority wins over the text pointer, no active change is allowed on failed validation, and all normal status/doctor operations are read-only.</action>
  <verify>python -m personal_knowledge.governance.preflight --ci</verify>
  <acceptance_criteria>Runbooks contain exact dry-run/write/status/validate/activate/rollback/doctor commands and failure semantics; governance preflight exits 0.</acceptance_criteria>
</task>
</tasks>

<verification>
python -m pytest tests/unit/test_product_sync_versions.py tests/unit/test_doctor_ku.py tests/governance/test_artifact_layer_policy.py -q
python -m personal_knowledge.governance.preflight --ci
python -m pytest -q
</verification>

<success_criteria>
- FOUND-01..05 are proven by product commands, tests and governance.
- Target A composite authority can be operated and recovered without hidden tribal steps.
</success_criteria>
