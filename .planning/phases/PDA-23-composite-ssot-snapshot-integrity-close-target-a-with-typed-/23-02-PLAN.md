---
phase: 23
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/personal_knowledge/application/serving/snapshots.py
  - src/personal_knowledge/application/serving/__init__.py
  - src/personal_knowledge/application/knowledge/promote_knowledge_index.py
  - tests/integration/test_serving_snapshot_activation.py
  - tests/integration/test_knowledge_checkpoint_rollback.py
autonomous: true
requirements: [FOUND-02, FOUND-05]
must_haves:
  truths:
    - D-23-05 one SQLite transaction changes serving authority; the text pointer is compatibility only.
    - D-23-06 validation completes before activation and failure preserves prior authority.
    - D-23-07 rollback activates a prior immutable snapshot without deleting history.
    - D-23-13 current live state bootstraps without implicit promotion.
  artifacts:
    - src/personal_knowledge/application/serving/snapshots.py
    - fault-injected activation and rollback tests
---

<objective>
Implement prepare, validate, activate and rollback for composite serving snapshots and route KU promotion through it.
</objective>

<threat_model>
- Split-brain serving state: all product readers use SQLite serving authority; pointer is verified projection only.
- TOCTOU between validation and activation: bind validated member hashes and recheck critical invariants inside activation transaction.
- Gate bypass or rollback tampering: require recorded PASS reference for product activation and append-only events.
- Severity: high findings block completion.
</threat_model>

<tasks>
<task type="execute">
  <name>Build snapshot prepare and validation service</name>
  <read_first>src/personal_knowledge/application/knowledge/promote_knowledge_index.py; src/personal_knowledge/application/knowledge/reconcile_knowledge_index.py; src/personal_knowledge/core/chroma_client.py; 23-RESEARCH.md</read_first>
  <action>Create `application/serving/snapshots.py` with typed prepare/validate/status APIs. Resolve member versions from the registry, canonical conversation/Turn/Google/KU stores and candidate collection. Validate registry coverage, SQLite FK/integrity, Chroma collection existence/count/ID checksum, canonical build linkage, evidence integrity, required eval PASS reference and monotonic watermarks. Persist immutable draft/validated manifests and refusal events; do not touch active authority or pointer.</action>
  <verify>python -m pytest tests/integration/test_serving_snapshot_activation.py -q -k "prepare or validate or refuse"</verify>
  <acceptance_criteria>A valid fixture produces a stable manifest hash; missing collection, count/checksum mismatch, failed gate, evidence failure or regressing watermark records refusal and leaves the old active snapshot unchanged.</acceptance_criteria>
</task>
<task type="execute">
  <name>Make activation and rollback atomic at the serving authority</name>
  <read_first>src/personal_knowledge/application/serving/snapshots.py; src/personal_knowledge/application/knowledge/promote_knowledge_index.py; tests/integration/test_knowledge_checkpoint_rollback.py</read_first>
  <action>Implement explicit-write activation and rollback that update only the singleton `serving_authority` plus append an event in one FK-enabled transaction. After commit, atomically project the selected KU collection to `knowledge_index_active.txt` and record parity; projection failure leaves SQLite authority active and is surfaced as critical drift. Refactor `promote()`/rollback to prepare/validate/activate snapshots while retaining current CLI arguments and return fields. Add failure injection immediately before transaction, after authority commit and during projection.</action>
  <verify>python -m pytest tests/integration/test_serving_snapshot_activation.py tests/integration/test_knowledge_checkpoint_rollback.py tests/integration/test_knowledge_index_promotion.py -q</verify>
  <acceptance_criteria>Every injected failure leaves exactly one resolvable active snapshot; rollback returns all named members to the previous snapshot; no historical snapshot/member/collection is deleted; legacy callers still receive promoted/previous/checksum fields.</acceptance_criteria>
</task>
</tasks>

<verification>
python -m pytest tests/integration/test_serving_snapshot_activation.py tests/integration/test_knowledge_checkpoint_rollback.py tests/integration/test_knowledge_index_promotion.py -q
</verification>

<success_criteria>
- SQLite is the only serving authority.
- Promote/rollback are fail-closed, auditable and crash-consistent from readers' perspective.
</success_criteria>
