---
phase: 23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - governance/policies/artifact_layers.yaml
  - src/personal_knowledge/governance/artifact_registry.py
  - src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py
  - tests/unit/test_artifact_registry.py
  - tests/integration/test_serving_snapshot_schema.py
autonomous: true
requirements: [FOUND-01, FOUND-02]
must_haves:
  truths:
    - D-23-01 D/S/R/A layer identities and metadata are machine validated.
    - D-23-02 tracked definitions contain no private payload and runtime versions are immutable metadata.
    - D-23-03 duplicate authority and invalid dependencies fail closed.
    - D-23-04 serving state has one immutable snapshot identifier.
  artifacts:
    - governance/policies/artifact_layers.yaml
    - src/personal_knowledge/governance/artifact_registry.py
    - serving snapshot runtime tables
---

<objective>
Create the typed artifact registry and runtime schema that all later serving operations use.
</objective>

<threat_model>
- Private payload leakage into tracked registry: prevent with an allowlisted metadata schema and secret/privacy tests.
- Authority spoofing or duplicate roles: reject unknown ids, invalid layers and duplicate authoritative roles.
- Schema corruption: use FK-enabled connections, transactional migration and integrity checks.
- Severity: high findings block completion.
</threat_model>

<tasks>
<task type="execute">
  <name>Define and validate the tracked D/S/R/A registry</name>
  <read_first>governance/policies/paths.yaml; governance/policies/architecture.yaml; .planning/ARCHITECTURE-LAYERING-DATA-GOVERNANCE-AUDIT-2026-07-17.md; src/personal_knowledge/governance/preflight.py</read_first>
  <action>Add `governance/policies/artifact_layers.yaml` with sanitized definitions for canonical conversation, canonical message, Turn summary/vector, Google normalized/assertion, canonical KU, KU vector index, serving snapshot and evaluation artifacts. Implement a typed loader/validator in `artifact_registry.py` enforcing ids, D/S/R/A values, authority-role uniqueness, allowed dependency direction, required producer/consumer/privacy/lifecycle/version/evidence fields and metadata-only content. Expose deterministic validation results for preflight reuse.</action>
  <verify>python -m pytest tests/unit/test_artifact_registry.py -q</verify>
  <acceptance_criteria>The registry covers every required serving role; duplicate role, unknown layer, missing metadata, invalid dependency and payload-like secret fixtures each produce a non-passing validation result.</acceptance_criteria>
</task>
<task type="execute">
  <name>Add immutable artifact and serving snapshot tables</name>
  <read_first>src/personal_knowledge/application/knowledge/migrate_add_knowledge_unit_tables.py; src/personal_knowledge/core/sqlite.py; tests/unit/test_inventory_registry_migration.py</read_first>
  <action>Extend the existing idempotent migration with `artifact_versions`, `source_watermarks`, `serving_snapshots`, `serving_snapshot_members`, singleton `serving_authority`, and append-only `serving_snapshot_events`. Add FK/check/unique constraints for registry ids, layer values, lifecycle, one member per serving role and one active snapshot. Store canonical manifest JSON and SHA-256; prohibit mutation of member rows belonging to an active snapshot. Add read-only schema inspection and current-state bootstrap planning functions; bootstrap defaults to draft and never changes active authority.</action>
  <verify>python -m pytest tests/integration/test_serving_snapshot_schema.py -q</verify>
  <acceptance_criteria>Migration is idempotent; `PRAGMA foreign_key_check` returns zero; active snapshot members cannot be edited; malformed hashes, duplicate roles and dangling members are rejected; bootstrap dry-run writes nothing.</acceptance_criteria>
</task>
</tasks>

<verification>
python -m pytest tests/unit/test_artifact_registry.py tests/integration/test_serving_snapshot_schema.py -q
</verification>

<success_criteria>
- FOUND-01 registry contract is enforceable.
- Snapshot schema can represent the complete composite authority without private content.
</success_criteria>
