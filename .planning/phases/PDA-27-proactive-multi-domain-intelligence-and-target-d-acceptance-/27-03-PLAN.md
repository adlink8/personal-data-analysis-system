---
phase: 27
plan: 03
type: execute
wave: 3
depends_on: [27-01, 27-02]
files_modified:
  - src/personal_knowledge/intelligence/proactive/schema.py
  - src/personal_knowledge/intelligence/proactive/runs.py
  - src/personal_knowledge/intelligence/proactive/controls.py
  - tests/unit/test_proactive_controls.py
  - tests/contract/test_proactive_boundaries.py
  - tests/integration/test_proactive_concurrency.py
  - tests/integration/test_proactive_privacy.py
autonomous: true
requirements: [TRUST-01]
must_haves:
  truths:
    - "User limit, suppress, snooze, revoke, correct, not-useful, wrong-timing and restore operations are append-only overlays; they never UPDATE/DELETE Phase 25/26, KU or historical proactive rows."
    - "Every control binds an exact target authority/type/ID/checksum, user actor hash, scope, expected sequence, idempotency key and previous-event checksum."
    - "Exact-target controls outrank scoped policy/domain/global controls; ambiguity and explicit denial fail closed, expiry uses explicit as_of, and restore is a compensating event."
    - "A canonical correction only emits canonical_correction_requested and remains blocked behind Phase 24 reviewed lifecycle authority."
    - "BEGIN IMMEDIATE, expected_sequence and idempotency make concurrent/replayed writes deterministic; stale, conflicting, tampered or faulted appends leave row counts/frontiers unchanged."
  artifacts:
    - path: src/personal_knowledge/intelligence/proactive/controls.py
      provides: "Append-only control stream, projection, precedence and compensating restore"
    - path: tests/unit/test_proactive_controls.py
      provides: "Scope/expiry/precedence/restore lifecycle proof"
    - path: tests/integration/test_proactive_concurrency.py
      provides: "Sequence, idempotency, replay, tamper and rollback proof"
  key_links:
    - from: src/personal_knowledge/intelligence/proactive/controls.py
      to: proactive_control_events
      via: "BEGIN IMMEDIATE expected-sequence checksum append"
    - from: src/personal_knowledge/intelligence/proactive/runs.py
      to: proactive_control_events
      via: "New runs bind the exact active control-frontier checksum"
    - from: canonical_correction_requested
      to: knowledge_lifecycle manifests/events
      via: "Request link only; Phase 24 human review remains mandatory"
---

<objective>
Give the user reversible, scoped and auditable trust control over higher-level intelligence.

Purpose: satisfy TRUST-01 without rewriting historical truth, impersonating a user or bypassing the reviewed canonical lifecycle workflow.
Output: append-only control streams, deterministic projection/precedence, restore/rollback and concurrency/privacy contracts.
</objective>

<execution_context>
Read before implementation:
- `.planning/phases/PDA-27-proactive-multi-domain-intelligence-and-target-d-acceptance-/27-RESEARCH.md`
- `.planning/phases/PDA-24-evaluation-closure-and-lifecycle-adoption-close-target-b-c-q/24-03-CHECKPOINT.md`
- `src/personal_knowledge/intelligence/proactive/schema.py`
- `src/personal_knowledge/intelligence/proactive/runs.py`
- `src/personal_knowledge/intelligence/proactive/ranking.py`
- `src/personal_knowledge/intelligence/decision/state_machine.py`
- `src/personal_knowledge/application/knowledge/reconcile_knowledge_lifecycle.py`

All control writes use disposable databases in this plan. Do not fabricate a human identity, apply a live control, finalize lifecycle review, change canonical KU content/lifecycle, mutate Phase 25/26 history or reinterpret read access as consent.
</execution_context>

<tasks>
<task type="execute">
  <name>Task 1: Establish GREEN append-only control streams and ownership boundaries</name>
  <files>src/personal_knowledge/intelligence/proactive/schema.py; src/personal_knowledge/intelligence/proactive/controls.py; tests/unit/test_proactive_controls.py; tests/contract/test_proactive_boundaries.py; tests/integration/test_proactive_concurrency.py; tests/integration/test_proactive_privacy.py</files>
  <action>Work test-first within this single independently executable task: add focused contracts for all eight control operations, exact targets/checksums, user-only actor identity, scope precedence, fixed-`as_of` expiry, ambiguity denial, correction-request boundaries, sequence/checksum chains, replay/idempotency conflict, stale/concurrent writers, tamper, valid restore/double restore, injected rollback and zero Phase 25/26/KU/lifecycle/serving/watermark mutation; run the focused suite once to observe RED; then implement the minimal frozen control/event/receipt records plus `BEGIN IMMEDIATE` expected-sequence/idempotent append and deterministic projection/restore needed to make the same suite GREEN. Do not leave an expected non-zero verify step.</action>
  <verify>python -m pytest tests/unit/test_proactive_controls.py tests/contract/test_proactive_boundaries.py tests/integration/test_proactive_concurrency.py tests/integration/test_proactive_privacy.py -q</verify>
  <done>The focused suite exits 0 and proves user ownership, immutable history, scope precedence, safe correction routing, concurrency safety and compensating rollback.</done>
</task>

<task type="execute">
  <name>Task 2: Harden projection precedence, expiry and compensating restore</name>
  <files>src/personal_knowledge/intelligence/proactive/schema.py; src/personal_knowledge/intelligence/proactive/controls.py</files>
  <action>Hydrate and checksum-verify the full target stream before every projection. Complete exact-target before domain/policy/global scope precedence; at equal specificity use the latest valid event while unresolved ambiguity and denial remain fail-closed. Evaluate snooze/expiry at explicit timezone-aware `as_of`. Harden restore as a new event with `rollback_of_event_id` plus before/after projected checksums and never erase history. Return stable `stale_sequence`, `idempotency_conflict`, `target_drift`, `invalid_restore` and integrity reason codes, extending the already-GREEN Task 1 behavior.</action>
  <verify>python -m pytest tests/unit/test_proactive_controls.py tests/integration/test_proactive_concurrency.py tests/integration/test_proactive_privacy.py -q</verify>
  <done>Control projection is deterministic, scoped and reversible; concurrent/replayed writes preserve one checksum-linked order and failures leave the stream unchanged.</done>
</task>

<task type="execute">
  <name>Task 3: Bind control frontiers to new proactive runs and enforce correction boundaries</name>
  <files>src/personal_knowledge/intelligence/proactive/runs.py; src/personal_knowledge/intelligence/proactive/controls.py; tests/contract/test_proactive_boundaries.py; tests/integration/test_proactive_concurrency.py</files>
  <action>Compute an immutable active-control frontier manifest/checksum by target and sequence and bind it into every proactive run. Revalidate the frontier inside publication; a control added after planning causes a new run or stable stale-frontier failure, never an edit of prior candidates. Apply projected limits/suppression/revocation before ranking eligibility. `correct` may produce only a metadata-only corrected A-layer interpretation or `canonical_correction_requested` link; it must not update Phase 25 assertions, Phase 26 recommendations, canonical KU or lifecycle tables. Preserve the exact Phase 24 checkpoint dependency and test that pending/unreviewed lifecycle proposals have zero canonical effect. Cover suppress→restore, scope limit→restore, snooze expiry, stale concurrent append and frontier-changed run publication.</action>
  <verify>python -m pytest tests/unit/test_proactive_controls.py tests/contract/test_proactive_boundaries.py tests/integration/test_proactive_runs.py tests/integration/test_proactive_concurrency.py -q</verify>
  <done>Every run reflects an exact immutable control frontier, user rollback changes future projection only, and canonical corrections cannot bypass Phase 24 review.</done>
</task>
</tasks>

<verification>
- `python -m pytest tests/unit/test_proactive_controls.py tests/contract/test_proactive_boundaries.py tests/integration/test_proactive_runs.py tests/integration/test_proactive_concurrency.py tests/integration/test_proactive_privacy.py -q`
- `python -m pytest tests/unit/test_decision_state_machine.py tests/integration/test_decision_feedback_concurrency.py tests/unit/test_lifecycle_events.py tests/integration/test_knowledge_index_reconcile.py -q`
- `python -m personal_knowledge.governance.preflight --ci`
- `git diff --check`
</verification>

<success_criteria>
- TRUST-01 has positive and negative proof for correction, limiting, suppression, snooze, revocation, feedback, restore and rollback.
- Controls are exact-target/scope bound, append-only, checksum linked, user-owned, idempotent and concurrency safe.
- Restore preserves history and changes only projected future eligibility.
- Canonical fact/KU corrections remain a request behind Phase 24 reviewed lifecycle gates; no historical authority is overwritten.
- No live control write, external action, private body, serving/pointer/watermark or Phase 24 checkpoint mutation occurs.
</success_criteria>
