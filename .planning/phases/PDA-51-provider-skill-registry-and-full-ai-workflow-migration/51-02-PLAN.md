---
phase: 51
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified: [governance/manifests/ai/pi-ai-entrypoints.json, src/personal_knowledge/intelligence/analysis/providers.py, src/personal_knowledge/services/orchestration_service.py, src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py, tests/governance/test_pi_ai_entrypoint_inventory.py, tests/integration/test_pi_workflow_migration.py]
autonomous: true
requirements: [MODEL-01, MODEL-02, SKILL-01]
must_haves:
  truths:
    - "D-04: every production AI callsite is classified and migrated."
    - "D-05: legacy is compatibility/rollback-only."
    - "D-06: Phase 51 proves replay parity, not real-provider quality."
---

# Phase 51-02 Plan — Full AI entrypoint migration and no-parallel-control gate

<objective>迁移 structured analysis、guarded generation、extraction/summary 等生产 AI 入口，并以静态与运行 receipt 证明没有并行主控制面。</objective>
<threat_model>Assets: all model side effects and compatibility behavior. Threats: missed callsite, double model call, legacy bypass, schema regression and false quality claim. Controls: generated inventory, guarded adapter, exact replay parity, one-call receipt and rollback-only policy.</threat_model>
<tasks>
<task type="execute">
  <name>51-02-01 — Build authoritative AI entrypoint inventory and migrate adapters</name>
  <read_first>
    - src/personal_knowledge/intelligence/analysis/providers.py
    - src/personal_knowledge/services/orchestration_service.py
    - src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py
    - src/personal_knowledge/core/llm.py
    - apps/personal_intelligence_kernel/src/models/provider-adapter.mjs
  </read_first>
  <files>governance/manifests/ai/pi-ai-entrypoints.json, src/personal_knowledge/intelligence/analysis/providers.py, src/personal_knowledge/services/orchestration_service.py, src/personal_knowledge/application/knowledge/build_knowledge_units_prod.py, tests/governance/test_pi_ai_entrypoint_inventory.py</files>
  <action>Generate a tracked inventory of every production network/model/CLI generation callsite with owner, purpose, current adapter, target Pi route and status migrated|deterministic_non_ai|test_only|rollback_only. Route structured analysis and orchestration generation through the Pi Domain/Provider adapter while preserving public Python request/result/error contracts. Convert extraction/summary model seams to the same Kernel task route without changing evaluation/promotion gates. Legacy implementations remain named rollback adapters and reject use outside runtime rollback mode.</action>
  <acceptance_criteria>
    - Repository scan has zero unclassified production AI callsites.
    - Existing public Provider/orchestration/extraction schemas remain compatible.
    - Non-rollback mode cannot instantiate legacy provider adapter.
    - Inventory governance test exits 0.</acceptance_criteria>
  <verify><automated>python -m pytest tests/governance/test_pi_ai_entrypoint_inventory.py -q</automated></verify>
</task>
<task type="execute">
  <name>51-02-02 — Verify replay parity and one-controller receipts end to end</name>
  <read_first>
    - governance/manifests/ai/pi-ai-entrypoints.json
    - apps/personal_intelligence_kernel/src/models/provider-adapter.mjs
    - tests/e2e/test_structured_llm_analysis.py
    - tests/contract/test_agent_ux_interfaces.py
  </read_first>
  <files>tests/integration/test_pi_workflow_migration.py, tests/contract/test_pi_provider_adapter.py</files>
  <action>For each migrated purpose, replay fixed inputs through legacy and Pi adapters and compare schema, safe errors, evidence/model/usage checksums and downstream Candidate behavior. Assert one request produces exactly one provider-side receipt and one Pi task/session chain; only explicit comparison harness may execute both replay arms. Run existing structured analysis, orchestration and extraction contract regressions without real calls.</action>
  <acceptance_criteria>
    - Every inventory migrated row has a passing parity fixture and Pi receipt.
    - Normal mode cannot produce simultaneous Pi and legacy provider receipts.
    - Existing guarded confirmation/evaluation/promotion tests remain green.
    - Reports state replay parity only and make no real quality/cost claim.</acceptance_criteria>
  <verify><automated>python -m pytest tests/integration/test_pi_workflow_migration.py tests/contract/test_pi_provider_adapter.py -q</automated></verify>
</task>
</tasks>
<verification>Run Phase 51 suites plus affected existing analysis/orchestration/extraction contracts; network Provider call count is zero.</verification>
<success_criteria>All production AI entrypoints route through Pi abstractions, with legacy limited to rollback and no double controller.</success_criteria>
