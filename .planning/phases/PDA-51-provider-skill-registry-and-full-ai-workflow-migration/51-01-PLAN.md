---
phase: 51
plan: 01
type: execute
wave: 1
depends_on: [50-02]
files_modified: [apps/personal_intelligence_kernel/src/models/routes.mjs, apps/personal_intelligence_kernel/src/models/provider-adapter.mjs, apps/personal_intelligence_kernel/src/skills/registry.mjs, governance/manifests/ai/pi-model-routes.json, governance/manifests/ai/pi-skills.json, tests/contract/test_pi_provider_adapter.py]
autonomous: true
requirements: [MODEL-01, MODEL-02, SKILL-01]
must_haves:
  truths:
    - "D-01: provider credentials are injected, never discovered."
    - "D-02: every model route has explicit purpose and budgets."
    - "D-03: Skills are checksum-bound and deterministic."
---

# Phase 51-01 Plan — Provider/model routes and deterministic Skill registry

<objective>建立唯一 Pi model adapter、预算路由和受控 Skill selection，同时保留 Python Provider contracts。</objective>
<threat_model>Assets: provider credentials/budget, model side effects and Skill tools. Threats: ambient auth, silent fallback, retry cost, Skill collision/drift and over-privileged Tool set. Controls: injected secrets, exact route/manifest, ceilings, zero/one deterministic selection and outcome reconciliation.</threat_model>
<tasks>
<task type="execute">
  <name>51-01-01 — Implement allowlisted model routes and Provider receipt adapter</name>
  <read_first>
    - src/personal_knowledge/intelligence/analysis/providers.py
    - .planning/spikes/pi-frontier-controls/006-provider-auth-and-budget/README.md
    - apps/personal_intelligence_kernel/src/tasks/ledger.mjs
    - .planning/phases/PDA-51-provider-skill-registry-and-full-ai-workflow-migration/51-AI-SPEC.md
  </read_first>
  <files>apps/personal_intelligence_kernel/src/models/routes.mjs, apps/personal_intelligence_kernel/src/models/provider-adapter.mjs, governance/manifests/ai/pi-model-routes.json, tests/contract/test_pi_provider_adapter.py</files>
  <action>Define exact purpose→provider/model routes with timeout, max input/output tokens, cost ceiling, attempts, retryable codes and no-fallback flag. Adapter receives injected credentials, maps Pi model events to project ProviderResult/Telemetry receipts, records task/session/event identities and reconciles outcome_unknown before retry. Add deterministic replay transport for tests; never scan user auth/settings or invoke real Provider in Phase 51.</action>
  <acceptance_criteria>
    - Unknown purpose/model/provider, missing credential, exceeded budget and silent fallback are typed failures.
    - Replay produces schema-equivalent Python ProviderResult/Telemetry and exact usage checksum.
    - Outcome_unknown is not retried before reconciliation.
    - Provider contract tests exit 0 with provider_calls=0.</acceptance_criteria>
  <verify><automated>python -m pytest tests/contract/test_pi_provider_adapter.py -q</automated></verify>
</task>
<task type="execute">
  <name>51-01-02 — Implement signed/checksum Skill registry and deterministic selection</name>
  <read_first>
    - .planning/spikes/pi-embedded-personal-kernel/003-skill-artifact-isolation/README.md
    - governance/manifests/ai/pi-domain-tools.json
    - apps/personal_intelligence_kernel/src/runtime/resource-policy.mjs
  </read_first>
  <files>apps/personal_intelligence_kernel/src/skills/registry.mjs, governance/manifests/ai/pi-skills.json, apps/personal_intelligence_kernel/test/skill-registry.test.mjs</files>
  <action>Define Skill manifest id/version/checksum/purpose/input/output/allowed_tools/privacy ceiling/owner/expiry. Load only repository allowlist, verify checksum and Tool subset, and select zero or one Skill by deterministic purpose+schema rules; ties/unknown/drift abstain. Keep SDK ambient Skill discovery disabled and bind selected Skill receipt to task/session/evidence.</action>
  <acceptance_criteria>
    - Ambient/global Skill files never appear in registry or selection.
    - Collision, checksum drift, expired owner or Tool escalation rejects the Skill.
    - Same request selects the same Skill/version/checksum or abstains.
    - Node Skill tests exit 0.</acceptance_criteria>
  <verify><automated>npm test --prefix apps/personal_intelligence_kernel -- --test-name-pattern=skill-registry</automated></verify>
</task>
</tasks>
<verification>Run Provider and Skill suites with no network Provider calls.</verification>
<success_criteria>All model and Skill control is explicit, budgeted, auditable and ambient-discovery-free.</success_criteria>
