---
phase: 61-conversation-first-desktop-harness-and-evidence-bound-reflection-loop
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/personal_intelligence_kernel/test/conversation-turn.test.mjs
  - apps/personal_intelligence_kernel/src/runtime/conversation-session.mjs
  - apps/personal_intelligence_kernel/src/conversation/turn-service.mjs
  - apps/personal_intelligence_kernel/src/kernel-host.mjs
  - apps/personal_intelligence_kernel/src/runtime/resource-policy.mjs
  - apps/personal_intelligence_kernel/src/server.mjs
autonomous: true
requirements: [HARNESS-02]
must_haves:
  truths:
    - "One conversation turn calls a real Pi AgentSession.prompt(), observes leased Tool calls/results, waits for idle, and returns only sanitized final answer/receipts."
    - "A turn selects zero or one primary Skill and at most one bounded supporting Skill; out-of-lease operations fail before bridge dispatch."
    - "Cancellation or outcome_unknown is retained as non-success and reconciles through existing Task/Session/Event semantics."
    - "The one Pi runtime exposes separate Conversation, Reflection, and Operator policy profiles; an ordinary conversation lease cannot see or mutate Reflection/Operator tools."
  artifacts:
    - path: apps/personal_intelligence_kernel/src/conversation/turn-service.mjs
      provides: Pi-owned conversation turn adapter
    - path: apps/personal_intelligence_kernel/src/runtime/resource-policy.mjs
      provides: explicit deny-by-default Conversation, Reflection, and Operator policy profiles
    - path: apps/personal_intelligence_kernel/test/conversation-turn.test.mjs
      provides: regression proof for real prompt lifecycle and provider mode
  key_links:
    - from: turn-service.mjs
      to: AgentSession.prompt
      via: per-turn leased session
      pattern: session.prompt
    - from: kernel-host.mjs
      to: conversation-turn route
      via: narrow dispatch without SkillEngine outer loop
      pattern: conversation.turn
---

<objective>
Replace the conversation execution branch with the real, contained Pi iterative Tool loop.

Purpose: prove the product's governed conversation runtime rather than a provider call plus a custom outer Agent loop (D-01, D-02, D-09–D-13, D-28–D-29).
Output: test-first conversation route, lease-scoped session factory, sanitized receipts and truthful recovery states. D-10 limits selection to one primary plus one bounded support Skill; D-11 preserves the governed Skill/operation base; D-12 keeps Broker checks ahead of execution.
</objective>
<execution_context>@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@apps/personal_intelligence_kernel/src/kernel-host.mjs
@apps/personal_intelligence_kernel/src/runtime/resource-policy.mjs
@apps/personal_intelligence_kernel/src/server.mjs
@apps/personal_intelligence_kernel/src/tools/domain-bridge.mjs
@governance/manifests/ai/pi-skills.json
@governance/manifests/capabilities/project-capabilities.json
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-PATTERNS.md
<interfaces>Use AgentSession subscribe, setActiveToolsByName, prompt, waitForIdle, abort, dispose. One Pi Runtime resolves exactly one named policy profile per task: Conversation is a small read-only base plus the selected Skill lease; Reflection may stage Candidate metadata only from deterministic events; Operator is explicit-confirmation/high-risk only. Initial ordinary-conversation Skill is `knowledge.research`; its lease is checksum-bound and read-only, and Plan 61-04 later adds the registered `evidence.sqlite_query` operation without creating another selector or supporting Skill. Profile resolution defaults to deny and never unions cross-profile tool names. Kernel Task/Session/Event stores remain metadata-only and retain existing cancel/reconcile controls. This plan owns only turn/cancel/resume/reconcile Kernel routes; canonical thread projection is Plan 61-05 and proactive routes are Plan 61-10.</interfaces>
</context>
<feature>
  <name>Real Pi lease-scoped conversation turn</name>
  <files>apps/personal_intelligence_kernel/test/conversation-turn.test.mjs, apps/personal_intelligence_kernel/src/runtime/conversation-session.mjs, apps/personal_intelligence_kernel/src/conversation/turn-service.mjs, apps/personal_intelligence_kernel/src/kernel-host.mjs, apps/personal_intelligence_kernel/src/runtime/resource-policy.mjs, apps/personal_intelligence_kernel/src/server.mjs</files>
  <behavior>
    - RED: deterministic runtime fixture proves route invokes `session.prompt`, has one active read-only Skill lease, receives tool call/result and `agent_settled`, then waits idle; provider-only and `SkillEngine.run()`-only branches fail.
    - RED: non-leased operation, manifest/checksum drift, raw private event, cancellation, and outcome_unknown cannot return a success envelope; the existing undefined-unless-supplied providerMode behavior remains unchanged.
    - GREEN: one named POST conversation-turn route creates a per-turn real model session and projects final answer plus bounded receipt metadata only.
  </behavior>
  <implementation>First add the deterministic Node test using the temporary-directory/loopback fixtures in kernel-workflow.test.mjs. Then create the per-turn runtime factory preserving every containment switch from resource-policy.mjs, set exactly the Skill manifest lease before prompt, subscribe before prompt and dispose in finally. Add turn-service event projection and narrow KernelHost/server dispatch; preserve the current user-owned kernel-host providerMode diff at lines identified by PATTERNS, remove no user behavior, and do not call providerAdapter.generate or SkillEngine.run as a second Agent loop for conversation turns. Bind task/correlation/idempotency, profile, privacy ceiling, budget, timeout, Skill checksum and capability checksum to sanitized receipts.</implementation>
</feature>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写出真实 Pi 生命周期、租约与用户 provider-mode 回归测试</name>
  <files>apps/personal_intelligence_kernel/test/conversation-turn.test.mjs</files>
  <read_first>Read kernel-host.mjs in full and preserve its existing user diff; read resource-policy.mjs, server.mjs, domain-bridge.mjs, capability-registry.mjs, test/kernel-workflow.test.mjs, installed agent-session.d.ts, 61-VALIDATION.md HARNESS-02/HARNESS-08, and the knowledge.research manifest entry.</read_first>
  <behavior>- `session.prompt()` and `waitForIdle()` are called; emitted tool-call/result and settled states yield only safe receipt fields. - Conversation, Reflection and Operator resolve as three explicit profile definitions on the one runtime; unknown profile and cross-profile tool lookup fail closed. - zero/one primary plus max one support Skill derives the Conversation active set; an operation outside it is rejected twice. - ordinary Conversation cannot stage Candidate metadata, mutate canonical state, or invoke Reflection/Operator-only operations; cancel/outcome_unknown are non-success; `providerMode` stays undefined unless options/environment supplies it.</behavior>
  <action>Create a deterministic real-session test double that implements the Pi lifecycle, profile resolver, leased read-only tool, safe event projection, cancellation and reconciliation. Assert the one runtime's Conversation, Reflection and Operator boundaries: unknown/cross-profile/foreign lease attempts reject before bridge and gateway dispatch, and ordinary Conversation cannot receive Reflection Candidate staging or Operator mutation authority. Assert no `body`, `prompt`, `completion`, `credential` or `secret` reaches Task/Session/Event/Candidate projections, and assert the legacy provider-mode behavior before touching host dispatch. Run it once in RED state and retain the failure evidence in the task commit.</action>
  <verify><automated>node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs</automated></verify>
  <acceptance_criteria>The new test fails against the current provider-plus-SkillEngine conversation path, missing profile definitions, cross-profile visibility, an ordinary conversation mutation lease, a restored implicit replay provider mode, or a duplicate outer agent loop.</acceptance_criteria>
  <done>Wave 0 Kernel behavior is expressed as executable regression tests.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 实现 Pi-owned conversation.turn 路径并保留现有账本语义</name>
  <files>apps/personal_intelligence_kernel/src/runtime/conversation-session.mjs, apps/personal_intelligence_kernel/src/conversation/turn-service.mjs, apps/personal_intelligence_kernel/src/kernel-host.mjs, apps/personal_intelligence_kernel/src/runtime/resource-policy.mjs, apps/personal_intelligence_kernel/src/server.mjs</files>
  <read_first>Read the failing conversation-turn test; reread only its named source contracts through PATTERNS.md references, then inspect current git diff for kernel-host.mjs before editing. Read docs/AGENTS.md privacy and activation rules.</read_first>
  <action>Per D-01/D-02/D-09/D-10/D-11/D-12/D-13/D-23/D-24/D-25/D-28/D-29 and R-61-01/R-61-05, define the Conversation, Reflection and Operator policy profiles in resource-policy.mjs for one contained Pi runtime, with explicit profile IDs, tool-name allowlists, side-effect ceilings and deny-by-default resolution. Conversation permits only its small read-only base plus checksum-bound selected Skill lease; Reflection accepts only deterministic-event Candidate staging; Operator requires existing explicit confirmation/high-risk governance. Create a separate per-turn Conversation session factory that uses the existing deterministic fixture/replay provider for required tests/UAT while still calling the real `AgentSession.prompt(... source: rpc)`, observing Tool events and awaiting idle; do not silently select a live provider. Implement turn-service to subscribe, project safe categories, call setActiveToolsByName, prompt, await idle, and unsubscribe/dispose. Add only bounded `/v1/conversations/turn`, `/v1/conversations/cancel`, `/v1/conversations/resume`, and `/v1/conversations/reconcile` server contracts with fixed typed input and safe no-store envelopes. Do not register recent/select/new/project-scope thread-projection routes here (Plan 61-05 owns Python-canonical reads), and do not register proactive routes or reference Plan 61-10's future contract here. In KernelHost dispatch these routes through existing ledger/journal/session/cancel/reconcile functions and the new service; the conversation path must not invoke `providerAdapter.generate()` or `SkillEngine.run()` for Tool iteration. Do not broaden Phase 60 primary activation or mutate capability authority; an optional live/paid smoke remains outside acceptance and requires separate human approval and budget.</action>
  <verify><automated>node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs && npm --prefix apps/personal_intelligence_kernel test</automated></verify>
  <acceptance_criteria>Tests show prompt→leased tool→receipt→continued reasoning→settled/idle; exact fixed navigation/recovery routes return safe models; a non-leased, unknown-profile or cross-profile tool and fingerprint/checksum drift fail closed; ordinary conversation never obtains Reflection/Operator mutation authority; cancellation/outcome_unknown has no false success; kernel-host user provider-mode behavior is preserved byte-for-byte outside the targeted dispatch addition.</acceptance_criteria>
  <done>The real Pi session owns the iterative loop and the existing governed Kernel owns durable metadata/control state.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Model/Pi session → active Tool set | Model proposals must not gain ambient access to 44 operations. |
| Kernel → Domain Bridge/Python authority | Execution binds task, lease, capability and receipt metadata. |
## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification evidence | Closure / blocking condition |
|---|---|---|---|---|---|---|---|
| T-61-AGENT-01 | Critical | Elevation of Privilege | Pi Tool lease and profile boundary | mitigate | `setActiveToolsByName`, checksum binding, explicit Conversation/Reflection/Operator profiles, and bridge/gateway double rejection/drift tests. | `node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs` proves unknown/cross-profile and ordinary Conversation mutation leases reject. | All profile/lease tests pass; otherwise block Plan 61-03 and every conversation/renderer/UAT dependent wave. |
| T-61-LOOP-01 | High | Tampering | conversation execution | mitigate | Test requires `session.prompt`/idle and rejects provider-only or outer SkillEngine loop. | `node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs`. | Real Pi lifecycle proof passes; otherwise block current plan and Phase 61. |
| T-61-RECOVER-01 | High | Repudiation | cancel/reconcile | mitigate | Existing Task/Session/Event controls retain cancellation/outcome_unknown until explicit reconciliation. | `node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs` covers cancel, resume and outcome_unknown/reconcile envelopes. | No recovery state normalizes to success before reconciliation; otherwise block Plan 61-03 and Plan 61-12 UAT. |
| T-61-TRACE-01 | High | Information Disclosure | Pi events/receipts | mitigate | Project only safe event categories and metadata; sentinel leak tests fail closed. | `node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs` sentinel projection cases pass. | No private sentinel is projected; otherwise stop and block Phase 61. |
</threat_model>

## Security Closure Gate
Every Critical/High row above is an execution gate. A missing or failing named evidence command blocks the current plan and prevents its dependent wave from starting; Phase 61 remains blocked until the row is closed.
<verification>Run the focused Node test at RED and GREEN, then the Kernel suite and git diff --check.</verification>
<success_criteria>A single governed Pi AgentSession carries one real conversation turn with no duplicate outer agent loop.</success_criteria>
<output>Create .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-03-SUMMARY.md when done</output>
