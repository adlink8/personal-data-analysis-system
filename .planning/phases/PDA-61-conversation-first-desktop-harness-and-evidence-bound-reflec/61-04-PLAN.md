---
phase: 61-conversation-first-desktop-harness-and-evidence-bound-reflection-loop
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - governance/manifests/capabilities/project-capabilities.json
  - src/personal_knowledge/services/evidence_sqlite_tool.py
  - src/personal_knowledge/services/pi_domain_gateway.py
  - tests/unit/test_evidence_sqlite_tool.py
  - tests/integration/test_evidence_sqlite_tool.py
autonomous: true
requirements: [HARNESS-03]
must_haves:
  truths:
    - "Only approved descriptor IDs and typed parameters read approved SQLite views/columns through Python RO connections."
    - "Mutation, ATTACH, extension loading, write PRAGMA, multi-statement/scope escape and resource overages fail closed without fingerprint change."
  artifacts:
    - path: src/personal_knowledge/services/evidence_sqlite_tool.py
      provides: descriptor-only SQLite authority Tool and immutable receipt
  key_links:
    - from: pi_domain_gateway.py
      to: evidence_sqlite_tool.py
      via: explicit evidence.sqlite_query operation map
      pattern: evidence.sqlite_query
---
<objective>Implement the bounded evidence SQLite Tool on the Python authority boundary, per D-16's direct-but-governed query contract.</objective>
<context>@src/personal_knowledge/adapters/agentsview.py
@src/personal_knowledge/services/pi_domain_gateway.py
@tests/integration/test_agentsview_source_adapter.py
@tests/contract/test_pi_domain_gateway.py
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-VALIDATION.md
<interfaces>Request descriptor: database_id, query_id, typed parameters, scope, task_id, idempotency_key, binding and capability. Receipt: database/source identity, source/schema/snapshot binding, statement checksum, row/byte/duration, truncation, status and receipt_id; never raw SQL parameters or bodies.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: 写入 SQLite descriptor 与不变性 Wave 0 测试</name>
<files>tests/unit/test_evidence_sqlite_tool.py, tests/integration/test_evidence_sqlite_tool.py</files>
<read_first>Read agentsview.py RO connection/schema gate, test_agentsview_source_adapter.py fixture/fingerprint patterns, pi_domain_gateway.py operation validation, test_pi_domain_gateway.py, capability manifest canonicalization pattern, and 61-VALIDATION.md HARNESS-03 row.</read_first>
<behavior>- An approved metadata/evidence descriptor returns only bounded projected rows and a complete receipt. - `INSERT`, `UPDATE`, `DELETE`, `ATTACH`, `load_extension`, write PRAGMA, writable CTE, semicolon/multi-statement, unapproved database/view/column/scope, invalid capability and row/byte/time overages reject. - Every success/rejection/timeout leaves database fingerprints and active pointer unchanged.</behavior>
<action>Create temporary redacted SQLite fixtures and baseline fingerprints; encode descriptor-only calls, including hostile query-like values, as tests. Assert the exact safe error codes and that read tests never change the source fixture. Do not use live data/ or var/ databases.</action>
<verify><automated>python -m pytest -q tests/unit/test_evidence_sqlite_tool.py tests/integration/test_evidence_sqlite_tool.py</automated></verify>
<acceptance_criteria>Tests fail while arbitrary SQL/path access is possible or receipts omit checksum/identity/bounds/truncation fields.</acceptance_criteria>
<done>SQLite authority acceptance and negative invariants are executable before implementation.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: 实现 allowlist SQLite Tool 并显式接入 Gateway</name>
<files>governance/manifests/capabilities/project-capabilities.json, src/personal_knowledge/services/evidence_sqlite_tool.py, src/personal_knowledge/services/pi_domain_gateway.py</files>
<read_first>Read the failing SQLite tests; read agentsview.py and pi_domain_gateway.py in full; read project-capabilities.json entries near knowledge.search/evidence.resolve; read 61-CONTEXT.md D-14–D-17 and D-26/D-29.</read_first>
<action>Per D-14/D-16/D-17/D-26/D-29, define versioned `database_id` and `query_id` descriptors whose compile-time templates project only approved normalized metadata/evidence views and columns; parameters only fill bound values and scope is checked before compile. Open `file:...?mode=ro` through Python sqlite3, set `PRAGMA query_only=ON`, enforce one compiled statement plus 50-row/16KiB/3s ceilings, and calculate database/source/schema/snapshot binding plus statement checksum. Register `evidence.sqlite_query` explicitly in the capability manifest and PiDomainGateway operation map with declared fields, capability/binding/idempotency checks and existing safe envelopes. Never accept SQL, paths, callable names or a Node-side database connection; never make it a promotion/canonical mutation route.</action>
<verify><automated>python -m pytest -q tests/unit/test_evidence_sqlite_tool.py tests/integration/test_evidence_sqlite_tool.py tests/contract/test_pi_domain_gateway.py</automated></verify>
<acceptance_criteria>All approved reads are descriptor-compiled and receipts include required identity/checksum/bounds; every denied variant fails before authority mutation and the active pointer fingerprint is unchanged.</acceptance_criteria>
<done>Python exclusively owns a direct-but-governed evidence query slice.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Pi/Kernel request → Python evidence authority | Untrusted model values cannot choose SQL, database paths or operations. |
| Python authority → SQLite evidence database | Read must not alter source or authority/promotion state. |
## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification evidence | Closure / blocking condition |
|---|---|---|---|---|---|---|---|
| T-61-SQL-01 | Critical | Tampering/Information Disclosure | SQLite Tool | mitigate | Descriptor allowlist, typed scope, RO URI/query_only, bounded execution and fingerprint tests. | `python -m pytest -q tests/unit/test_evidence_sqlite_tool.py tests/integration/test_evidence_sqlite_tool.py`. | Approved/hostile query cases and fingerprints pass; otherwise block Plan 61-04 and all dependent freshness/renderer/UAT waves. |
| T-61-SQL-02 | Critical | Elevation of Privilege | Gateway operation map | mitigate | Explicit operation registration and capability/binding/idempotency validation; no dynamic callable/path. | `python -m pytest -q tests/unit/test_evidence_sqlite_tool.py tests/integration/test_evidence_sqlite_tool.py tests/contract/test_pi_domain_gateway.py`. | Dynamic path/callable/capability bypass is rejected; otherwise block current plan and Phase 61. |
| T-61-AUTH-01 | High | Tampering | canonical/promotion pointers | mitigate | Before/after fingerprints for success, reject and timeout; no write operation exists. | `python -m pytest -q tests/integration/test_evidence_sqlite_tool.py`. | All non-append-only authority fingerprints remain unchanged; otherwise stop execution and block Phase 61. |
</threat_model>

## Security Closure Gate
Every Critical/High row above is an execution gate. A missing or failing named evidence command blocks the current plan and prevents its dependent wave from starting; Phase 61 remains blocked until the row is closed.
<verification>Use only redacted temporary fixtures; run the listed pytest command and git diff --check.</verification>
<success_criteria>Evidence access is useful to Pi but cannot become arbitrary SQL or an authority bypass.</success_criteria>
<output>Create .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-04-SUMMARY.md when done</output>
