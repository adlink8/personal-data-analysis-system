---
phase: 61-conversation-first-desktop-harness-and-evidence-bound-reflection-loop
plan: 10
type: tdd
wave: 7
depends_on: [61-07, 61-09]
files_modified:
  - apps/personal_intelligence_kernel/src/kernel-host.mjs
  - apps/personal_intelligence_kernel/src/server.mjs
  - apps/personal_intelligence_kernel/test/conversation-turn.test.mjs
  - src/personal_knowledge/services/harness_conversation_service.py
  - src/personal_knowledge/services/pi_domain_gateway.py
  - tests/integration/test_harness_proactive.py
autonomous: true
requirements: [HARNESS-05, HARNESS-08]
must_haves:
  truths:
    - "Fixed proactive state, controls, dismiss and undo routes consume only the deterministic adapter and never schedule, learn permissions, change values or mutate canonical authority."
    - "Global/project/category controls, quiet hours, cluster deduplication and append-only dismissal feedback are validated by the Kernel and Python authority before the renderer can consume them."
  artifacts:
    - path: tests/integration/test_harness_proactive.py
      provides: fixed proactive provider and no-autonomy contract
    - path: apps/personal_intelligence_kernel/src/server.mjs
      provides: four fixed proactive Kernel routes
  key_links:
    - from: apps/personal_intelligence_kernel/src/server.mjs
      to: KernelHost.getProactiveState
      via: fixed state route
      pattern: /v1/proactive/state
    - from: apps/personal_intelligence_kernel/src/server.mjs
      to: KernelHost.updateProactiveControls
      via: fixed controls route
      pattern: /v1/proactive/controls
    - from: apps/personal_intelligence_kernel/src/server.mjs
      to: KernelHost.dismissProactive
      via: fixed dismiss route
      pattern: /v1/proactive/dismiss
    - from: apps/personal_intelligence_kernel/src/server.mjs
      to: KernelHost.undoProactiveDismissal
      via: fixed undo route
      pattern: /v1/proactive/undo
---

<objective>
Solely bind deterministic proactive presentation controls to four fixed governed routes after their adapter and shared Kernel provider contracts exist.

Purpose: make D-23–D-25 usable without turning quiet hours, feedback or cards into scheduling, permission or canonical authority.
Output: test-first proactive route contracts and bounded Kernel→Gateway wiring through `getProactiveState`, `updateProactiveControls`, `dismissProactive`, and `undoProactiveDismissal`.
</objective>

<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@src/personal_knowledge/application/conversation/harness_proactive.py
@src/personal_knowledge/services/harness_conversation_service.py
@src/personal_knowledge/services/pi_domain_gateway.py
@apps/personal_intelligence_kernel/src/kernel-host.mjs
@apps/personal_intelligence_kernel/src/server.mjs
@apps/personal_intelligence_kernel/test/conversation-turn.test.mjs
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-07-PLAN.md
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-09-PLAN.md
<interfaces>Plan 61-10 is the sole implementation owner of four KernelHost methods and their routes: `getProactiveState` → `proactive.state.get` → `/v1/proactive/state`; `updateProactiveControls` → `proactive.controls.update` → `/v1/proactive/controls`; `dismissProactive` → `proactive.dismiss` → `/v1/proactive/dismiss`; `undoProactiveDismissal` → `proactive.dismiss.undo` → `/v1/proactive/undo`. Scope is exactly global or approved project; category is exactly `同步`, `简报`, or `反思候选`. Responses are no-store safe metadata only: active/quiet status, `quiet_until`, one cluster card's merged count and source/time/receipt/support/conflict refs, control state and append-only feedback ID. No route accepts endpoint/provider override, schedule, permission, personal-value or canonical command.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写入四个 proactive 固定 provider 的失败契约</name>
  <files>apps/personal_intelligence_kernel/test/conversation-turn.test.mjs, tests/integration/test_harness_proactive.py</files>
  <read_first>Read Plan 61-07 deterministic proactive adapter contract, existing Kernel safe-route test conventions, pi_domain_gateway.py guarded-operation patterns, and 61-VALIDATION.md HARNESS-05/HARNESS-08 rows.</read_first>
  <behavior>- The four fixed KernelHost methods `getProactiveState`, `updateProactiveControls`, `dismissProactive`, and `undoProactiveDismissal`, and only their matching `/v1/proactive/` routes, reach their matching named Gateway provider; unknown paths, endpoint/provider overrides, foreign scope/item/feedback IDs, malformed quiet hours, unknown category and missing binding/idempotency/capability reject. - State returns deterministic quiet/global/project/category/cluster metadata only. - Controls, dismiss and undo preserve append-only feedback and cannot reorder manual messages, invoke learned scheduling, change permissions/values, or write canonical/promotion/rollback/watermark/active-pointer state.</behavior>
  <action>Create redacted deterministic event/cluster fixtures and authority fingerprints. Invoke the KernelHost/server→Gateway boundary, not helper functions directly, for state, controls, dismiss and undo. Assert global/project/category scope validation, quiet-hour boundary behavior, one-card-per-cluster metadata, exact idempotent dismissal/undo history, safe error redaction and every endpoint/provider/authority bypass negative. Run in RED state without live data, var stores or provider calls.</action>
  <verify><automated>node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs && python -m pytest -q tests/integration/test_harness_proactive.py</automated></verify>
  <acceptance_criteria>Tests fail if any proactive route is dynamic, accepts an unknown typed field/ID, permits schedule/permission/value/canonical mutation, loses quiet/control/cluster semantics, or produces mutable/non-auditable dismiss/undo feedback.</acceptance_criteria>
  <done>Proactive route governance is executable before fixed transport wiring exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 绑定四个 fixed Kernel→Gateway proactive 路由</name>
  <files>apps/personal_intelligence_kernel/src/kernel-host.mjs, apps/personal_intelligence_kernel/src/server.mjs, src/personal_knowledge/services/harness_conversation_service.py, src/personal_knowledge/services/pi_domain_gateway.py</files>
  <read_first>Read the failing Task 1 tests, Plan 61-07 adapter output, Plan 61-09 fixed route/context patterns, current Kernel safe route dispatch and Context D-23–D-25/D-26/D-28/D-29.</read_first>
  <action>Per D-23/D-24/D-25/D-26/D-28/D-29 and R-61-05, register the four named Gateway providers and implement only `KernelHost.getProactiveState`, `KernelHost.updateProactiveControls`, `KernelHost.dismissProactive`, and `KernelHost.undoProactiveDismissal`, bound respectively to fixed `/v1/proactive/state`, `/v1/proactive/controls`, `/v1/proactive/dismiss`, and `/v1/proactive/undo` server routes. Validate capability, binding, task/idempotency, approved scope/category, quiet-hour format, item/feedback identity and sender before calling the deterministic Plan 61-07 adapter; normalize no-store metadata-only envelopes. Preserve global/project/category controls (`同步`, `简报`, `反思候选`), `quiet_until`, cluster merged evidence references and append-only dismiss/undo feedback. Reject endpoint/path/provider overrides, arbitrary payload fields, learned scheduling, autonomous action, permission/value change and every canonical/promotion/rollback/watermark/active-pointer mutation. Do not add a store or render UI in this plan.</action>
  <verify><automated>node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs && python -m pytest -q tests/integration/test_harness_proactive.py tests/contract/test_pi_domain_gateway.py</automated></verify>
  <acceptance_criteria>Exactly four fixed KernelHost methods (`getProactiveState`, `updateProactiveControls`, `dismissProactive`, `undoProactiveDismissal`), their matching `/v1/proactive/` routes, and named providers exist; all accepted requests are schema-bound to deterministic proactive state/control/feedback behavior, all override and authority-mutation variants fail closed, and fingerprints prove only intended append-only feedback can change.</acceptance_criteria>
  <done>Renderer consumers can reach governed deterministic proactive data without a broad control plane.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Desktop/Kernel intent → proactive authority | UI feedback must not become scheduling or broader authority. |
| Kernel route → Python adapter | Only named, typed deterministic operations may cross the loopback boundary. |

## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification evidence | Closure / blocking condition |
|---|---|---|---|---|---|---|---|
| T-61-PROACTIVE-02 | High | Tampering/Repudiation | proactive fixed routes | mitigate | Four fixed typed Gateway/Kernel routes validate scope/category/binding/idempotency and retain append-only feedback. | `node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs && python -m pytest -q tests/integration/test_harness_proactive.py`. | Route/feedback negative tests pass; otherwise block renderer/UAT. |
| T-61-PROACTIVE-03 | Critical | Elevation of Privilege | proactive controls | mitigate | Reject dynamic endpoints, learned scheduling, permission/value changes and canonical lifecycle writes; fingerprint tests prove the boundary. | `python -m pytest -q tests/integration/test_harness_proactive.py tests/contract/test_pi_domain_gateway.py`. | No authority mutation is possible; otherwise Phase 61 remains blocked. |
</threat_model>

## Security Closure Gate
Every Critical/High row above is an execution gate. A missing or failing named evidence command blocks this plan and all later desktop/UAT work.
<verification>Run the focused Kernel/Python tests at RED and GREEN, then `git diff --check`.</verification>
<success_criteria>Deterministic proactive presentation remains governed, reversible and non-autonomous.</success_criteria>
<output>Create .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-10-SUMMARY.md when done</output>
