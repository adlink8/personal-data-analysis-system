---
phase: 61-conversation-first-desktop-harness-and-evidence-bound-reflection-loop
plan: 06
type: tdd
wave: 3
depends_on: [61-05]
files_modified:
  - apps/personal_intelligence_kernel/src/events/schema.mjs
  - apps/personal_intelligence_kernel/src/events/journal.mjs
  - apps/personal_intelligence_kernel/src/reflection/conversation-delta-dispatcher.mjs
  - apps/personal_intelligence_kernel/src/server.mjs
  - apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs
  - src/personal_knowledge/application/sync.py
  - tests/integration/test_harness_reflection.py
autonomous: true
requirements: [HARNESS-05]
must_haves:
  truths:
    - "A committed canonical sync or close emits exactly one durable `conversation.delta.committed` event after checksum and watermark commit; dry runs and mismatches emit nothing."
    - "The EventJournal dispatcher replays from a durable cursor, advances only after a successful guarded dispatch, and treats an exact retry as one event rather than a second delta."
  artifacts:
    - path: apps/personal_intelligence_kernel/src/reflection/conversation-delta-dispatcher.mjs
      provides: durable cursor/replay dispatcher for committed conversation deltas
    - path: src/personal_knowledge/application/sync.py
      provides: post-commit canonical-delta publisher hook
  key_links:
    - from: src/personal_knowledge/application/sync.py
      to: EventJournal conversation.delta.committed
      via: fixed publisher after canonical checksum/watermark commit
      pattern: conversation\\.delta\\.committed
    - from: conversation-delta-dispatcher.mjs
      to: EventJournal replay/checkpoint history
      via: named consumer cursor and deterministic event binding
      pattern: conversation-reflection-v1
---

<objective>
Create the real post-commit canonical conversation-delta producer and durable EventJournal dispatcher contract.

Purpose: establish the only deterministic entry path for reflection, before any Python inference/Candidate work (D-18–D-20, D-26, D-28, D-29).
Output: a committed-only publisher, append-only consumer checkpoints, replay-safe dispatcher, and focused production-path tests.
</objective>

<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@AGENTS.md
@docs/AGENTS.md
@apps/personal_intelligence_kernel/src/events/schema.mjs
@apps/personal_intelligence_kernel/src/events/journal.mjs
@apps/personal_intelligence_kernel/src/server.mjs
@src/personal_knowledge/application/sync.py
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-CONTEXT.md
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-VALIDATION.md
<interfaces>Event type is exactly `conversation.delta.committed`. Its metadata-only payload binds event ID, canonical checksum, committed watermark/publication version, source/scope reference, occurred time, snapshot and idempotency key. The named consumer is `conversation-reflection-v1`; it may invoke only an injected guarded staging seam and records a checkpoint only after that seam returns success. It never accepts a public generic event, renderer/model trigger, body, prompt, credential, SQL, or canonical/promotion mutation.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 固化 committed-only delta 发布与 durable replay 的失败契约</name>
  <files>apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs, tests/integration/test_harness_reflection.py</files>
  <read_first>Read events/schema.mjs and events/journal.mjs in full; read server.mjs safe internal-route conventions; read application/sync.py `_cmd_conversations`, `run_agentsview_stage`, and `_record_conversation_versions`; read 61-VALIDATION.md HARNESS-05 and D-14/D-15/D-18/D-26/D-28/D-29.</read_first>
  <behavior>- The test enters through the actual `pk-sync conversations --write` post-commit seam and the matching committed close seam, not an adapter helper. - Dry run, absent/mismatched canonical checksum or watermark, and public/renderer/model trigger publish no event. - Exact retry returns the same EventJournal event/sequence; divergent identity fails closed. - Dispatcher restart resumes its persisted cursor, never advances it after a failed staging callback, and sends only event/checksum/watermark/rule-version metadata to that callback.</behavior>
  <action>Create redacted temporary canonical/publication/EventJournal fixtures and baseline fingerprints. Write the Node and Python integration assertions in RED state against the real production publisher seam and Journal replay API: append-only event identity, committed ordering, fixed internal route gate, replay/restart cursor behavior, failure checkpoint retention, source-to-AgentView and AgentView-to-canonical binding presence, and no body/secret/private parameter persistence. Do not call `harness_reflection` or any Candidate helper as the event producer or dispatcher entry point.</action>
  <verify><automated>node --test apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs && python -m pytest -q tests/integration/test_harness_reflection.py</automated></verify>
  <acceptance_criteria>RED tests prove that only the real canonical post-commit sync/close publisher emits `conversation.delta.committed`, the published canonical checksum and watermark/publication version match the committed record, and every failure, dry-run, uncommitted, missing, mismatched or ambiguous pre-commit state emits nothing.</acceptance_criteria>
  <done>Tests fail unless the canonical production path emits a committed, metadata-only, replay-safe delta and the dispatcher retains durable recovery truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 post-commit publisher、Journal checkpoint 与 replay dispatcher</name>
  <files>apps/personal_intelligence_kernel/src/events/schema.mjs, apps/personal_intelligence_kernel/src/events/journal.mjs, apps/personal_intelligence_kernel/src/reflection/conversation-delta-dispatcher.mjs, apps/personal_intelligence_kernel/src/server.mjs, src/personal_knowledge/application/sync.py</files>
  <read_first>Read the failing Task 1 tests, the full EventJournal migration/append/replay APIs, server internal-capability checks, and sync transaction order. Read Plan 61-05 dual-freshness contract; preserve existing append-only EventJournal and metadata-only Session/Event rules.</read_first>
  <action>Per D-14/D-15/D-18/D-19/D-20/D-26/D-28/D-29, register only `conversation.delta.committed` in the project-owned EventJournal schema. Add an internal-capability-protected fixed producer endpoint—not the public generic events endpoint—that derives event ID, snapshot and metadata-only payload server-side from canonical checksum, committed watermark/publication version, source/scope, occurred time, binding and idempotency. Add append-only named consumer-checkpoint history. In `pk-sync conversations --write` and the matching canonical conversation-close path, publish only after canonical records and watermark are committed and the observed checksum equals the committed watermark; dry-run/uncommitted/missing/mismatched/ambiguous freshness states publish nothing. Implement `conversation-reflection-v1` to replay batches after its checkpoint, validate type/source/authority/snapshot/checksum/watermark and bind `event_id + canonical_checksum + watermark + rule_version`; call its injected guarded staging seam, then append the checkpoint only on success. Exact retry is duplicate-safe; divergent replay fails. Do not create a queue/store, invoke a model, stage a Candidate directly, write AgentView, change promotion/active pointers, or activate a provider.</action>
  <verify><automated>node --test apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs && python -m pytest -q tests/integration/test_harness_reflection.py</automated></verify>
  <acceptance_criteria>The dispatcher cursor advances only after its guarded staging seam acknowledges handling; exact replay of the same event/checksum/watermark/rule version is deduplicated, divergent replay fails, and no producer, consumer, helper or handler can stage a Candidate directly outside the acknowledged dispatcher seam.</acceptance_criteria>
  <done>The sole delta producer is post-commit canonical sync/close and the sole consumer path is a durable, metadata-only EventJournal replay dispatcher.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| canonical publication → EventJournal | Only committed canonical facts may become reflection triggers. |
| EventJournal replay → guarded staging seam | Retries must not bypass identity/binding/cursor protection. |

## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification evidence | Closure / blocking condition |
|---|---|---|---|---|---|---|---|
| T-61-EVENT-01 | Critical | Tampering/Repudiation | canonical delta publisher | mitigate | Post-commit checksum/watermark equality and fixed internal endpoint reject dry-run/direct triggers. | `node --test apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs` | Every non-committed or mismatched trigger is rejected; otherwise block Plans 61-06 onward. |
| T-61-EVENT-02 | High | Repudiation | EventJournal dispatcher | mitigate | Append-only named checkpoint advances only after guarded callback success; exact replay deduplicates. | `node --test apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs` | Restart/failure/replay assertions pass; otherwise block reflection and review waves. |
| T-61-EVENT-03 | High | Information Disclosure | event payload | mitigate | Metadata-only payload and sentinel tests prohibit conversation bodies, prompts, credentials and SQL. | `python -m pytest -q tests/integration/test_harness_reflection.py` | No sentinel reaches Journal/checkpoint/callback; otherwise block Phase 61. |
</threat_model>

## Security Closure Gate
Every Critical/High row above is an execution gate. A missing or failing named evidence command blocks this plan and every dependent wave.
<verification>Run the focused Node/Python tests at RED and GREEN, then `git diff --check`.</verification>
<success_criteria>One actual committed canonical delta reaches an authenticated, replay-safe dispatch seam and no other trigger path exists.</success_criteria>
<output>Create .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-06-SUMMARY.md when done</output>
