---
phase: 61-conversation-first-desktop-harness-and-evidence-bound-reflection-loop
plan: 06
type: tdd
wave: 3
depends_on: [61-05]
files_modified:
  - apps/personal_intelligence_kernel/src/events/schema.mjs
  - apps/personal_intelligence_kernel/src/events/journal.mjs
  - apps/personal_intelligence_kernel/src/reflection/conversation-delta-dispatcher.mjs
  - apps/personal_intelligence_kernel/src/server.mjs
  - apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs
  - src/personal_knowledge/application/sync.py
  - src/personal_knowledge/application/conversation/harness_reflection.py
  - src/personal_knowledge/application/conversation/harness_proactive.py
  - src/personal_knowledge/services/pi_domain_gateway.py
  - tests/integration/test_harness_reflection.py
  - tests/integration/test_harness_proactive.py
autonomous: true
requirements: [HARNESS-05]
must_haves:
  truths:
    - "The real canonical sync/close publisher emits one durable `conversation.delta.committed` EventJournal event only after its canonical checksum and watermark are committed; a retry is an exact journal replay rather than a second delta."
    - "The reflection subscriber consumes `conversation.delta.committed` through its durable cursor/replay path, binds event/checksum/watermark evidence before dispatch, and never invokes reflection directly or from a model wake-up."
    - "An exact replay yields one evidence-linked Candidate with conflict/time/confidence/receipt, never a direct fact."
    - "Only deterministic proactive events are projected through governed quiet-hours and global/project/category controls; one evidence cluster becomes one drillable card without reordering manual messages."
  artifacts:
    - path: apps/personal_intelligence_kernel/src/reflection/conversation-delta-dispatcher.mjs
      provides: idempotent EventJournal cursor/replay subscriber for canonical conversation deltas
    - path: src/personal_knowledge/application/sync.py
      provides: post-commit canonical sync publication hook
    - path: src/personal_knowledge/application/conversation/harness_reflection.py
      provides: idempotent delta-to-Candidate adapter
    - path: src/personal_knowledge/application/conversation/harness_proactive.py
      provides: deterministic proactive controls/read model and evidence-cluster deduplication
  key_links:
    - from: application/sync.py
      to: EventJournal `conversation.delta.committed`
      via: fixed internal publisher after canonical publication checksum/watermark commit
      pattern: conversation\.delta\.committed
    - from: conversation-delta-dispatcher.mjs
      to: harness_reflection.py
      via: replay cursor plus event/checksum/watermark binding and idempotent stage dispatch
      pattern: reflection_key
---
<objective>Build the deterministic first half of the evidence-bound reflection loop, including D-20's one delta-to-reviewable-Candidate Walking Skeleton path.</objective>
<context>@apps/personal_intelligence_kernel/src/events/schema.mjs
@apps/personal_intelligence_kernel/src/events/journal.mjs
@apps/personal_intelligence_kernel/src/server.mjs
@src/personal_knowledge/application/sync.py
@apps/personal_intelligence_kernel/src/candidates/store.mjs
@src/personal_knowledge/intelligence/analysis/
@src/personal_knowledge/intelligence/state_projection.py
@tests/integration/test_analysis_candidates.py
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-CONTEXT.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: 为真实 canonical 发布、回放订阅和 Candidate 真值边界写 Wave 0 测试</name>
<files>apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs, tests/integration/test_harness_reflection.py</files>
<read_first>Read events/schema.mjs and events/journal.mjs in full, server.mjs event-route and safe-error conventions, application/sync.py `_cmd_conversations`/`_record_conversation_versions`, candidates/store.mjs metadata limits, state_projection.py validation, test_analysis_candidates.py, 61-VALIDATION.md HARNESS-05, and CONTEXT D-14/D-15/D-18–D-21/D-23/D-25.</read_first>
<behavior>- The only fixture producer calls the actual `pk-sync conversations --write` post-commit publication seam (and the matching guarded conversation-close seam when it has committed canonical state), which verifies the committed canonical checksum equals the publication watermark before posting the fixed internal event; direct handler invocation is rejected by the test. - The accepted event type is exactly `conversation.delta.committed`, carries only event/snapshot/canonical-checksum/watermark references, and exact retry returns the same EventJournal row/sequence while mismatched retry fails. - Dispatcher restart/replay resumes from its durable consumer cursor, binds event ID + canonical checksum + watermark + rule version before staging, does not advance its cursor on failed dispatch, and creates exactly one Candidate on an exact replay. - Candidate has immutable evidence refs, Observation/inference status, confidence, time scope, support/conflict sets and receipt metadata; model self-wakeup, generated prose-as-fact, stale/foreign canonical binding and source/AgentView/canonical freshness collapse are rejected.</behavior>
<action>Create redacted temporary canonical/publication/EventJournal fixtures. Route each happy and retry case through the production publisher and EventJournal replay API, then through the dispatcher—not by calling `harness_reflection` as the entry point. Assert append-only journal integrity, persisted consumer checkpoint/dedup behavior across a dispatcher restart, safe failure before a missing/changed watermark, no Candidate source body/secret, no direct canonical/promotion write, no message-order rewrite, and no trigger path other than the declared committed event.</action>
<verify><automated>node --test apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs && python -m pytest -q tests/integration/test_harness_reflection.py</automated></verify>
<acceptance_criteria>The test detects a pre-commit/dry-run event, direct handler shortcut, duplicate Candidate, cursor advancement on failed dispatch, missing event/checksum/watermark provenance, freshness mislabel, or arbitrary/model-triggered reflection invocation.</acceptance_criteria>
<done>The canonical event path and its reflection consumer are executable before implementation.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: 实现 post-commit `conversation.delta.committed` EventJournal 生产者和可恢复订阅者</name>
<files>apps/personal_intelligence_kernel/src/events/schema.mjs, apps/personal_intelligence_kernel/src/events/journal.mjs, apps/personal_intelligence_kernel/src/reflection/conversation-delta-dispatcher.mjs, apps/personal_intelligence_kernel/src/server.mjs, src/personal_knowledge/application/sync.py</files>
<read_first>Read the failing Node/Python tests; read the full EventJournal schema/migration/replay API, server internal-capability gate, sync publication order, Kernel Candidate/Event lifecycle, and the source/AgentView/canonical freshness contracts from Plan 61-05. Preserve append-only journal and no-body event restrictions.</read_first>
<action>Per D-14/D-15/D-18/D-19/D-20/D-26/D-28/D-29, add exactly `conversation.delta.committed` to the project-owned EventJournal schema. Add one internal-capability-protected fixed producer route (not the public generic events endpoint) that accepts only canonical conversation publication metadata: canonical conversation checksum, committed watermark/publication version, source/scope reference, occurred time, binding and idempotency key; it derives the event ID, snapshot and metadata-only payload ref server-side. Extend the Journal with an append-only named consumer-checkpoint history so `conversation-reflection-v1` can resume from a verified sequence after restart. In `pk-sync conversations --write`, and only after `run_agentsview_stage` plus `_record_conversation_versions` have committed and the observed canonical checksum equals the recorded canonical watermark, invoke the fixed publisher; give the matching canonical conversation-close path the same post-commit publisher contract, never a renderer/model shortcut. A dry run, uncommitted/mismatched checksum, missing watermark or source/AgentView/canonical freshness ambiguity must publish nothing. Implement the dispatcher to read only `journal.replay` batches after its checkpoint, validate event type/source/authority/snapshot/payload checksum and watermarks, bind `event_id + canonical_checksum + watermark + rule_version`, invoke the guarded reflection staging seam, emit existing `candidate_staged` receipt metadata only after success, and append its consumer checkpoint only then. Exact retries are duplicate-safe; divergent identity or checksum fails closed. Do not persist bodies, create a parallel event queue/store, write AgentView, auto-wake a model, mutate canonical/promotion/active pointers, or activate a provider.</action>
<verify><automated>node --test apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs && python -m pytest -q tests/integration/test_harness_reflection.py</automated></verify>
<acceptance_criteria>The sole Phase 61 delta producer is the committed canonical sync/close publication path; the durable EventJournal and subscriber survive replay/restart without duplicate Candidate creation, retain both freshness legs truthfully, and fail closed before any reflection dispatch when bindings do not prove a committed canonical delta.</acceptance_criteria>
<done>A real committed canonical event reaches a replay-safe reflection subscriber.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: 通过受控 Python 适配将已提交 event 转为 Candidate，并实现确定性主动提醒 read model</name>
<files>src/personal_knowledge/application/conversation/harness_reflection.py, src/personal_knowledge/application/conversation/harness_proactive.py, src/personal_knowledge/services/pi_domain_gateway.py, tests/integration/test_harness_reflection.py, tests/integration/test_harness_proactive.py</files>
<read_first>Read the failing integration tests, the dispatcher contract created in Task 2, existing intelligence analysis/Candidate APIs, state_projection.py, proactive control/history/read-model modules, 61-CONTEXT.md D-18–D-25, the full UI-SPEC "Proactive and deep views" section, and 61-VALIDATION.md HARNESS-05/HARNESS-08 invariants.</read_first>
<behavior>- The guarded reflection provider accepts only dispatcher-authenticated `conversation.delta.committed` bindings and returns duplicate/staged/failed safe states; same event ID/canonical checksum/watermark/rule version returns duplicate and a changed input under that identity fails rather than overwrites. - It constructs immutable Evidence and reproducible Observation metadata before a reviewable Candidate with provenance_class inference, confidence, time scope, support/conflict sets and receipt metadata. - Only declared deterministic event inputs produce proactive projections; no model wake-up, learned scheduler or message-order rewrite is possible. - Global/project/category (`同步`, `简报`, `反思候选`) enablement and quiet-hours start/end resolve to a read model that returns active or `quiet_until`; same evidence-cluster events emit one card with merged_count and a drilldown list retaining source, time, receipt and support/conflict links.</behavior>
<action>Implement the narrow guarded Python reflection staging provider consumed by the dispatcher, not a publicly callable direct handler. Register only `conversation.reflection.stage` in `PiDomainGateway` with its exact dispatcher-only binding, task/idempotency/capability checks and a dependency-injected `HarnessConversationService` provider; reject renderer/model/public-route invocation and undeclared fields. Re-validate event/checksum/watermark/source/AgentView/canonical bindings and compute/persist `reflection_key` before any inference work; construct separate immutable Evidence and reproducible Observation metadata, then stage a reviewable Candidate with provenance_class inference, evidence/contradiction refs, valid interval, confidence, uncertainty and receipt. Preserve existing candidate-staged idempotency/receipt semantics. Build the deterministic proactive adapter/read model from the same committed-event boundary: resolve global/project/category controls and quiet hours before display, keep a quiet badge anchor without moving manual messages, stable-deduplicate by evidence-cluster key, and expose merged evidence drilldown metadata. A suppressed/dismissed item retains append-only metadata for Plan 61-07's governed feedback command. Do not create an autonomous Agent, a parallel store, learned timing, external action, direct canonical fact, promotion, active-pointer mutation or provider activation path.</action>
<verify><automated>node --test apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs && python -m pytest -q tests/integration/test_harness_reflection.py tests/integration/test_harness_proactive.py tests/integration/test_analysis_candidates.py</automated></verify>
<acceptance_criteria>Only the actual committed-event subscriber can stage the Candidate; exact replay is duplicate-safe and divergent replay fails; source/AgentView/canonical freshness stays distinct; Candidate remains non-factual with complete evidence/temporal/conflict/receipt fields; proactive controls remain deterministic, granular and non-autonomous.</acceptance_criteria>
<done>The Walking Skeleton receives a real canonical event and produces one reviewable evidence-bound Candidate plus truthful proactive projections.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| canonical event → reflection adapter | Event retries/model actions must not create duplicated or authority-bearing claims. |
## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification evidence | Closure / blocking condition |
|---|---|---|---|---|---|---|---|
| T-61-REFLECT-01 | High | Tampering/Repudiation | delta trigger | mitigate | Stable reflection key, uniqueness before inference and existing event receipt/idempotency handling. | `python -m pytest -q tests/integration/test_harness_reflection.py`. | Deterministic replay creates one Candidate and divergent replay fails; otherwise block Plan 61-06 and dependent review/renderer/UAT waves. |
| T-61-CAND-01 | Critical | Elevation of Privilege | Candidate staging | mitigate | Candidate is inference/review metadata only; no direct canonical/promotion write. | `python -m pytest -q tests/integration/test_harness_reflection.py tests/integration/test_analysis_candidates.py`. | Candidate remains non-factual and cannot change promotion/authority fingerprints; otherwise block current and later Phase 61 execution. |
| T-61-LEAK-03 | High | Information Disclosure | evidence metadata | mitigate | Redacted metadata fixtures and private-body/secret rejection tests. | `python -m pytest -q tests/integration/test_harness_reflection.py`. | Sentinel/private-body rejection passes; otherwise stop and block Phase 61. |
| T-61-PROACTIVE-01 | High | Tampering/Repudiation | deterministic proactive controls and cluster projection | mitigate | Deterministic inputs, user-owned global/project/category/quiet-hours controls, evidence-cluster key and append-only feedback prevent autonomous scheduling or hidden deduplication. | `python -m pytest -q tests/integration/test_harness_proactive.py tests/integration/test_harness_reflection.py`. | Control, quiet-hour, one-card-per-cluster and ordering tests pass; otherwise block Plan 61-06 and every dependent renderer/UAT wave. |
</threat_model>

## Security Closure Gate
Every Critical/High row above is an execution gate. A missing or failing named evidence command blocks the current plan and prevents its dependent wave from starting; Phase 61 remains blocked until the row is closed.
<verification>Run the focused reflection suite and candidate regression after each implementation change.</verification>
<success_criteria>One deterministic delta produces one auditable Candidate and nothing may self-promote.</success_criteria>
<output>Create .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-06-SUMMARY.md when done</output>
