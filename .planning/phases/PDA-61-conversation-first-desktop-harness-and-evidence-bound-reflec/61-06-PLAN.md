---
phase: 61-conversation-first-desktop-harness-and-evidence-bound-reflection-loop
plan: 06
type: tdd
wave: 3
depends_on: [61-05]
files_modified:
  - src/personal_knowledge/application/conversation/harness_reflection.py
  - src/personal_knowledge/application/conversation/harness_proactive.py
  - tests/integration/test_harness_reflection.py
  - tests/integration/test_harness_proactive.py
autonomous: true
requirements: [HARNESS-05]
must_haves:
  truths:
    - "Only deterministic canonical conversation-delta events trigger reflection."
    - "An exact replay yields one evidence-linked Candidate with conflict/time/confidence/receipt, never a direct fact."
    - "Only deterministic proactive events are projected through governed quiet-hours and global/project/category controls; one evidence cluster becomes one drillable card without reordering manual messages."
  artifacts:
    - path: src/personal_knowledge/application/conversation/harness_reflection.py
      provides: idempotent delta-to-Candidate adapter
    - path: src/personal_knowledge/application/conversation/harness_proactive.py
      provides: deterministic proactive controls/read model and evidence-cluster deduplication
  key_links:
    - from: harness_reflection.py
      to: canonical delta event
      via: sha256(event_id + snapshot checksum + rule version)
      pattern: reflection_key
---
<objective>Build the deterministic first half of the evidence-bound reflection loop, including D-20's one delta-to-reviewable-Candidate Walking Skeleton path.</objective>
<context>@apps/personal_intelligence_kernel/src/events/journal.mjs
@apps/personal_intelligence_kernel/src/candidates/store.mjs
@src/personal_knowledge/intelligence/analysis/
@src/personal_knowledge/intelligence/state_projection.py
@tests/integration/test_analysis_candidates.py
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-CONTEXT.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: 写 deterministic delta 的去重、证据和真值边界测试</name>
<files>tests/integration/test_harness_reflection.py</files>
<read_first>Read events/journal.mjs idempotency behavior, candidates/store.mjs metadata limits, state_projection.py validation, test_analysis_candidates.py, 61-VALIDATION.md HARNESS-05, and CONTEXT D-18–D-21/D-23/D-25.</read_first>
<behavior>- Same event_id/snapshot checksum/rule version returns duplicate and creates exactly one Candidate. - Changed input under an existing key fails rather than overwrites. - Candidate has immutable evidence refs, Observation/inference status, confidence, time scope, support/conflict sets, receipt metadata; a model self-wakeup or generated prose-as-fact is rejected.</behavior>
<action>Create a redacted canonical-delta fixture and replay it twice; assert no Candidate source body/secret, no direct canonical/promotion write, no message-order rewrite, and no trigger path other than declared deterministic events.</action>
<verify><automated>python -m pytest -q tests/integration/test_harness_reflection.py</automated></verify>
<acceptance_criteria>The test detects duplicate Candidates, missing provenance/temporal/conflict fields, or an arbitrary/model-triggered reflection invocation.</acceptance_criteria>
<done>Reflection is pinned to deterministic, replay-safe evidence before implementation.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: 实现 event 到 Evidence/Observation/Candidate 的受控适配</name>
<files>src/personal_knowledge/application/conversation/harness_reflection.py</files>
<read_first>Read the failing reflection test and its named source contracts; read application/conversation summary/event conventions and existing intelligence analysis/Candidate APIs.</read_first>
<action>Per D-18/D-19/D-20/D-21/D-23/D-25/D-28/D-29, implement a narrow handler whose only inputs are completed AgentView/canonical sync, conversation close, decision/outcome change, or scheduled deterministic event with snapshot/watermark binding. Compute and persist a stable reflection_key before any inference work; construct separate immutable Evidence and reproducible Observation metadata then stage a reviewable Candidate with provenance_class inference, evidence/contradiction refs, valid interval, confidence, uncertainty and receipt. Emit the existing candidate-staged event and use its existing idempotency/receipt semantics. Do not create an autonomous Agent, parallel store, learned trigger, direct canonical fact, promotion, active pointer or provider activation path.</action>
<verify><automated>python -m pytest -q tests/integration/test_harness_reflection.py tests/integration/test_analysis_candidates.py</automated></verify>
<acceptance_criteria>Exact replay is duplicate-safe; a divergent replay fails; the Candidate remains explicitly non-factual and contains all evidence/temporal/conflict/receipt fields.</acceptance_criteria>
<done>The Walking Skeleton can produce one reviewable evidence-bound reflection Candidate.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: 实现确定性主动提醒控制、同簇合并和可钻取只读模型</name>
<files>src/personal_knowledge/application/conversation/harness_proactive.py, tests/integration/test_harness_proactive.py</files>
<read_first>Read 61-CONTEXT.md D-23/D-24/D-25, the full UI-SPEC "Proactive and deep views" section, existing proactive control/history/read-model modules under src/personal_knowledge/intelligence/, state_projection.py, and 61-VALIDATION.md HARNESS-05/HARNESS-08 invariants.</read_first>
<behavior>- Only declared deterministic event inputs produce proactive projections; no model wake-up, learned scheduler or message-order rewrite is possible. - Global/project/category (`同步`, `简报`, `反思候选`) enablement and quiet-hours start/end resolve to a read model that returns active or `quiet_until` status. - Items with the same evidence-cluster key emit one card with merged_count and a drilldown list retaining source, time, receipt and support/conflict links. - A suppressed/dismissed item retains append-only metadata for the later governed feedback command and never becomes an autonomous permission or value update.</behavior>
<action>Write redacted integration fixtures first for enabled, globally/project/category-disabled, inside/outside quiet hours, same-cluster replay and conflicting-evidence scenarios. Then implement the narrow deterministic proactive adapter/read model: reuse existing user-owned control/history authority where available; otherwise add only typed append-only control/read projection seams, never a second fact store. Resolve global/project/category controls and quiet-hours before display, keep a quiet badge anchor without moving manual messages, stable-deduplicate by evidence-cluster key, and expose merged evidence drilldown metadata. Do not add learned timing, autonomous scheduling, automatic permission expansion, external actions or generated-event triggering.</action>
<verify><automated>python -m pytest -q tests/integration/test_harness_proactive.py tests/integration/test_harness_reflection.py</automated></verify>
<acceptance_criteria>Tests prove deterministic-only triggering, granular controls, quiet-until status, one card per evidence cluster with source/receipt drilldown, preserved manual ordering and no learned/autonomous behavior.</acceptance_criteria>
<done>The phase has the governed D-23/D-24 proactive read slice required for a truthful conversation presentation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| canonical event → reflection adapter | Event retries/model actions must not create duplicated or authority-bearing claims. |
## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification evidence | Closure / blocking condition |
|---|---|---|---|---|---|---|---|
| T-61-REFLECT-01 | High | Tampering/Repudiation | delta trigger | mitigate | Stable reflection key, uniqueness before inference and existing event receipt/idempotency handling. | `python -m pytest -q tests/integration/test_harness_reflection.py`. | Deterministic replay creates one Candidate and divergent replay fails; otherwise block Plan 61-06 and dependent review/renderer/UAT waves. |
| T-61-CAND-01 | Critical | Elevation of Privilege | Candidate staging | mitigate | Candidate is inference/review metadata only; no direct canonical/promotion write. | `python -m pytest -q tests/integration/test_harness_reflection.py tests/integration/test_analysis_candidates.py`. | Candidate remains non-factual and cannot change promotion/authority fingerprints; otherwise block current and later Phase 61 execution. |
| T-61-LEAK-03 | High | Information Disclosure | evidence metadata | mitigate | Redacted metadata fixtures and private-body/secret rejection tests. | `python -m pytest -q tests/integration/test_harness_reflection.py`. | Sentinel/private-body rejection passes; otherwise stop and block Phase 61. |
| T-61-PROACTIVE-01 | High | Tampering/Repudiation | deterministic proactive controls and cluster projection | mitigate | Deterministic inputs, user-owned global/project/category/quiet-hours controls, evidence-cluster key and append-only feedback prevent autonomous scheduling or hidden deduplication. | `python -m pytest -q tests/integration/test_harness_proactive.py tests/integration/test_harness_reflection.py`. | Control, quiet-hour, one-card-per-cluster and ordering tests pass; otherwise block Plan 61-06 and every dependent renderer/UAT wave. |
</threat_model>

## Security Closure Gate
Every Critical/High row above is an execution gate. A missing or failing named evidence command blocks the current plan and prevents its dependent wave from starting; Phase 61 remains blocked until the row is closed.
<verification>Run the focused reflection suite and candidate regression after each implementation change.</verification>
<success_criteria>One deterministic delta produces one auditable Candidate and nothing may self-promote.</success_criteria>
<output>Create .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-06-SUMMARY.md when done</output>
