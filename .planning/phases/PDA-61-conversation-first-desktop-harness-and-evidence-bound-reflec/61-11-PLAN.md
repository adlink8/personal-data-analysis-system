---
phase: 61-conversation-first-desktop-harness-and-evidence-bound-reflection-loop
plan: 11
type: execute
wave: 8
depends_on: [61-02, 61-03, 61-04, 61-05, 61-07, 61-08, 61-09, 61-10]
files_modified:
  - apps/personal_intelligence_desktop/src/desktop-api-schema.mjs
  - apps/personal_intelligence_desktop/src/main.mjs
  - apps/personal_intelligence_desktop/src/preload.mjs
  - apps/personal_intelligence_desktop/src/renderer/index.html
  - apps/personal_intelligence_desktop/src/renderer/app.mjs
  - apps/personal_intelligence_desktop/src/renderer/styles.css
  - apps/personal_intelligence_desktop/test/main-preload.test.mjs
  - apps/personal_intelligence_desktop/test/renderer-view-model.test.mjs
autonomous: true
requirements: [HARNESS-01, HARNESS-02, HARNESS-03, HARNESS-04, HARNESS-06, HARNESS-07, HARNESS-08]
must_haves:
  truths:
    - "The Electron shell opens directly into canonical last conversation or an explicit empty state, with named recent/new/project-scope navigation and no browser dashboard."
    - "New conversation creates only Kernel empty Session metadata; scope/history reads remain Python-canonical-authoritative and selected display text remains ephemeral."
    - "The SQLite card displays only server-derived checksum-bound `statement_display` under an expandable controlled-query surface; it never renders raw SQL or physical schema/values."
    - "Renderer actions use only named preload methods and fixed provider routes for turn, review, projection and deterministic proactive controls."
  artifacts:
    - path: apps/personal_intelligence_desktop/src/renderer/app.mjs
      provides: conversation-first presentation and named bridge wiring
    - path: apps/personal_intelligence_desktop/src/main.mjs
      provides: fixed local route map with no renderer-selected endpoint
  key_links:
    - from: window.harness.newConversation
      to: conversation.session.create
      via: fixed main/preload/Kernel route and approved project scope
      pattern: conversation\.session\.create
    - from: window.harness.listProjectScopes
      to: conversation.project_scopes.list
      via: fixed main/preload/Python canonical provider route
      pattern: conversation\.project_scopes\.list
    - from: renderer SQLite card
      to: evidence.sqlite_query receipt.statement_display
      via: expandable controlled-query view only
      pattern: statement_display
---

<objective>
Wire the secure Electron shell to every provider-complete conversation-first contract and render the approved desktop path without creating a second authority surface.

Purpose: implement D-04–D-08, D-15, D-17, D-20 and D-24 through named bridges while preserving Python/Kernal authority split and privacy boundaries.
Output: test-first renderer, fixed navigation/session/provider transport, accessible controlled-query display and bounded deep surfaces.
</objective>

<execution_context>@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-UI-SPEC.md
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-02-PLAN.md
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-04-PLAN.md
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-05-PLAN.md
@apps/personal_intelligence_desktop/src/desktop-api-schema.mjs
@apps/personal_intelligence_desktop/src/preload.mjs
<interfaces>Renderer calls only `window.harness`. `listProjectScopes()` maps exactly to Python `conversation.project_scopes.list`; `selectProjectScope({projectScopeId})` maps exactly to Python `conversation.project_scope.select`; `newConversation({projectScopeId})` maps exactly to Kernel `conversation.session.create`. Thread history reads remain Python canonical, while new session response contains only governed Session metadata and an empty `ConversationThreadView`; no desktop localStorage/disk conversation persistence is legal. `statement_display` is received only in the immutable `evidence.sqlite_query` receipt/result along with checksum/query ID/version/parameter-name set; the renderer shows it only in expandable `受控查询` / `已执行的脱敏 allowlisted statement` content and cannot edit it. All other existing named methods retain the Plan 61-02 schema: turn/recovery, review, projection, and four proactive methods. No generic fetch, raw IPC, URL/path/SQL/provider selection, physical schema, parameter value, body, credential, trace or authority command is available.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 写入 desktop 导航、受控查询和深层交互的 view-model 失败契约</name>
  <files>apps/personal_intelligence_desktop/test/main-preload.test.mjs, apps/personal_intelligence_desktop/test/renderer-view-model.test.mjs</files>
  <read_first>Read the complete UI-SPEC, Plan 61-02 bridge test, Plans 61-04/05/08/09/10 interface contracts and 61-VALIDATION.md UAT sequence. Do not use Cockpit components or dashboard layout as an analog.</read_first>
  <behavior>- Startup, recent/select, scope list/select and new conversation invoke only their exact named intents; empty/stale/partial/paginated states use safe copy. - New conversation returns only empty Kernel Session metadata/empty view and cannot be labelled canonical history. - The SQLite card expands only `statement_display`; raw SQL, physical table/column names, parameter values, display overrides and receipt checksum mismatch reject. - Answer disclosures, collapsed tool row, individual four-option Candidate review, versioned Projection, deterministic proactive cards/settings and the two-action command palette remain accessible, truthful and bridge-bound.</behavior>
  <action>Create deterministic sanitized navigation, receipt, review/projection and proactive fixtures plus DOM/view-model assertions. Assert fixed bridge intent names and provider envelopes for the three navigation/session actions, rejected unknown/foreign/stale scope behavior, empty Session status, no selected-text localStorage/disk/log/receipt persistence, and no generic IPC/fetch. Assert the query card labels `受控查询` and `已执行的脱敏 allowlisted statement`, renders exactly `receipt.statement_display` only after checksum/query/version/name-set validation, and rejects raw SQL/physical schema/parameter-value/sentinel leakage or a renderer-supplied display. Retain UI-SPEC contract assertions for dual freshness, collapsed receipts, cancellation/outcome_unknown truth, Candidate confirmation/conflict four-value modal/no batch acceptance, derived projection disclosure, deterministic proactive quiet/control/cluster/dismissal feedback, Ctrl/Cmd+K's exact `receipt.open`/`proactive.manage` set, focus trapping, Esc order and focus restoration. Run in RED state without Electron automation or private data.</action>
  <verify><automated>node --test apps/personal_intelligence_desktop/test/main-preload.test.mjs apps/personal_intelligence_desktop/test/renderer-view-model.test.mjs</automated></verify>
  <acceptance_criteria>Tests fail if navigation is unbound or dynamic, session creation writes/claims canonical history, scope reads mutate canonical data, selected text persists, SQLite display is caller-controlled or leaks physical/raw content, any deep surface bypasses named bridges, or the UI mislabels freshness/Candidate/projection/cancel state.</acceptance_criteria>
  <done>All user-visible desktop boundaries are executable presentation contracts before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 Codex-style 对话优先 renderer 和只读受控查询卡</name>
  <files>apps/personal_intelligence_desktop/src/renderer/index.html, apps/personal_intelligence_desktop/src/renderer/app.mjs, apps/personal_intelligence_desktop/src/renderer/styles.css</files>
  <read_first>Read failing renderer tests, the complete UI-SPEC, selected sketch findings only through UI-SPEC, and the Plan 61-04/05 interfaces. Do not edit sketches/MANIFEST or Cockpit files.</read_first>
  <action>Per D-05/D-06/D-07/D-08/D-15/D-17/D-20/D-23/D-24/D-25/D-27/D-28/D-29, build dependency-free local HTML/CSS/JS with 232px left navigation (new conversation, recent conversations, project scopes, personal/system) and a 760–820px central conversation thread with fixed `继续这段对话…` composer. On startup call named last/recent reads; map `listProjectScopes`, `selectProjectScope`, and `newConversation` only through the exact Plan 61-05 authority contracts. Keep selected text in ephemeral renderer memory only; render new sessions as empty/runtime-scoped, never canonical history. Render answers with `依据`/`新鲜度`/`限制`, collapsed Tool rows and an inline `SQLite · 只读查询` card. Its expandable surface is labelled `受控查询` / `已执行的脱敏 allowlisted statement` and renders only checksum-validated server `statement_display`; never render raw SQL, physical schema, parameter values, editor or run button. Retain accessible Candidate individual review/confirmation/four-option conflict modal, derived Projection, deterministic proactive drawer/control/quiet/cluster/dismissal surfaces and the fixed two-action palette from their provider plans. Use UI-SPEC tokens, keyboard order, aria-live/alert, 2px focus ring, Esc/focus restoration and reduced motion. Do not add a dashboard, third-party UI registry, local conversation/body store, generic command path or authority call.</action>
  <verify><automated>node --test apps/personal_intelligence_desktop/test/renderer-view-model.test.mjs</automated></verify>
  <acceptance_criteria>Renderer tests pass; startup/navigation/new-session state is conversation-first and truthful; only server `statement_display` appears in the controlled-query expansion; raw SQL/schema/value/body/credential/trace and local body persistence remain absent; all deep interactions use the fixed safe state contracts.</acceptance_criteria>
  <done>The desktop displays the complete vertical path without becoming a conversation fact store or SQL surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 在 provider 完备后绑定 named desktop route map</name>
  <files>apps/personal_intelligence_desktop/src/desktop-api-schema.mjs, apps/personal_intelligence_desktop/src/main.mjs, apps/personal_intelligence_desktop/src/preload.mjs, apps/personal_intelligence_desktop/test/main-preload.test.mjs</files>
  <read_first>Read the failing desktop tests, Plan 61-02 injected provider seam, exact Plan 61-04/05/08/09/10 request-response schemas and existing main/preload source. Confirm no renderer field can select an endpoint or provider.</read_first>
  <action>After all dependency contracts exist, extend desktop schemas/preload with exact parsed named methods and one IPC channel each. Bind `listProjectScopes` only to the Python canonical `conversation.project_scopes.list` fixed route, `selectProjectScope` only to `conversation.project_scope.select`, and `newConversation` only to Kernel `conversation.session.create`; validate sender, scope ID, response privacy ceiling and safe empty/unknown/stale states. Configure an unexported localhost-only main map for those routes plus existing fixed thread/turn/recovery/review/projection/proactive routes. Validate all envelopes, use no-store headers, normalize receipt `statement_display` only when checksum/query ID/version/parameter-name set verify, and redact logs/telemetry to IDs/counts/checksums/status. Reject endpoint/path/provider/SQL/display/localStorage overrides, invalid/foreign IDs, private response fields and unavailable providers. Do not change Python canonical data from navigation, write desktop bodies, call the live AgentView DB, alter Cockpit, or change Phase 60 activation/provider settings.</action>
  <verify><automated>node --test apps/personal_intelligence_desktop/test/main-preload.test.mjs apps/personal_intelligence_desktop/test/renderer-view-model.test.mjs</automated></verify>
  <acceptance_criteria>Every desktop action reaches only its declared fixed provider; three navigation/session actions map exactly to the named Plan 61-05 contracts; receipt display verifies server binding; all malformed/sender/authority/privacy variants fail closed and no generic network/IPC/SQLite/storage command escape exists.</acceptance_criteria>
  <done>The secure desktop shell is connected to provider-backed conversation, controlled-evidence and proactive contracts with no dependency cycle.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Renderer → preload → main | Untrusted renderer content must not gain native, route-selection or storage authority. |
| Desktop → Python canonical / Kernel Session | Scope/history reads and empty session metadata have separate authorities. |
| Evidence receipt → renderer | Displayed query information must not become raw SQL/schema/parameter disclosure. |

## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification evidence | Closure / blocking condition |
|---|---|---|---|---|---|---|---|
| T-61-UI-02 | High | Information Disclosure | controlled query card | mitigate | Only server-derived checksum-bound `statement_display` is rendered; tests reject raw SQL/schema/value/display override. | `node --test apps/personal_intelligence_desktop/test/main-preload.test.mjs apps/personal_intelligence_desktop/test/renderer-view-model.test.mjs`. | No raw/physical/query-value leak; otherwise block UAT. |
| T-61-UI-04 | Critical | Elevation of Privilege | navigation/session/review controls | mitigate | Named bridge, sender/schema checks, Python read authority and Kernel metadata-only session creation reject dynamic/provider/authority bypass. | Same focused desktop tests. | Navigation/session and deep-action negatives pass; otherwise block Plan 61-12. |
| T-61-UI-05 | High | Repudiation | freshness/recovery labels | mitigate | Two-leg freshness and non-success cancel/outcome projections are view-model tested. | `node --test apps/personal_intelligence_desktop/test/renderer-view-model.test.mjs`. | Truthful state tests pass; otherwise block UAT. |
| T-61-UI-06 | High | Tampering/Repudiation | proactive UI | mitigate | Renderer consumes only Plan 61-10 deterministic metadata and append-only feedback. | Same renderer test. | No scheduling/reordering/feedback bypass; otherwise block UAT. |
</threat_model>

## Security Closure Gate
Every Critical/High row above is an execution gate. A missing or failing named evidence command blocks this plan and Plan 61-12 UAT.
<verification>Run both focused desktop Node tests after each task, then `git diff --check`.</verification>
<success_criteria>The conversation-first desktop is useful, privacy-bound and backed by real providers rather than schemas alone.</success_criteria>
<output>Create .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-11-SUMMARY.md when done</output>
