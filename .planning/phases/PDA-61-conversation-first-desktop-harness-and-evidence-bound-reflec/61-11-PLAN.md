---
phase: 61-conversation-first-desktop-harness-and-evidence-bound-reflection-loop
plan: 11
type: execute
wave: 8
depends_on: [61-03, 61-04, 61-05, 61-06, 61-07, 61-08, 61-09, 61-10]
files_modified:
  - apps/personal_intelligence_desktop/test/desktop-uat-fixture.mjs
  - apps/personal_intelligence_desktop/test/desktop-uat-record.md
  - tests/integration/test_pi_kernel_events.py
  - .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-VALIDATION.md
autonomous: false
requirements: [HARNESS-08]
must_haves:
  truths:
    - "Automated regressions prove IPC isolation, receipts/leases, SQLite immutability, freshness truthfulness, committed canonical EventJournal reflection, fixed review/projection-provider governance and truthful recovery."
    - "Automated regressions also prove safe navigation, profile separation, proactive controls/quiet-hours/cluster deduplication/dismissal feedback and truthful recovery."
    - "A six-step manual desktop UAT uses the existing zero-paid deterministic fixture/replay provider while traversing real Pi `AgentSession.prompt()` event/Tool/idle lifecycle, committed delta producer/subscriber, review/projection contracts and the two-action command palette, with no browser product surface."
    - "Phase 55–60 activation/rollback gates stay unchanged and Phase 60 primary remains unactivated."
  artifacts:
    - path: apps/personal_intelligence_desktop/test/desktop-uat-fixture.mjs
      provides: redacted deterministic replay provider and sentinel fixtures
    - path: apps/personal_intelligence_desktop/test/desktop-uat-record.md
      provides: private-body-free UAT evidence record
  key_links:
    - from: desktop-uat-fixture.mjs
      to: Electron app and loopback test routes
      via: fixed turn/delta/cancel/outcome_unknown replay
      pattern: outcome_unknown
---
<objective>Close the Walking Skeleton with automated security regressions and a blocking desktop UAT.</objective>
<context>@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-VALIDATION.md
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-CONTEXT.md
@.planning/STATE.md
@apps/personal_intelligence_kernel/test/kernel-workflow.test.mjs
@tests/integration/test_pi_kernel_events.py
<interfaces>Fixture supplies only redacted history/evidence/Candidate/Projection metadata plus SECRET_SENTINEL and RAW_BODY_SENTINEL negative inputs. It uses the existing deterministic fixture/replay provider with zero paid/live calls, but must execute real Pi `AgentSession.prompt()`, observe event/Tool results, and wait for idle; it is not a mocked lifecycle. UAT record contains command/version/checksum/status/receipt IDs and screenshots with private bodies redacted; it never records prompts, completions, credentials, raw SQL, tokens or ConversationThreadView display text.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: 构建不可泄露的端到端 replay fixture 与 recovery 回归</name>
<files>apps/personal_intelligence_desktop/test/desktop-uat-fixture.mjs, tests/integration/test_pi_kernel_events.py</files>
  <read_first>Read 61-VALIDATION.md all Required Negative/Invariants and Desktop UAT Sequence; read desktop/Kernel/Python focused tests produced by Plans 61-02 through 61-10; read existing test_pi_kernel_events.py and kernel-workflow.test.mjs recovery patterns; read STATE.md Phase 53–60 activation statements.</read_first>
<behavior>- Fixture performs safe last/recent/new/select/project-scope navigation through canonical `ConversationThreadView`, one approved read-only Skill turn using real `AgentSession.prompt()`/Tool events/idle with the deterministic replay provider, evidence SQLite result/rejection, actual committed canonical `conversation.delta.committed` publication plus duplicate EventJournal replay, guarded proactive controls/quiet hours/cluster deduplication/dismissal feedback, fixed-route edit/accept/ignore/undo, later governed projection retrieval, cancel, resume and outcome_unknown reconcile. - Ctrl/Cmd+K exposes only `receipt.open` and `proactive.manage`, and Esc restores focus. - selected display text is bounded ephemeral UI state; sentinel body/secret never appears in UI-safe projections, local storage, logs, receipts or UAT record. - profile and lease fingerprints prove Conversation cannot obtain Reflection/Operator mutation authority. - fingerprint/active pointer stays stable except intended append-only reviewed projection/feedback state, and activation/promotion/rollback are never claimed as complete.</behavior>
<action>Create the deterministic fixture/replay provider and extend the existing Kernel event integration test for canonical safe navigation, cancel, resume, outcome_unknown and reconciliation status. The fixture must call the production Pi `AgentSession.prompt()` lifecycle, observe the leased Tool call/result and `agent_settled`, then wait for idle while retaining zero paid/live calls; a fake or provider-only lifecycle is a test failure. Feed the reflection path only through the post-commit canonical sync/close publisher and EventJournal subscriber/replay API, assert failed-dispatch cursor handling and never invoke the Python reflection handler directly. Exercise fixed `candidate.review` and `personal.model_projection.get` bindings with edit-checksum/confirmation/version/binding/idempotency negatives and assert the next turn receives only approved compatible derived projection context. Exercise Ctrl/Cmd+K/receipt.open/proactive.manage/Esc/focus restoration with unknown command rejection. Feed only temporary/redacted stores, assert source/evidence/authority fingerprints, ConversationThreadView pagination/partial/stale privacy states, registry/Skill lease/profile fingerprints, proactive control/history fingerprints and cluster counts around each scenario, and make fixture output a safe status bundle rather than personal content. Exercise quiet-hours, global/project/category controls and dismissal/undo without learned scheduling or permission mutation. Assert selected body text is absent from disk/localStorage and all logs/receipts/telemetry. Do not use live data/, var/, real paid model calls, or change Phase 60 provider/activation configuration. An optional live/paid smoke is outside acceptance and requires a separate explicit human approval and budget checkpoint.</action>
<verify><automated>node --test apps/personal_intelligence_desktop/test/*.test.mjs apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs && python -m pytest -q tests/integration/test_pi_kernel_events.py tests/unit/test_evidence_sqlite_tool.py tests/integration/test_evidence_sqlite_tool.py tests/contract/test_harness_freshness.py tests/contract/test_harness_conversation_projection.py tests/integration/test_harness_reflection.py tests/integration/test_harness_proactive.py tests/contract/test_harness_candidate_review.py tests/contract/test_pi_domain_gateway.py tests/integration/test_harness_projection.py</automated></verify>
<acceptance_criteria>Fixture and automated suite prove no raw leakage, false success, unsafe navigation selection, cross-profile lease/authority drift, unauthorized write, duplicate Candidate or proactive cluster, quiet-hours/control bypass, dismissal-driven permission change, or stale-data mislabel while remaining independent of private runtime data.</acceptance_criteria>
<done>All security-critical paths have deterministic automated evidence.</done>
</task>
<task type="auto">
<name>Task 2: 准备无私有正文的桌面 UAT 记录和验证状态更新</name>
<files>apps/personal_intelligence_desktop/test/desktop-uat-record.md, .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-VALIDATION.md</files>
<read_first>Read the passing automated results, 61-VALIDATION.md, approved UI-SPEC, all phase SUMMARY files, and project privacy rules in docs/AGENTS.md.</read_first>
  <action>Create a UAT record template with exact six steps from 61-VALIDATION.md, including canonical last/recent/selected/new/project-scope navigation and empty/stale/partial/pagination privacy checks in step 1; a zero-paid deterministic replay provider with real Pi prompt/event/Tool/idle evidence plus Ctrl/Cmd+K, `receipt.open`, `proactive.manage` and Esc/focus restoration in step 2; committed canonical sync/close publisher → `conversation.delta.committed` EventJournal replay/subscriber evidence in step 4; fixed `candidate.review` plus `personal.model_projection.get` next-turn assertion and governed proactive settings/quiet-hours/cluster/dismissal checks in step 5. Step 5 must open one high-impact/conflicting Candidate, show all four fixed options `keep_existing`/`保留旧结论`, `replace_existing`/`用新结论取代`, `coexist_by_context`/`按情境共存`, and `defer_judgment`/`暂不判断` with consequence text, prove an unknown disposition rejects, and prove no batch-accept control exists. Step 6 covers cancel/resume/outcome_unknown reconciliation. Include expected UI labels, fixed route/receipt/checksum/watermark/cursor/fingerprint fields, pass/fail/blocked status, screenshot redaction instruction, and explicit assertions that no browser is used, no display text is retained in storage/logs/receipts/telemetry, no live/paid provider smoke occurred, and no Phase 60 primary activation/promotion/rollback changes. Update VALIDATION.md only with test file existence, exact executed commands and factual automated status; leave manual status pending until the human checkpoint and set nyquist_compliant/wave_0_complete true only when their evidence exists.</action>
<verify><automated>node -e "const fs=require('fs');const p='apps/personal_intelligence_desktop/test/desktop-uat-record.md';const s=fs.readFileSync(p,'utf8');for(const x of ['1.','2.','3.','4.','5.','6.','SECRET_SENTINEL','outcome_unknown','不使用浏览器'])if(!s.includes(x))throw Error(x)"</automated></verify>
  <acceptance_criteria>The UAT record provides six executable checks covering navigation, proactive governance, the four-option individual conflict review with consequence text/unknown-value rejection/no batch acceptance, recovery and safe evidence fields; VALIDATION.md does not claim UAT/nyquist completion before actual evidence.</acceptance_criteria>
<done>The last human verification has a privacy-safe, deterministic checklist.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
<name>Task 3: 运行六步本地 Electron Walking Skeleton UAT</name>
<files>apps/personal_intelligence_desktop/test/desktop-uat-record.md, .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-VALIDATION.md</files>
<read_first>Read desktop-uat-record.md, 61-VALIDATION.md, UI-SPEC, and the latest PLAN summaries. Confirm current git status preserves the pre-existing kernel-host.mjs provider-mode edit and does not include unrelated sketches/MANIFEST changes.</read_first>
<action>Run the six numbered UAT steps against the redacted deterministic fixture/replay provider; record per-step PASS/FAIL/BLOCKED, evidence that real Pi prompt/event/Tool/idle lifecycle ran, safe receipt/checksum/fingerprint metadata and redacted screenshot references in desktop-uat-record.md. Update only factual manual-UAT evidence in 61-VALIDATION.md after every step has a recorded result; do not change primary activation, promotion/rollback state, provider budget/approval state, or unrelated user changes.</action>
<what-built>Automated end-to-end fixture/recovery checks plus a local Electron conversation-first application.</what-built>
  <how-to-verify>1. Start the approved zero-paid fixture/replay dependencies and launch the Electron app; verify canonical last allowed conversation or declared empty state, recent-list/select, new conversation, project-scope list/select, plus empty/stale/partial/pagination states without a browser or body persistence. 2. Send the fixed historical/project prompt; verify the deterministic provider traverses real Pi `AgentSession.prompt()` → leased Tool event/result → settled/idle, then shows one read-only Skill, collapsed Tool row, evidence/freshness/limitations and expandable safe receipt. Press Ctrl/Cmd+K: only `receipt.open` and `proactive.manage` appear; trigger each, then press Esc and verify focus returns to the palette trigger. 3. Open the SQLite card; verify registered `conversation.evidence_messages.v1` identity/checksum/two freshness legs/bounds and a rejected unknown-query/lease path with unchanged fingerprint. 4. Enter through the committed canonical sync/close fixture publisher, observe durable `conversation.delta.committed` EventJournal sequence/cursor/checksum/watermark evidence, replay it twice and observe exactly one inline Candidate with evidence/conflict/time/confidence/receipt; set a category and project/global scope control plus quiet hours, then verify `静默至 {HH:MM}` without reordering manual messages. 5. Open the one cluster card's merged-evidence drilldown and a high-impact/conflicting Candidate; verify the individually processed modal visibly offers `keep_existing`/`保留旧结论`, `replace_existing`/`用新结论取代`, `coexist_by_context`/`按情境共存`, and `defer_judgment`/`暂不判断`, each with consequence text, keyboard focus, Esc close and focus return. Submit an unknown disposition and verify rejection; verify no batch-accept control is present. Then invoke fixed-route edit with matching payload checksum then explicitly accept, invoke ignore and undo, dismiss and undo the proactive card, and verify `personal.model_projection.get` contributes only a versioned derived projection to the next turn with append-only feedback. Confirm a draft/ignored Candidate does not enter next-turn context. 6. Cancel a fixed long turn, exercise the fixed resume entry, force outcome_unknown reconciliation, and verify neither says success, no partial unauthorized write is claimed, display text never enters storage/logs/receipts/telemetry, and record only redacted screenshots/metadata.</how-to-verify>
<resume-signal>Record PASS/FAIL/BLOCKED for each step in desktop-uat-record.md, then reply "Phase 61 desktop UAT approved" or describe the failing step.</resume-signal>
<verify><automated>node -e "const fs=require('fs');const s=fs.readFileSync('apps/personal_intelligence_desktop/test/desktop-uat-record.md','utf8');for(let i=1;i<=6;i++)if(!s.includes(String(i)+'.'))throw Error('missing UAT step '+i)"</automated></verify>
  <acceptance_criteria>All six UAT steps pass with no browser surface, no body/credential/trace/SQL leak, no unsafe navigation selection, no cross-profile authority, no proactive control/quiet-hour/cluster/dismissal breach, no false cancel/resume/outcome_unknown success, no unauthorized canonical/promotion write, and no activation/rollback drift. On a failure, keep VALIDATION.md truthful and do not declare the phase complete.</acceptance_criteria>
<done>A complete redacted UAT record exists, or the record truthfully shows the failing/blocked step and prevents phase completion.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| deterministic fixture → full desktop stack | Test data must prove security without exposing local personal evidence. |
| user-visible completion → runtime truth | Cancel/reconcile and authority state can otherwise be falsely presented as success. |
## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification evidence | Closure / blocking condition |
|---|---|---|---|---|---|---|---|
| T-61-VERIFY-01 | Critical | Information Disclosure | fixtures/logs/UAT record | mitigate | Sentinel scans, redacted fixtures/screenshots and metadata-only UAT record. | `node --test apps/personal_intelligence_desktop/test/*.test.mjs` plus `rg -n "SECRET_SENTINEL|RAW_BODY_SENTINEL" apps/personal_intelligence_desktop/test/desktop-uat-record.md`. | Automated sentinel tests pass and record contains no leaked fixture value; otherwise block the UAT checkpoint and Phase 61 closeout. |
| T-61-VERIFY-02 | Critical | Repudiation | cancel/resume/outcome_unknown/reconcile | mitigate | Automated event regression plus manual reconciliation check prohibits success until reconciled. | `python -m pytest -q tests/integration/test_pi_kernel_events.py` and recorded UAT step 6. | Both automated recovery test and human step 6 PASS; otherwise block plan completion and Phase 61. |
| T-61-VERIFY-03 | Critical | Tampering | authority/activation fingerprints | mitigate | Before/after DB/active-pointer/Skill-registry/profile/control checks and explicit Phase 55–60 regression gate. | Focused suite in Task 1 plus `git diff --check`; UAT record fingerprints and Phase 60 legacy/blocked status are recorded. | All fingerprints are stable except declared append-only paths and activation remains unchanged; otherwise block phase completion. |
| T-61-VERIFY-04 | Critical | Elevation of Privilege | desktop end-to-end | mitigate | UAT rechecks IPC isolation, profile/lease boundaries, SQLite rejects, safe navigation and guarded Candidate/proactive controls. | `node --test apps/personal_intelligence_desktop/test/*.test.mjs` plus six-step UAT record. | Automated suite and all six UAT steps PASS; otherwise no phase completion. |
</threat_model>

## Security Closure Gate (Phase Aggregation)
Plan 61-11 aggregates closure for every Critical/High register row in Plans 61-01 through 61-10. Before the human UAT may approve Phase 61, its UAT record and VALIDATION.md must name the executed evidence command, status, receipt/checksum/fingerprint reference and closure result for each inherited gate. Any missing, failing or BLOCKED Critical/High item blocks the current checkpoint, Plan 61-11 completion and Phase 61 completion; it cannot be waived by a visual-only UAT result.
<verification>Run all listed automated tests before the checkpoint; after approved UAT, update VALIDATION.md strictly from recorded evidence and run git diff --check.</verification>
<success_criteria>The phase can be judged from redacted evidence: one complete vertical path works, its security boundaries hold, and no deferred authority/activation scope was silently expanded.</success_criteria>
<output>Create .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-11-SUMMARY.md when done</output>
