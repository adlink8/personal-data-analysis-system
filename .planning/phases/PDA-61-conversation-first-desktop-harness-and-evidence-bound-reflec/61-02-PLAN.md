---
phase: 61-conversation-first-desktop-harness-and-evidence-bound-reflection-loop
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - apps/personal_intelligence_desktop/package.json
  - apps/personal_intelligence_desktop/package-lock.json
  - apps/personal_intelligence_desktop/src/desktop-api-schema.mjs
  - apps/personal_intelligence_desktop/src/main.mjs
  - apps/personal_intelligence_desktop/src/preload.mjs
  - apps/personal_intelligence_desktop/test/main-preload.test.mjs
autonomous: true
requirements: [HARNESS-01, HARNESS-08]
must_haves:
  truths:
    - "The local Electron shell has no renderer Node authority and opens only local app assets."
    - "Only named, schema-validated desktop actions can reach the loopback conversation authority, including recent/select/new/project-scope and cancel/resume/reconcile intents."
    - "The shell exposes safe last/recent/selected/new conversation and project-scope schemas, including the `ConversationThreadView` privacy ceiling, without binding a live route provider before its authorities exist."
  artifacts:
    - path: apps/personal_intelligence_desktop/src/main.mjs
      provides: secure BrowserWindow and fixed IPC handlers
    - path: apps/personal_intelligence_desktop/src/preload.mjs
      provides: narrow contextBridge API
  key_links:
    - from: preload.mjs
      to: desktop-api-schema.mjs
      via: named contextBridge methods and safe response normalizers
      pattern: getLastConversation|listRecentConversations|selectConversation|newConversation|listProjectScopes|selectProjectScope
---

<objective>
Create the dependency-approved, least-privilege Electron shell and its Wave 0 security contracts.

Purpose: establish the desktop boundary for the conversation-first skeleton without using the Cockpit as a visual or authority base (D-04–D-08, D-27–D-29).
Output: local Electron scripts plus a tested main/preload/schema boundary. This implements the safe bridge portion of D-05, D-06 and D-07 exactly; Plan 61-10 binds its fixed local providers only after their contracts exist.
</objective>

<execution_context>@C:/Users/li/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-ELECTRON-QUALIFICATION.json
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-UI-SPEC.md
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-PATTERNS.md
<interfaces>Desktop bridge must expose only getLastConversation(), listRecentConversations(), selectConversation({conversationId}), newConversation({projectScopeId?}), listProjectScopes(), selectProjectScope({projectScopeId}), sendTurn(input), cancelTurn({taskId}), resumeTurn({taskId}), reconcileTurn({taskId}), reviewCandidate(input), getProactiveState({projectScopeId?}), updateProactiveControls({scope,category,enabled,quietHours}), dismissProactive({itemId,reason?}), and undoProactiveDismissal({feedbackId}). Every response is a safe `{ok,status/error.code,data}` view model. Navigation lists contain allowlisted IDs, labels, timestamps, selected flags, scope metadata and bounded status only. `ConversationThreadView` is Python-canonical-authoritative and may contain only normalized user/assistant display messages with stable message ID, role, display text, created time, source/evidence ref, pagination/truncation and freshness; empty/stale/partial states are explicit. Thinking, raw Tool bodies, `input_json`, provider bodies, credentials, diagnostic fields, URL/path/raw IPC, SQL parameters and process handles never cross the bridge. No desktop layer persists selected-thread text, and no local route is bound in this plan.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 先写桌面 IPC 与窗口隔离的 Wave 0 失败契约</name>
  <files>apps/personal_intelligence_desktop/src/desktop-api-schema.mjs, apps/personal_intelligence_desktop/test/main-preload.test.mjs</files>
  <read_first>Read the APPROVED Electron qualification record; read 61-VALIDATION.md Wave 0 and HARNESS-01/HARNESS-08 rows; read 61-PATTERNS.md "Electron shell and conversation renderer" and apps/personal_intelligence_kernel/test/kernel-workflow.test.mjs for Node test setup. Do not read or copy Cockpit renderer code as a UI base.</read_first>
  <behavior>- Reject unknown action/channel, endpoint/path override, malformed input, raw error/body/secret keys, and an untrusted IPC sender. - Require nodeIntegration false, contextIsolation true, sandbox true, restrictive local CSP, denied navigation/new-window/permission requests, and no raw ipcRenderer export. - Accept only schema-shaped last/recent/select/new/project-scope-list/project-scope-select, turn, cancel, resume, reconcile, review, proactive-read, proactive-controls, proactive-dismiss and proactive-undo inputs and safe-code envelopes. - Reject invalid/foreign conversation, scope, task, proactive-item and feedback IDs; a cancelled or outcome_unknown envelope cannot normalize to success.</behavior>
  <action>Create pure schema/normalizer and injectable main-handler guard functions, then write Node built-in tests that fail until those guards exist. Define fixed intent schemas for recent-list, conversation-select, conversation-new, project-scope-list, project-scope-select, cancel, resume, reconcile, proactive read/control/dismiss/undo alongside the existing start/turn/review contracts. Per R-61-02, distinguish allowed normalized user/assistant `displayText` in a selected `ConversationThreadView` from forbidden thinking, raw Tool/provider/input bodies and private diagnostics; assert stable message ID, role, created time, source/evidence ref, pagination/truncation and empty/stale/partial states. Use `SECRET_SENTINEL`, `RAW_BODY_SENTINEL`, unsafe endpoint input and foreign IDs to prove all other navigation/recovery/proactive data is redacted and non-success states stay truthful. Keep the test independent of a live Electron window and network provider so it runs under Node in under 60 seconds.</action>
  <verify><automated>node --test apps/personal_intelligence_desktop/test/main-preload.test.mjs</automated></verify>
  <acceptance_criteria>The test asserts all Electron hardening flags, named-intent allowlisting, sender validation, navigation/new-window denial, CSP presence, raw IPC non-exposure, schema-valid recent/new/select/scope, cancel/resume/reconcile and proactive control/dismiss inputs, truthful outcome_unknown handling, and secret/body-safe errors before main/preload implementation is accepted.</acceptance_criteria>
  <done>Wave 0 desktop boundary tests fail for missing implementation rather than silently passing.</done>
</task>

<task type="auto">
  <name>Task 2: 安装已批准的 Electron 并锁定精确制品</name>
  <files>apps/personal_intelligence_desktop/package.json, apps/personal_intelligence_desktop/package-lock.json</files>
  <read_first>Read 61-ELECTRON-QUALIFICATION.json and proceed only when `approver_status` is exactly APPROVED and exact version/integrity fields are populated. Read apps/personal_intelligence_kernel/package.json for Node/test script conventions and 61-RESEARCH.md "Standard Stack"; inspect the desktop directory to ensure no unapproved dependency exists.</read_first>
  <action>Before any package-manager command, parse the approval JSON and fail closed unless `approver_status === "APPROVED"`, `approved_version === "43.3.0"`, and `approved_integrity` is nonempty exact registry integrity. Only then create the independent desktop package with `start` and `test` scripts and run `npm --prefix apps/personal_intelligence_desktop install --save-dev electron@43.3.0`. Do not add an app packager, component/UI registry, Cockpit dependency or runtime dependency. Compare the installed lock entry against approved JSON; missing fields or any mismatch fail closed.</action>
  <verify><automated>node -e "const fs=require('fs');const a=JSON.parse(fs.readFileSync('./.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-ELECTRON-QUALIFICATION.json','utf8'));if(a.approver_status!=='APPROVED'||a.approved_version!=='43.3.0'||typeof a.approved_integrity!=='string'||!a.approved_integrity.trim())throw Error('electron approval invalid');const p=JSON.parse(fs.readFileSync('./apps/personal_intelligence_desktop/package-lock.json','utf8'));const x=(p.packages&&p.packages['node_modules/electron'])||(p.dependencies&&p.dependencies.electron);if(!x||x.version!==a.approved_version||x.version!=='43.3.0'||x.integrity!==a.approved_integrity)throw Error('electron lock mismatch')"</automated></verify>
  <acceptance_criteria>package-lock contains exactly Electron 43.3.0 with the approved integrity, package.json has no unauthorized package, and no installer/packager framework is introduced.</acceptance_criteria>
  <done>The approved desktop dependency is installed reproducibly.</done>
</task>

<task type="auto">
  <name>Task 3: 实现安全主进程与最小 preload 桥</name>
  <files>apps/personal_intelligence_desktop/src/main.mjs, apps/personal_intelligence_desktop/src/preload.mjs, apps/personal_intelligence_desktop/src/desktop-api-schema.mjs</files>
  <read_first>Read the Wave 0 test, desktop-api-schema.mjs, 61-UI-SPEC.md Information Architecture/State sections, Electron security requirements in 61-RESEARCH.md, and apps/personal_intelligence_kernel/src/server.mjs safe error/JSON route analog.</read_first>
  <action>Per D-05/D-06/D-07/D-08/D-14/D-23/D-24/D-25/D-27/D-28/D-29 and R-61-02/R-61-05, implement a local-assets BrowserWindow whose webPreferences are nodeIntegration false, contextIsolation true, sandbox true and preload points only to preload.mjs. Register exact named IPC handlers, but make them call only an injected, unconfigured route-provider seam and return the declared `ROUTE_PROVIDER_UNAVAILABLE` envelope until Plan 61-10 supplies fixed providers; do not bind loopback URLs, launch a network request, or claim a last/selected thread in this plan. Validate sender URL and each intent schema, normalize `ConversationThreadView` to its privacy ceiling, use Cache-Control no-store, and preserve empty/stale/partial/paginated status. Do not create a desktop conversation/body retention store, localStorage use, body-bearing log/receipt/telemetry, or direct AgentsView query. Reuse existing Kernel Task/Session/Event cancel, resume and outcome_unknown/reconcile semantics once a provider is bound; do not invent parallel desktop recovery state. Deny webContents navigation, setWindowOpenHandler, permission requests, and arbitrary endpoint override. In preload.mjs expose only these parsed named methods through contextBridge; never export ipcRenderer or generic invoke/send.</action>
  <verify><automated>node --test apps/personal_intelligence_desktop/test/main-preload.test.mjs</automated></verify>
  <acceptance_criteria>All Wave 0 assertions pass; the main process has fixed local-only navigation, turn, recovery and proactive routes, preload has no generic IPC API, a foreign/malformed ID cannot select data or mutate controls, and returned start/navigation/recovery/proactive states are safe last/recent/new/scope/empty models rather than a browser Cockpit redirect, learned scheduling action or false success.</acceptance_criteria>
  <done>The Electron privilege boundary and safe conversation entry seam exist.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Renderer → preload → Electron main | Untrusted rendered content requests native/loopback actions. |
| Electron main → loopback services | Only fixed, validated requests may cross into local authorities. |
## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification evidence | Closure / blocking condition |
|---|---|---|---|---|---|---|---|
| T-61-UI-01 | Critical | Elevation of Privilege | renderer/preload IPC | mitigate | Context isolation, sandbox, named bridge, sender/schema validation and raw-IPC negative tests. | `node --test apps/personal_intelligence_desktop/test/main-preload.test.mjs` covers named navigation/turn/recovery allowlisting, schema rejection and untrusted sender denial. | All IPC-negative tests pass; otherwise block Plan 61-02 and its dependent renderer/UAT waves. |
| T-61-NAV-01 | High | Elevation of Privilege | BrowserWindow navigation | mitigate | Local assets/CSP plus denied navigation, new-window and permission handlers tested directly. | `node --test apps/personal_intelligence_desktop/test/main-preload.test.mjs` asserts CSP and navigation/new-window/permission denial. | Hardening assertions pass; otherwise block Plan 61-02 and Phase 61 desktop execution. |
| T-61-LEAK-01 | High | Information Disclosure | error/view-model projection | mitigate | Schema sanitizer rejects body/prompt/completion/credential/secret and uses safe codes only. | `node --test apps/personal_intelligence_desktop/test/main-preload.test.mjs` sentinel safe-envelope cases pass. | No sentinel/raw field crosses the bridge; otherwise stop current work and block later waves. |
</threat_model>

## Security Closure Gate
Every Critical/High row above is an execution gate. A missing or failing named evidence command blocks the current plan and prevents its dependent wave from starting; Phase 61 remains blocked until the row is closed.
<verification>Run the desktop Node test after each task and `git diff --check` before handoff.</verification>
<success_criteria>The approved local shell is isolated, schema constrained, browser-free, and ready for renderer wiring.</success_criteria>
<output>Create .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-02-SUMMARY.md when done</output>
