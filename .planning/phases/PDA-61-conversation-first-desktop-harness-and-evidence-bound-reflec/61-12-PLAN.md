---
phase: 61-conversation-first-desktop-harness-and-evidence-bound-reflection-loop
plan: 12
type: execute
wave: 9
depends_on: [61-03, 61-04, 61-05, 61-06, 61-07, 61-08, 61-09, 61-10, 61-11]
files_modified:
  - apps/personal_intelligence_desktop/test/desktop-uat-fixture.mjs
  - apps/personal_intelligence_desktop/test/desktop-uat-record.md
  - tests/integration/test_pi_kernel_events.py
  - .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-VALIDATION.md
autonomous: false
requirements: [HARNESS-08]
must_haves:
  truths:
    - "Automated regressions prove IPC isolation, real Pi lifecycle, governed SQLite display integrity, canonical navigation/session split, reflection/review/projection/proactive contracts and truthful recovery."
    - "A six-step desktop UAT uses redacted deterministic fixtures, never a browser or private runtime data, and records factual PASS/FAIL/BLOCKED evidence."
    - "Phase 55–60 activation/rollback gates remain unchanged and Phase 60 primary stays unactivated."
    - "D-01: Phase evidence proves a personal-intelligence Harness rather than another coding executor."
    - "D-02: Pi owns the generic Agent loop while governed project modules own the second-loop differentiation."
    - "D-03: Acceptance is limited to the approved vertical Walking Skeleton."
    - "D-04: Electron supplies the desktop Web shell without making the browser Cockpit the product surface."
    - "D-05: Startup restores the last safe conversation projection with Codex-style interaction density."
    - "D-06: Navigation remains limited to new/recent conversations, project scopes and personal/system entry points."
    - "D-07: Conversation remains primary and deeper capability appears only through on-demand surfaces."
    - "D-08: Tool details stay collapsed until safe evidence and receipt drilldown is requested."
    - "D-09: One Pi runtime enforces Conversation, Reflection and Operator profiles."
    - "D-10: A turn selects zero or one primary Skill without recursive Skill orchestration."
    - "D-11: Existing governed Skills and operations remain the capability foundation."
    - "D-12: Capability policy enforces scope, effects, confirmation, idempotency, timeout and receipts."
    - "D-13: AgentSession.prompt owns the iterative Tool loop without a duplicate outer loop."
    - "D-14: AgentView remains the supported cross-Agent aggregation source."
    - "D-15: Both freshness/backlog legs remain explicit and truthful."
    - "D-16: SQLite access remains descriptor-bound, read-only, scoped and bounded."
    - "D-17: SQLite results preserve identity, freshness/version, checksum, truncation and receipts."
    - "D-18: Reflection begins only from deterministic committed events."
    - "D-19: Evidence, Observation, Candidate, Canonical, Projection, Outcome and Calibration remain distinct."
    - "D-20: The vertical path connects committed delta, Candidate review and versioned projection."
    - "D-21: Personal understanding remains a temporal projection with evidence, confidence and conflicts."
    - "D-22: Agent agreement never grants promotion or high-risk authority."
    - "D-23: Proactive cards come only from deterministic events and preserve conversation ordering."
    - "D-24: Proactive UX includes tiered surfaces, quiet hours, granular controls, cluster deduplication and feedback."
    - "D-25: Review feedback remains append-only and cannot silently expand permissions or values."
    - "D-26: Python authority remains exclusive for formal facts and lifecycle mutations."
    - "D-27: Desktop supervision may retain internal loopback IPC and ports."
    - "D-28: Existing Task, Session, Event, Candidate, control, SSE and receipt stores are reused."
    - "D-29: Bodies, credentials, arbitrary SQL/capabilities and authority mutations never leak through diagnostic surfaces."
  artifacts:
    - path: apps/personal_intelligence_desktop/test/desktop-uat-fixture.mjs
      provides: redacted deterministic full-path replay fixture
    - path: apps/personal_intelligence_desktop/test/desktop-uat-record.md
      provides: privacy-safe six-step UAT evidence record
  key_links:
    - from: desktop-uat-fixture.mjs
      to: Electron/Kernal/Python routes
      via: fixed navigation, turn, controlled query, delta/review/projection/proactive/recovery replay
      pattern: conversation\.session\.create|statement_display|outcome_unknown
---

<objective>
Close Phase 61 with deterministic full-path security regressions and a blocking Electron UAT.

Purpose: prove all components connect through their intended authorities without leakage, false success or activation drift.
Output: fixture, UAT template/record and factual validation status.
</objective>

<execution_context>@$HOME/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-VALIDATION.md
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-11-PLAN.md
@.planning/STATE.md
@docs/AGENTS.md
<interfaces>Fixtures use only redacted synthetic metadata plus `SECRET_SENTINEL` and `RAW_BODY_SENTINEL` negatives. They traverse real `AgentSession.prompt()`/Tool/idle lifecycle and fixed provider routes. UAT record contains commands, versions, checksums, safe receipt IDs, status and redacted screenshot references only—never prompt/completion/body/credential/token/raw SQL/physical schema/parameter value or selected thread display text.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 构建全链路 redacted replay fixture 并聚合自动回归</name>
  <files>apps/personal_intelligence_desktop/test/desktop-uat-fixture.mjs, apps/personal_intelligence_desktop/test/desktop-uat-record.md, tests/integration/test_pi_kernel_events.py</files>
  <read_first>Read all Required Negative/Invariants and UAT sequence in 61-VALIDATION.md, the Phase 55–60 regression gate in that file, focused tests from Plans 61-02 through 61-11, kernel workflow/recovery tests, Phase 55–60 VALIDATION/SUMMARY artifacts, current kernel package.json scripts, and STATE.md Phase 55–60 activation statements.</read_first>
  <behavior>- Fixture traverses canonical last/recent/select, `conversation.project_scopes.list`, `conversation.project_scope.select` and `conversation.session.create`; new session remains empty/runtime-scoped. - It traverses real Pi prompt/tool/idle, `evidence.sqlite_query` receipt with checksum-bound `statement_display`, committed delta/replay, review/projection, proactive fixed routes, cancel/resume/outcome_unknown reconcile. - Sentinels, raw SQL/physical schema/parameter values, local body persistence, authority mutations, duplicate Candidate/cluster and false success never appear.</behavior>
  <action>Create a deterministic zero-paid replay provider and temporary authority fixtures. Exercise fixed scope/history reads and empty session creation, then assert no canonical history/Candidate/promotion/active-pointer/localStorage write or claim. Exercise one safe query and display binding plus unknown query/tampered display rejection; assert the fixture/UI-safe results contain no raw SQL/schema/value. Enter reflection only through committed publisher and EventJournal replay, then exercise individual review, next-turn derived projection, four proactive routes and recovery state. Assert all profile/lease/freshness/fingerprint/privacy invariants and Phase 60 activation state. Before UAT, run each of these existing deterministic/read-only regressions independently: `python -m pytest tests/contract/test_project_capability_registry.py -q`; `python -m pytest tests/security/test_pi_warehouse_tool_containment.py -q`; `npm test --prefix apps/personal_intelligence_kernel -- --test-name-pattern=skill-engine`; `python -m pytest tests/integration/test_pi_skill_recovery.py -q`; `node --test apps/personal_intelligence_kernel/test/runtime-control.test.mjs`; `python -m pytest tests/integration/test_pi_runtime_control.py -q`; `python -m pytest tests/e2e/test_pi_snapshot_release.py tests/e2e/test_pi_capability_os_activation.py -q`; and, after Plan 61-03 creates it, `node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs` for the non-default provider-mode regression. In the redacted UAT record, record each command verbatim with its exit code, PASS/FAIL result, registry/Skill/policy/provider-mode checksum or authority fingerprint as applicable, timestamp and redacted fixture ID; a missing checksum/fingerprint or non-zero exit blocks Phase 61. Do not use live data/, var/ or paid models; do not call activation, promotion, rollback, pointer-change or destructive CLI paths.</action>
  <verify><automated>node --test apps/personal_intelligence_desktop/test/*.test.mjs apps/personal_intelligence_kernel/test/conversation-delta-reflection.test.mjs && python -m pytest -q tests/integration/test_pi_kernel_events.py tests/unit/test_evidence_sqlite_tool.py tests/integration/test_evidence_sqlite_tool.py tests/contract/test_harness_freshness.py tests/contract/test_harness_conversation_projection.py tests/integration/test_harness_reflection.py tests/integration/test_harness_proactive.py tests/contract/test_harness_candidate_review.py tests/contract/test_pi_domain_gateway.py tests/integration/test_harness_projection.py && python -m pytest tests/contract/test_project_capability_registry.py -q && python -m pytest tests/security/test_pi_warehouse_tool_containment.py -q && npm test --prefix apps/personal_intelligence_kernel -- --test-name-pattern=skill-engine && python -m pytest tests/integration/test_pi_skill_recovery.py -q && node --test apps/personal_intelligence_kernel/test/runtime-control.test.mjs && python -m pytest tests/integration/test_pi_runtime_control.py -q && python -m pytest tests/e2e/test_pi_snapshot_release.py tests/e2e/test_pi_capability_os_activation.py -q && node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs</automated></verify>
  <acceptance_criteria>The automated suite proves named navigation/session routing, display integrity/no physical leak, no local or second fact store, real Pi loop, authority fingerprints, deterministic reflection/proactive behavior and non-success recovery without private runtime data. The redacted UAT record contains a PASS exit code 0 and the required applicable registry/Skill/policy/provider-mode checksum or authority fingerprint for every exact Phase 55–60/provider-mode regression command; all use deterministic temporary or replay fixtures and leave real activation, promotion, rollback and active pointers unchanged.</acceptance_criteria>
  <done>All critical full-path assertions have deterministic automated evidence.</done>
</task>

<task type="auto">
  <name>Task 2: 准备无私有正文的六步 UAT 记录和验证更新</name>
  <files>apps/personal_intelligence_desktop/test/desktop-uat-record.md, .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-VALIDATION.md</files>
  <read_first>Read passing Task 1 output, 61-VALIDATION.md, UI-SPEC, all Phase 61 SUMMARYs and docs/AGENTS.md privacy rules.</read_first>
  <action>Create the six-step UAT record. Step 1 covers last/recent/select, project scope list/select, new empty session and empty/stale/partial/pagination states without browser/body persistence. Step 2 proves real Pi lifecycle and palette focus. Step 3 opens the SQLite card and verifies identity/checksum/two freshness legs and the expandable `受控查询`/`已执行的脱敏 allowlisted statement` surface renders only server `statement_display`, never raw SQL/physical schema/parameter values; it includes unknown-query/tamper rejection. Step 4 covers committed delta/replay. Step 5 covers four-option Candidate review, derived next-turn projection and fixed proactive control/quiet/cluster/dismiss/undo routes. Step 6 covers cancel/resume/outcome_unknown. Include the exact Phase 55–60/provider-mode regression commands from Task 1 with per-command exit code, PASS/FAIL result, applicable registry/Skill/policy/provider-mode checksum or authority fingerprint, timestamp and redacted fixture ID; also include safe receipt status fields, redaction instructions, no-browser/no-paid/no-activation assertions and PASS/FAIL/BLOCKED rows. Update VALIDATION.md only with factual test existence/commands/status; leave UAT pending until checkpoint evidence and set nyquist/Wave 0 true only when evidence exists.</action>
  <verify><automated>node -e "const s=require('fs').readFileSync('apps/personal_intelligence_desktop/test/desktop-uat-record.md','utf8');for(const x of ['1.','2.','3.','4.','5.','6.','conversation.session.create','statement_display','受控查询','outcome_unknown','不使用浏览器'])if(!s.includes(x))throw Error(x)"</automated></verify>
  <acceptance_criteria>The record supplies six executable checks covering every new provider contract and contains no private content or premature completion claim; VALIDATION references Plan 61-12 as final closure.</acceptance_criteria>
  <done>A human has a complete privacy-safe verification script with truthful pending state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 运行六步 Electron Walking Skeleton UAT</name>
  <files>apps/personal_intelligence_desktop/test/desktop-uat-record.md, .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-VALIDATION.md</files>
  <read_first>Read desktop-uat-record.md, VALIDATION, UI-SPEC and all Phase 61 summaries. Confirm git status excludes unrelated REQUIREMENTS/STATE/kernel-host/sketch changes.</read_first>
  <action>Run the six numbered checks against the redacted deterministic fixture. Record per-step PASS/FAIL/BLOCKED, safe Pi/route/receipt/checksum/fingerprint evidence and redacted screenshot references. Update only factual manual evidence after each result. Do not change primary activation, promotion/rollback, provider budget or unrelated files.</action>
  <what-built>Automated full-path fixture and local Electron Walking Skeleton.</what-built>
  <how-to-verify>Follow the record's six numbered steps. In step 1 verify scope/history Python authority and Kernel empty-session split. In step 3 verify only immutable server `statement_display` is displayed under the controlled-query expansion, with no raw SQL/schema/value. Confirm all remaining safety, reflection, review/projection/proactive and recovery checks pass.</how-to-verify>
  <resume-signal>Record PASS/FAIL/BLOCKED for steps 1–6, then reply “Phase 61 desktop UAT approved” or identify the failing step.</resume-signal>
  <verify><automated>node -e "const s=require('fs').readFileSync('apps/personal_intelligence_desktop/test/desktop-uat-record.md','utf8');for(let i=1;i<=6;i++)if(!s.includes(String(i)+'.'))throw Error('missing UAT step '+i)"</automated></verify>
  <acceptance_criteria>All six UAT steps pass with no browser, raw SQL/schema/value/body/credential leak, false recovery success, canonical/authority mutation, second conversation store or activation/rollback drift. A failed step remains factual and blocks phase completion.</acceptance_criteria>
  <done>A complete redacted UAT record exists or explicitly blocks completion.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Deterministic fixtures → full desktop stack | Test evidence must not leak private content or mask an authority breach. |
| User-visible UAT status → runtime truth | A visual success must not hide cancellation, control, receipt or authority failures. |

## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification evidence | Closure / blocking condition |
|---|---|---|---|---|---|---|---|
| T-61-VERIFY-01 | Critical | Information Disclosure | fixture/log/UAT record | mitigate | Sentinel scans and display-integrity tests reject body/credential/raw SQL/schema/value leakage. | Task 1 automated suite and record review. | No leakage; otherwise block checkpoint and phase closeout. |
| T-61-VERIFY-02 | Critical | Repudiation | cancel/resume/outcome_unknown | mitigate | Automated and manual reconciliation checks prohibit success until actual reconciliation. | `python -m pytest -q tests/integration/test_pi_kernel_events.py` plus UAT step 6. | Both pass; otherwise block phase completion. |
| T-61-VERIFY-03 | Critical | Tampering/Elevation of Privilege | authority/navigation/session boundaries | mitigate | Fixture fingerprints prove scope reads, empty session and every full path keep canonical/promotion/active pointers stable. | Task 1 automated suite plus recorded UAT fingerprints. | No prohibited mutation/second store; otherwise block Phase 61. |
</threat_model>

## Security Closure Gate
Plan 61-12 aggregates closure for all Critical/High rows in Plans 61-01 through 61-11. Missing, failing or BLOCKED evidence prevents Phase 61 completion.
<verification>Run all listed Phase 61 and exact Phase 55–60/provider-mode regressions before the checkpoint; after UAT update VALIDATION strictly from the recorded exit-code/result/checksum-or-fingerprint evidence and run `git diff --check`.</verification>
<success_criteria>The phase can be judged from redacted evidence: one complete vertical path works and all authority/desktop boundaries hold.</success_criteria>
<output>Create .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-12-SUMMARY.md when done</output>

## Decision Coverage Index

Phase-close verification traces every locked decision to the implementing plans and recorded UAT evidence:

- D-01: Personal-intelligence Harness identity is verified by the cross-Agent evidence-to-projection vertical path, not coding execution.
- D-02: Pi owns the generic Agent loop while project modules own governed context, Skills, evidence, review, projection and proactive presentation.
- D-03: Phase 61 closes only the approved Walking Skeleton and does not claim the complete long-term second loop.
- D-04: Electron provides the desktop Web shell without making the browser Cockpit the product surface.
- D-05: Startup restores the last safe conversation projection and preserves Codex-style switching ergonomics.
- D-06: Navigation remains minimal: new/recent conversations, project scopes and personal/system entry points.
- D-07: Conversation is primary; cards, drawers, command palette and confirmation modal expose deeper capability on demand.
- D-08: Tool execution stays collapsed by default and expands only safe evidence, freshness, limitations and receipts.
- D-09: One Pi runtime enforces Conversation, Reflection and Operator policy profiles.
- D-10: Each turn uses zero or one primary Skill without recursive Skill orchestration.
- D-11: Existing governed Skills and operations remain the capability foundation and Skill leases remain scoped.
- D-12: Capability checks enforce profile, scope, arguments, effects, confirmation, idempotency, timeout and receipts.
- D-13: AgentSession.prompt owns the iterative model/tool/receipt loop without a duplicate outer Agent loop.
- D-14: AgentView remains the supported cross-Agent aggregation source; no source-specific parser is added.
- D-15: Both source-to-AgentView and AgentView-to-canonical freshness legs remain visible and truthful.
- D-16: SQLite access is descriptor-bound, read-only, scoped and bounded, with unsafe statements and capabilities denied.
- D-17: SQLite results preserve source identity, freshness/version, checksum, truncation and drillable receipt metadata.
- D-18: The reflection loop starts only from deterministic committed events.
- D-19: Evidence, Observation, Candidate, Canonical, Projection, Outcome and Calibration remain distinct layers.
- D-20: A committed conversation delta becomes a reviewable Candidate and accepted versioned projection.
- D-21: Personal understanding remains a temporal projection with evidence, confidence, conflict and supersession.
- D-22: Agent agreement never grants Candidate promotion or high-risk authority.
- D-23: Proactive cards come only from deterministic events and do not rewrite manual conversation ordering.
- D-24: Proactive UX supports tiered surfaces, quiet hours, granular controls, evidence-cluster deduplication and dismissal feedback.
- D-25: Accept/edit/ignore/dismiss feedback is append-only and does not silently expand permissions or values.
- D-26: Python authority remains exclusive for facts, transactions, watermarks, evaluation, promotion, pointers and rollback.
- D-27: The desktop may supervise local processes and consume loopback IPC without requiring port elimination.
- D-28: Existing Task, Session, Event, Candidate, cancel/resume/reconcile, SSE and receipt stores are reused.
- D-29: Personal bodies, credentials, arbitrary SQL/capabilities and authority mutations never leak through logs, telemetry or model-visible receipts.
