---
phase: 61-conversation-first-desktop-harness-and-evidence-bound-reflection-loop
plan: 05
type: tdd
wave: 2
depends_on: [61-03, 61-04]
files_modified:
  - apps/personal_intelligence_kernel/src/conversation/session-service.mjs
  - apps/personal_intelligence_kernel/src/kernel-host.mjs
  - apps/personal_intelligence_kernel/src/server.mjs
  - apps/personal_intelligence_kernel/test/conversation-turn.test.mjs
  - src/personal_knowledge/application/conversation/harness_freshness.py
  - src/personal_knowledge/services/harness_conversation_service.py
  - src/personal_knowledge/services/pi_domain_gateway.py
  - tests/contract/test_harness_freshness.py
  - tests/contract/test_harness_conversation_projection.py
autonomous: true
requirements: [HARNESS-01, HARNESS-04]
must_haves:
  truths:
    - "Conversation and evidence answers disclose source→AgentView and AgentView→canonical freshness/backlog separately."
    - "Unknown or stale legs cannot be projected as current/complete."
    - "Python canonical conversation repository is the sole authority for a safe last/recent/selected `ConversationThreadView`; display content is bounded and ephemeral at desktop consumers."
    - "Python canonical authority supplies schema-bound project-scope list/select metadata, while the Kernel Session store owns only new empty-session metadata and an empty safe thread view."
  artifacts:
    - path: src/personal_knowledge/application/conversation/harness_freshness.py
      provides: typed dual-watermark projection
    - path: tests/contract/test_harness_conversation_projection.py
      provides: Python privacy and retention contract for canonical conversation projection
    - path: apps/personal_intelligence_kernel/src/conversation/session-service.mjs
      provides: governed empty Kernel Session metadata for `conversation.session.create`
  key_links:
    - from: harness_conversation_service.py
      to: harness_freshness.py
      via: safe desktop answer/evidence view model
      pattern: source_to_agentsview
    - from: harness_conversation_service.py
      to: conversation_repository.py
      via: canonical-only ConversationThreadView projection route
      pattern: ConversationThreadView
    - from: session-service.mjs
      to: PiDomainGateway conversation.project_scope.select
      via: approval and scope validation before empty Kernel Session metadata creation
      pattern: conversation\.session\.create
---
<objective>Make dual freshness, canonical conversation/project-scope navigation, and governed empty-session creation first-class safe contracts without claiming stale or incomplete data is current.</objective>
<context>@src/personal_knowledge/adapters/agentsview.py
@src/personal_knowledge/core/conversation_repository.py
@src/personal_knowledge/services/evidence_sqlite_tool.py
@src/personal_knowledge/services/pi_domain_gateway.py
@apps/personal_intelligence_kernel/src/kernel-host.mjs
@apps/personal_intelligence_kernel/src/server.mjs
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-UI-SPEC.md
@.planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-VALIDATION.md
<interfaces>Python canonical authority owns read-only `conversation.thread.last`, `conversation.thread.recent`, `conversation.thread.select`, `conversation.project_scopes.list`, and `conversation.project_scope.select` providers. `conversation.project_scopes.list` returns only safe `{project_scope_id,label,thread_count,last_activity_at,freshness}` items; unknown/empty/stale states are explicit. `conversation.project_scope.select` validates scope ownership and returns a safe selected-scope projection plus paginated recent-thread metadata, but does not mutate canonical data. The Kernel owns named `conversation.session.create`: after an approved selected scope, it creates only existing governed Session metadata `{session_id,project_scope_id,created_at,status:"empty"}` and an empty safe `ConversationThreadView`; it must not write canonical conversation bodies, Candidate, promotion, active pointer, or desktop localStorage. Before a later canonical ingestion event, the active body is runtime/session-scoped only and is never represented as canonical history. Desktop later maps `listProjectScopes`, `selectProjectScope`, and `newConversation` exactly to those three intents; no second conversation fact store is permitted.</interfaces>
 </context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: 以契约固定双水位、canonical 导航和空 Session 真值</name>
<files>apps/personal_intelligence_kernel/test/conversation-turn.test.mjs, tests/contract/test_harness_freshness.py, tests/contract/test_harness_conversation_projection.py</files>
<read_first>Read agentsview.py SourceProbe/RO metadata, conversation_repository.py, evidence_sqlite_tool.py receipt shape, 61-UI-SPEC Conversation turn/SQLite card text, and 61-VALIDATION.md HARNESS-01/HARNESS-04 rows.</read_first>
<behavior>- A healthy source probe does not imply canonical current. - stale, unknown, missing watermark and nonzero backlog show their own status/limitation. - `ConversationThreadView` resolves last/recent/selected from canonical source only; selected thread includes only normalized user/assistant display messages with stable message ID, role, display text, created time, source/evidence ref, pagination/truncation and freshness. - `conversation.project_scopes.list` returns safe scope ID/label/count/last activity/freshness only; `conversation.project_scope.select` returns safe selected scope and paginated thread metadata without canonical mutation. - `conversation.session.create` accepts only an approved scope and returns governed empty Kernel Session metadata plus an empty thread view; it writes no canonical body, Candidate, promotion, active pointer or desktop persistence. - Empty, stale, partial and paginated states are explicit. Thinking, raw Tool bodies, `input_json`, provider body, credentials and private diagnostics never appear, and no desktop persistence/log/receipt/telemetry contract receives display text.</behavior>
<action>Create deterministic metadata-only freshness fixtures, temporary canonical-repository scope/thread fixtures, and an injected Kernel SessionStore fixture. Assert the exact two-leg schema and prohibit scalar `current`/`complete` claims whenever either leg lacks proof. Assert list responses have metadata only, project scope rows expose only allowed fields, selected scope is read-only and paginated, selected display text is normalized and bounded, forbidden sentinels are denied/redacted, foreign/legacy/live-AgentsView sources and unknown/foreign/stale scopes are rejected or safe-stated, pagination preserves stable IDs, and only IDs/counts/checksums/status may reach receipt/log fixtures. Assert `conversation.session.create` checks the selected canonical scope then creates exactly empty Session metadata and an empty safe thread view; it cannot write canonical history/Candidate/promotion/active-pointer/localStorage or describe runtime text as canonical. Do not use live data/ or var/ databases.</action>
<verify><automated>node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs && python -m pytest -q tests/contract/test_harness_freshness.py tests/contract/test_harness_conversation_projection.py</automated></verify>
<acceptance_criteria>The tests fail for a one-number freshness response, hidden backlog, noncanonical thread source, missing pagination/partial state, unsafe scope field, scope-selection mutation, unknown/foreign scope acceptance, nonempty/authority-writing session creation, private-payload leakage, or selected-thread text retained outside the safe ephemeral projection boundary.</acceptance_criteria>
<done>Freshness, Python-canonical navigation, and Kernel-only empty-session contracts are test-first and independent of UI styling.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: 实现 Python canonical 导航和 Kernel 空 Session provider</name>
<files>apps/personal_intelligence_kernel/src/conversation/session-service.mjs, apps/personal_intelligence_kernel/src/kernel-host.mjs, apps/personal_intelligence_kernel/src/server.mjs, src/personal_knowledge/application/conversation/harness_freshness.py, src/personal_knowledge/services/harness_conversation_service.py, src/personal_knowledge/services/pi_domain_gateway.py</files>
<read_first>Read the failing contract tests, agentsview.py, conversation_repository.py, evidence_sqlite_tool.py, pi_domain_gateway.py, existing Kernel SessionStore/route patterns, and state/receipt safe-envelope patterns in 61-PATTERNS.md.</read_first>
<action>Per D-05/D-06/D-14/D-15/D-26/D-28/D-29 and R-61-02, implement typed source-to-AgentView and AgentView-to-canonical legs from existing probe/snapshot/watermark authority; classify absence as unknown/stale rather than infer current. Use `ConversationRepository` in explicit canonical mode as the sole last/recent/selected source. Register Python-owned named schema-bound read-only providers `conversation.project_scopes.list` and `conversation.project_scope.select`: the list returns only `project_scope_id`, label, thread count, last activity and freshness; select validates the scope and returns only safe selected-scope projection plus paginated recent-thread metadata. Neither provider writes canonical data. Keep existing thread intents in the same Python canonical authority with safe empty/stale/partial/pagination envelopes and no direct renderer database access. Normalize only user/assistant display messages to stable message ID, role, display text, created time and source/evidence ref; never expose thinking, raw Tool/input/provider bodies, credentials or diagnostics. Keep selected-thread content only in caller memory, and ensure logs, receipts and telemetry receive IDs/counts/checksums/status only. Have harness_conversation_service compose safe answer/evidence models with `freshness`, `limitations`, source identity and receipt references. Register all canonical reads in PiDomainGateway as explicit read-only authority with no canonical/promotion operation. In the Kernel add only named `conversation.session.create` dispatch/service: validate sender/schema and request an approved `project_scope_id` through the named canonical select provider, then use the existing governed Session store to persist only `{session_id, project_scope_id, created_at, status:"empty"}` and return an empty safe `ConversationThreadView`. It must not write canonical conversation bodies, Candidate, promotion, active pointer, desktop localStorage, or any second conversation fact store; until a later canonical ingestion event, any active body remains runtime/session-scoped and is never claimed as canonical history.</action>
<verify><automated>node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs && python -m pytest -q tests/contract/test_harness_freshness.py tests/contract/test_harness_conversation_projection.py tests/unit/test_evidence_sqlite_tool.py tests/integration/test_evidence_sqlite_tool.py tests/contract/test_pi_domain_gateway.py</automated></verify>
<acceptance_criteria>Canonical-only last/recent/selected/project-scope reads return safe explicit status states; `conversation.project_scopes.list` and `conversation.project_scope.select` expose no body/private data and never mutate canonical data; `conversation.session.create` has a fixed Kernel route, validates approved scope and persists only empty governed Session metadata plus empty view; every evidence/query response carries both freshness legs and a limitation; neither stale source nor canonical backlog is rendered as complete/current; receipts/logs/telemetry contain no display text and no second conversation fact store exists.</acceptance_criteria>
<done>Conversation scope/history remain Python-canonical-authoritative, while a new active conversation has only governed Kernel Session metadata and a truthful empty runtime view.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| AgentView/canonical metadata → desktop view model | Separate watermarks can otherwise be collapsed into misleading certainty. |
## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan | Verification evidence | Closure / blocking condition |
|---|---|---|---|---|---|---|---|
| T-61-FRESH-01 | High | Spoofing/Information Disclosure | freshness projection | mitigate | Typed two-leg state, safe metadata-only shape and stale/unknown contract tests. | `python -m pytest -q tests/contract/test_harness_freshness.py`. | Current/stale/unknown/backlog contract passes; otherwise block Plan 61-05 and all dependent reflection/renderer/UAT work. |
| T-61-LEAK-02 | High | Information Disclosure | desktop view model | mitigate | Reject body/credential/raw query fields; return source identifiers and receipts only. | `python -m pytest -q tests/contract/test_harness_freshness.py tests/unit/test_evidence_sqlite_tool.py`. | Projection has metadata-only fields with no sentinel leakage; otherwise stop and block Phase 61. |
| T-61-AUTH-02 | High | Tampering | canonical navigation/read routes | mitigate | Explicit read-only Python providers for scope/history and fingerprint tests prohibit canonical, promotion or active-pointer writes. | `python -m pytest -q tests/contract/test_harness_freshness.py tests/contract/test_harness_conversation_projection.py tests/contract/test_pi_domain_gateway.py`. | Read-only gateway and stable authority fingerprints pass; otherwise block this plan and dependent waves. |
| T-61-SESSION-01 | Critical | Tampering/Elevation of Privilege | Kernel empty-session creation | mitigate | Named schema-bound route validates Python-approved scope and writes only governed metadata; tests reject canonical/Candidate/promotion/active-pointer/localStorage writes and history claims. | `node --test apps/personal_intelligence_kernel/test/conversation-turn.test.mjs`. | Empty-session/no-second-store assertions pass; otherwise block renderer and UAT waves. |
</threat_model>

## Security Closure Gate
Every Critical/High row above is an execution gate. A missing or failing named evidence command blocks the current plan and prevents its dependent wave from starting; Phase 61 remains blocked until the row is closed.
<verification>Run the focused contract plus SQLite regressions and `git diff --check`.</verification>
<success_criteria>Freshness and backlog are always visible as two independently truthful legs.</success_criteria>
<output>Create .planning/phases/PDA-61-conversation-first-desktop-harness-and-evidence-bound-reflec/61-05-SUMMARY.md when done</output>
