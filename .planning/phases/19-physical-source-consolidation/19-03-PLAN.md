---
phase: 19
plan: "03"
wave: 3
depends_on: ["19-02"]
requirements: [PHY-03, PHY-05, PHY-07]
autonomous: false
---
# 19-03 Consumer cutover, shim and tools consolidation

<task id="19-03-01" type="auto"><files>tests/, docs/, README.md, src/</files><action>迁移所有旧裸import、文件路径和命令消费者到personal_knowledge/console入口；AST+文本allowlist复查。</action><verify>consumer scan old runtime refs=0；full collect。</verify><acceptance_criteria>86 shim consumer=0或明确正式入口替代。</acceptance_criteria><done>旧入口无消费者。</done></task>
<task id="19-03-02" type="auto"><files>governance/manifests/source/root-shims.json</files><action>生成88根脚本exact move/inverse，consumer必须为0，目标tools/compat/v1_1。</action><verify>executor --dry-run + consumer scan</verify><acceptance_criteria>consumer=0、dirty conflicts=0。</acceptance_criteria><done>shim preview完成。</done></task>
<task id="19-03-03" type="checkpoint:human-verify"><files>governance/manifests/source/root-shims.json</files><action>只审批manifest checksum。</action><verify>人工检查88 old/new及inverse。</verify><acceptance_criteria>不在checkpoint移动。</acceptance_criteria><done>cohort批准/defer。</done></task>
<task id="19-03-04" type="auto"><files>integration/scripts/*.py, tools/compat/v1_1/, var/runtime/migration/source-root-shims.journal.jsonl</files><action>批准后apply，运行parity/full tests/rollback drill并re-apply。</action><verify>root py count=0；python -m pytest -q；五console smoke。</verify><acceptance_criteria>无删除、compat不参与runtime。</acceptance_criteria><done>根脚本清空。</done></task>
<task id="19-03-05" type="auto"><files>governance/manifests/source/tools.json</files><action>生成_tools/examples到四类目标的immutable preview/inverse。</action><verify>executor --dry-run；tool registry coverage=100%</verify><acceptance_criteria>dirty conflicts=0；checksum固定。</acceptance_criteria><done>preview完成。</done></task>
<task id="19-03-06" type="checkpoint:human-verify"><files>governance/manifests/source/tools.json</files><action>只审批manifest checksum。</action><verify>人工核对分类/old/new/inverse。</verify><acceptance_criteria>不移动。</acceptance_criteria><done>批准/defer。</done></task>
<task id="19-03-07" type="auto"><files>integration/scripts/_tools/, integration/scripts/examples/, tools/, var/runtime/migration/source-tools.journal.jsonl</files><action>获批后apply，失败rollback。</action><verify>registry/docs/preflight/full pytest/rollback drill。</verify><acceptance_criteria>旧目录不存在；每个tool有分类。</acceptance_criteria><done>工具收口。</done></task>
