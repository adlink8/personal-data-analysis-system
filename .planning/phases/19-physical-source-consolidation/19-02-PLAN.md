---
phase: 19
plan: "02"
wave: 2
depends_on: ["19-01"]
requirements: [PHY-01, PHY-02, PHY-07]
autonomous: false
---
# 19-02 Canonical Python src-layout migration

<task id="19-02-01" type="auto"><files>governance/manifests/source/canonical-src.json</files><action>生成core→domains→retrieval→evaluation→services→governance exact manifest、consumer rewrites、prestate bytes和inverse。</action><verify>python -m personal_knowledge.governance.apply_source_migration --manifest governance/manifests/source/canonical-src.json --dry-run</verify><acceptance_criteria>dirty/case/unicode/reparse/path-length/capacity checks PASS；Phase17路径包含。</acceptance_criteria><done>immutable preview完成。</done></task>
<task id="19-02-02" type="checkpoint:human-verify"><files>governance/manifests/source/canonical-src.json</files><action>仅审批manifest checksum和exact old/new paths，不执行写操作。</action><verify>人工确认operations/inverse/dirty conflicts。</verify><acceptance_criteria>批准记录绑定checksum。</acceptance_criteria><done>source cohort获准或defer。</done></task>
<task id="19-02-03" type="auto"><files>src/personal_knowledge/, integration/scripts/, var/runtime/migration/source-canonical-src.journal.jsonl</files><action>获批后用executor apply；失败整批逆序恢复到prestate bytes。</action><verify>apply命令后targeted pytest+console/import parity；随后rollback drill再re-apply。</verify><acceptance_criteria>实现只在src；旧重复=0；rollback精确。</acceptance_criteria><done>canonical source物理收口。</done></task>
<task id="19-02-04" type="auto"><files>pyproject.toml, README.md, docs/</files><action>启用 rag-pipeline/search/api/mcp/dashboard console scripts，更新全部用户入口。</action><verify>五命令--help/import smoke。</verify><acceptance_criteria>用户入口不依赖integration/scripts根文件。</acceptance_criteria><done>正式入口切换。</done></task>
