---
phase: 20
plan: "04"
wave: 4
depends_on: ["20-03"]
requirements: [DATA-04, DATA-05]
autonomous: false
---
# 20-04 Archive and vendor-reference
<task id="20-04-01" type="auto"><files>governance/manifests/data/archive.json</files><action>为.gsd/.ai-bridge/_recycle分别生成exact preview；.agents/.codex/.workbuddy/.github/.planning/governance/root configs显式retain-in-place。</action><verify>disposition coverage=100%；executor --dry-run；capacity/prestate/inverse PASS。</verify><acceptance_criteria>_recycle独立cohort；不读取R4正文。</acceptance_criteria><done>archive preview完成。</done></task>
<task id="20-04-02" type="checkpoint:human-verify"><files>governance/manifests/data/archive.json</files><action>逐cohort审批manifest checksum；_recycle单独审批哈希算法、范围、分块大小与仅本地读取授权，不要求审批前存在正文digest。</action><verify>人工检查容量/备份/rollback及授权边界。</verify><acceptance_criteria>checkpoint不迁移；不解析、不打印、不外传正文。</acceptance_criteria><done>批准/defer。</done></task>
<task id="20-04-03" type="auto"><files>_recycle/, .gsd/, .ai-bridge/, archive/, var/runtime/migration/data-archive.journal.jsonl</files><action>仅对批准cohort执行；_recycle第一步按批准参数计算source digest并写入gitignored本地journal，再stage-copy、验证digest、原子切换并保留backup；禁止解析或输出正文。</action><verify>executor apply + source/stage/cutover digest一致。</verify><acceptance_criteria>无内容丢失；retained root tooling不动。</acceptance_criteria><done>archive切换。</done></task>
<task id="20-04-04" type="auto"><files>var/runtime/migration/data-archive.journal.jsonl</files><action>独立执行inventory count/size/depth、digest、preflight、archive exclusion和consumer scan。</action><verify>所有批准cohort verification PASS。</verify><acceptance_criteria>source/stage/cutover digest一致；archive不进入runtime/test/import。</acceptance_criteria><done>archive验证完成。</done></task>
<task id="20-04-05" type="checkpoint:human-verify"><files>var/runtime/migration/data-archive.journal.jsonl</files><action>检查验证证据后逐cohort批准rollback drill；_recycle rollback后再次比较本地digest。</action><verify>人工确认backup、inverse、容量和恢复窗口。</verify><acceptance_criteria>checkpoint本身不迁移；仅获批cohort drill。</acceptance_criteria><done>批准/defer rollback drill。</done></task>
<task id="20-04-06" type="auto"><files>var/runtime/migration/data-archive.journal.jsonl</files><action>仅对获批cohort执行rollback→digest验证→restore→digest验证；不解析、不输出R4正文。</action><verify>source/stage/cutover/rollback/restore本地digest一致。</verify><acceptance_criteria>演练可逆且最终恢复archive新路径。</acceptance_criteria><done>rollback drill完成。</done></task>
