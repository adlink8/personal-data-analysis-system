---
phase: 20
plan: "02"
wave: 2
depends_on: ["20-01"]
requirements: [DATA-01, DATA-05, DATA-06, DATA-07]
autonomous: false
---
# 20-02 Agent Google imports cohorts
<task id="20-02-01" type="auto"><files>governance/manifests/data/agent-google-imports.json</files><action>生成Agent/Google/imports逐cohort snapshot、capacity、writers、type checks、inverse和config-fallback preview。</action><verify>python -m personal_knowledge.governance.apply_data_migration --manifest governance/manifests/data/agent-google-imports.json --dry-run</verify><acceptance_criteria>每cohort独立checksum；外部AgentsView protected-external。</acceptance_criteria><done>preview完成。</done></task>
<task id="20-02-02" type="checkpoint:human-verify"><files>governance/manifests/data/agent-google-imports.json</files><action>逐cohort只审批manifest checksum。</action><verify>人工确认容量/备份/WAL/rollback。</verify><acceptance_criteria>checkpoint不执行迁移。</acceptance_criteria><done>cohort批准/defer。</done></task>
<task id="20-02-03" type="auto"><files>Agent/, Google/, imports/, data/, var/runtime/migration/data-agent-google-imports.journal.jsonl</files><action>仅对批准cohort stage-copy→validate→atomic cutover→config fallback；失败逆序rollback。</action><verify>executor apply + immediate type checks。</verify><acceptance_criteria>内容等价、外部只读、old backup保留。</acceptance_criteria><done>输入数据切换。</done></task>
<task id="20-02-04" type="auto"><files>var/runtime/migration/data-agent-google-imports.journal.jsonl</files><action>独立执行row/schema/count/checksum、pipeline smoke和consumer scan。</action><verify>所有批准cohort verification PASS。</verify><acceptance_criteria>运行事实等价；old-path consumer=0。</acceptance_criteria><done>切换验证完成。</done></task>
<task id="20-02-05" type="checkpoint:human-verify"><files>var/runtime/migration/data-agent-google-imports.journal.jsonl</files><action>检查verification证据后批准逐cohort rollback drill；checkpoint本身不切换数据。</action><verify>人工确认backup、inverse和恢复窗口。</verify><acceptance_criteria>仅获批cohort进入rollback drill。</acceptance_criteria><done>批准/defer rollback drill。</done></task>
<task id="20-02-06" type="auto"><files>var/runtime/migration/data-agent-google-imports.journal.jsonl</files><action>仅对获批cohort执行rollback→验证旧状态→restore→再验证新状态。</action><verify>两端schema/count/checksum与pointer均PASS。</verify><acceptance_criteria>演练可逆且最终恢复新路径。</acceptance_criteria><done>rollback drill完成。</done></task>
