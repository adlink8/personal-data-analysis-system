---
phase: 20
plan: "03"
wave: 3
depends_on: ["20-02"]
requirements: [DATA-02, DATA-03, DATA-05, DATA-07]
autonomous: false
---
# 20-03 var DB runtime reports logs
<task id="20-03-01" type="auto"><files>governance/manifests/data/var.json</files><action>覆盖integration/{db,runtime,analysis,raw_index,structured}、integration根logs/JSON、root logs/HTML/.pytest_cache/__pycache__，生成按DB/runtime/report/cache exact preview。</action><verify>executor --dry-run + node disposition coverage=100%</verify><acceptance_criteria>所有实际路径有决定；cache用配置重定向而非复制。</acceptance_criteria><done>var preview完成。</done></task>
<task id="20-03-02" type="checkpoint:human-verify"><files>governance/manifests/data/var.json</files><action>逐cohort只审批checksum。</action><verify>人工确认active pointer/backup/capacity/inverse。</verify><acceptance_criteria>checkpoint不迁移。</acceptance_criteria><done>批准/defer。</done></task>
<task id="20-03-03" type="auto"><files>integration/, logs/, var/, pytest.ini, var/runtime/migration/data-var.journal.jsonl</files><action>仅批准cohort按类型协议迁移并更新producer/consumer/gitignore/cache_dir。</action><verify>executor apply + immediate type checks。</verify><acceptance_criteria>backup保留且pointer/config原子切换。</acceptance_criteria><done>var切换。</done></task>
<task id="20-03-04" type="auto"><files>var/runtime/migration/data-var.journal.jsonl</files><action>独立验证DB integrity/active identity/API/MCP/search/report/full tests和consumer scan。</action><verify>所有批准cohort verification PASS。</verify><acceptance_criteria>运行事实等价，旧路径consumer=0。</acceptance_criteria><done>var验证完成。</done></task>
<task id="20-03-05" type="checkpoint:human-verify"><files>var/runtime/migration/data-var.journal.jsonl</files><action>检查verification证据后批准逐cohort rollback drill；checkpoint本身不切换数据。</action><verify>人工确认backup、inverse、active pointer恢复窗口。</verify><acceptance_criteria>仅获批cohort进入rollback drill。</acceptance_criteria><done>批准/defer rollback drill。</done></task>
<task id="20-03-06" type="auto"><files>var/runtime/migration/data-var.journal.jsonl</files><action>仅对获批cohort执行rollback→旧路径验证→restore→新路径验证。</action><verify>DB identity、pointer、API/MCP/search smoke两端PASS。</verify><acceptance_criteria>演练可逆且最终恢复新路径。</acceptance_criteria><done>rollback drill完成。</done></task>
