{
  "name": "personal-intelligence-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pi local desktop shell (Electron) — approved dependency only; renderer/UI wiring deferred to later 61 waves.",
  "scripts": {
    "start": "electron .",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "devDependencies": {
    "electron": "43.3.0"
  }
}
