{
  "name": "personal-intelligence-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/main.mjs",
  "description": "Pi local desktop shell (Electron) — conversation-first renderer wired to fixed localhost providers (Phase 61); approved dependency only (electron@43.3.0).",
  "scripts": {
    "start": "electron .",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "devDependencies": {
    "electron": "43.3.0"
  }
}
